# Claude Code project guidance

@AGENTS.md

Use `docs/context/CURRENT_STATE.md` to select context, and delegate narrow reviews to `.claude/agents` when useful. Reusable workflows live in `.claude/skills`. Return compact handoffs with: outcome, changed files, validation, risks, and next action.

On this workstation PHP is normally `C:/Users/medion/php83/php.exe`; `brain.ps1` discovers it automatically. After route or dependency changes run the generators listed in `AGENTS.md`. Never approve or execute production actions.

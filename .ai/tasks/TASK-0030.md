# TASK-0030: An API token never opens a console by accident

- **Status:** IN_PROGRESS
- **Priority:** P0
- **Risk:** HIGH
- **Owner:** onhost-security
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0030-an-api-token-never-opens-a-console-by-ac`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0030-an-api-token-never-opens-a-console-by-ac`
- **Opened:** 2026-09-25T18:20:01Z

## Objective

An API token never opens a console, runs a command or adds an SSH key unless the customer issued it with the new
scope `services:console` on purpose; a HIGH action through a token is always refused; a `svc_console` guest gets the
console the role promises.

### WP-A (onhost-security): explicit token scope map, services:console, console-token route and UI, console-token resource scope

Outcome: the permission → token-scope decision is ONE explicit, deny-by-default map
(`domains/Identity/Authorization/TokenScopes.php`); `services:console` gates the console token, command.run/command.send and
shell.*; a token session never holds a step-up; IssueConsoleTokenCommand is decided on the service (resource scope).

## Context

Audit C13-H2c: `ApiContext::assertTokenScope` decided by prefix, so every `service.*` permission that was not
manage/delete fell to `services:read` — a read-only token got a noVNC console (`GET /v1/services/{id}/console-token`) and
the portal's "operate services" preset (read + power) ran commands and added SSH keys. `StepUpService::activeGrant`
matched session-less grants for token sessions. `IssueConsoleTokenCommand` used the organization scope, so a `svc_console`
guest (resource binding) was refused (audit §4).

D-4 check (WP-A): commands whose `permission()` returns null — `CreateOrganizationCommand` (route family `organizations`,
closed to tokens) and `AnnounceDiskTotalCommand` (console command only). None is dispatched from a token-reachable family
(services, invoices, documents, wallet, tickets, dns, zones, domains). VERIFIED by grep.

## Scope (owned paths — locked)

- `app/Http/Support/ApiContext.php`
- `app/Http/Middleware/TokenRouteScope.php`
- `domains/Identity/Commands/ApiTokenCommand.php`
- `domains/Services/Commands/IssueConsoleTokenCommand.php`
- `app/Http/Controllers/Web/ConsoleRelayController.php`
- `apps/surfaces/api/onhost-panel-account.api.js`
- `tests/Feature/Http/ApiTokenScopeMapTest.php`

## Out of scope

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

Endpoints, events and payload keys, schema, config keys, provider calls — or "none".

## Acceptance criteria

WP-A:
- [x] TokenScopes::decisions() holds exactly PermissionCatalog::keys(); unknown/null permission → not available to tokens.
- [x] services:read token refused console-token (GET/POST), command.run preview; no vncproxy call, no console audit.
- [x] operate-services preset refused console-token and every action permissionFor() decides as service.console; power start still 202.
- [x] token with services:console gets a con_ console token; console-only token too (D-8).
- [x] HIGH action through a token refused with step_up_required even with a session-less grant; portal control 202.
- [x] svc_console guest, org developer, project developer get a console token; svc_manage guest and viewer do not.
- [x] services:read token no longer sees what only managing shows (ApiContext::can is token-aware).
- [x] every permission literal checked in app/Http/Controllers has a token decision; the panel's presets never carry services:console.

## Required tests

Failing-first tests to write; existing tests that must stay green; critical flows touched (`.ai/baseline/baseline.json`).

## Security considerations

## Migration considerations

## Rollback

## Red / green evidence (WP-A)

RED at 53b392d + test only (`php artisan test --compact tests/Feature/Http/ApiTokenScopeMapTest.php`): 9 tests, 0 passed, 6 failed, 2 errors, 1 skipped.
- (1) map + (2) unknown permission: `Class "Onhost\Domain\Identity\Authorization\TokenScopes" not found`.
- (3) read-only token: `Expected response status code [403] but received 200` (console-token GET issued a con_ token).
- (4) operate-services preset: `Expected response status code [403] but received 200` (POST console-token).
- (5) console token: `Expected [200] but received 403 … The API token lacks the services:read scope.` (console-only token).
- (6) HIGH through a token: `Expected response status code [403] but received 202` (terminate ran on a session-less grant).
- (7) guests: `svc_console guest answered 403: Missing permission service.console` (org scope in IssueConsoleTokenCommand).
- (8) read token + can(): `Failed asserting that true is false.`

GREEN after the change: same command, 8 passed, 1 skipped (case 9 waits for TASK-0029).
Related suites green: PanelApiTest, AuthApiTest, ReadOnlyRoleTest, ServiceAccessShareTest, PayAndRestoreTest,
CreditOrderApprovalTest, StaffConsolePageTest, WebToolsFeatureTest, TenantIsolationSweepTest, EndpointSweepTest — 98/98.
Full gate (`.\brain.ps1 gate -Task TASK-0030`): Pint pass, Pest 1376 passed / 1 skipped (1377), build pass, Larastan 1 error
(`IssueConsoleTokenCommand::scope()` never returns null) → return type narrowed to `CommandScope`; Larastan rerun 0 errors,
focused tests 24/24 + 1 skipped, `.\brain.ps1 gate -Quick -Task TASK-0030` PASS (158/158 critical flows).

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

## Log

- 2026-09-25T18:20:01Z opened by `brain.ps1 task start`.
- 2026-09-25 onhost-security: WP-A implemented test-first (ApiTokenScopeMapTest red 8/8 → green 8/8, 1 skipped for TASK-0029); status IN_PROGRESS (WP-B open).

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

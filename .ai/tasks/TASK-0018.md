# TASK-0018: A test's credentials do not outlive the test

- **Status:** SELF_VERIFIED
- **Priority:** P1
- **Risk:** LOW
- **Owner:** onhost-qa
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0018-a-test-s-credentials-do-not-outlive-the`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0018-a-test-s-credentials-do-not-outlive-the`
- **Opened:** 2026-09-24T22:25:15Z

## Objective

A credential one test puts into the process environment ($_ENV, putenv()) is gone before the next test starts, so no test's result depends on which file ran before it.

## Context

Adopted from TASK-0002, an external session that left this fix uncommitted in the main checkout on `development` and is no longer running (lock released 2026-09-25). `.ai/baseline/baseline.json` carried `RenewalGuardTest` as a known ORDER-DEPENDENT failure: `CheckoutTest` set COMGATE_MERCHANT/COMGATE_SECRET and never removed them, so the `env` secrets driver made Comgate look configured to every later test.

## Scope (owned paths — locked)

- `tests/Pest.php`
- `tests/Feature/Orders/CheckoutTest.php`
- `.ai/baseline/baseline.json`
- `.ai/handoffs/TASK-0018.md`

## Out of scope

Refreshing the rest of the baseline (test counts, revision) - separate housekeeping.

## Dependencies

none

## Contracts affected

none (test harness only)

## Acceptance criteria

- [x] CheckoutTest then RenewalGuardTest in one process: 11/11 pass (before the fix: RenewalGuardTest:50 fails, "true is false").
- [x] One mechanism: a global beforeEach/afterEach in tests/Pest.php (Feature, Contract, Unit) restores $_ENV and putenv() keys; the per-file afterEach from TASK-0002 was dropped as redundant (the pair still passes without it).
- [x] baseline.json no longer lists RenewalGuardTest as a known failure.
- [x] Removed process keys are restored too (unit test, red on the first version).
- [ ] Full gate PASS.

## Required tests

Red/green proof by running the pair in the failing order (see acceptance). Critical flows touched: checkout, renewal - whole suite must stay green.

## Security considerations

None at runtime; tests only. Fewer leaked secrets between tests.

## Migration considerations

none

## Rollback

`git revert` of the fix commit.

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

- MEDIUM tests/Pest.php `restoreEnvironment` — a process key a test REMOVED (`putenv('X')`) was not put back (the loop only walked the post-test `getenv()`). Fixed: added keys unset, baseline keys re-set when missing or changed; `tests/Unit/TestEnvironmentRestoreTest.php` fails on the old version, passes now.
- LOW global afterEach runs before file-level afterEach hooks — no file-level env hook remains; accepted.
- LOW two `getenv()` copies per test — accepted (cheap).

## Log

- 2026-09-24T22:25:15Z opened by `brain.ps1 task start`.
- 2026-09-25 orchestrator: moved the TASK-0002 diff here byte-for-byte, proved red/green, dropped the redundant per-file hook, cleared the baseline entry.

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

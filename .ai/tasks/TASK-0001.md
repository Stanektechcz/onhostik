# TASK-0001: AI orchestration bootstrap

- **Status:** SELF_VERIFIED
- **Priority:** P2
- **Risk:** LOW (no application code, schema, route or config change)
- **Owner:** orchestrator (main session)
- **Reviewers:** onhost-reviewer, onhost-security (settings, scripts that run git/composer), human
- **Branch:** `chore/TASK-0001-ai-orchestration`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0001-ai-orchestration`
- **Opened:** 2026-09-23T21:36:00Z

## Objective

Give ONHOST a persistent, repository-backed multi-agent development system — baseline, knowledge layer, agents,
task/lock/worktree tooling, quality gate, review and integration rules — without changing application behaviour.

## Context

User request "ONHOST AI DEVELOPMENT TEAM" (2026-09-23). Existing Brain layer (`brain.ps1`, `scripts/ai`,
`docs/context`) and the unmerged `origin/chore/onhost-brain` were found and extended (`.ai/decisions/AI-0001`).
A second Claude session was committing in the main checkout at the same time, so all work happened in this worktree.

## Scope (owned paths — locked)

- `.ai/`, `.claude/`, `CLAUDE.md`, `AGENTS.md`, `brain.ps1`, `scripts/ai/`

## Out of scope

Any ONHOST feature work; fixing pre-existing test failures; merging `origin/chore/onhost-brain`; the Brain vault;
pushing, PRs, CI changes.

## Contracts affected

`brain.ps1`: two new commands (`gate`, `task`) and an optional trailing-argument parameter; existing commands unchanged.

## Acceptance criteria

- [x] Baseline recorded and machine-readable (`.ai/baseline/baseline.json`), pre-existing failures separated.
- [x] Knowledge files derived from the repository (`.ai/*.md`), linking to existing docs instead of copying.
- [x] Agents for the real stack with scope, allowed/forbidden work, checks, handoff, integration (`.claude/agents/onhost-*`).
- [x] Task, lock, handoff and integration protocols (`.ai/DEVELOPMENT_RULES.md`, `.ai/INTEGRATION_RULES.md`, template).
- [x] Worktree workflow implemented and exercised (`brain.ps1 task start|claim|release|board|finish`).
- [x] Deterministic quality gate from existing tooling with baseline comparison (`brain.ps1 gate`).
- [x] A new session can recover state (`CLAUDE.md` → `.ai/PROJECT_STATE.md` → board → task file).
- [x] No application behaviour changed, no secrets, no destructive Git, no production action.
- [ ] Independent review done (see Findings).
- [ ] Integrated into `development` (human decision).

## Required tests

`scripts/ai/tests/Orchestration.Tests.ps1`; `brain.ps1 gate -Quick` and full `brain.ps1 gate -Task TASK-0001`;
manual exercise of every `task` action (log below).

## Security considerations

`.claude/settings.json` only adds deny rules (secret paths unreadable, prototype files not editable); no permission
mode, no hooks. Scripts run `git`, `composer install` and `cmd mklink/rmdir` only inside worktrees they create;
`finish` removes a worktree only when clean and merged and removes the `node_modules` junction (not its target) first.

## Rollback

Revert the TASK-0001 commit(s); delete `..\onhost-worktrees\` and the main checkout's `.ai/state/` (untracked).

## Findings

- NOTE (pre-existing) `tests/Feature/Orders/CheckoutTest.php` leaves Comgate credentials in `$_ENV`/`putenv`, which
  fails `tests/Feature/Billing/RenewalGuardTest.php:50` when run after it. Proven in both orders; recorded in the
  baseline; follow-up task suggested.
- NOTE (pre-existing) `scripts/ai/tests/Config.Tests.ps1` fails on `development` (Brain branch config not merged).

## Log

- 2026-09-23 21:36 worktree created from `8e4614a`; `vendor` junction broke autoloading → real `composer install`.
- 2026-09-23 21:40 baseline: Pest 856/856, Larastan 0, Pint/build/audits clean, 59 migrations on scratch SQLite.
- 2026-09-23 21:48 first `gate -Quick`: RenewalGuardTest failure → proven order-dependent, pre-existing.
- 2026-09-23 21:53 board showed the peer session's WIP lock with 0 changes → it had committed `85e9cbf`; lock released.
- 2026-09-23 22:00 `task start` fixed for git stderr; `start` now commits the task file; `finish` refusals verified
  (dirty worktree, unmerged branch); self-test worktrees and branches removed.
- 2026-09-23 22:05 branch fast-forwarded to `85e9cbf`.

## Handoff

See `.ai/handoffs/TASK-0001.md`.

## Integration

Pending: independent review, full gate report `.ai/reports/TASK-0001-gate.md`, human approval.

# TASK-0026: Owner decisions 2026-09-25 recorded: ADR-0007, runbooks, vault

- **Status:** SELF_VERIFIED (part A; part B is the final docs wave after the other tasks merge)
- **Priority:** P2
- **Risk:** LOW
- **Owner:** onhost-docs
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0026-owner-decisions-2026-09-25-recorded-adr`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0026-owner-decisions-2026-09-25-recorded-adr`
- **Opened:** 2026-09-25T03:37:19Z

## Objective

Package docs-vault, part A: the owner's 25 decisions of 2026-09-25 are recorded once, with authority, in
`docs/adr/0007-owner-decisions-2026-09-25.md`. Each decision names its implementing task (TASK-0019 … TASK-0025), how
existing customers are protected, and the state of the code on 2026-09-25. Decision 22 gets an operator runbook, and
the ONHOST-BRAIN vault mirrors the decisions: card states, postponed cards and the metering plan.

## Context

- Source: `ROZHODNUTI-VLASTNIKA-2026-09-25.md` (the owner's answers, all recommendations accepted), the planner's spec
  `docs-vault.json` and the plan critic. Orchestrator adjustments override the spec:
  - decision 23 = pay-and-restore (TASK-0025), not "missing";
  - decision 22 = a customer-run import into a NEW platform-created site, with operator assistance; the historical
    resource is never bound or modified;
  - decision 21 = postponed, and TASK-0022 hides the public page;
  - decision 24 = postponed, and the draft products already exist;
  - decision 13: TASK-0022 owns the `ADMIN_CONFIGURATION_ARCHITECTURE.md` change.
- Base: `stack/base-0017-0019` (development + TASK-0019 + TASK-0017), so `backups.compute` and
  `onhost:backups:compute-plan` exist here.
- Out of scope (part A): `docs/context/CURRENT_STATE.md`, `docs/runbooks/production-readiness-audit.md`,
  `docs/runbooks/backups.md`, `docs/runbooks/approvals.md`, `docs/runbooks/security-boundaries.md`,
  `docs/runbooks/web-tools.md`, `docs/product/ADMIN_CONFIGURATION_ARCHITECTURE.md`. Their text is below under
  "Docs for integration".

## Scope (owned paths — locked)

- `docs/adr/0007-owner-decisions-2026-09-25.md`
- `.ai/DECISIONS.md`
- `docs/runbooks/historical-site-import.md`
- `docs/runbooks/README.md`

Outside Git (vault `C:\Users\medion\Desktop\ONHOST-BRAIN\ONHOST-BRAIN`, edited directly and not committed; the vault
does have its own `.git`):
`Hosting/OWNER-DECISIONS-2026-09-25.md` (new), `Hosting/README.md`, `Hosting/ASSESSMENT-2026-09-19.md`,
`Hosting/METERING-PLAN-2026-09-24.md` (was already untracked there before this task),
`Hosting/H01, H02, H03, H06, H07, H08, H09, H11, H12, H13, H14, H16, H17, H18, H23, H29, H30, H304, H428, H429.md`,
`System/CAPABILITY-COVERAGE.md` (regenerated by the vault's own `Update-CapabilityCoverage.ps1`).

## Out of scope

Any PHP, config, route, migration, seeder or surface change. The hot files and runbooks listed under Context belong to
the final docs wave (part B).

## Dependencies

TASK-0017 and TASK-0019 (base). ADR rows 1–21 and 23 describe pending work of TASK-0019 … TASK-0025.

## Contracts affected

None.

## Acceptance criteria

- [x] ADR-0007 in `docs/adr/` with a `**Status:** accepted` line that `Update-CodeMap.ps1` can read, one table row per
  decision 1–25, each row naming its implementing task, row 23 = TASK-0025, and row 22 pointing to the runbook.
- [x] `.ai/DECISIONS.md` indexes ADR-0007; the next number is 0008.
- [x] `docs/runbooks/historical-site-import.md` uses only existing tools, lists what must never happen to the
  historical resource and what is not available, and is listed in `docs/runbooks/README.md`.
- [x] Every repository path the ADR and the runbook cite exists; `ONHOST_UPLOAD_MAX_BYTES`, `ONHOST_FOUR_EYES` and
  `onhost:backups:compute-plan` exist in the code.
- [x] Vault: exactly the 16 P0 cards are `assessed` (H01 H02 H03 H06 H07 H08 H09 H11 H12 H13 H14 H16 H17 H18 H23 H29),
  each citing its assessment addendum and keeping "Aktuálně: neověřeno"; H30/H428/H429/H304 stay `proposed` with an
  owner-decision section; H06/H23 record the postponed OV/anti-DDoS; the hub note has rows 1–25 and the owner rules of
  2026-09-24; Hosting/README counts 770 cards; the metering plan names phase owners and the decided questions; wiki
  links in the touched notes resolve.

## Required tests

Documentation only, so no Pest test is added. The failing-first test is a read-only acceptance script, 50 checks:
`scratchpad/docs-vault-check.ps1` (session scratchpad, not committed). Existing suites run: the full Pest suite,
`scripts/ai/tests/Orchestration.Tests.ps1` (it asserts `.ai/DECISIONS.md` exists), and the vault coverage assertions
of `Test-ONHOST-Brain.ps1`.

## Security considerations

The runbook for decision 22 is the only text with a security side. It forbids binding or changing a historical
resource, requires an ownership check before anything is copied, and says there is no staff path that runs
`import.run` for a customer. No secrets, hostnames with credentials or customer data are in the docs.

## Migration considerations

None.

## Rollback

Revert the commit. The vault edits are plain file edits in the vault's working tree (`git -C <vault> diff` shows them).

## Findings

- NOTE `docs/runbooks/historical-site-import.md` — NOT VERIFIED: whether pairing the real domain works when the new
  site landed on the historical site's node (the guards refuse a same-named site on that node, and whether an alias
  domain collides was not tested). The runbook says to stop and escalate in that case.
- NOTE: the provider error texts ("adopting it is an explicit operator decision", `AaPanelWebProvider.php:99`,
  `IspConfigWebProvider.php:196`, `:590`) now have a procedure behind them. Rewording them is a code change and was
  left out.
- NOTE: the vault link `[[Project-Docs/adr/0007-owner-decisions-2026-09-25]]` and the runbook link resolve only after
  this branch reaches the main checkout (`Project-Docs` is a junction to `onhost-platform/docs`).
  `System/CODE-MAP.md` lists ADR-0007 only after `Update-CodeMap.ps1` runs on the merged main checkout.

## Log

- 2026-09-25T03:37:19Z opened by `brain.ps1 task start`.
- 2026-09-25 onhost-docs: part A written (ADR-0007, DECISIONS index, historical-site-import runbook and index row,
  vault). Acceptance script red (45 of 47 checks failed) → green (50/50). Status → SELF_VERIFIED (part A).

## Red / green evidence

- RED, before any edit: `powershell -File scratchpad/docs-vault-check.ps1` → exit 1, 47 checks ran and 45 failed. The 2
  that passed are the path-existence and wiki-link checks, which had nothing to check yet. The three "… exists in the
  code base" checks run only once the docs name the key.
- GREEN, after the edits: the same command → exit 0, "All checks passed." (50 checks).

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next — see the
final report of the docs-vault part A run. Short form:

- Outcome: decisions 1–25 are recorded (ADR-0007), decision 22 has a runbook, and the vault mirrors the decisions.
- Contracts: none.
- Next: part B after the other tasks merge (texts below).

## Docs for integration

Part B texts for the final docs wave (not applied here):

- `docs/context/CURRENT_STATE.md`. Replace the sentence "Adoption of a historical site is an explicit operator decision
  of its own (brain H304), never a side effect of an order." with: "A historical site is taken over only at its
  owner's explicit request, as a customer-run import into a NEW platform-created site with operator assistance
  (ADR-0007, decision 22; `docs/runbooks/historical-site-import.md`); the historical resource itself is never bound
  or modified, and never as a side effect of an order." Add a bullet: "Owner decisions of 2026-09-25 are recorded in
  ADR-0007 (25 decisions, each naming its implementing task TASK-0019 … TASK-0025); the 16 P0 vault cards are
  `assessed`."
- `docs/runbooks/production-readiness-audit.md`, a new row in the audit's style: "Rozhodnutí vlastníka 2026-09-25 |
  25 otevřených otázek (zálohy, ceník, měření, čtyři oči, identita, odstoupení, historické weby) blokovalo navazující
  práci | zapsáno v ADR-0007 s úkolem u každého bodu (TASK-0019 … TASK-0025); převzetí historického webu jen jako import
  do nového webu (`docs/runbooks/historical-site-import.md`); 16 karet P0 v trezoru `assessed` | dokumentace, bez
  změny kódu". Row 1: replace "rozhodnout frekvence záloh v tarifech" with "frekvence záloh = ceník (ADR-0007 bod 18);
  `1h` scheduler nezná → TASK-0024". Row 6: append "ADR-0007 bod 13: HIGH = step-up, čtyři oči jen CRITICAL a změny cen
  a tarifů (TASK-0022); výjimky jednotlivých operací zůstávají k rozhodnutí."
- `docs/runbooks/backups.md` (after TASK-0019/0024): the frequency is what the plan version sells (ADR-0007 §18),
  there is no lower platform default, and the `1h` gap is closed by TASK-0024's operator command. In the
  `backups.compute` section: "Owner decision (ADR-0007 §1): switch on once compute-plan lists no MISSING
  backup_storage." In "Still NOT built": PITR → "off the price list in a new plan version (ADR-0007 §2, TASK-0022)";
  mail `backup_days` → "decided: retention from backup_days (ADR-0007 §3, TASK-0024)".
- `docs/runbooks/approvals.md` (after TASK-0022): "Owner decision 2026-09-25 (ADR-0007 §13): HIGH is a fresh step-up
  and nothing more; CRITICAL and every price or plan change take the second person. With `ONHOST_FOUR_EYES=false`
  these are waived and audited like every critical action."
- `docs/runbooks/web-tools.md`, Import row: append "; a historical site (not created by ONhost) only via
  historical-site-import.md".

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

# TASK-0024: Backup operations: doctor rows, frequency as sold, mailbox retention

- **Status:** IN_PROGRESS
- **Priority:** P0
- **Risk:** HIGH
- **Owner:** onhost-provisioning
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0024-backup-operations-doctor-rows-frequency`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0024-backup-operations-doctor-rows-frequency`
- **Opened:** 2026-09-25T03:36:14Z

## Objective

What outcome the user gets, in one or two sentences.

### Package backup-ops (points 1 and 18)

Operators see in `onhost:doctor` whether server and database backups sold to customers are actually taken (rule state,
missing `backup_storage`, stalled/paused schedules of every family, undeletable and orphaned volumes, backup tick
liveness and budget), a retried server backup no longer leaves a second full VM copy behind, and plans can be backed up
as often and as long as sold behind the owner's switch `backups.as_sold` with a read-only dry run first.

## Context

Why now; links to the request, ADR, runbook, audit row, or failing test.

### Package backup-ops

Owner decisions 1 and 18 (2026-09-25); spec `scratchpad/specs/backup-ops.json` with the orchestrator's adjustments: no
legacy-marker branch (TASK-0019 unchanged), retention = frequency as sold for the generations + one backup a day for
`backup_days` behind the new default-off rule `backups.as_sold`, orphaned volumes report-only (never auto-deleted).
Base: `stack/base-0017-0019` (development + TASK-0019 + TASK-0017). Verified on the way: the spec's claim that a
`backup_unconfirmed` retry dumps again is wrong — that retry re-polls (the runner keeps `external_handle`); the real
double-dump path is a POST /vzdump whose answer is lost (5xx/timeout, retryable), which reruns the step from the top.

## Scope (owned paths — locked)

- `app/Console/Commands/BackupFrequencyPlan.php`
- `providers/Contracts/MailboxBackupRetention.php`
- `providers/IspConfig/IspConfigMailBackups.php`
- `domains/Services/Mail/MailboxBackupPolicy.php`
- `domains/Services/Mail/MailboxBackupRetentionPlan.php`
- `tests/Feature/Services/MailBackupRetentionTest.php`
- `tests/Feature/Platform/DoctorBackupOpsTest.php`
- backup-ops also added (new files): `domains/Services/Web/BackupOperationsCheck.php`, `domains/Services/Web/BackupDailyKeepers.php`,
  `tests/Feature/Services/BackupFrequencyAsSoldTest.php`, `tests/Feature/Services/ServerBackupRetryTest.php`
- backup-ops edited (minimal hunks): `app/Console/Commands/Doctor.php`, `domains/Services/Web/BackupScheduler.php`,
  `domains/Services/Commands/WebToolsCommandHandler.php`, `domains/Provisioning/AutomationLedger.php` (one rule),
  `domains/Provisioning/Workflows/ServiceActionWorkflow.php` (backup step), `docs/runbooks/backups.md`,
  `docs/runbooks/go-live-checklist.md`, `docs/runbooks/provider-onboarding.md`, `resources/views/admin/integrations.blade.php`

## Out of scope

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

Endpoints, events and payload keys, schema, config keys, provider calls — or "none".

backup-ops: new automation rule `backups.as_sold` (default_off, settings key `automation.enabled` as before); new
artisan command `onhost:backups:frequency-plan {--limit=500}` (read-only, auto-discovered); `backups.run` ledger stats
gain `seconds`; `backups.meta.orphan_volumes` (list of volids, report only); scheduler array gains `as_sold`. No
route, event, migration, config key or provider call added.

## Acceptance criteria

- [ ] …

backup-ops:
- [x] doctor counts stalled/paused schedules of every scheduled family (web, managed, mail, cloud, data)
- [x] doctor rows: compute rule state vs sold, instances without `backup_storage` (blocking only while `backups.compute` is on), `meta.delete_blocked`, `meta.orphan_volumes`, backup tick freshness/budget/errors, as-sold state, server coverage (only while `backups.compute` is on)
- [x] a server backup retried after a lost POST /vzdump answer adopts the first volume by the row's marker instead of dumping again; extra marked volumes go to `meta.orphan_volumes`, never deleted
- [x] `backups.as_sold` off: schedule, customer ceiling and prune byte-for-byte as before (tests assert it)
- [x] `backups.as_sold` on: `1h` → hourly (managed-woo, shop-growth), daily keepers for `backup_days` on web/managed
- [x] `onhost:backups:frequency-plan` lists sold/now/as-sold/history/extra storage and writes nothing
- [x] docs: backups.md (doctor rows, retry, as-sold table, operator go-live steps), go-live-checklist and provider-onboarding (`backup_storage`, not `pbs_datastore`), admin help text

## Required tests

Failing-first tests to write; existing tests that must stay green; critical flows touched (`.ai/baseline/baseline.json`).

### backup-ops — red / green evidence

RED (before the code, `php vendor/bin/pest` on the three new files, 27 tests: 3 passed = the guards, 7 failed, 17 errors):
- `DoctorBackupOpsTest`: every new row missing ("Expecting null not to be null") — compute rule state, delete_blocked,
  orphan, coverage, as-sold; `counts stalled and paused schedules of managed databases too` → `'OK'` instead of `'WARN'`
  (Doctor looked at web/managed/mail only).
- `BackupFrequencyAsSoldTest`: `BackupScheduler::AS_SOLD_RULE` / `normalizeFrequency()` undefined, command
  `onhost:backups:frequency-plan` does not exist.
- `ServerBackupRetryTest` (after re-aiming it at the real path, a lost POST /vzdump answer):
  `adopts the first attempt's volume…` and `records a second volume… as an orphan` → operation `PENDING` with "the panel
  reports the backup done, but its list holds no archive that was not there before" — the retry dumped a second time and
  could not adopt either volume. The first version of the test (unconfirmed after the task finished) passed against the
  old code: that retry re-polls, it never dumped twice — the spec's premise there was wrong, recorded in Context.
- Guards green from the start by design: `prunes to the generation cap exactly as before while the rule is off`,
  `a caller cannot switch the marker requirement off through the request`, `says nothing is wrong when no server is sold backups`.

GREEN: the three files 27/27 (107 assertions); related suites (ComputeBackupSchedule, ProxmoxContract,
BackupScheduleMisses, WebBackupArchive, LegalHoldRetention, ArchiveExpiry, AddonDelivery, WebToolsFeature,
AutomationSwitch, StaffConsole, ServiceAction, SafetyCopy, RestoreTest, DoctorCommand, OperationLatency,
PanelVersionGate, AuditBlock5t, AuditBlock5u, PlanPromises, PricingRules) 120/120; full Pest suite 1012/1012
(`vendor/bin/pest`, 581 s); Pint `--test` clean on every changed PHP file; Larastan 0 errors on the changed files.

## Security considerations

backup-ops: no route, permission or customer parameter added. The backup marker is still derived from the row id only
(never from `desired`/context; a test sends `backup_marker: ''` and `backup_legacy: true` and they are ignored). Orphaned
volumes are only recorded, never deleted: the orchestrator's rule "never auto-delete a Proxmox volume that is not provably
ours". Both behaviour changes for existing services (`1h` → hourly, daily keepers = more storage) sit behind the
default-off rule `backups.as_sold`. Open (not this package): `automation.toggle` is NORMAL risk, so switching a
default-off rule on needs no step-up (risk-policy package).

## Migration considerations

backup-ops: none (no migration; `meta.orphan_volumes` lives in the existing JSON column).

## Rollback

backup-ops: revert the commit. Operationally: switch `backups.as_sold` off — new hourly slots and daily keepers stop, nothing is deleted at the switch, surplus rows are pruned down to the generation cap over the next ticks (50 per tick). Doctor rows and the dry-run command are read-only.

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

## Log

- 2026-09-25T03:36:14Z opened by `brain.ps1 task start`.
- 2026-09-25 onhost-provisioning: package backup-ops implemented (doctor rows, retry adoption + orphan report, `backups.as_sold` + `onhost:backups:frequency-plan`, docs); package SELF_VERIFIED, task stays IN_PROGRESS (mail-backups package not started here).

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next.

### backup-ops

- From / To: onhost-provisioning → orchestrator · package backup-ops SELF_VERIFIED (task IN_PROGRESS).
- Outcome: see Objective/Acceptance above.
- Files: see Scope (backup-ops lines).
- Contracts: rule `backups.as_sold` (default_off); command `onhost:backups:frequency-plan`; ledger stat `seconds`;
  `backups.meta.orphan_volumes`. Hot-file hunks: AutomationLedger RULES +1 entry right after `backups.compute`;
  Doctor.php: two family lists → `BackupScheduler::SCHEDULED_FAMILIES` + a 3-line loop over `BackupOperationsCheck::rows()`.
- Verification — VERIFIED: tests and tools listed in the red/green section. ASSUMED: that PVE's storage listing returns
  `notes` (TASK-0019 assumption, unchanged); that "one backup a day for backup_days beyond the generations" is what the
  owner means by "Zálohy 30/90 dní" (orchestrator decision). NOT TESTED: real Proxmox; PostgreSQL (`whereIn` on the JSON
  path `entitlements->backup_frequency` in the doctor row — CI pest-postgres decides); tick duration on production.
- Concerns: (1) PlanPromises/MetricRegistry untouched: the `backup_days` reason "kept for web/managed" is fully true only
  with `backups.as_sold` on; the mail-backups package owns that entry. (2) A 15m plan still risks overlapping backups on
  one service and overrunning ticks (doctor now shows the overrun). (3) `automation.toggle` NORMAL risk (risk-policy).
- Next: mail-backups package on top of this branch; catalogue versions should spell `hourly` instead of `1h`.

#### Docs for integration

- `docs/runbooks/production-readiness-audit.md` (Czech, audit style), row text:
  "Zálohy serverů a DB — provoz (TASK-0024, backup-ops): `onhost:doctor` nově hlásí stav pravidla `backups.compute` proti
  prodaným zálohám, instance Proxmoxu bez `backup_storage` (blokující až při zapnutém pravidle), zastavené a pozastavené
  plány všech rodin (i `cloud`/`data`), zálohy, jejichž svazek nešlo smazat (`meta.delete_blocked`), osiřelé svazky
  (`meta.orphan_volumes`, jen hlášení, nic se nemaže) a běh `onhost:backups:run` (čerstvost, rozpočet 720 s, chyby).
  Opakovaná záloha po ztracené odpovědi na POST /vzdump převezme svazek s vlastní značkou místo druhého vzdumpu.
  Frekvence podle ceníku (`1h` = hodinově) a denní zálohy po dobu `backup_days` jen s pravidlem `backups.as_sold`
  (výchozí vypnuto); dopad ukáže `onhost:backups:frequency-plan` (nic nemění). Stav: hotovo v kódu, zapnutí je
  rozhodnutí vlastníka; neověřeno proti skutečnému Proxmoxu."
- `docs/context/CURRENT_STATE.md` bullet (English): "Backup operations (TASK-0024 backup-ops): the doctor reports
  server/database backup readiness (`backups.compute` vs sold, missing `backup_storage`, delete-blocked and orphaned
  volumes, stalled/paused schedules of every family, backup tick age/budget/errors); a retried Proxmox backup adopts its
  first marked volume instead of dumping twice (extra marked volumes are reported in `meta.orphan_volumes`, never
  deleted); plans are backed up as often and as long as sold (`1h` → hourly, one backup a day kept for `backup_days` on
  web/managed) only under the default-off rule `backups.as_sold`, reviewed first with `onhost:backups:frequency-plan`."
- `docs/runbooks/security-boundaries.md`: nothing new from this package.
- `docs/runbooks/web-tools.md`: the backup schedule ceiling reads `1h` as `hourly` only under `backups.as_sold`
  (`PUT /v1/services/{id}/backups/schedule`); otherwise unchanged.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

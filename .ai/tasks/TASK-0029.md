# TASK-0029: A service action asks for its own permission and risk

- **Status:** IN_PROGRESS
- **Priority:** P0
- **Risk:** HIGH
- **Owner:** onhost-security
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0029-a-service-action-asks-for-its-own-permis`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0029-a-service-action-asks-for-its-own-permis`
- **Opened:** 2026-09-25T18:19:16Z

## Objective

Every service action asks for its own permission and risk: `ServiceActionCommand::permissionFor()` has no default arm (an unknown action fails closed),
deleting a backup or snapshot asks for its own permission with a fresh step-up, actions that hand out a shell or root ask for
`service.console`, and every destructive action (DestructivePreview) is HIGH with step-up. The secondary gates (spec apply,
action hooks, Discord) ask the same map (WP2).

## Context

Onboarding audit verified 2026-09-25 (`.ai/audits/2026-09-25-onboarding-audit/response-verified-2026-09-25.md`):

- **C13** / **C13-H1** (CONFIRMED): 121 of 132 service actions fell through `permissionFor()` to `service.manage` at NORMAL risk;
  7 of the 13 DestructivePreview actions had no step-up; a `services:power` token passed.
- **C13-FM**: `backup.delete` is CRITICAL in the catalogue and was never asked. Interim decided (D29.2): the map plus HIGH + step-up
  for customers (no customer four-eyes; the catalogue rating stays as documentation).
- **C13-H1b** (CONFIRMED): a `svc_manage` guest (documented as without a shell) reached root/console via `access.reset`, `rescue.start`,
  `subuser.create` and command schedules.
- **C13-H1c** (CONFIRMED): `archive.restore` ran without step-up.

Orchestrator decisions D29.1–D29.10 and the critic adjustments 1–11 are the spec (handoff of the orchestrator, 2026-09-25).

## Scope (owned paths — locked)

- `domains/Services/Commands/ServiceActionCommand.php`
- `domains/Services/ServiceSpecService.php`
- `domains/Services/DestructivePreview.php`
- `domains/Services/CustomerActionParams.php`
- `domains/Integrations/ActionHookService.php`
- `domains/Integrations/DiscordService.php`
- `domains/Identity/Authorization/RoleCatalog.php`
- `domains/Identity/Authorization/PermissionCatalog.php`
- `tests/Feature/Services/ServiceActionPermissionMapTest.php`
- `tests/Feature/Services/ServiceActionRoleMatrixTest.php`
- `domains/Services/ServiceService.php` (minimal hunks: gbackup.delete / snapshot.delete / gbackup.lock protected-row refusals)
- `domains/Provisioning/Workflows/ServiceActionWorkflow.php` (minimal local hunks: run-time re-check of gbackup.delete / snapshot.delete)
- `domains/Support/Assistant/AssistantProposals.php`, `domains/Support/Assistant/AssistantScope.php` (staging.push out; mayRun fails closed)
- `tests/Feature/Compliance/LegalHoldRetentionTest.php`, `tests/Feature/Provisioning/WebFeatureActionsTest.php`, `tests/Feature/Services/MailBackupRetentionTest.php` and other existing tests that now need a step-up grant (no weakened assertion)

## Out of scope

- D29.8: wiring the family permissions `mail.manage`, `database.manage`, `apps.deploy`, `compute.vm.manage`, `service.credentials.rotate` into the day-to-day actions (follow-up; needs its own decision).
- Token scopes (`ApiContext::assertTokenScope`, `TokenScopes`): TASK-0030. `ServiceController.php`: TASK-0030 only.
- WP2 (this task): spec apply / action hooks / Discord gates, the `token_scope:<action>` skip, `RevokeDelegatedAccess` at console level.

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

No route, event, schema or config change. Behaviour of `POST /v1/services/{id}/actions`: new 403 `access_not_approved` (missing permission) and 403
`step_up_required` answers per the map; new 409 `backup_protected` for gbackup.delete / snapshot.delete / gbackup.lock unlock.
`GET /v1/services/{id}/actions/{action}/preview` with an unknown action now answers 422 `service_action_unknown` (via `permissionFor()`).

## Acceptance criteria

WP1 (done, see Log):
- [x] `ServiceActionCommand::PERMISSIONS` lists exactly `ServiceActionWorkflow::ACTIONS` (132); `permissionFor()` throws 422 `service_action_unknown` for anything else (owner and system alike, no operation).
- [x] backup.delete → `backup.delete`, gbackup.delete → `game.manage`, snapshot.delete → `compute.vm.delete`; all HIGH + step-up.
- [x] access.reset, rescue.start, subuser.create, command.*, shell.* → `service.console`; schedule.create is `service.console` unless every task is power/backup (fail closed).
- [x] Every DestructivePreview action is HIGH + step-up (archive.restore included); no service action is CRITICAL.
- [x] mailbox.backup_retention → `backup.policy.manage` (customers keep getting refused; `requiresApproval` when `allow_prune`).
- [x] gbackup.delete refuses a protected/final/legal-hold row at request (409 `backup_protected`) and again at run time; snapshot.delete and gbackup.lock unlock refuse a protected row.
- [x] staging.push is no longer an assistant proposal; `AssistantScope::mayRun()` answers false for an unknown action.
- [x] Role matrix: owner/org_admin keep everything, cloud_operator VM snapshot deletion, game_operator game backup deletion; developer, svc_manage/svc_console lose backup deletion; svc_manage loses root/rescue/sub-user/command schedules.

WP2:
- [ ] spec apply, action hooks and Discord ask `ServiceActionCommand::permissionFor()`; `token_scope:<action>` skip for console actions on token sessions; RevokeDelegatedAccess at console level.

## Required tests

- `tests/Feature/Services/ServiceActionPermissionMapTest.php` (sapm): two-way map, unknown action fails closed, every action list names real actions, bulk actions are plain manage, copies/console/schedule mapping, destructive = HIGH + step-up, nothing CRITICAL, no spec-reachable action takes four-eyes, retention is the operator's.
- `tests/Feature/Services/ServiceActionRoleMatrixTest.php` (sarm): customer + staff role matrix, svc_manage guest refused over HTTP, step-up for backup.delete / archive.restore, the operation re-checks `service.console` for a command schedule, protected game backups.
- Existing suites that must stay green: the full Pest suite; directly related: WebBackupArchiveTest, GameToolsFeatureTest, LegalHoldRetentionTest, WebFeatureActionsTest, MailBackupRetentionTest, AssistantProposalsTest, ServiceAccessShareTest.

## Security considerations

- The map has no default; unknown actions throw 422 before the authorizer decides (system actor included). Read paths (`ServiceFeatures::features`, `AssistantScope::mayRun`) only ever pass mapped actions or check first.
- Residual risk (not fixed here): `service.manage` (svc_manage, developer, power tokens) still reaches code execution as the site user through cron.create/update, file.save, node.create, file upload and deploy hooks. The console line covers shells, not code execution.
- Staff widening at bus level: `backup_dr_admin` holds `backup.delete`, `infrastructure_admin`/`backup_dr_admin` hold `backup.policy.manage`. Over the customer API they are still refused because `/v1/services/{id}` first asks `service.read` (proved in ServiceActionRoleMatrixTest).

## Migration considerations

## Rollback

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

## Log

- 2026-09-25T18:19:16Z opened by `brain.ps1 task start`.
- 2026-09-25 onhost-security WP1 RED: `php artisan test --compact --filter='ServiceActionPermissionMap|ServiceActionRoleMatrix'` → 16 tests, 1 passed, 11 failed, 4 errors (undefined `PERMISSIONS`/`STEP_UP`/`HIGH_RISK`; `permissionFor('no.such')` returned `service.manage`; backup.delete/access.reset/schedule.create(command)/mailbox.backup_retention mapped to `service.manage`; svc_manage guest's access.reset passed the bus and was refused only by the handler (422); owner backup.delete without step-up → 202; protected gbackup.delete → 202; command schedule operation `authorized_permission` = `service.manage`).
- 2026-09-25 onhost-security WP1 GREEN: same filter → 16/16 passed. Existing tests adapted (step-up grants, no weakened assertion): WebFeatureActionsTest (database.delete), LegalHoldRetentionTest (legal hold 423 still what is seen), MailBackupRetentionTest (bus refuses first: `access_not_approved` instead of `operator_only`; no operation, no panel call). Full suite `php artisan test --compact` → 1384/1384 passed. Pint clean on changed files; Larastan on the changed source files: 0 errors, no baseline change. Note: `DomainError::forbidden()` answers the slug `access_not_approved` (the spec said 'forbidden').
- 2026-09-25 onhost-security WP1: added the VM snapshot case to ServiceActionRoleMatrixTest after the full run (protected safety snapshot refused at request with 409 `backup_protected`, a snapshot protected before the run fails with 'protected' and no DELETE reaches Proxmox) → `--filter=ServiceActionRoleMatrix` 7/7. WP1 committed; WP2 open.

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

# TASK-0031: A VAT number that was given is checked, and reverse charge works

- **Status:** SELF_VERIFIED
- **Priority:** P0
- **Risk:** HIGH
- **Owner:** onhost-billing
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0031-a-vat-number-that-was-given-is-checked-a`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0031-a-vat-number-that-was-given-is-checked-a`
- **Opened:** 2026-09-25T18:20:59Z

## Objective

A VAT number a customer gave is checked in the EU register (VIES) and the answer is kept as evidence; reverse charge applies only to an EU business customer of another member state whose number VIES confirmed (at most 30 days ago), otherwise destination VAT with a review flag.

**WP A (this commit):** VIES behind a provider contract, the recorded check on organizations (migration 000880), when checks run (queue after the number is set/changed, operator command, default-off monthly re-check), the event `tax.vat_number.checked` with notifications, and four doctor rows. The tax decision itself (quote/checkout/renewal/invoice, review flag, staff override, partner self-billing) is WP B; public copy, runbook and provider doc are WP C.

**WP B (second commit):** the tax decision uses the recorded check. Every tax input is built by `VatStanding::taxCustomer()` (quote, cart, guest checkout, staff quote/assisted order, renewals, rated usage, domain renewal, SLA credit, marketplace, work offers, archive fee, wallet forecast, receipts, smoke order). Reverse charge applies only to a business of another EU state whose number was VIES-valid at most 30 days before the quote or issue, or when a staff override is in force. Otherwise the customer pays today's destination VAT, plus a `vat_review` flag when a VAT ID was given. The order and the document keep the check they were decided on: both VAT IDs and the consultation number are printed under the reverse-charge legend. Finance can override the status by hand (CRITICAL: step-up + four eyes, 1–30 days, evidence kept). Partner self-billing VAT comes from the same standing, at the `standard_rates` rate.

**WP C (third commit):** the public promise is true and written down. The knowledge-base article *Firemní faktury, DPH a kredit* (`faktury-dph`) keeps its promise word for word and gains one qualifier in cs and en (content data only: `database/seeders/data/prototype-content.json`): until the VAT ID is verified in VIES, the customer's country's VAT is charged. `VatPromiseTest` pins the text and that the code keeps it once VIES is on. New runbook `docs/runbooks/vat-and-vies.md`, new provider doc `docs/provider-adapters/vies.md` (+ README row), and the go-live row for tax/VIES.

## Context

`domains/Tax/ViesClient.php` had no caller, so `organizations.vat_status` never became `valid`: every EU B2B customer outside CZ who gave a VAT ID was charged OSS destination VAT with no review flag, while the public content promises automatic VIES checks and reverse charge. ViesClient also called HTTP from `domains/` (provider rule). Orchestrator decisions D31.1–D31.10 and the critic adjustments (no mass UPDATE in the migration, read-time normalisation, non-EU numbers never called invalid, neutral wording for CZ) are in the work-package brief.

## Scope (owned paths — locked)

- `domains/Tax`
- `providers/Vies`
- `providers/Contracts/VatNumberValidator.php`
- `domains/Organizations/OrganizationService.php`
- `domains/Partners/PartnerService.php`
- `domains/Orders/QuoteService.php`
- `app/Http/Controllers/Api/V1/CheckoutController.php`
- `app/Http/Controllers/Api/V1/CartController.php`
- `domains/Orders/Commands/OrdersCommandHandler.php`
- `app/Http/Controllers/Api/V1/Staff/CustomerController.php`
- `database/seeders/TaxRuleSeeder.php`
- `tests/Feature/Tax`
- `tests/Contract/ViesContractTest.php`
- `docs/runbooks/vat-and-vies.md`

## Out of scope

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

WP A:
- Provider contract `providers/Contracts/VatNumberValidator` (`check(countryCode, number, timeoutSeconds): VatCheckResult`, never throws); `providers/Vies/ViesVatNumberValidator` (POST `onhost.vies.endpoint`, body countryCode/vatNumber/requesterMemberStateCode/requesterNumber, provider_calls row with the answer withheld) and `DisabledVatNumberValidator` (switch off).
- Schema: migration `0001_01_01_000880_record_vat_number_checks` — organizations + vat_checked_at, vat_checked_number(20), vat_consultation_number(80), vat_validation_id(40), vat_status_source(12), vat_override_until, index (vat_status, country); vat_validations + status, reason, note, evidence, actor, requester_vat_id, country_code, expires_at. No data UPDATE.
- Command `tax.vat_number.record` (RecordVatCheckCommand, system actor only, NORMAL).
- Event `tax.vat_number.checked` (organization): result, previous, effective, changed, source, trigger, country, number_hint, consultation_number — never name/address/full number. Routed: invalid → billing contacts (portal note + mail `vat-number-invalid`, neutral wording for CZ), became valid → in-app note, source staff → finance inbox.
- Config `onhost.vies.*` (enabled=false default, endpoint, requester_vat_id, timeout_seconds, checkout_timeout_seconds, freshness_days 30, override_days 30, retry_after_minutes 10, recheck_after_days 25); env ONHOST_VIES_ENABLED, ONHOST_VIES_REQUESTER_VAT_ID, ONHOST_VIES_TIMEOUT, ONHOST_VIES_CHECKOUT_TIMEOUT.
- Console: `onhost:vat:verify` (dry run default, `--apply`), `onhost:vat:recheck` (rule `tax.vies_recheck`, default_off, daily 04:20).
- No route change in WP A.

WP B:
- The TaxEngine::calculate() signature is unchanged. The result gains `vat_review`, also stored in TaxCalculation.result. The optional input `vat_reason` (from VatStanding) flags a reverse charge that rests only on a legacy row.
- Quote `versions` gain `vat_review` and `vat` (VatStanding::snapshot). Order meta gains `vat_review` and `vat`.
- Invoice meta gains `vat_review`, from VatStanding::invoiceNeedsReview, the same predicate `onhost:vat:verify` uses for past documents.
- Invoice buyer snapshot: `vat_id` falls back to the DIČ; `vat_status` is the effective standing; new `vat_check`, which holds the quoted check when the order carried one.
- Invoice template: the buyer line prints `DIČ / VAT ID`.
  - Under the AE legend it prints `DIČ dodavatele … · DIČ odběratele … · ověřeno ve VIES <date> (č. <consultation>)`, or `registrace k DPH doložena mimo VIES` for a staff override.
  - The rate printer no longer turns a stored `0` into an empty string.
- Command `tax.vat_status.override` (OverrideVatStatusCommand): permission `billing.tax_rule.manage`, CRITICAL, step-up + approval, no new catalogue key. It publishes the event `tax.vat_number.checked` with `source: staff`.
- Route **POST /v1/staff/customers/{organization}/vat-status** (202) is the only route change. The orchestrator regenerates OpenAPI.
- Staff customer detail gains `vat` (snapshot + needs_check). The organization presenter's `billing.vat_status` is now the effective standing, and it gains `vat_checked_at` (additive).
- The partner self-billing snapshot gains `tax_category`, `note_vat` and `vat_check`. A missing standard rate returns 409 `tax_rate_missing` before anything is written.

## Acceptance criteria

WP A:
- [x] VIES is called only through the provider contract and the provider HTTP client; `domains/Tax/ViesClient.php` removed (and its phpstan-baseline entry).
- [x] A number given at registration, guest checkout or organization update is checked on the queue after commit (system actor, through the bus); the evidence is kept (vat_validations row, organization vat_checked_*), customer_class is never written by the check.
- [x] An unknown answer (outage, busy register, open breaker, connection failure) writes nothing and publishes nothing; the stored status is kept.
- [x] A malformed EU-prefixed number is recorded invalid without a call; a non-EU number or an organization outside the EU is never checked and never called invalid.
- [x] A result for a number that changed meanwhile is discarded; a recorded check from any actor but the system is refused.
- [x] `onhost:vat:verify` dry run makes no HTTP call and writes nothing, lists unchecked/stale/legacy organizations and past VAT invoices to EU B2B customers with a VAT ID (optionally CSV); `--apply` checks and records.
- [x] `tax.vies_recheck` is default_off; once on, it re-checks only VIES-valid numbers older than 25 days.
- [x] Doctor prints four non-blocking tax rows and makes no HTTP call.
- [x] VIES off by default; no live VIES call in any test (fakes + preventStrayRequests).
- [x] WP B: reverse charge applies only when a staff override is in force, or when the number is a VIES-valid number of another EU state checked ≤ 30 days before the quote or issue. Otherwise today's money applies (domestic / destination VAT), with `vat_review` when a VAT ID was given. This holds on cart quote, guest checkout, staff quote and assisted order, renewals and invoices.
- [x] WP B: the order and the document keep the check they were decided on. The invoice prints both VAT IDs and the consultation number under the reverse-charge legend. The UBL buyer tax scheme gets the DIČ fallback.
- [x] WP B: every `vat_status` reader goes through VatStanding (arch test; only VatStanding is excused). No `payer`, no `rates.<CC>`.
- [x] WP B: the staff override sits behind step-up + four eyes (step-up only with ONHOST_FOUR_EYES=false). It writes an evidence row, an audit record and an event, and lapses after 1–30 days. The controller only validates and dispatches.
- [x] WP B: partner self-billing VAT comes from the same standing, at `standard_rates`.
  - A missing rate is refused before any write.
  - Issued payouts are never rewritten.
  - PartnersTest and DevAccountSeeder use the real vocabulary.
- [x] WP B: CZ customers are unchanged. customer_class, WithdrawalPolicy, issued documents, *.dc.html, PermissionCatalog/RoleCatalog and ApiContext are untouched.
- [x] WP C: the public statements are true once `ONHOST_VIES_ENABLED=true`: the KB article (cs `DPH`, en `VAT`) keeps its promise and gains the one qualifier; `Onhost.dc.html` 5661/5670 ("we verify it at order time" = the sync check at quote) and `Onhost-app.dc.html` 11704 ("validated automatically" = the check on save) needed no change; every `*.dc.html` and `onhost-content.js` byte-identical (`git diff --stat -- apps/surfaces/` empty).
- [x] WP C: `docs/runbooks/vat-and-vies.md` and `docs/provider-adapters/vies.md` written (+ README row); go-live-checklist row 61 replaced.

## Required tests

WP A (new): `tests/Contract/ViesContractTest.php` (27 incl. dataset), `tests/Feature/Tax/VatNumberCheckTest.php` (13), `tests/Feature/Tax/VatVerifyCommandTest.php` (5), `tests/Feature/Tax/VatDoctorTest.php` (2). Existing that must stay green: EnvTemplateTest, AutomationSwitchTest, StaffConsoleTest (dynamic rule count), OutboxPayloadKeysTest, SecretsInLogsTest, organization/registration/guest-checkout suites — the whole suite.

### Red evidence (WP A)

The earlier attempt wrote tests and code before it was interrupted and recorded no red. Red was therefore observed afterwards by running the new tests against the base code: `git archive HEAD` (7480a79 = 052ceff + task file) extracted to a scratch folder with a copy of `vendor/`, the four new test files copied in, `php artisan test --compact tests/Contract/ViesContractTest.php tests/Feature/Tax` → **42 tests: 1 passed, 8 failed, 33 errors** (2026-09-25):
- 20 × `Target class [Onhost\Providers\Vies\ViesVatNumberValidator] does not exist` (contract tests), 2 × VatNumberChecks, 1 × VatNumberValidator, 2 × `Class VatNumber not found`, 1 × RecordVatCheckCommand not found;
- 4 × `The command "onhost:vat:verify" does not exist`; 2 × `table organizations has no column named vat_status_source`;
- behaviour failures: registration / guest checkout / AT company → `vat_status` expected `valid`, got `unknown` (nobody called VIES); invalid number → expected `invalid`, got `unknown`, no customer notification; number change → `vat_checked_number` null; doctor → no tax rows (`[]` instead of four);
- the one test that passed at base: "never checks a number from outside the EU and never calls it invalid" — true at base only because nothing was ever checked (a guard test, not a red one).
- NOT observed red: "asks nothing and keeps nothing for a number longer than the evidence columns" (added in this session together with its guard; at the uncommitted code before the guard it would have recorded a 22-character number — ASSUMED from reading).

### Green evidence (WP A)

- `php artisan test --compact tests/Contract/ViesContractTest.php tests/Feature/Tax` → 42 passed before this session’s fixes; after them the full suite below.
- `php artisan test --compact --parallel` → first run 1409/1410 (EnvTemplateTest: `ONHOST_VIES_CHECKOUT_TIMEOUT` missing from .env.example — fixed); second run **1411/1411 passed**.
- `vendor/bin/phpstan analyse` → first run 10 errors in the new code (one-line docblocks hid `@return Builder<Organization>`, datetime columns typed as string, nullsafe on the left of `??`, an always-false ternary) — fixed; then **0 errors**.
- `vendor/bin/pint --test` on the changed files → passed.

### Required tests (WP B)

New files:
- `tests/Feature/Tax/ReverseChargeTest.php` (13)
- `tests/Feature/Tax/TaxEngineVatReviewTest.php` (4)
- `tests/Feature/Tax/VatStatusOverrideTest.php` (3)
- `tests/Feature/Tax/VatStatusReadersArchTest.php` (3)
- `tests/Feature/Partners/PartnerSelfBillingVatTest.php` (6)

Changed: `tests/Feature/Partners/PartnersTest.php`. It now records the partner's DIČ as VIES-valid through RecordVatCheckCommand (helper `recordValidVatForTest`) instead of writing `payer`, and the 21 % / 605000 assertion is unchanged.

Existing suites that had to stay green without edits: TaxEngineTest, PaidForIsWhatYouGetTest, StaffTriggerStepUpTest and the whole suite.

### Red evidence (WP B)

The interrupted attempt wrote the tests together with the code and recorded no red. Red was observed afterwards against the WP A commit:
- **Method:** `git archive fabbd9d` was extracted to a scratch folder with a copy of `vendor/`. The five new test files and the changed PartnersTest were copied in. Then `php artisan test tests/Feature/Tax/{ReverseCharge,TaxEngineVatReview,VatStatusOverride,VatStatusReadersArch}Test.php tests/Feature/Partners/{PartnerSelfBillingVat,Partners}Test.php` was run (2026-09-26).
- **Result:** **29 tests: 3 passed, 14 failed, 12 errors.**

Behaviour failures at base:
- **Stale check:** a check older than 30 days still gave `0` instead of `19`, because the raw column was the verdict.
- **Unchecked number:** a number not checked yet was never asked about at the cart quote (`S` instead of `AE`).
- **Number of another country:** a valid number of another country than the organization gave `AE` instead of `S`.
- **Staff assisted quote:** made no VIES check (`S` instead of `AE`).
- **QuoteService with the organization:** a stale column gave `AE`.
- **Override route:** absent (3 × 404).
- **Arch test:** 16 files passed `'vat_status' => $x->vat_status`, and PartnerService checked `payer`.
- **Partner self-billing:** a VIES-valid CZ partner got 0.0 instead of 21.0 (twice), a missing rate was not refused, and PartnersTest in the real vocabulary got 0.0 instead of 21.0.
- **Missing keys:** 12 errors on `vat_review` / `tax_category` keys that did not exist (TaxEngine 4, ReverseCharge 6, partner 2).

Passed at base, as guard tests rather than red ones:
- "makes no HTTP call from the tax domain" (WP A had already removed ViesClient);
- "never rewrites a payout document already issued";
- PartnersTest "turns a reseller application into a partner".

Two tests were added in this session and run red against base the same way (2 of 2 red):
- The DIČ fallback in the buyer snapshot and UBL failed with `null` instead of `CZ12345678`.
- The staff customer detail had no `vat` block ("Trying to access array offset on null").

Not observed red separately: the tightening of the arch-test exclusion list to VatStanding alone. It is a stricter guard, green on the new code.

### Green evidence (WP B)

- `php artisan test --compact tests/Feature/Tax tests/Feature/Partners tests/Contract/ViesContractTest.php`: 75 passed, then 13/13 for ReverseChargeTest after the two additions.
- `php artisan test --compact --parallel` (PowerShell): **1440/1440 passed**. Run from Git Bash, the same suite reported 128 errors, all `getenv(): Argument #1 must be of type ?string, int given` in tests/Pest.php:61, the environment-restore helper. That is an environment artefact of the Git Bash process (a numeric env key), not a code failure: the identical code is green from PowerShell.
- `vendor/bin/phpstan analyse`: **0 errors**. `vendor/bin/pint` on the changed files fixed import order in 3 files, then clean.
- `.\brain.ps1 gate -Task TASK-0031`: **PASS** (Pint, Larastan 0, Pest 1440/1440, frontend build; .ai/reports/TASK-0031-gate.md).

### Required tests (WP C)

New: `tests/Feature/Tax/VatPromiseTest.php` (2): the article's VAT section in cs and en still starts with the promise word for word, contains `VIES` and ends with the qualifier; with `onhost.vies.enabled=true` the contract resolves to `ViesVatNumberValidator`, a DE b2b organization recorded valid is quoted `AE` 0 % without a VIES call, and an unverified one (VIES answering `MS_UNAVAILABLE`) is quoted `S` 19 % with `vat_review` and stays `unknown`, after exactly one call. Existing that must stay green: `tests/Feature/Content/ContentTest.php`, the whole suite.

### Red evidence (WP C)

Written first and run against 0adef5a (WP B) before the content change: `php artisan test --compact tests/Feature/Tax/VatPromiseTest.php` → **2 tests: 1 failed, 1 passed** (2026-09-26). The failure is the right one: the cs section ended with "…DIČ ověřujeme automaticky proti systému VIES." and not with the qualifier. The second test passed at 0adef5a as a guard: the behaviour it pins was built (and observed red) in WP A/B.

### Green evidence (WP C)

- `php artisan test --compact tests/Feature/Tax/VatPromiseTest.php tests/Feature/Content/ContentTest.php` → 4/4 passed.
- `php artisan test --compact tests/Feature/Tax tests/Feature/Content tests/Feature/Partners tests/Contract/ViesContractTest.php` → 81/81 passed.
- `.\brain.ps1 gate -Task TASK-0031` (PowerShell) → **PASS**: Pint passed, Larastan 0 errors, Pest **1442/1442**, frontend build ok.
- `git diff --stat -- apps/surfaces/` → empty; `prototype-content.json` still parses (JSON_THROW_ON_ERROR) and keeps LF line endings; the diff is two lines.

### Required tests (review round 1)

New: `tests/Feature/Tax/VatReviewRoundOneTest.php` (16 incl. datasets). Added to: `tests/Feature/Partners/PartnerSelfBillingVatTest.php` (+4), `tests/Feature/Tax/VatStatusOverrideTest.php` (+2), `tests/Feature/Tax/VatVerifyCommandTest.php` (+1), `tests/Feature/Tax/VatDoctorTest.php` (+1, and the green test now switches the rule on), `tests/Contract/ViesContractTest.php` (+1). Changed helpers: `vatRcCartQuote` (ReverseChargeTest) and `vatPromiseCartQuote` (VatPromiseTest) now assert that the customer's cart answer carries no `vat` / `vat_review` and read them from the stored quote.

### Red evidence (review round 1)

Every test was written before its fix and run against a2f6d7c (2026-09-26):
- `PartnerSelfBillingVatTest`: 10 tests, 7 passed, 1 failed, 2 errors — the S partner's payment posted `asset:bank:bank:CZK:credit 100000` and no `liability:vat` line (expected 121000 and 21000); `transfer` missing from the presenter (2 errors). The legacy-payer test passed: a guard (VatStanding::isVatPayer's legacy branch already worked, nothing pinned it).
- `VatReviewRoundOneTest` + `VatStatusOverrideTest` + `VatVerifyCommandTest`: 25 tests, 13 passed, 7 failed, 5 errors — the customer re-save turned an `invalid` override `valid` (VIES called); `check()` returned `valid` instead of `skipped` for vat_id_changed / checkout / recheck; the handler recorded (`recorded: true`) over the override; an override for a DE number still gave `staff_override` after the move to AT; the CSV wrote `=HYPERLINK(` raw; `name_mismatch` missing (5 errors). Passed as guards: the bearer-token refusal (2: `403 access_not_approved`, staff routes are closed to tokens before the bus), `--apply` skipping an override (VatVerify's candidate groups never included one), the double-queued job (1 call: the one-hour guard), both legacy groups listed and checked.
- `VatDoctorTest`: 3 tests, 1 failed — with VIES on and the rule off the re-check row was `OK`.
- `ViesContractTest --filter=quota`: 1 failed — the third call within a quota of 2 answered `valid` (no quota on bucket `vies`).
- Customer cart presenter: `ReverseChargeTest` + `VatPromiseTest` run with the HEAD CartController → 15 tests, 9 failed, all "Expecting […] not to have key 'vat'" (the new controller then restored).

### Green evidence (review round 1)

- `php artisan test --compact tests/Feature/Tax tests/Feature/Partners tests/Contract/ViesContractTest.php` → 101/101.
- `php artisan test --compact --parallel` (PowerShell) → **1465/1465**.
- `vendor/bin/pint --test` → passed. `vendor/bin/phpstan analyse` → **0 errors**, baseline unchanged. `git diff --stat -- apps/surfaces/ phpstan-baseline.neon` → empty.

### Required tests (review round 2)

New: `tests/Feature/Tax/VatReviewRoundTwoTest.php` (13): existing CZ partner listed (group `partner`) and S after `--apply`; doctor row (b) counts it; neutral self-billing wording + `vat_review` for an unchecked number; "není plátcem" kept for a checked-invalid number; CheckVatNumber queued on partner apply/approve; invalid override survives X→Y→X (VIES faked valid, preventStrayRequests, no call for X, quote S, snapshot source staff); the operator's check still replaces a number-bound override; 10 alternating PATCHes → 5 VIES calls; SLA credit note takes the credited line's AE/0 %, and today's decision without such a line; Czech invalid → info portal note, no mail; engine `vat_review` only for b2b; customer invoice API reduces `buyer.vat_check`, staff keep it.

### Red evidence (review round 2)

Written first and run against 07d0ad3 (2026-09-26): `php artisan test --compact tests/Feature/Tax/VatReviewRoundTwoTest.php` → **13 tests: 2 passed, 10 failed, 1 error**. Failures, each for the right reason: the dry run listed 0 organizations (the CZ partner missing); doctor row (b) `OK` instead of `WARN`; `note_vat` was "Dodavatel není plátcem DPH." for an unchecked partner; no CheckVatNumber pushed on apply; after X→Y→X the standing was `valid (fresh)` — VIES asked about X; 10 VIES requests instead of 5; the SLA credit line `S` instead of `AE`; the Czech note `warn` instead of `info`; `vat_review` true for a b2c buyer; the customer's `buyer.vat_check` carried number/status/reason/stored_status/override_until/name_mismatch. Error: `Undefined array key "vat_review"` in the self-billing snapshot. Passed as guards: the operator's check replacing an override (the row lookup must not shadow it) and the SLA fallback to today's decision.

### Green evidence (review round 2)

- `php artisan test --compact tests/Feature/Tax/VatReviewRoundTwoTest.php` → 13/13.
- `php artisan test --compact tests/Feature/Tax tests/Feature/Partners tests/Feature/Incidents tests/Feature/Notifications tests/Contract/ViesContractTest.php` → 146/146.
- `php artisan test --compact --parallel` → **1478/1478** (18883 assertions).
- `vendor/bin/pint --test` → passed. `vendor/bin/phpstan analyse` → **0 errors**; `git diff --stat -- phpstan-baseline.neon apps/surfaces/` → empty.

### Required tests (review round 3)

New: `tests/Feature/Tax/VatReviewRoundThreeTest.php` (8): CZ partner + VIES-valid SK number → not a payer, E 0 %, `vat_review` + `vat_review_reason: vat_country_mismatch`, transfer = net, no `liability:vat` posting; CZ partner + VIES-valid CZ DIČ that VIES registers to another trader → E 0 %, `name_mismatch`, transfer = net; the same with a staff override to valid → S at `standard_rates.CZ` 21 %, transfer 121000, input VAT 21000; SK partner + valid SK number (own name) → AE, no VAT transferred; `standing()` and `payerStanding()` give the same `vat_country_mismatch`; the partner's view of a payout (presenter and `POST`/`GET /v1/partner/payouts`) has no `vat_review` / `vat_review_reason` and `vat_check` reduced to `checked_at`, `consultation_number`, `source`, while staff (`PartnerPresenters::payout(…, true)`, `GET /v1/staff/partners/payouts`) keep them; a partner whose override ended is told "Registrace dodavatele k DPH neověřena." (review, `unknown`), not "Dodavatel není plátcem DPH.". Unchanged and green: the legacy `payer` row (source NULL) test in `PartnerSelfBillingVatTest`, `PartnersTest`, `VatReviewRoundOneTest`/`VatReviewRoundTwoTest` (exact `standing()` shape), the cart-quote helpers that assert no `vat` / `vat_review` in the customer's answer.

### Red evidence (review round 3)

Written first and run against `1beb127` (the six changed source files swapped back to their HEAD content, then restored) on 2026-09-26: `php artisan test --compact tests/Feature/Tax/VatReviewRoundThreeTest.php` → **8 tests: 1 passed, 4 failed, 3 errors**.
- Failed for the right reason: `VatStanding::isVatPayer()` was `true` for the CZ partner with a valid SK number and for the CZ partner whose DIČ VIES registers to another trader (so `selfBillingVat` billed S 21 % and `markPayoutPaid` would transfer 121000 — by reading; the test stops at the first assertion); the partner's own payout (presenter and `POST /v1/partner/payouts`) carried `vat_review`.
- Errors: `Undefined array key "vat_review_reason"` (the override case, already 21 % at HEAD — the key is new) and 2 × `VatStanding::payerStanding()` undefined.
- The expired-override test, re-run at HEAD without its `payerStanding` line: failed with `'Dodavatel není plátcem DPH.'` instead of `'Registrace dodavatele k DPH neověřena.'`.
- Passed at HEAD, a guard: the SK partner with a valid SK number was already AE with no VAT transferred.

### Green evidence (review round 3)

- `php artisan test --compact tests/Feature/Tax/VatReviewRoundThreeTest.php` → 8/8.
- `php artisan test --compact tests/Feature/Tax tests/Feature/Partners tests/Feature/Incidents tests/Feature/Notifications tests/Contract/ViesContractTest.php` → 154/154.
- `vendor/bin/pint --test` → passed. `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` → **0 errors** (one error on the way: `PartnerPayout::$self_billing` was inferred `string`; fixed with an `@property array<string,mixed>|null` on the model, no baseline entry); `git diff --stat -- phpstan-baseline.neon apps/surfaces/` → empty.
- `php artisan test --compact` (PowerShell, serial) → **1486/1486 passed** (18938 assertions).

### Required tests (closing review, fix round 2)

New: `tests/Feature/Tax/VatClosingReviewTest.php` (7): a CZ partner with a real company's DIČ and that company's VIES name as its own name before the check → `payerStanding` `identity_unconfirmed`, E 0 %, `vat_review_reason: identity_unconfirmed`, transfer = net, no `liability:vat`; the same when the partner renames itself to the VIES name after the check (`OrganizationService::update`); finance's confirmation (override to valid) outlives the override: after 31 days the dry run lists the partner in group `partner`, the operator's check restores `fresh`, S 21 %, transfer 121000, input VAT 21000; a genuine name difference finance confirmed stays accepted after the override ends and VIES re-confirms; a rename after the confirmation → `identity_changed`, E; `onhost:vat:verify` lists a VIES-valid unconfirmed partner in group `supplier_identity` and `--apply` skips it (no HTTP); an SK partner with its own valid number stays AE without a confirmation. Changed (the old rule "VIES alone pays VAT out" encoded): `PartnerSelfBillingVatTest` (helper `vatPartnerRecordConfirmedPayer`: override, then the operator's VIES check, for the 4 CZ S cases), `PartnersTest` (`recordValidVatForTest` confirms first; 21 % / 605000 unchanged), `VatReviewRoundTwoTest` (the `--apply` test now asserts `identity_unconfirmed` after the check and S after finance's override).

### Red evidence (closing review, fix round 2)

Written first and run against `f7ceb69` (HEAD, no source change yet) on 2026-09-26: `php artisan test --compact tests/Feature/Tax/VatClosingReviewTest.php` → **7 tests: 1 passed, 6 failed**, each for the right reason:
- before-the-check and after-the-rename: `payerStanding` was `{payer: true, reason: fresh}` (so S 21 %, 121 % transferred) instead of `identity_unconfirmed`;
- override ended: the dry run did not contain `partner` (the partner was not listed, so nothing could re-check it);
- genuine name difference after the override: `{payer: false, reason: name_mismatch}` instead of `fresh`;
- rename after the confirmation: `{payer: true, reason: fresh}` instead of `identity_changed`;
- the dry run did not contain `supplier_identity`.
- Passed at HEAD, a guard: the SK partner under reverse charge.

After the fix, the three existing tests that encoded the old rule failed as expected (6 cases: PartnerSelfBillingVatTest 4, PartnersTest 1, VatReviewRoundTwoTest 1 — 0.0 instead of 21.0 / no input VAT) and were changed to add finance's confirmation (see above).

### Green evidence (closing review, fix round 2)

- `php artisan test --compact tests/Feature/Tax tests/Feature/Partners tests/Feature/Incidents tests/Feature/Notifications tests/Contract/ViesContractTest.php` → **161/161**.
- DevAccountSeeder: a scratch test (not committed) seeded Catalog/TaxRule/LegalEntity/DevAccount and asserted the dev partner reads `{payer: true, reason: fresh}`: passed.
- `vendor/bin/pint --test` → passed. `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` → **0 errors**; baseline untouched.
- `php artisan test --compact` (PowerShell, serial) → **1493/1493 passed** (18976 assertions).

## Security considerations

WP A: RecordVatCheckCommand is system-only (a customer or staff actor is refused 403 `system_only`), so nobody can declare their own number valid; it carries no permission and NORMAL risk because it only records what the register answered. The VIES answer names the trader: `secretResponse` keeps it out of provider_calls, the name/address are stripped from the command audit (AUDIT_STRIP) and never enter the event (only country + last three characters). The endpoint comes from config, not from users. No HTTP call inside a bus transaction (row lock never waits for VIES). Needs onhost-security and onhost-qa review (money + outbound HTTP).

## Migration considerations

000880 adds nullable columns and one index; no data UPDATE (critic item 1: a mass write to existing customer rows). Old code ignores the columns. Legacy rows (vat_status_source NULL) keep today’s money: stored `valid` stays effective valid with reason `legacy_unverified`, stored `payer` stays a VAT payer for self-billing, any other non-vocabulary value reads as `unknown` — until `onhost:vat:verify --apply`. Width note: `vat_checked_number` and `vat_validations.vat_id` are 20; a 19–20 digit unprefixed number (22 with the country) is skipped, never stored.

## Rollback

WP A: set `ONHOST_VIES_ENABLED=false` (the default) — no check runs and every number is charged as before; revert the commit and roll back 000880 (`down()` drops the added columns and the index). Recorded vat_validations rows are evidence and harmless to keep.

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

- NOTE (WP A self-review) domains/Tax/VatNumberChecks.php — an unprefixed 19–20 digit number becomes 21–22 characters with the country prefix and would not fit vat_validations.vat_id(20) on PostgreSQL — now skipped (no call, nothing stored) with a test.
- RESOLVED in WP B (the item below): every reader now goes through VatStanding, PartnerService uses the standing and `standard_rates`, and DevAccountSeeder writes the real vocabulary.
- NOTE (WP B self-review) resources/views/invoices/invoice.blade.php: `rtrim(rtrim('0','0'),'.')` printed a reverse-charge rate as " %" and a qty/rate of `10` as `1`. It now trims only after a decimal point.
- NOTE (WP B self-review) domains/Tax/VatStanding.php: a staff override that ended used to fall through to the check branch with `vat_checked_number` set and could read as a "fresh" VIES check. It now reads `unknown` (reason `override_expired`), and `isVatPayer` ignores an ended override.
- NOTE (WP B) the customer's own cart quote response carries `versions.vat_review` and `versions.vat`, as required by acceptance test (a). The existing `tax_review_required` / `tax_reasons` were already there. The reviewer should confirm this matches "vat_review is staff-only": no customer notification is sent for it. — SETTLED (review rounds 1 and 3): the cart answer drops `vat` / `vat_review` since round 1; round 3 found the last customer response carrying the raw flag (the partner's own payout) and removed it there too. See Review round 3.
- (history) OPEN for WP B: every raw `vat_status` reader (QuoteService ~125/336, CartController ~69, CheckoutController ~106 incl. `refreshBeforeQuote` after the transaction, OrdersCommandHandler ~120, Staff/CustomerController ~193, PlanChangeService ~116, RatingService, SubscriptionService, InvoiceService ~94/540, DomainService, SlaService, MarketplaceService, WorkOfferService, WalletForecast, ServiceArchiveService, SmokeOrder) still passes the stored column; PartnerService ~757 still checks `payer` and `rates.<CC>`; DevAccountSeeder:68 still writes `payer` (honoured as legacy by VatStanding::isVatPayer).
- RESOLVED in WP C: docs/provider-adapters/vies.md and docs/runbooks/vat-and-vies.md are written.
- NOTE (WP C) apps/surfaces/onhost-content.js:243 was NOT edited, although the brief said to keep it consistent with prototype-content.json: it is a protected prototype surface (AGENTS.md rule 6; `.claude/settings.json` denies Edit/Write on `apps/surfaces/onhost-*.js`). The export script reads the external prototype (`../../../..` from `database/seeders/data`), not this copy. A re-export that drops the qualifier fails VatPromiseTest; the owner of the prototype should add the same sentence there.
- NOTE (WP C) providers/Vies/ViesVatNumberValidator.php names bucket `vies`, but nothing calls `configureBucket('vies', …)`, so no quota applies; VIES's own concurrency limits answer as `*_MAX_CONCURRENT_REQ` (unknown, retryable) and the operator command pauses 500 ms between calls. Documented in vies.md; a follow-up may add a configured quota. (Superseded in review round 1: the quota exists now.)

Review round 1 (onhost-reviewer billing, onhost-security, onhost-qa):

- HIGH domains/Partners/PartnerService.php:337 — markPayoutPaid posted and paid only the net while the S self-billing document says net + 21 % (the partner owes VAT on money never received; no input VAT in the ledger) — FIXED: the payment is the document's total (`PartnerPayout::transferAmount()`, read from the snapshot); postings: commission expense = net (debit), `liability:vat` = VAT (debit, input VAT), bank/offset = total (credit); AE/E unchanged; presenter, event and audit gain `transfer`. Tests: PartnerSelfBillingVatTest "pays a VAT-payer partner the total…" (red) + AE/E dataset.
- HIGH domains/Tax/VatNumberChecks.php:43, domains/Tax/Commands/RecordVatCheckHandler.php:53 — a customer re-saving the same vat_id undid a CRITICAL four-eyes override (queued check, VIES valid, source vies) — FIXED: check() skips every trigger but `operator` while `staff_override` is in force; the handler refuses the same case (`recorded:false, reason:staff_override`) as a second line; an operator's replacement publishes `previous_source: staff`. Tests: VatReviewRoundOneTest (customer PATCH under Http::preventStrayRequests, assertNothingSent; three triggers; handler refusal; red).
- MEDIUM domains/Tax/VatStanding.php:216, domains/Tax/TaxEngine.php:92 — anyone could get reverse charge on somebody else's valid number; the VIES trader name was stored but never compared — FIXED: `VatStanding::nameMismatch()` compares the disclosed VIES name with organizations.name (case, accents, punctuation, legal forms and joining words set aside; subset or run-together match); the verdict stays, `vat_review` + reason `name_mismatch` on the quote/order (TaxCalculation), `vat_check.name_mismatch` → invoice `vat_review`; `onhost:vat:verify` lists group `name_mismatch` (not re-asked). Go-live item for the accountant in the handoff. Tests: VatReviewRoundOneTest (red; no flag for the same name written otherwise, or undisclosed).
- MEDIUM domains/Tax/VatHealth.php:49, docs/runbooks/vat-and-vies.md (Switching it on, step 2), docs/runbooks/go-live-checklist.md (row 61) — renewals judge freshness at issue but never trigger a check, and tax.vies_recheck is default_off: verified customers lose reverse charge at the first renewal more than 30 days later — FIXED both ways the reviewer offered: the runbook and the go-live row make switching on `tax.vies_recheck` a required step together with ONHOST_VIES_ENABLED, and the doctor row is red whenever VIES is on and the rule is off (before anybody lapses). The rule itself stays default_off (owner rule: no behaviour reaching existing customers without a switch). Orchestrator: record the accepted coupling in DECISIONS (text in the handoff). Test: VatDoctorTest (red).
- MEDIUM app/Console/Commands/VatVerify.php:200 — the accountant's CSV wrote the customer-typed buyer name / VAT ID raw (formula injection) — FIXED: every cell goes through the rule of Staff\CustomerController::csvCell (leading `=+-@` → apostrophe; the rule is copied, the controller's helper is private and outside this change). Test: VatReviewRoundOneTest `=HYPERLINK(` + `+DE…` (red).
- HIGH tests/Feature/Partners/PartnerSelfBillingVatTest.php (VatStanding::isVatPayer legacy branch) — no test pinned that a legacy `payer` row (source NULL) keeps self-billing VAT — ADDED "keeps VAT on a legacy payer row with no source until the operator re-checks it" (S, 21 %, total 121000, snapshot source null / stored payer). Green at a2f6d7c: a guard, not a bug. The reviewer's sketch asserted `vat_check.reason = legacy_unverified`; that reason belongs to a stored `valid` — a stored `payer` reads `stored_unknown` for the tax decision (never reverse charge) and counts as a payer only for self-billing — so the test asserts the real snapshot.
- MEDIUM app/Http/Controllers/Api/V1/Staff/CustomerController.php:241 — no test that a bearer token (with the tax-rule scope) cannot dispatch the CRITICAL override — ADDED to VatStatusOverrideTest, datasets four eyes / one operator, a token with every scope (`*`) and the owner's fresh step-up: 403 and nothing written. Green at a2f6d7c: today staff routes answer `access_not_approved` to any token before the bus; the test asserts only the refusal so TASK-0030's scope map cannot silently open it.
- MEDIUM domains/Tax/Jobs/CheckVatNumber.php:48 — no test for the same number queued twice — ADDED (VatReviewRoundOneTest: two dispatchSync → Http::assertSentCount(1), one vat_validations row; green, a guard). Truly concurrent jobs before either records may still ask twice; documented in vat-and-vies.md as accepted (bounded by the new quota; no lock around an 8-second HTTP call).
- LOW domains/Tax/Commands/RecordVatCheckHandler.php:53 — a later VIES verdict always cleared an override — FIXED with the HIGH above (only `operator` replaces it, event `previous_source`). The reviewer's first remedy already held: `onhost:vat:verify --apply` never lists an organization under an override (its groups legacy/stale/unchecked exclude it) and `VatHealth::staleValid` filters `vat_status_source = vies`; pinned by the test "leaves an organization under a staff override out of the operator's --apply".
- LOW app/Http/Controllers/Api/V1/CartController.php:80 (reported at QuoteService.php:368) — the customer's cart quote exposed `versions.vat_review` and the full VatStanding snapshot — FIXED: the cart answer drops `vat` and `vat_review` (and the `name_mismatch` reason from `tax_reasons`); the stored quote, the order, the invoice and staff keep them. The test helpers in ReverseChargeTest/VatPromiseTest assert the absence (red with the old controller) and read the stored quote.
- LOW domains/Tax/VatStanding.php:63 — the staff_override branch skipped the country check: a DE-number override reverse-charged an AT address — FIXED: an override to valid counts only while the overridden number is the current one and its country is the organization's (`vat_country_mismatch` otherwise). Test: VatReviewRoundOneTest (customer PATCH country AT; red).
- LOW providers/Vies/ViesVatNumberValidator.php:45 — bucket `vies` had no quota (abuse → IP_BLOCKED/VAT_BLOCKED, platform-wide loss of reverse charge) — FIXED: the adapter configures the bucket with `onhost.vies.per_minute` (ONHOST_VIES_PER_MINUTE, default 150 — above the 120/min of the operator commands at 500 ms); beyond it RATE_LIMIT → unknown, retryable. The go-live row names it. A per-IP limit on guest checks is not added (the guest checkout already sits behind throttle:auth); recorded as a follow-up. Test: ViesContractTest quota (red).
- LOW app/Console/Commands/VatVerify.php, tests/Feature/Tax/VatVerifyCommandTest.php — no test with a legacy valid and a legacy payer row together — ADDED: both listed distinctly (`valid (legacy_unverified)` / reverse charge, `domestic · self-billing VAT payer`), `--apply` asks about both (2 calls). Green: a guard.

Review round 2 (onhost-reviewer billing, onhost-security):

- HIGH app/Console/Commands/VatVerify.php:140, domains/Tax/VatStanding.php:278, domains/Tax/VatHealth.php:90, domains/Partners/PartnerService.php:800/811 — D31.6 never reached an existing Czech partner: needsCheck skips the supplier country, so an unchecked CZ partner with a DIČ was not listed, not checked by --apply, not counted by the doctor, and its self-billing document said "Dodavatel není plátcem DPH." — FIXED: `VatStanding::payerUnverified()` (well-formed EU number, CZ included, not a payer, no recorded check for the current number, no override); `onhost:vat:verify` group `partner` (partner not closed) listed in the dry run and checked on --apply (operator only — owner rule); doctor row (b) counts `P partner(s)` and is WARN while any; PartnerService queues CheckVatNumber on apply/approve (VIES on, within the budget); selfBillingVat keeps E/0 % but says "Registrace dodavatele k DPH neověřena." with snapshot `vat_review: true` for such a number, and "Dodavatel není plátcem DPH." only when no number or a check said so. Tests: VatReviewRoundTwoTest (5, red).
- HIGH domains/Tax/VatStanding.php:72/294, domains/Organizations/OrganizationService.php:317 — a customer could undo a four-eyes `invalid` override in two saves (X→Y→X wipes the row, the queued check asks VIES, "valid") — FIXED: the override belongs to the number. When the row holds no recorded check of the current number, `standing()` looks at the newest vat_validations row for (organization, number); an unexpired `staff` row is in force (`staff_override`, with the same country rule). VatNumberChecks (non-operator) and RecordVatCheckHandler read that standing, so nothing is asked or recorded; queueVatCheck does not even queue. `snapshot()` reports source staff and the row's end (the invoice prints "doložena mimo VIES"). An operator's later verdict is the newest row and the row's recorded check, so it still replaces the override. Tests: VatReviewRoundTwoTest X→Y→X (red) + operator replacement (guard).
- MEDIUM domains/Incidents/SlaService.php:405/436 — an SLA credit note's tax came from today's standing, so a reverse-charged month could be credited with 19–21 % negative VAT once the check lapsed — FIXED here (minimal): `creditedTax()` takes category and rate from the issued invoice/statement line of the service whose period covers the incident start (same currency, positive net); only without such a line does today's decision apply. Tests: VatReviewRoundTwoTest AE credit (red) + fallback (guard). Runbook: "Credits follow the supply they correct".
- MEDIUM domains/Tax/VatNumberChecks.php:67/111 — one customer alternating numbers could use up the global VIES bucket (150/min) — FIXED: per-organization budget `onhost.vies.per_organization_per_hour` = 5 (RateLimiter, key per organization, every trigger but `operator`); above it outcome `limited` (tallied unknown, the job does not retry) and OrganizationService/PartnerService queue nothing. Test: 10 alternating PATCHes → `Http::assertSentCount(5)` (red: 10). Guest checkout per-IP limit stays the recorded follow-up.
- LOW domains/Notifications/NotificationRouter.php:419 — a Czech organization whose DIČ is (rightly) not in VIES got a warn mail inviting a manual check — FIXED: supplier-country organizations get an info portal note only, "DIČ … není v registru plátců DPH (VIES). Pokud jste plátce DPH, zkontrolujte ho ve fakturačních údajích." (Lexicon en added); no mail. Kept a customer note (not internal only) because a Czech partner's self-billing VAT depends on it. Events catalog wording updated. Test (red).
- LOW domains/Tax/TaxEngine.php:103 — engine `vat_review` fired for any class while VatStanding::invoiceNeedsReview requires b2b — FIXED: guarded with `$class === 'b2b'`. Test (red).
- LOW app/Http/Presenters/Presenters.php:234/254 — the customer's invoice API returned the whole `buyer.vat_check` (name_mismatch, reason, stored status) — FIXED: the customer presenter reduces it to `checked_at`, `consultation_number`, `source` (staff | vies | null) — what the PDF prints; the staff customer detail passes `forStaff: true` and keeps the full snapshot. Test (red), staff side asserted too.

Review round 3 (onhost-security HIGH; onhost-reviewer billing rated the same defect MEDIUM):

- HIGH domains/Tax/VatStanding.php:180-193 (isVatPayer), domains/Partners/PartnerService.php:782-806 (selfBillingVat), domains/Partners/Models/PartnerPayout.php (transferAmount) — isVatPayer took a VIES `valid` for the current number as proof of VAT-payer status without the two rules standing() applies to customers: the number's country must be the organization's, and a number VIES registers to another trader is not proof. A partner controls its own name, country, dic and vat_id (OrganizationService::update queues the check), so it could type a real Czech company's DIČ, or keep a valid SK/DE number with a CZ address, and be billed at standard_rates.CZ with 21 % transferred on markPayoutPaid and booked as input VAT the tax office would deny. New exposure (every document was 0 % before TASK-0031). — FIXED: the acceptance rules live in one private helper, `VatStanding::verdict()` (override bound to its number and country, legacy rows, number checked = current number, country match, freshness as a parameter, and the VIES trader name → `name_mismatch`); `standing()`, `taxCustomer()`, `snapshot()` and the new `payerStanding()` all read it — `isVatPayer()` is `payerStanding()['payer']`, no copy. A partner is a payer only on a staff override to valid, a legacy `payer` row (source NULL — the critic's rule, unchanged), or a VIES answer about its current number of its own country whose trader name matches; a registration does not lapse in a month, so for the payer the window is off (reason `registered` for an older check). Not accepted → the self-billing document is E 0 % with note "Registrace dodavatele k DPH neověřena.", `vat_review: true` and new `vat_review_reason` (`vat_country_mismatch`, `name_mismatch`, or `unknown` for a well-formed number nothing proved either way — this now includes an ended override, which used to print "Dodavatel není plátcem DPH."); `transferAmount()` reads the document, so nothing is transferred or booked as VAT. "Dodavatel není plátcem DPH." only without a number or when a check of the number (or staff) said invalid. A genuine name difference is accepted only through the existing CRITICAL override. A partner of another member state with a valid number of its own country stays AE (reverse charge, no VAT paid out), as a customer is. Customers are unchanged: a name mismatch keeps the reverse charge under `vat_review` (round 1 decision) — the asymmetry is deliberate and written on payerStanding(): a customer's reverse charge only moves the tax to the buyer, a partner's VAT is cash ONhost pays and deducts. `onhost:vat:verify` group `name_mismatch` now reads the same helper (snapshot) instead of its own expression. Issued self-billing documents and payouts are never rewritten (existing test). Tests: VatReviewRoundThreeTest (red 4 failed + 3 errors, 1 guard).
- QUESTION (critic: "vat_review is staff-only") app/Http/Controllers/Api/V1/CartController.php:80, domains/Partners/PartnerPresenters.php:24 — the customer quote carried `versions.vat_review`. — SETTLED: the cart answer has dropped `vat` / `vat_review` (and the `name_mismatch` reason) since review round 1 and keeps the customer-facing explanation (`tax_reasons`, the line category/rate/note); no customer UI reads `vat_review` (grep of apps/ and resources/: none), so nothing needed a justification. The last customer response that still carried the raw flag was the partner's own payout (`GET`/`POST /v1/partner/payouts`, `self_billing.vat_review` and the full `vat_check` incl. `name_mismatch`): `PartnerPresenters::payout()` now gives the partner the document without `vat_review` / `vat_review_reason` and with `vat_check` reduced by `VatStanding::customerEvidence()` — the same reduction the customer invoice API uses (Presenters::invoiceBuyer now calls it; no copy); `note_vat`, rate, category and totals (why VAT was or was not billed) stay. Staff keep everything: `PartnerPresenters::payout($p, true)` in the staff payout list and in finance's approve/reject/pay answers. Order and invoice presenters expose no `meta.vat_review` to customers (checked). Tests: VatReviewRoundThreeTest (presenter + portal/staff routes; red).

Closing review, fix round 2 (onhost-security MEDIUM, onhost-reviewer billing MEDIUM — the same defect; both said "needs an owner/orchestrator decision"; the owner delegated such decisions (2026-09-25), so the choice below is recorded for DECISIONS in the handoff):

- MEDIUM domains/Organizations/OrganizationService.php:237,268-273; domains/Tax/VatStanding.php:293-304 (security) — the name rule is controlled by the partner: `name` is self-service, a change neither resets the check nor queues one, so a real Czech company's DIČ plus that company's VIES name as the organization name (before or after the check) passed every rule → S 21 %, 121 % transferred, input VAT the tax office would deny — FIXED with the security option (1), which also covers the billing reviewer's "freeze the identity": VAT paid out in cash (a partner of the supplier's country, category S) needs finance's confirmation of the supplier, given through the existing CRITICAL override to valid (four eyes). `VatStanding::payerStanding()` → private `supplierIdentity()`: the newest `staff` vat_validations row for the current number must say `valid` and carry the organization's current name (same words once case, accents, punctuation and legal form are set aside); none → `identity_unconfirmed`, renamed since → `identity_changed`, a changed number has none. `OverrideVatStatusHandler` now keeps the organization name in its evidence row (`vat_validations.name`). The confirmation outlives the override's 1–30 days (VIES carries on from there, including a name difference finance accepted — the round-3 "only via override" is now durable); `payerUnverified()` also covers an ended override, so group `partner` / doctor row (b) / the apply-approve queue reach it. `selfBillingVat` refuses both reasons like `name_mismatch` (E 0 %, "Registrace dodavatele k DPH neověřena.", `vat_review` + reason). `onhost:vat:verify` group `supplier_identity` (finance only, skipped by `--apply`). A partner of another member state (AE, no VAT paid out) needs no confirmation; customers are unchanged. DevAccountSeeder adds the dev partner's confirmation row. Runbook partner section updated. Tests: VatClosingReviewTest (red 6 failed, 1 guard).
- MEDIUM domains/Organizations/OrganizationService.php:237; domains/Tax/VatStanding.php:293-303 (billing) — the name guard can be bypassed by the same actor (the partner edits `name` freely; it binds to nothing) — FIXED by the same change: the supplier's identity (number + name; the country through the existing country rule) is bound to finance's confirmation, and any later change of name or number makes the document E with `vat_review_reason` `identity_changed` / `identity_unconfirmed` until finance confirms again. Not frozen at the partner approval: the approval is not a VAT decision (it can be taken by the system actor and is not four-eyes), so the CRITICAL override is the one confirmation. Not done (follow-up, recorded in the handoff): paying VAT only to an IBAN published for that DIČ in the Czech register of VAT payers (§109 ZDPH) — needs a new provider (ADIS/CRPDPH) behind a contract.

Stack polish (onhost-billing with onhost-security, TASK-0027 worktree, 2026-09-26):

- MEDIUM domains/Tax/Commands/OverrideVatStatusHandler.php:52-74, OverrideVatStatusCommand.php, app/Http/Controllers/Api/V1/Staff/CustomerController.php:241-249 — the override did not bind the subject it confirms: the payload was `{organization_id, status, reason, evidence, days}` and the handler read the organization's current number and name when the approved command ran (the approval lives up to 24 h). A partner that controls `dic`/`vat_id`/`name` could switch to another real company's DIČ and name in that window; the evidence row then confirmed the wrong supplier and `supplierIdentity()` stayed confirmed, so VAT was paid out on it. — FIXED: the controller puts the subject as the requester sees it into the payload (`vat_number` = `VatStanding::subject()->value`, `organization_name`; read only for staff who hold `billing.tax_rule.manage`), so the approval's hash binds it and the approver reads it; the handler refuses a missing or different subject with 409 `vat_override_subject_changed` before any write. The customer reverse-charge override is the same command and route. Direct handler calls in five test files use the new `vatOverrideSubject()` helper (tests/Pest.php).
  RED (`tests/Feature/Tax/VatOverrideSubjectTest.php`, 7 tests, before the fix): 6 failed, 1 passed (the unchanged-subject guard). The approval payload had no `vat_number` (null); the repeat after the partner switched to another company's DIČ and name answered **202** (the override was applied to the new subject); the handler applied a stale payload for another DIČ, another name, and both ("an override was applied to a subject the requester never saw"); a payload without a subject was applied.
  GREEN: VatOverrideSubjectTest 7/7 — the repeat is a new request (403 `approval_required`, bound to the new number; the old approval stays unused), no staff row, `payerStanding()` not a payer; a stale payload → 409 `vat_override_subject_changed`, nothing written; an unchanged subject → 202, the evidence row keeps number and name, the supplier is confirmed. `tests/Feature/Tax` + `tests/Feature/Partners` 113/113.

## Log

- 2026-09-25T18:20:59Z opened by `brain.ps1 task start`.
- 2026-09-25T21:40Z onhost-billing: WP A (VIES contract, recorded check, migration 000880, triggers, event, doctor rows, verify/recheck commands) completed from the interrupted attempt’s uncommitted work, red recorded against base, suite green; status IN_PROGRESS (WP B, WP C open).
- 2026-09-26 onhost-billing: WP B was completed from the interrupted attempt's uncommitted work and adds:
  - the tax decision through VatStanding, the vat_review flag, and the order/invoice evidence;
  - the staff override (CRITICAL);
  - partner self-billing VAT;
  - two further tests and a tighter arch-test exclusion.

  Red was recorded against fabbd9d; the suite is 1440/1440 and the gate PASSes. Status stays IN_PROGRESS because WP C is open.
- 2026-09-26 onhost-billing: WP C — KB qualifier (content data only), VatPromiseTest (red 1/2 against 0adef5a, then green), docs/runbooks/vat-and-vies.md, docs/provider-adapters/vies.md (+ README row), go-live row; gate PASS 1442/1442. All three work packages done: status SELF_VERIFIED (needs onhost-reviewer, onhost-qa and onhost-security review).
- 2026-09-26 onhost-billing: review round 1 — 3 HIGH, 5 MEDIUM, 5 LOW resolved: 8 fixed with failing-first tests (red recorded), 5 pinned by guard tests; suite 1465/1465, Pint, Larastan 0. Status SELF_VERIFIED, back to review.
- 2026-09-26 onhost-billing: review round 2 — 2 HIGH, 2 MEDIUM, 3 LOW all fixed with failing-first tests (13 tests: red 10 failed + 1 error, 2 guards); suite 1478/1478, Pint, Larastan 0, baseline untouched. Status SELF_VERIFIED, back to review.
- 2026-09-26 onhost-billing: review round 3 — 1 HIGH (partner payer status without the country/name rules; one acceptance helper `VatStanding::verdict()`, `payerStanding()`) and the `vat_review` question (settled: staff-only; the partner portal payout no longer carries it) fixed with failing-first tests (8: red 4 failed + 3 errors, 1 guard); focused 154/154, suite 1486/1486, Pint, Larastan 0, baseline untouched. Status SELF_VERIFIED, back to review.
- 2026-09-26 onhost-billing: closing review fix round 2 — 2 MEDIUM (partner-controlled name rule) fixed with one change: VAT paid out to a Czech partner needs finance's confirmation of the supplier (the CRITICAL override; kept by number + name, outlives the override); failing-first VatClosingReviewTest (7: red 6 failed, 1 guard), 3 existing tests updated to the new rule; focused 161/161, Pint, Larastan 0, baseline untouched; whole suite: **1493/1493 passed** (18976 assertions, 425 s). Status SELF_VERIFIED, back to review.
- 2026-09-26 onhost-billing: verification on 6ceafa5: `php artisan test --compact` 1442/1442 (18687 assertions), `pint --test` passed, Larastan 0 errors (baseline: only the ViesClient entry removed); no fix needed. Status SELF_VERIFIED; handoff written to `.ai/handoffs/TASK-0031.md`.

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next.

### WP A (onhost-billing → orchestrator)

- **Outcome:** a VAT number given at registration, guest checkout or organization update is checked in VIES on the queue (system actor, bus) and kept as evidence; the check is off until `ONHOST_VIES_ENABLED=true`. Tax decisions do not use it yet (WP B).
- **Files:** providers/Contracts/{VatNumberValidator,VatCheckResult}.php, providers/Vies/{ViesVatNumberValidator,DisabledVatNumberValidator}.php, domains/Tax/{VatNumber,VatStanding,VatNumberChecks,VatHealth}.php, domains/Tax/Commands/RecordVatCheck{Command,Handler}.php, domains/Tax/Jobs/CheckVatNumber.php, domains/Tax/ViesClient.php (deleted), database/migrations/0001_01_01_000880_record_vat_number_checks.php, app/Console/Commands/{VatVerify,VatRecheck}.php, domains/Organizations/OrganizationService.php, domains/Organizations/Models/Organization.php, domains/Tax/Models/VatValidation.php; delimited blocks in config/onhost.php, .env.example, app/Providers/{PlatformServiceProvider,DomainServiceProvider}.php, app/Console/Commands/Doctor.php, domains/Provisioning/AutomationLedger.php, routes/console.php, domains/Notifications/{NotificationRouter,Lexicon}.php, database/seeders/NotificationTemplateSeeder.php, docs/architecture/events-catalog.md; phpstan-baseline.neon (ViesClient entry deleted only); tests/Contract/ViesContractTest.php, tests/Feature/Tax/*.
- **Verification:** VERIFIED — red against base (42 tests: 1 passed, 8 failed, 33 errors, see Required tests), focused tests green, full suite 1411/1411 (parallel and in the gate), Larastan 0, Pint clean, `brain.ps1 gate -Task TASK-0031` PASS (.ai/reports/TASK-0031-gate.md; no regressions). ASSUMED — the VIES REST shapes follow the EC swagger as the orchestrator confirmed; no live VIES call was made. NOT TESTED — PostgreSQL (CI pest-postgres), a real queue worker.
- **Concerns:** WP B must route every `vat_status` reader through VatStanding (list in Findings) and call `refreshBeforeQuote()` at cart quote / guest checkout / staff quote; until then a VIES-valid number already gives reverse charge through the raw column readers (TaxEngine reads `valid`) but without the 30-day freshness rule — keep the switch off until WP B lands.
- **Next:** WP B (tax decision, review flag, staff override, partner self-billing), then WP C.

**Docs for integration (text for files this task must not edit):**
- docs/runbooks/production-readiness-audit.md / go-live: “VIES: set ONHOST_VIES_REQUESTER_VAT_ID (our CZ VAT ID) and ONHOST_VIES_ENABLED=true only after TASK-0031 WP B is integrated; run `onhost:vat:verify` (dry run) and hand the document list (`--csv`) to the accountant; accountant signs off the reverse-charge wording and the past invoices (D31.9) before `--apply`; decide whether to switch on tax.vies_recheck.”
- docs/context/CURRENT_STATE.md: “VAT numbers are checked in VIES through providers/Vies (contract VatNumberValidator); evidence in organizations.vat_checked_* and vat_validations; one vocabulary unknown|valid|invalid; legacy valid/payer rows honoured until onhost:vat:verify --apply.”
- Legal sign-off list: vat_validations rows (trader name/address from VIES) are kept on erasure — ComplianceService unchanged in this task.

### WP B (onhost-billing → orchestrator)

- **Outcome:** the tax decision uses the recorded VIES check. Reverse charge applies only to a business of another EU state whose check is fresh (≤ 30 days at quote or issue), or when a staff override is in force.
  - Otherwise the money is exactly today's, and `vat_review` is added when a VAT ID was given.
  - Legacy `valid` rows keep reverse charge but are flagged `legacy_unverified`.
  - Partner self-billing VAT works: 21 % for a VIES-valid CZ partner, AE for another EU state, 0 % otherwise.
- **Files:**
  - domains/Tax/{TaxEngine,VatStanding}.php and domains/Tax/Commands/OverrideVatStatus{Command,Handler}.php
  - domains/Orders/{QuoteService,CheckoutService}.php and domains/Orders/Commands/OrdersCommandHandler.php
  - domains/Invoicing/InvoiceService.php and resources/views/invoices/invoice.blade.php
  - domains/Partners/PartnerService.php
  - domains/{Billing/RatingService,Billing/SubscriptionService,Domains/DomainService,Incidents/SlaService,Marketplace/MarketplaceService,Services/PlanChangeService,Services/ServiceArchiveService,Support/WorkOfferService,WalletLedger/WalletForecast}.php
  - app/Http/Controllers/Api/V1/{CartController,CheckoutController,Staff/CustomerController}.php
  - app/Http/Presenters/Presenters.php and app/Console/Commands/{SmokeOrder,VatVerify}.php
  - app/Providers/DomainServiceProvider.php (TASK-0031 block) and routes/api.php (TASK-0031 block)
  - database/seeders/DevAccountSeeder.php
  - tests listed above
- **Verification:**
  - VERIFIED: red against fabbd9d (29 tests: 3 passed, 14 failed, 12 errors, plus 2/2 for the later additions), focused tests green, full suite 1440/1440, Larastan 0, Pint clean, gate PASS.
  - ASSUMED: the reverse-charge wording and the VIES line on the invoice are acceptable to the accountant (go-live sign-off, D31.9).
  - NOT TESTED: PostgreSQL (CI pest-postgres), a real queue worker, a live VIES call (never made).
- **Concerns:**
  - New for staff: the organization presenter now reports the effective `vat_status` instead of the stored column. The staff and customer UI reads `billing.vat_status`. A legacy `payer` now reads `unknown` and a stale `valid` reads `unknown`, which is the intended truth. No file under apps/ reads `vat_status` (grep).
  - Every new invoice meta now carries `vat_review` (false for most), and quote versions carry `vat_review` and `vat`.
  - PlanChangeService, LimitRaiseService and ServiceReinstatement quote with the organization; this is proven by a test.
  - `OrdersCommandHandler` makes no VIES call inside the bus transaction. The controller asks before dispatching, and only when the staff member may place the order.
- **Next:**
  - The orchestrator regenerates OpenAPI for **POST /v1/staff/customers/{organization}/vat-status** (the only route change).
  - After rebasing, run StaffTriggerStepUpTest: the route authorizes only through the bus (no `$this->api->authorize`).
  - Then WP C (public copy, runbook, provider doc, go-live row).

**Docs for integration (WP B additions):**
- **Go-live, accountant sign-off (D31.9):**
  - The legend `Daň odvede zákazník (reverse charge, čl. 196 směrnice 2006/112/ES).` is followed by the line `DIČ dodavatele … · DIČ odběratele … · ověřeno ve VIES <datum> (č. <consultation>)`. For a staff override the line reads `registrace k DPH doložena mimo VIES` instead.
  - The self-billing notes `Daň odvede odběratel (reverse charge, čl. 196 směrnice 2006/112/ES).` and `Dodavatel není plátcem DPH.`
  - The list of past documents from `onhost:vat:verify --csv`.
- **CURRENT_STATE:** "The tax input is built only by VatStanding::taxCustomer(). Reverse charge needs a VIES-valid number of another EU state, checked ≤ 30 days before the quote or issue, or a staff override (`tax.vat_status.override`, CRITICAL, 1–30 days). `vat_review` is carried on the quote, the order, the invoice and TaxCalculation. Partner self-billing VAT comes from the same standing at `standard_rates`."
- **Security boundaries:** "A customer can never claim a VAT status. The cart and the organization quote ignore request input (PaidForIsWhatYouGetTest). Only the system actor records a VIES verdict. Staff overrides are CRITICAL, need four eyes, are audited and lapse."
- **Release note:** existing customers' money is unchanged until the operator runs `onhost:vat:verify --apply` or switches VIES on.
  - Stored `valid` rows without a source keep reverse charge and are flagged for review.
  - A stored `payer` row (written only by the old dev seeder) keeps self-billing VAT. For a CZ partner it stays 21 %, now read from `standard_rates` instead of the silent 21 fallback; a legacy `payer` of another EU state now gets reverse charge instead of the fallback 21 %. Every real partner document was 0 % before, because nothing wrote `payer`.
  - The dev partner seed now records a VIES-valid DIČ.

### WP C (onhost-billing → orchestrator)

- **Outcome:** the public promise "VAT IDs are verified in VIES and reverse charge applies" is true once `ONHOST_VIES_ENABLED=true`, and the one remaining imprecision is now said on the page: until a VAT ID is verified (e.g. while VIES is not answering) the customer's country's VAT is charged. Operators have a runbook, the adapter has its provider doc, the go-live checklist has the tax/VIES row.
- **Files:** database/seeders/data/prototype-content.json (2 lines: the `faktury-dph` VAT section, cs and en), tests/Feature/Tax/VatPromiseTest.php (new), docs/runbooks/vat-and-vies.md (new), docs/provider-adapters/vies.md (new), docs/provider-adapters/README.md (one row), docs/runbooks/go-live-checklist.md (row 61 replaced), .ai/tasks/TASK-0031.md, .ai/reports/TASK-0031-gate.md.
- **Contracts:** none changed (content data and docs only). Existing environments get the qualifier on the next `db:seed --class=ContentSeeder` (updateOrCreate by slug; it rewrites the seeded articles' bodies as before).
- **Verification:** VERIFIED — red 1/2 against 0adef5a for the right reason, VatPromiseTest + ContentTest 4/4, Tax/Content/Partners/Vies contract 81/81, gate PASS (Pint, Larastan 0, Pest 1442/1442, build), `git diff --stat -- apps/surfaces/` empty. ASSUMED — the statements in Onhost.dc.html 5661/5670 and Onhost-app.dc.html 11704 are true by reading WP A/B code (sync check at quote, queued check on save); Onhost-app.dc.html 11646 is prototype mock data replaced by TASK-0030's account view; Onhost-admin.dc.html 3825 is a staff mock. NOT TESTED — the live public site after a reseed, PostgreSQL.
- **Concerns:** apps/surfaces/onhost-content.js:243 still lacks the qualifier (protected surface, not edited — see Findings); its static text is only a prototype fallback, the API serves the seeded article. The tax/VIES go-live row was edited in place (row 61, as the brief allowed); the orchestrator may move it to its own go-live integration instead.
- **Next:** reviews (onhost-reviewer, onhost-qa, onhost-security — money + outbound HTTP), then the orchestrator's rebase (0029 → 0030 → 0031), StaffTriggerStepUpTest, full gate, OpenAPI regeneration.

**Docs for integration (WP C; text for files this task must not edit):**
- **docs/context/CURRENT_STATE.md and .ai/PROJECT_STATE.md:** "VIES wired (TASK-0031): contract VatNumberValidator / providers/Vies, evidence on organizations (000880), reverse charge only on a check ≤30 days or a staff override, vat_review flag, onhost:vat:verify, rule tax.vies_recheck (default off). ONHOST_VIES_ENABLED defaults to false and is the go-live switch; runbook docs/runbooks/vat-and-vies.md."
- **docs/runbooks/security-boundaries.md (~466):** "Every reverse-charge (AE) decision now carries its evidence: `orders.meta.vat` / `invoices.buyer.vat_check` with status valid and reason fresh or staff_override (legacy_unverified only on rows from before TASK-0031, flagged `vat_review`). A row without it is a finding. Query (PostgreSQL): `SELECT i.id, i.number, i.issued_at, i.buyer->>'vat_id' AS vat_id, i.buyer->'vat_check'->>'reason' AS reason FROM invoices i WHERE EXISTS (SELECT 1 FROM invoice_lines l WHERE l.invoice_id = i.id AND l.tax_category = 'AE') AND (i.buyer->'vat_check' IS NULL OR i.buyer->'vat_check'->>'status' <> 'valid' OR i.buyer->'vat_check'->>'reason' NOT IN ('fresh','staff_override')) ORDER BY i.issued_at;` — documents issued before TASK-0031 have no vat_check and are expected in the result (none should be AE, since nothing wrote `valid` before)." (Columns checked against migration 000200: invoice_lines.invoice_id, invoice_lines.tax_category, invoices.issued_at, invoices.buyer JSON; the query itself was NOT run.)
- **docs/runbooks/production-readiness-audit.md:** close C1a (ViesClient had no caller — VIES now checked through providers/Vies), C1b (vat_status never valid — recorded by RecordVatCheckCommand), C1c (callers passed the column in their own vocabularies — VatStanding::taxCustomer only, arch test), C1d (no review flag — vat_review on quote/order/invoice/TaxCalculation); partner self-billing always 0 % (payer + rates.<CC>) — fixed at standard_rates, 409 tax_rate_missing; public copy (KB "DIČ ověřujeme automaticky proti systému VIES") — true, with the qualifier sentence. C1e stays as corrected (WithdrawalPolicy decides from order.meta.customer_class); C1f is a separate task.
- **.ai/DECISIONS.md:** D31.1–D31.10 as in the brief, plus the settled defaults: VIES default off; vat_id falling back to dic; country mismatch → no reverse charge + vat_review; HTTP outside the bus; an unknown answer writes nothing; malformed → invalid only for an EU prefix; non-EU numbers never checked or called invalid; secretResponse on; 30-day window at quote time for orders, at issue time elsewhere; tax.vies_recheck default_off and not for legacy rows; verify dry run without HTTP; re-queue on re-submit with the 1-hour guard; sync checks only at cart quote, guest checkout and staff quote/assisted order; override reuses billing.tax_rule.manage, set-only, 1–30 days; legacy valid/payer honoured at read time (no migration UPDATE); vat_review staff-facing; masked event payload; doctor rows WARN only; vat_validations kept on erasure pending legal sign-off.
- **Go-live items (accountant / legal sign-off, D31.9):** (i) the reverse-charge legend and the line with both VAT IDs and the VIES consultation number (and "registrace k DPH doložena mimo VIES" under an override); (ii) the list of past VAT invoices to EU B2B customers who gave a VAT ID (`onhost:vat:verify --csv=…`) before `--apply`; (iii) partner self-billing VAT categories (S at standard_rates.CZ, AE, E) including the *identifikovaná osoba* edge case (a VIES-valid CZ DIČ that is not a domestic VAT payer is treated as a payer); (iv) that vat_validations evidence (trader name/address from VIES) is retained on erasure (ComplianceService unchanged).
- **Go-live row:** already replaced in docs/runbooks/go-live-checklist.md §3 by this package.
- **OpenAPI:** the regeneration must include **POST /v1/staff/customers/{organization}/vat-status** (the only route change of TASK-0031).
- **Prototype owner:** add the same qualifier sentence to the prototype's onhost-content.js (the export source), so a future re-export keeps it.
- **docs/runbooks/README.md** (index, not edited here): add `vat-and-vies.md` — VAT numbers, VIES and reverse charge.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

### Stack 0029 → 0030 → 0031 on 052ceff (onhost-architect, 2026-09-26)

- **Rebase:** fix/TASK-0029 unchanged (a9e0291, on 052ceff); fix/TASK-0030 rebased onto it without conflicts (c52b18a);
  fix/TASK-0031 rebased onto that. Conflicts only in two hot files, both append-only blocks kept in order (TASK-0029 block,
  then TASK-0031 block): `docs/architecture/events-catalog.md` (`service.access.reduced` / `tax.vat_number.checked`) and
  `domains/Notifications/NotificationRouter.php` (the same two routes). `phpstan-baseline.neon` auto-merged (only removals).
- **Seams checked:** TokenScopes decides every permission ServiceActionCommand can return (service.console → services:console;
  service.manage, service.delete, backup.restore, backup.delete, game.manage, compute.vm.delete → services:power;
  backup.policy.manage, service.panel_account.manage → none), now pinned by ApiTokenScopeMapTest "decides a token scope for
  every permission the service action map can ask". TASK-0031 added no permission key (reuses billing.tax_rule.manage), so
  the TokenScopes TASK-0031 block stays empty; POST /v1/staff/customers/{organization}/vat-status dispatches
  OverrideVatStatusCommand only (StaffTriggerStepUpTest green).
- **Un-skipped (TASK-0030 tests waiting for TASK-0029):** ApiTokenScopeMapTest "keeps a power-only token from creating a
  console schedule through spec apply" (written at integration; it was a `$this->fail()` placeholder) and "refuses deleting a
  backup through a token even when the person has stepped up".
- **Improvement (TASK-0029 file, TASK-0030 map):** ServiceSpecService asks TokenScopes::for() + the token's can() for each
  spec step instead of refusing only console steps on any token session — a console-scoped token may schedule a console
  command (as on /actions), a `token:` session whose token is gone is refused. Commit a28c103.
- **Red evidence:** with the token guard disabled, the power-token spec case fails (`skipped` is `[]`, a console schedule
  would have been queued); against the previous console-only guard, the console-token case fails (the step is skipped as
  `token_scope:schedule.create`). The backup-deletion case's red (202) is recorded in TASK-0030 review round 2.
- **Green:** ApiTokenScopeMapTest 18/18; spec/secondary-gate/game tools 47/47.
- **OpenAPI:** regenerated (20f05d9): adds `staffCustomerOverrideVatStatus`. The document names no per-scope token
  requirements anywhere (customer routes `bearer: []`, staff routes `bearer: [staff]`), so there is no place where
  `services:console` would be listed; the scope is documented in docs/development/docs-laravel-backend.md.
- **Gate:** `brain.ps1 gate -Task TASK-0031` PASS on 20f05d9 — Pint, Larastan 0 errors (no new baseline entries), Pest
  1558/1558, frontend build (.ai/reports/TASK-0031-gate.md).

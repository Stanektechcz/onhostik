# TASK-0031: A VAT number that was given is checked, and reverse charge works

- **Status:** IN_PROGRESS
- **Priority:** P0
- **Risk:** HIGH
- **Owner:** onhost-billing
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0031-a-vat-number-that-was-given-is-checked-a`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0031-a-vat-number-that-was-given-is-checked-a`
- **Opened:** 2026-09-25T18:20:59Z

## Objective

A VAT number a customer gave is checked in the EU register (VIES) and the answer is kept as evidence; reverse charge applies only to an EU business customer of another member state whose number VIES confirmed (at most 30 days ago), otherwise destination VAT with a review flag.

**WP A (this commit):** VIES behind a provider contract, the recorded check on organizations (migration 000880), when checks run (queue after the number is set/changed, operator command, default-off monthly re-check), the event `tax.vat_number.checked` with notifications, and four doctor rows. The tax decision itself (quote/checkout/renewal/invoice, review flag, staff override, partner self-billing) is WP B; public copy, runbook and provider doc are WP C.

## Context

`domains/Tax/ViesClient.php` had no caller, so `organizations.vat_status` never became `valid`: every EU B2B customer outside CZ who gave a VAT ID was charged OSS destination VAT with no review flag, while the public content promises automatic VIES checks and reverse charge. ViesClient also called HTTP from `domains/` (provider rule). Orchestrator decisions D31.1–D31.10 and the critic adjustments (no mass UPDATE in the migration, read-time normalisation, non-EU numbers never called invalid, neutral wording for CZ) are in the work-package brief.

## Scope (owned paths — locked)

- `domains/Tax`
- `providers/Vies`
- `providers/Contracts/VatNumberValidator.php`
- `domains/Organizations/OrganizationService.php`
- `domains/Partners/PartnerService.php`
- `domains/Orders/QuoteService.php`
- `app/Http/Controllers/Api/V1/CheckoutController.php`
- `app/Http/Controllers/Api/V1/CartController.php`
- `domains/Orders/Commands/OrdersCommandHandler.php`
- `app/Http/Controllers/Api/V1/Staff/CustomerController.php`
- `database/seeders/TaxRuleSeeder.php`
- `tests/Feature/Tax`
- `tests/Contract/ViesContractTest.php`
- `docs/runbooks/vat-and-vies.md`

## Out of scope

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

WP A:
- Provider contract `providers/Contracts/VatNumberValidator` (`check(countryCode, number, timeoutSeconds): VatCheckResult`, never throws); `providers/Vies/ViesVatNumberValidator` (POST `onhost.vies.endpoint`, body countryCode/vatNumber/requesterMemberStateCode/requesterNumber, provider_calls row with the answer withheld) and `DisabledVatNumberValidator` (switch off).
- Schema: migration `0001_01_01_000880_record_vat_number_checks` — organizations + vat_checked_at, vat_checked_number(20), vat_consultation_number(80), vat_validation_id(40), vat_status_source(12), vat_override_until, index (vat_status, country); vat_validations + status, reason, note, evidence, actor, requester_vat_id, country_code, expires_at. No data UPDATE.
- Command `tax.vat_number.record` (RecordVatCheckCommand, system actor only, NORMAL).
- Event `tax.vat_number.checked` (organization): result, previous, effective, changed, source, trigger, country, number_hint, consultation_number — never name/address/full number. Routed: invalid → billing contacts (portal note + mail `vat-number-invalid`, neutral wording for CZ), became valid → in-app note, source staff → finance inbox.
- Config `onhost.vies.*` (enabled=false default, endpoint, requester_vat_id, timeout_seconds, checkout_timeout_seconds, freshness_days 30, override_days 30, retry_after_minutes 10, recheck_after_days 25); env ONHOST_VIES_ENABLED, ONHOST_VIES_REQUESTER_VAT_ID, ONHOST_VIES_TIMEOUT, ONHOST_VIES_CHECKOUT_TIMEOUT.
- Console: `onhost:vat:verify` (dry run default, `--apply`), `onhost:vat:recheck` (rule `tax.vies_recheck`, default_off, daily 04:20).
- No route change in WP A.

## Acceptance criteria

WP A:
- [x] VIES is called only through the provider contract and the provider HTTP client; `domains/Tax/ViesClient.php` removed (and its phpstan-baseline entry).
- [x] A number given at registration, guest checkout or organization update is checked on the queue after commit (system actor, through the bus); the evidence is kept (vat_validations row, organization vat_checked_*), customer_class is never written by the check.
- [x] An unknown answer (outage, busy register, open breaker, connection failure) writes nothing and publishes nothing; the stored status is kept.
- [x] A malformed EU-prefixed number is recorded invalid without a call; a non-EU number or an organization outside the EU is never checked and never called invalid.
- [x] A result for a number that changed meanwhile is discarded; a recorded check from any actor but the system is refused.
- [x] `onhost:vat:verify` dry run makes no HTTP call and writes nothing, lists unchecked/stale/legacy organizations and past VAT invoices to EU B2B customers with a VAT ID (optionally CSV); `--apply` checks and records.
- [x] `tax.vies_recheck` is default_off; once on, it re-checks only VIES-valid numbers older than 25 days.
- [x] Doctor prints four non-blocking tax rows and makes no HTTP call.
- [x] VIES off by default; no live VIES call in any test (fakes + preventStrayRequests).
- [ ] WP B: tax decision, review flag, staff override, partner self-billing VAT, all `vat_status` readers through VatStanding.
- [ ] WP C: public copy true, docs/runbooks/vat-and-vies.md, docs/provider-adapters/vies.md, go-live row.

## Required tests

WP A (new): `tests/Contract/ViesContractTest.php` (27 incl. dataset), `tests/Feature/Tax/VatNumberCheckTest.php` (13), `tests/Feature/Tax/VatVerifyCommandTest.php` (5), `tests/Feature/Tax/VatDoctorTest.php` (2). Existing that must stay green: EnvTemplateTest, AutomationSwitchTest, StaffConsoleTest (dynamic rule count), OutboxPayloadKeysTest, SecretsInLogsTest, organization/registration/guest-checkout suites — the whole suite.

### Red evidence (WP A)

The earlier attempt wrote tests and code before it was interrupted and recorded no red. Red was therefore observed afterwards by running the new tests against the base code: `git archive HEAD` (7480a79 = 052ceff + task file) extracted to a scratch folder with a copy of `vendor/`, the four new test files copied in, `php artisan test --compact tests/Contract/ViesContractTest.php tests/Feature/Tax` → **42 tests: 1 passed, 8 failed, 33 errors** (2026-09-25):
- 20 × `Target class [Onhost\Providers\Vies\ViesVatNumberValidator] does not exist` (contract tests), 2 × VatNumberChecks, 1 × VatNumberValidator, 2 × `Class VatNumber not found`, 1 × RecordVatCheckCommand not found;
- 4 × `The command "onhost:vat:verify" does not exist`; 2 × `table organizations has no column named vat_status_source`;
- behaviour failures: registration / guest checkout / AT company → `vat_status` expected `valid`, got `unknown` (nobody called VIES); invalid number → expected `invalid`, got `unknown`, no customer notification; number change → `vat_checked_number` null; doctor → no tax rows (`[]` instead of four);
- the one test that passed at base: "never checks a number from outside the EU and never calls it invalid" — true at base only because nothing was ever checked (a guard test, not a red one).
- NOT observed red: "asks nothing and keeps nothing for a number longer than the evidence columns" (added in this session together with its guard; at the uncommitted code before the guard it would have recorded a 22-character number — ASSUMED from reading).

### Green evidence (WP A)

- `php artisan test --compact tests/Contract/ViesContractTest.php tests/Feature/Tax` → 42 passed before this session’s fixes; after them the full suite below.
- `php artisan test --compact --parallel` → first run 1409/1410 (EnvTemplateTest: `ONHOST_VIES_CHECKOUT_TIMEOUT` missing from .env.example — fixed); second run **1411/1411 passed**.
- `vendor/bin/phpstan analyse` → first run 10 errors in the new code (one-line docblocks hid `@return Builder<Organization>`, datetime columns typed as string, nullsafe on the left of `??`, an always-false ternary) — fixed; then **0 errors**.
- `vendor/bin/pint --test` on the changed files → passed.

## Security considerations

WP A: RecordVatCheckCommand is system-only (a customer or staff actor is refused 403 `system_only`), so nobody can declare their own number valid; it carries no permission and NORMAL risk because it only records what the register answered. The VIES answer names the trader: `secretResponse` keeps it out of provider_calls, the name/address are stripped from the command audit (AUDIT_STRIP) and never enter the event (only country + last three characters). The endpoint comes from config, not from users. No HTTP call inside a bus transaction (row lock never waits for VIES). Needs onhost-security and onhost-qa review (money + outbound HTTP).

## Migration considerations

000880 adds nullable columns and one index; no data UPDATE (critic item 1: a mass write to existing customer rows). Old code ignores the columns. Legacy rows (vat_status_source NULL) keep today’s money: stored `valid` stays effective valid with reason `legacy_unverified`, stored `payer` stays a VAT payer for self-billing, any other non-vocabulary value reads as `unknown` — until `onhost:vat:verify --apply`. Width note: `vat_checked_number` and `vat_validations.vat_id` are 20; a 19–20 digit unprefixed number (22 with the country) is skipped, never stored.

## Rollback

WP A: set `ONHOST_VIES_ENABLED=false` (the default) — no check runs and every number is charged as before; revert the commit and roll back 000880 (`down()` drops the added columns and the index). Recorded vat_validations rows are evidence and harmless to keep.

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

- NOTE (WP A self-review) domains/Tax/VatNumberChecks.php — an unprefixed 19–20 digit number becomes 21–22 characters with the country prefix and would not fit vat_validations.vat_id(20) on PostgreSQL — now skipped (no call, nothing stored) with a test.
- OPEN for WP B: every raw `vat_status` reader (QuoteService ~125/336, CartController ~69, CheckoutController ~106 incl. `refreshBeforeQuote` after the transaction, OrdersCommandHandler ~120, Staff/CustomerController ~193, PlanChangeService ~116, RatingService, SubscriptionService, InvoiceService ~94/540, DomainService, SlaService, MarketplaceService, WorkOfferService, WalletForecast, ServiceArchiveService, SmokeOrder) still passes the stored column; PartnerService ~757 still checks `payer` and `rates.<CC>`; DevAccountSeeder:68 still writes `payer` (honoured as legacy by VatStanding::isVatPayer).
- OPEN for WP C: docs/provider-adapters/vies.md and docs/runbooks/vat-and-vies.md are referenced by config/.env.example/comments but not written yet.

## Log

- 2026-09-25T18:20:59Z opened by `brain.ps1 task start`.
- 2026-09-25T21:40Z onhost-billing: WP A (VIES contract, recorded check, migration 000880, triggers, event, doctor rows, verify/recheck commands) completed from the interrupted attempt’s uncommitted work, red recorded against base, suite green; status IN_PROGRESS (WP B, WP C open).

## Handoff

From / To · Outcome · Files · Contracts · Verification (VERIFIED / ASSUMED / NOT TESTED) · Concerns · Next.

### WP A (onhost-billing → orchestrator)

- **Outcome:** a VAT number given at registration, guest checkout or organization update is checked in VIES on the queue (system actor, bus) and kept as evidence; the check is off until `ONHOST_VIES_ENABLED=true`. Tax decisions do not use it yet (WP B).
- **Files:** providers/Contracts/{VatNumberValidator,VatCheckResult}.php, providers/Vies/{ViesVatNumberValidator,DisabledVatNumberValidator}.php, domains/Tax/{VatNumber,VatStanding,VatNumberChecks,VatHealth}.php, domains/Tax/Commands/RecordVatCheck{Command,Handler}.php, domains/Tax/Jobs/CheckVatNumber.php, domains/Tax/ViesClient.php (deleted), database/migrations/0001_01_01_000880_record_vat_number_checks.php, app/Console/Commands/{VatVerify,VatRecheck}.php, domains/Organizations/OrganizationService.php, domains/Organizations/Models/Organization.php, domains/Tax/Models/VatValidation.php; delimited blocks in config/onhost.php, .env.example, app/Providers/{PlatformServiceProvider,DomainServiceProvider}.php, app/Console/Commands/Doctor.php, domains/Provisioning/AutomationLedger.php, routes/console.php, domains/Notifications/{NotificationRouter,Lexicon}.php, database/seeders/NotificationTemplateSeeder.php, docs/architecture/events-catalog.md; phpstan-baseline.neon (ViesClient entry deleted only); tests/Contract/ViesContractTest.php, tests/Feature/Tax/*.
- **Verification:** VERIFIED — red against base (42 tests: 1 passed, 8 failed, 33 errors, see Required tests), focused tests green, full suite 1411/1411 (parallel and in the gate), Larastan 0, Pint clean, `brain.ps1 gate -Task TASK-0031` PASS (.ai/reports/TASK-0031-gate.md; no regressions). ASSUMED — the VIES REST shapes follow the EC swagger as the orchestrator confirmed; no live VIES call was made. NOT TESTED — PostgreSQL (CI pest-postgres), a real queue worker.
- **Concerns:** WP B must route every `vat_status` reader through VatStanding (list in Findings) and call `refreshBeforeQuote()` at cart quote / guest checkout / staff quote; until then a VIES-valid number already gives reverse charge through the raw column readers (TaxEngine reads `valid`) but without the 30-day freshness rule — keep the switch off until WP B lands.
- **Next:** WP B (tax decision, review flag, staff override, partner self-billing), then WP C.

**Docs for integration (text for files this task must not edit):**
- docs/runbooks/production-readiness-audit.md / go-live: “VIES: set ONHOST_VIES_REQUESTER_VAT_ID (our CZ VAT ID) and ONHOST_VIES_ENABLED=true only after TASK-0031 WP B is integrated; run `onhost:vat:verify` (dry run) and hand the document list (`--csv`) to the accountant; accountant signs off the reverse-charge wording and the past invoices (D31.9) before `--apply`; decide whether to switch on tax.vies_recheck.”
- docs/context/CURRENT_STATE.md: “VAT numbers are checked in VIES through providers/Vies (contract VatNumberValidator); evidence in organizations.vat_checked_* and vat_validations; one vocabulary unknown|valid|invalid; legacy valid/payer rows honoured until onhost:vat:verify --apply.”
- Legal sign-off list: vat_validations rows (trader name/address from VIES) are kept on erasure — ComplianceService unchanged in this task.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.

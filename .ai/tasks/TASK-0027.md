# TASK-0027: Stack coherence and the docs that describe it

- **Status:** READY_FOR_INTEGRATION
- **Priority:** P1
- **Risk:** MEDIUM
- **Owner:** onhost-docs
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0027-stack-coherence-and-the-docs-that-descri`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0027-stack-coherence-and-the-docs-that-descri`
- **Opened:** 2026-09-25T09:01:41Z

## Objective

What outcome the user gets, in one or two sentences.

**Coherence fixes C1–C4 (onhost-billing + onhost-backend, integrator's list):** the stacked tasks agree with each other —
one gate decides who spends the credit (C1), a restore never switches auto-renew on without a holder's consent (C2), staff
hear the four events whose staff branch never ran (C3), and no plan version sold from now on promises dedicated PHP
workers the platform cannot deliver on aaPanel (C4).

## Context

Why now; links to the request, ADR, runbook, audit row, or failing test.

- C1: TASK-0021 `CreditOrderPolicy::assertMaySpend` applies only while `ONHOST_ORDER_CREDIT_APPROVAL` is on; TASK-0025
  `ServiceReinstatement` checked `billing.wallet.spend` itself (and the bus demanded it) whenever `services.reinstate` was
  on — two gates with different switches and different errors (`credit_spend_not_allowed` vs `reinstatement_spend_required`).
- C2: `SubscriptionService::restartAfterRestore()` set `auto_renew = true` whenever the paid period had ended (staff resume,
  the audit command's `--apply`, the customer's covered-by-nothing undo), so the next renewal was charged to the credit
  without any holder's consent — against TASK-0021 (switching auto-renew on is a holder's act) and TASK-0025 round 3.
- C3: `domains/Notifications/NotificationRouter.php` named `service.restore_test.failed`, `service.database.import.failed`,
  `service.backup.schedule.paused` and `service.backup.schedule.stalled` twice in the main match (a customer arm, then the
  staff list); PHP runs the first arm only, so staff were never told. Original intent (`git log -S`: d49a8ab H458, f246399
  H447, b81f560 H456/H467, and `docs/architecture/events-catalog.md`): "customer + operator" for all four.
- C4: decision 7 leftover — `eshop/shop-peak` still sells `php_workers_dedicated` ("24 PHP workerů (dedikované)") on aaPanel,
  where one PHP pool serves the whole node; TASK-0023's `PlacementRules` could not move it to ISPConfig (WAF rate limit,
  client-wide site count) and left it as "owner decision pending". Decision: the next catalogue revision (TASK-0022
  `CatalogRevisions`, operator command `onhost:catalog:revise`) publishes new versions without it.

## Scope (owned paths — locked)

- `.ai/PROJECT_STATE.md`
- `.ai/TECH_STACK.md`
- `.ai/baseline/baseline.json`
- `tests/Pest.php`
- `tests/Unit/TestEnvironmentRestoreTest.php`
- `scripts/ai/task.ps1`
- `scripts/ai/tests/Orchestration.Tests.ps1`
- `docs/provider-adapters/ispconfig.md`
- `docs/provider-adapters/aapanel.md`
- `docs/development`
- `docs/runbooks/go-live-checklist.md`
- `docs/runbooks/preproduction-audit.md`
- `.claude/agents/onhost-docs.md`

Paths the orchestrator assigned beyond the list locked at `task start` (review round 1, MEDIUM: the list above was only the
docs/test-infra slice). Coherence fixes C1–C4 and review round 1:

- `domains/Billing/ServiceReinstatement.php`, `domains/Billing/SubscriptionService.php`,
  `domains/Billing/Commands/ReinstateServiceCommand.php`, `app/Console/Commands/ReinstatementAudit.php`
- `domains/Services/Commands/ServicesCommandHandler.php` (review round 1: the staff resume warning)
- `domains/Catalog/CatalogRevisions.php`, `domains/Catalog/PlanPromises.php`, `app/Console/Commands/CatalogRevise.php`
- `domains/Notifications/NotificationRouter.php`
- `domains/Provisioning/Scheduling/CapacityDoctor.php`, `domains/Provisioning/Scheduling/PlacementRules.php`,
  `domains/Services/Metering/MetricRegistry.php`
- `tests/Feature/Billing/PayAndRestoreTest.php`, `tests/Feature/Billing/ReinstatementAuditCommandTest.php`,
  `tests/Feature/Catalog/CatalogRevisionTest.php`, `tests/Feature/Catalog/PlanPromisesTest.php`,
  `tests/Feature/Notifications/NotificationRouterArmsTest.php`, `tests/Feature/Platform/DoctorCommandTest.php`,
  `tests/Feature/Services/BackupScheduleMissesTest.php`, `tests/Feature/Services/DatabaseImportSafetyTest.php`,
  `tests/Feature/Services/RestoreTestTest.php`

Docs wave: `docs/context`, `docs/runbooks`, `docs/adr/0007-owner-decisions-2026-09-25.md`, `.ai/tasks/TASK-0001…0018`,
`.ai/decisions/AI-0001-orchestration-layer.md`, `.ai/baseline/baseline.md`, `.ai/reports`, `.ai/handoffs/TASK-0027.md`.

The live lock board still shows only the original list for TASK-0027: the domain paths above are locked by the stacked
tasks TASK-0017/0022/0023/0025 of the same linear stack and single pull request, so `brain.ps1 task claim` would refuse them
as an overlap (exit 2) and this worker does not write the main checkout's coordination state. No task outside this stack
holds any of them (board read 2026-09-25T11:40Z). The orchestrator re-points the locks when the stack's tasks are released.

## Out of scope

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

Endpoints, events and payload keys, schema, config keys, provider calls — or "none".

- C1: `POST /v1/services/{service}/reinstate` → `ReinstateServiceCommand` bus permission `billing.wallet.spend` →
  `billing.wallet.topup` (the permission of `invoice.pay_from_wallet`; still no API-token scope), plus
  `CreditOrderPolicy::assertMaySpend` → 403 `credit_spend_not_allowed` + `permission: billing.wallet.spend` while
  `onhost.orders.credit_approval.enabled` is on. Customer resume of a cancellation with `services.reinstate` on: 403
  `reinstatement_spend_required` is gone, replaced by the same `credit_spend_not_allowed` under the same switch. OpenAPI
  regenerated: unchanged. Review round 1: that resume first needs `billing.wallet.topup` held by a user at the organization
  (403 `credit_spend_not_allowed` + `permission: billing.wallet.topup`, whatever the switch), then the credit gate
  (`permission: billing.wallet.spend` while it is on). `POST /v1/services/{service}/actions` (resume) may answer
  `warning: {code: restore_ends_at_renewal, message}` next to the operation. Idempotency key of a subscription's expiry
  termination `sub_expire:{subscription}` → `sub_expire:{subscription}:{period end timestamp}`.
- C4: new catalogue revision id `2026-09-shared-php-workers` (new optional revision shape `drop_undelivered`); doctor
  capacity row detail wording; no endpoint, event, schema or config key.

## Acceptance criteria

- [ ] …
- [x] C1 one credit gate: paying for a restore, taking back a cancellation that bills again and a recorded restore request
  all ask `CreditOrderPolicy` (switch off: whoever may pay from the credit — for the resume a user holding
  `billing.wallet.topup` at the organization, review round 1; switch on: owner + billing admin, `credit_spend_not_allowed`;
  staff and the platform are not asked); a recorded request is paid only while who asked still holds the command permission and passes
  the gate as it stands at payment time. Both switch states tested.
- [x] C2 a restore keeps exactly the subscription's previous auto-renew (this cancellation's saga record for this
  subscription, else the cancelled row's own value, nothing = off) — whoever triggers it; tested staff-triggered (off stays
  off, no charge at the renewal pass; on stays on), payment-triggered (off stays off) and the row fallback.
- [x] C3 each of the four events has one arm that tells staff (internal, `hot`, `/sprava/sluzby`, the staff list's title) and
  the customer (unchanged wording, `warn`); a structural test proves no event name is named by two arms of the main match.
- [x] C4 revision `2026-09-shared-php-workers` drops `php_workers_dedicated` from every plan whose current version sells it
  where `PlacementRules::undelivered()` (found at run time, today only shop-peak); applied only by `onhost:catalog:revise`;
  held versions, prices and profi untouched; the price list says "Sdílené PHP workery"; `apply()` reads each revision's
  pending changes just before it runs (two revisions on one plan: v1 → v2 → v3); MetricRegistry reason, KNOWN_GAPS
  `php_workers` wording, CapacityDoctor detail, PlacementRules docblock, ratchet test and `docs/runbooks/pricing.md`
  updated; KNOWN_GAPS count unchanged at 8 (the runbook's "currently 8 entries" is right).

## Required tests

Failing-first tests to write; existing tests that must stay green; critical flows touched (`.ai/baseline/baseline.json`).

## Security considerations

## Migration considerations

## Rollback

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

Review round 1 (reviewer, security, billing lenses):

- HIGH `domains/Billing/ServiceReinstatement.php:305` (security + billing, same finding twice) — with `services.reinstate`
  on and credit approval off, `CreditOrderPolicy::maySpend` is true for everyone, so a developer, cloud/game operator, a
  `svc_manage`/`svc_console` guest, their `services:power` token and an assistant could take back the owner's cancellation
  and restart its renewals from the credit (TASK-0025 round 2 had refused them) — FIXED: `assertCustomerMayResume` first
  asks `actorMay($context, ReinstateServiceCommand::PERMISSION, org)` (`billing.wallet.topup`, a user at the organization,
  as `requesterMaySpend()` does), 403 `credit_spend_not_allowed` + `permission: billing.wallet.topup`, then the credit
  gate. Failing first: `refuses a developer, a guest of the service, their token and an assistant …` got 202 for the guest.
  Runbook `billing-dunning.md`, the C1 log line and the handoff corrected. The synthetic spend-only role of `lets only who
  may spend the credit take back a cancellation …` gains `billing.wallet.topup` (no real role has spend without topup).
- HIGH `domains/Billing/SubscriptionService.php:464` (billing) — `expire()` asked for the termination under
  `sub_expire:{subscription}`; a subscription a restore started again (staff resume, `reinstatement-audit --apply`) whose
  first end had come from `expire()` got that first operation back, was set CANCELLED and the service ran on unbilled —
  FIXED: key `sub_expire:{subscription}:{period end timestamp}` (one termination per period that ended; existing
  operations keep their old key, nothing is rewritten). Staff are told on the resume itself: `ServiceReinstatement::
  restoreEndsAgain()` + `SubscriptionService::restartEndsAgain()` (same conditions as RestartBillingAfterRestore, read
  before the resume) add `warning.code = restore_ends_at_renewal` to the action answer
  (`domains/Services/Commands/ServicesCommandHandler.php:55`). Failing first: `ends a restored service again at the renewal
  pass even when an earlier expiry …` — no warning, and with only the key reverted `Failed asserting that 1 is identical to
  2` (no second termination). Guard: `does not warn staff when a restore keeps a paid period or the auto-renew …`.
- MEDIUM `.ai/tasks/TASK-0027.md` Scope (reviewer) — the locked list named only the docs/test-infra slice — FIXED: Scope
  lists every path C1–C4, the docs wave and this round touched; why the live board cannot show them under TASK-0027 is
  recorded there (the stacked tasks of the same PR hold those locks; a claim would be refused as an overlap).
- LOW `domains/Billing/ServiceReinstatement.php:191` (security) — `reinstate()` refused staff with the switch on while
  `requesterMaySpend()` lets a staff request be paid — FIXED: staff and the platform (`actsForPlatform`) skip the credit
  gate in `reinstate()` too. Failing first: `lets staff pay for a customer's restore while credit approval is on …` (a
  `cloud_vps_admin`, who holds no customer wallet permission, got `credit_spend_not_allowed`).
- LOW `domains/Billing/ServiceReinstatement.php:282` (billing) — the docblock said an assistant may not undo a
  cancellation while `CreditOrderPolicy::principal()` let an `ai` actor pass as its user — FIXED by the HIGH fix above
  (`actorMay` refuses every non-user actor, whatever the switch; asserted in the same test) and the docblocks of
  `assertCustomerMayResume` and `actorMay` now say what the code does (a user's API token acts as that user).
- LOW `domains/Catalog/CatalogRevisions.php:154` (billing) — the dry run of every revision counted each from today's
  version (`shop-peak v1 → v2` twice) while `--apply` makes v2 then v3 — FIXED in the command output
  (`app/Console/Commands/CatalogRevise.php:54`: `eshop/shop-peak is changed by 2 revisions: --apply publishes them one after
  another (v1 → v2 → v3)`) and `docs/runbooks/pricing.md`. Failing first: the dry-run test's new expectation.
- NOTE (open, not changed) — an owner's or org_admin's personal API token with `services:power` may still take back a
  cancellation (it acts as its user, who holds `billing.wallet.topup`), as it could before TASK-0027; `POST /reinstate`
  itself has no token scope. Refusing tokens here would narrow TASK-0025 behaviour beyond this round's findings — owner /
  security decision.

## Log

- 2026-09-25T09:01:41Z opened by `brain.ps1 task start`.
- 2026-09-25 onhost-billing C1 (one credit gate): RED — `tests/Feature/Billing/PayAndRestoreTest.php` 5 failed: the two
  existing spend tests moved under the switch (`'credit_spend_not_allowed'` expected, got `access_not_approved` from the bus /
  `reinstatement_spend_required`), and the three new tests `pays for a restore under the one credit gate …`, `takes back a
  cancellation that bills again under the one credit gate …`, `pays a recorded restore request under the one credit gate …`
  (each `Expected response status code [202] but received 403` for an org_admin with the switch off). GREEN after
  `ReinstateServiceCommand::PERMISSION = billing.wallet.topup`, `ServiceReinstatement::reinstate()` /
  `assertCustomerMayResume()` → `CreditOrderPolicy::assertMaySpend`, `requesterMaySpend()` → command permission +
  `CreditOrderPolicy::maySpend` (staff/system pass): PayAndRestoreTest 26/26; `tests/Feature/Billing`, `CreditSpendGateTest`,
  `CreditOrderApprovalTest` 136/136. Runbook `docs/runbooks/billing-dunning.md` updated. Consequence recorded: with the
  switch off (default) an org_admin may now commit the organization to renewals — exactly as for invoices, auto-renew and
  auto-upgrade under TASK-0021's switch-off semantics. (Corrected in review round 1: a `service.manage` guest, a developer
  or an operator role could too — that was the HIGH finding; they are refused again, see Findings.)
- 2026-09-25 onhost-billing C2 (auto-renew after a restore): RED — 4 new tests in `PayAndRestoreTest.php`, 2 failed for the
  named reason: `keeps auto-renew off when staff bring back a service whose paid period had ended` (`Failed asserting that
  true is false` — `restartAfterRestore` forced it on) and `takes the cancelled row's own auto-renew when the cancellation
  recorded none …` (`false is true` — the row's own value was ignored); the two guards (`… staff … customer had it on`,
  `… a paid invoice brings back a service dunning cancelled`) passed before and after. GREEN after `previousAutoRenew()`
  falls back to the cancelled row's own value and the restarted branch keeps `$restore['auto_renew']`:
  `tests/Feature/Billing` 76/76. `ReinstatementAuditCommandTest` changed on purpose (it had pinned `auto_renew` true for a
  leak whose row says off): now false, and `--apply` warns "auto-renew stays off …". Consequence recorded in the runbook: a
  restored service whose customer had auto-renew off and whose period is over ends again at the next renewal pass unless it
  is paid for or a holder switches auto-renew on.
- 2026-09-25 onhost-backend C3 (router arms): RED — new `tests/Feature/Notifications/NotificationRouterArmsTest.php` 5/5
  failed: `routes every event through one arm …` listed exactly the four duplicated names, and the dataset `tells both the
  customer and staff …` (4 events) `Failed asserting that actual size 0 matches expected size 1` for the internal
  notification. GREEN after the four arms became one `customerAndStaff()` arm each (internal kind/title/surface as the
  staff list had them, payload via the extracted `internalBody()`) and the names left the staff list. Three older tests read
  `$told->first()->body` and now got the staff row first: changed to the customer row and to expect `internal` too
  (`RestoreTestTest`, `BackupScheduleMissesTest` ×2, `DatabaseImportSafetyTest`). `tests/Feature/Notifications` + those
  three files 30/30; pint, phpstan clean.
- 2026-09-25 onhost-billing C4 (shared PHP workers): RED — `CatalogRevisionTest` `withdraws dedicated PHP workers from every
  plan whose panel has no pool per site …` (`Expected status code 0 but received 1`: unknown revision), `PlanPromisesTest`
  `sells dedicated PHP workers only where a pool per site keeps them …` (`eshop/shop-peak sells dedicated PHP workers in a
  family no pool keeps them for`), `DoctorCommandTest` `shows a catalogue revision not yet applied …` extended (the pending
  row lacked `2026-09-shared-php-workers: eshop/shop-peak (−php_workers_dedicated)`). GREEN after the revision, the
  `drop_undelivered` shape and the doctor wording. Found on the way and proven: `apply()` read every revision's pending
  list up front, so a second revision on the same plan published against a stale `base_version` and was refused
  (`catalog_changed_since_request`, exit 1) — with the lazy per-revision read the same test passes; reverting only that
  loop turns it red again (`Expected status code 0 but received 1`). Existing tests changed on purpose: dry-run
  `pendingKeys` gains `php_workers_dedicated`; the apply-all test expects shop-peak without `php_workers_dedicated` too; the
  bus-count test applies `2026-09-honest-promises` alone (its 6 publishes, one reason). `tests/Feature/Catalog`,
  `tests/Feature/Platform`, `tests/Feature/Provisioning`, `PublicPagesSeamTest`, `LimitRaiseTest`, `PlanFitTest` 434/434;
  pint, phpstan clean. ADR-0007 row 7 still says "Pending" — left to the docs owner of this task.
- 2026-09-25 onhost-billing + onhost-backend C1–C4 whole verification on b7bd4a4: `php artisan test --compact` 1364/1364
  (18258 assertions); `pint --test` passed; phpstan 0 errors, `phpstan-baseline.neon` unchanged; `onhost:openapi` unchanged.
- 2026-09-25 onhost-docs (docs wave, paths assigned by the orchestrator beyond the locked list: `docs/context`, `docs/runbooks`,
  `docs/adr/0007-…`, `.ai/tasks/TASK-0005…0016`, `.ai/decisions/AI-0001`, `.ai/baseline/baseline.md`). Baseline re-measured
  first on `edb635b` (clean): `.\brain.ps1 gate -Task TASK-0027` full **PASS** — Pint, Larastan 0, Pest 1364/1364 (18258
  assertions, 363 s), build; routes 543 (493 `/v1`), 61 migrations on a scratch SQLite file, composer validate/audit and
  npm audit clean, `Orchestration.Tests.ps1` PASS, `Config.Tests.ps1` still the pre-existing failure. Every claim written was
  checked against code first (KNOWN_GAPS = 8 entries, 7 `default_off` rules of 38, config defaults of every new switch —
  `ONHOST_PASSWORD_CHANGE_REVOKES_API_ACCESS` is the one default **on** —, every command and test path named, C1
  `ReinstateServiceCommand::PERMISSION = billing.wallet.topup`, ISPConfig `onh_` client at `IspConfigWebProvider.php:1148`,
  `awaitStatus`/`datalogRow`, `dropSiteChildren`, aaPanel `resize` `applied: false`, `CustomerUsage` for H286). Commits:
  `1a4130a` cherry-pick of 37ba9e9 (DAILY_WORKFLOW path); `d308377` historical headers (five docs/development files,
  preproduction-audit) + DAILY_WORKFLOW worktree/PR wording; `20c723b` ispconfig.md/aapanel.md; `04f311b` security-boundaries
  (§3, §5, §6 stale line, §19, new §21–§25, staging checks), web-tools, backups, approvals, billing-dunning; `939e1cd` audit
  rows 105–119, rows 1/4/6/16/28/34/55/83/92/104 updated, game-panel keys and ClamAV contradictions reconciled, runbook
  index; `9ce66d8` go-live checklist Status column + §6 operator steps; `540607a` CURRENT_STATE (2026-09-25, bullets
  TASK-0018…0027, switch list, baseline, Known risk, Next decision); `5967bcb` ADR-0007 implementation addendum (row 7 and
  every Pending row now name where they were built). Then `.ai` state (this commit): PROJECT_STATE rewritten, TASK-0005…0016
  INTEGRATED with PR and merge commit from `gh pr list --state merged` (TASK-0006…0009 filled from their handoffs /
  commit 634ba12), AI-0001 accepted, TECH_STACK counts, baseline.json/.md re-measured on the stack tip, onhost-docs agent
  next ADR 0008, gate report `.ai/reports/TASK-0027-gate.md`.
- 2026-09-25 onhost-docs vault (`C:\Users\medion\Desktop\ONHOST-BRAIN\ONHOST-BRAIN`, own Git, local commits only, nothing
  pushed): `8ebfa41` commits TASK-0026's pending vault changes unchanged (OWNER-DECISIONS-2026-09-25 with the standing owner
  rules of 2026-09-24, METERING-PLAN-2026-09-24, 16 P0 cards assessed, README, assessment); `9e9c376` the 82nd assessment
  addendum (one paragraph per task TASK-0009…TASK-0027, links ADR-0007). `Update-CodeMap.ps1` lives at the vault root, not in
  `System/`; read first — it only reads the main checkout and writes `System/CODE-MAP.{md,json}` (git-ignored) — and was run:
  map now at `development @ 2426c17`; it lists ADR-0007 only after the stack is merged into the main checkout.
- 2026-09-25 onhost-docs final verification on the stack tip `04f3b05` (clean tree): `php artisan test --compact`
  1364/1364 (18258 assertions, 388 s); `vendor/bin/pint --test` passed; `phpstan analyse --memory-limit=2G --no-progress`
  0 errors; `git diff development -- phpstan-baseline.neon` empty; `scripts/ai/tests/Orchestration.Tests.ps1` all passed.
  No failure, nothing to fix. Status → SELF_VERIFIED; handoff `.ai/handoffs/TASK-0027.md`.
- 2026-09-25 onhost-billing review round 1 (7 findings, see Findings): RED — `PayAndRestoreTest` 3 new tests failed for the
  named reasons (guest resume 202 instead of 403; no `restore_ends_at_renewal` warning, and with only the key reverted no
  second termination `1 is identical to 2`; `cloud_vps_admin` refused `credit_spend_not_allowed`), `CatalogRevisionTest`
  dry run lacked the two-revisions line. GREEN in `77a06e7`: `tests/Feature/Billing` 80/80, `tests/Feature/Catalog` +
  `DoctorCommandTest` 49/49. Whole verification on `77a06e7`: `php artisan test --compact` 1368/1368 (18287 assertions,
  604 s); `vendor/bin/pint --test` passed; `phpstan analyse --memory-limit=2G --no-progress` 0 errors;
  `git diff development -- phpstan-baseline.neon` empty; `onhost:openapi` unchanged. Status stays SELF_VERIFIED.

## Handoff

See `.ai/handoffs/TASK-0027.md`.

## Integration

Gate report, review results, merge commit, post-integration gate, CI run.
- 2026-09-25 orchestrator: final full gate PASS on the stack tip 22991d7 (Pest 1368/1368, 18 287 assertions; .ai/reports/TASK-0027-gate.md). Reviews reviewer/security/billing APPROVE. The whole stack (TASK-0019, 0017, 0020, 0026, 0024, 0021, 0022, 0023, 0025, 0018, 0003, 0027) goes to development in one pull request.

# TASK-0008: An ISPConfig client's limits are the organizations, not one services

- **Status:** INTEGRATED
- **Priority:** P0
- **Risk:** HIGH
- **Owner:** onhost-provisioning
- **Reviewers:** (from .ai/DOMAIN_MAP.md; always onhost-reviewer + onhost-qa; onhost-security when .ai/SECURITY_RULES.md §1 applies)
- **Branch:** `fix/TASK-0008-an-ispconfig-client-s-limits-are-the-org`
- **Worktree:** `C:\Users\medion\Desktop\ONHOST-NEW\onhost-worktrees\TASK-0008-an-ispconfig-client-s-limits-are-the-org`
- **Opened:** 2026-09-24T13:50:45Z

## Objective

A customer's second web hosting on ISPConfig is no longer refused by the panel after payment: the client account's
limits are what the organization holds on that panel, not what one service sells.

## Context

Audit row 95, CURRENT_STATE bullet *An ISPConfig client's limits are the organization's*. One client per organization
(`onh_<org>`) carried limits written once from the first service's plan; `ensureClient()` reused it untouched, and a plan
change wrote one service's numbers over the whole customer's.

## Scope (owned paths — locked)

- `providers/IspConfig`
- `domains/Services/Web/ClientAllowance.php`
- `domains/Provisioning/Workflows/ProvisionWebsiteWorkflow.php`
- `tests/Contract`
- `.ai/tasks/TASK-0008.md`
- `.ai/handoffs/TASK-0008.md`

## Out of scope

## Dependencies

Tasks or contracts that must exist first.

## Contracts affected

Endpoints, events and payload keys, schema, config keys, provider calls — or "none".

## Acceptance criteria

- [x] `ClientAllowance` sums what the organization holds on the instance (carried sites counted once, everything but
  TERMINATED); the provisioning spec carries it as `client_entitlements`; an existing client is raised to it through
  `updateClientLimits()` (a panel call only when something differs).
- [x] The resize half (Concern 1 of the handoff) was done in the same PR: `ServiceActionWorkflow` passes
  `client_entitlements` on a plan change (`7fac8f0`).
- [x] Failing first: "raises the client limits …" and "creates a new client with what the organization holds …"
  (`tests/Contract/IspConfigContractTest.php`, `tests/Contract/ClientAllowanceTest.php`).

## Required tests

Failing-first tests to write; existing tests that must stay green; critical flows touched (`.ai/baseline/baseline.json`).

## Security considerations

## Migration considerations

## Rollback

## Findings

Reviewer / QA / security findings: `SEVERITY file:line — finding — resolution`.

## Log

- 2026-09-24T13:50:45Z opened by `brain.ps1 task start`.
- 2026-09-25 onhost-docs (TASK-0027): status INTEGRATED — PR #10, `68111fa`.

## Handoff

Full handoff: `.ai/handoffs/TASK-0008.md`. VERIFIED: `IspConfigContractTest` 10/10, `ClientAllowanceTest` 3/3, suite
910 tests / 14 822 assertions, Larastan 0, Pint clean. Still open (audit row 95): clients already on the live panel keep
their old limits until that customer's next provisioning; no sweep exists.

## Integration

- Integrated: PR [#10](https://github.com/Stanektechcz/onhostik/pull/10) into `development`, merged 2026-09-24T14:23:50Z (rebase merge); last commit on `development`: **`68111fa`** (read from `gh pr list --state merged`, 2026-09-25).
- The PR carried three commits: `f65e274` (client limits from the organization on provisioning), `7fac8f0` (the resize half of Concern 1: a plan change sizes the client account for the whole customer, `ServiceActionWorkflow` passes `client_entitlements`) and `68111fa` (audit rows 94 and 95, CURRENT_STATE).
- Recorded by TASK-0027 (docs); the post-integration gate of that day is not repeated here — `development` at `2426c17` (PR #19) is the base of the stack, whose gate PASSED on 2026-09-25 (1 364 tests).

# Handoff — TASK-0023

## placement-capacity (owner decisions 7 and 19)

```
From / To:        onhost-provisioning → orchestrator
Task / Status:    TASK-0023 package placement-capacity / SELF_VERIFIED (package); task continues with other packages
```

**Outcome.** A plan that sells dedicated PHP workers on an ISPConfig product (web-hosting/profi) can no longer be placed,
pinned, scheduled, sold or plan-changed onto a panel with one PHP pool for the whole node; every other web/managed plan
says "Sdílené PHP workery" on the price list. Node room is judged per dimension (`CapacityBasis`: disk sold / RAM and CPU
measured) with the disk switch **off** (`ONHOST_CAPACITY_DISK_BASIS=measured`) until an operator has read
`php artisan onhost:capacity:basis`; sold disk no longer counts included sites and staging copies twice. Web and managed
cart lines are judged like servers (behind the existing `ONHOST_CAPACITY_GATE`). No running service is moved or changed.

**shop-peak was verified NOT to land cleanly on ISPConfig** and is therefore not restricted: its WAF `pro` level promises
a rate limit ISPConfig does not apply (aaPanel does), `ClientAllowance` does not count a site for a plan without `sites`
(post-payment refusal for an organization with another ISPConfig hosting), and ISPConfig has no site file manager. It is
reported as an undelivered promise by `onhost:doctor` (capacity WARN) and `onhost:capacity:basis`. **Owner decision
needed** (new shop-peak plan version without the dedicated claim, or make ISPConfig fit and extend the rule).

**Files.** New: `domains/Provisioning/Scheduling/{PlacementRules,CapacityBasis,CartCapacity,CapacityReport,CapacityDoctor}.php`,
`app/Console/Commands/CapacityBasisReport.php`, `tests/Feature/Provisioning/{DedicatedPhpPlacementTest,CapacityBasisTest}.php`.
Changed (local hunks): `domains/Provisioning/Scheduling/NodeScheduler.php`, `domains/Provisioning/PlacementService.php`,
`domains/Provisioning/Workflows/Steps/ScheduleNodeStep.php`, `domains/Provisioning/CapacityForecast.php`,
`domains/Services/PlanFit.php`, `domains/Services/ServiceService.php` (desiredSpec only), `domains/Orders/QuoteService.php`
(assertCapacity only), `app/Console/Commands/Doctor.php` (one loop), `app/Http/Controllers/Api/V1/Staff/ProvisioningController.php`
(capacity `basis`), `app/Http/Support/CatalogPresentation.php` (php_workers wording, 9 lines), `app/Http/Controllers/Web/SurfaceDataController.php`
(eshop lead), `apps/surfaces/api/onhost-svc-pages.api.js` (lead seam, prototype untouched), `config/onhost.php` + `.env.example`
(delimited block), `domains/Services/Metering/MetricRegistry.php` + `domains/Catalog/PlanPromises.php` (php_workers_dedicated
retired for family web), `docs/runbooks/provisioning-queue.md` (§ capacity), `docs/architecture/events-catalog.md` (appended
delimited note), tests `PlanPlacementTest`, `PlanFitTest`, `TenantIsolationSweepTest`, `PublicPagesSeamTest`.

**Contracts.** See `.ai/tasks/TASK-0023.md` § Contracts affected → placement-capacity.

**Verification.**
- VERIFIED: red run before code (16 tests: 2 passed, 14 failed/errored for the expected reasons); green: new + seam + PlanPromises
  tests 25/25, then DedicatedPhpPlacementTest 9/9 and CapacityBasisTest 8/8; full suite `php artisan test --compact --parallel`
  1003/1003 (15382 assertions); `pint --test` on all changed PHP files passed; Larastan on all changed non-test PHP files 0 errors.
- ASSUMED: production has ISPConfig web nodes in every region selling profi (else profi is `capacity_sold_out` in the cart —
  intended); no production placement pins profi/web-hosting to aaPanel (if one exists it is ignored for profi and listed).
- NOT TESTED: `.\brain.ps1 gate` (full) and CI PostgreSQL; real production numbers of sold vs measured disk (only the
  operator's report run shows them); the full Larastan run over the whole tree (only changed files analysed).

**Release notes / operator actions.**
1. After deploy run `php artisan onhost:capacity:basis` (read-only) and review nodes that would close and plans lost under sold disk.
2. Then set `ONHOST_CAPACITY_DISK_BASIS=sold` (and `ONHOST_DISK_SELL_RATIO` if 0.85 is not wanted), config cache, restart workers.
3. Make sure regions selling web-hosting/profi have a usable ISPConfig instance with web-role nodes; remove/narrow any placement the
   doctor lists as ignored for a dedicated plan; decide per service what to do with dedicated-plan services already on aaPanel
   (never moved by the platform; a move across panel types is a manual, customer-agreed operation).
4. Panels with the old `options.capacity_basis: sold` keep RAM+disk sold; drop it to follow decision 19 (RAM measured).
5. Web/managed orders are now refused in the cart (`capacity_sold_out`, 409, nothing charged) when no node can take them;
   `ONHOST_CAPACITY_GATE=false` is the emergency brake.
6. Owner/legal: approve the price-list wording "Sdílené PHP workery" / "N dedikovaných" and the new eshop page lead.

**Concerns.** (1) shop-peak's dedicated promise stays undelivered (owner decision). (2) `php_workers_dedicated` left
`PlanPromises::KNOWN_GAPS` because the boolean text scan now finds it; the managed-family gap lives in the MetricRegistry
reason + doctor WARN instead of the ratchet (reviewer should agree). (3) A web plan pinned to an instance whose nodes were not
imported is now `capacity_sold_out` in the cart instead of stalling after payment.

**Next.** Owner decision on shop-peak; then metering-core / web-disk-total packages (web-disk-total may change what `nvme_gb`
means for a site — `NodeScheduler::commitments()` assumes the owner's `nvme_gb` is the account total).

### Docs for integration

**CURRENT_STATE bullet (English):**
- Placement and capacity (TASK-0023, owner decisions 7/19): a web plan that sells dedicated PHP workers (web-hosting/profi)
  runs only on ISPConfig — placements, staff pins, the scheduler, the cart and plan changes enforce it; other web/managed
  plans are sold as "shared PHP workers". Node capacity is judged per dimension (`CapacityBasis`: disk sold, RAM/CPU
  measured); the disk switch `ONHOST_CAPACITY_DISK_BASIS` stays `measured` until an operator has read the read-only
  `php artisan onhost:capacity:basis`. Web/managed cart lines are refused with `capacity_sold_out` when no node can take
  them. eshop/shop-peak still sells dedicated workers on aaPanel (cannot move to ISPConfig without losing its WAF rate
  limit); doctor WARN until the owner decides.

**production-readiness-audit row (Czech, audit style):**
| N | Umístění podle toho, co tarif prodává, a kapacita uzlů | **Nalezeno a opraveno 2026-09-25 (TASK-0023, rozhodnutí 7 a 19):** (1) tarif s **vyhrazenými PHP workery** (web-hosting/profi) šel přes umístění, jednorázový pin i celoproduktové umístění postavit na aaPanel, kde jeden PHP pool slouží celému uzlu — zákazník zaplatil vyhrazené workery a dostal podíl ze sdíleného poolu. Teď umístění i pin vrátí `placement_requires_dedicated_php`, `resolve()` takové umístění pro profi přeskočí, nová specifikace nese `requires.php = dedicated`, plánovač ji jinam nepostaví, košík ji bez uzlu ISPConfig prodá jako vyprodanou a změna tarifu u služby na aaPanelu skončí `plan_change_does_not_fit`. Ostatní tarify ceník uvádí jako **„Sdílené PHP workery“**. (2) Kapacita uzlu se posuzuje **po rozměrech**: disk podle prodaného, RAM a CPU podle naměřeného (`CapacityBasis`); prodaný disk už nepočítá dvakrát přidružené weby a testovací kopie. Přepínač disku (`ONHOST_CAPACITY_DISK_BASIS`) zůstává `measured`, dokud operátor nepřečte `onhost:capacity:basis` (jen čte). Webové objednávky se v košíku posuzují jako servery. **Otevřené:** eshop/shop-peak dál prodává vyhrazené workery na aaPanelu — přesun na ISPConfig by rozbil rate limit WAF „pro“ a klientský počet webů; čeká na rozhodnutí vlastníka, doctor to hlásí jako WARN. Nic běžícího se nepřesouvá. |

**security-boundaries / web-tools:** no text needed from this package.

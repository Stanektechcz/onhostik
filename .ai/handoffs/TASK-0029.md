# Handoff — TASK-0029

```
From / To:        onhost-security → orchestrator
Task / Status:    TASK-0029 / SELF_VERIFIED (WP1 + WP2 + review rounds 1 and 2, 2026-09-26, on the task branch)
```

**Outcome.** Every service action asks for its own permission and risk. `ServiceActionCommand::PERMISSIONS` names each of the
132 `ServiceActionWorkflow::ACTIONS` exactly once and has no default: an unmapped action is refused with 422
`service_action_unknown` before anything runs (system actor included). Deleting a copy is its own permission at HIGH with a
fresh step-up; anything that hands out a shell or root is `service.console`; every `DestructivePreview` action is HIGH + step-up.
The secondary gates (spec apply, action hooks, Discord) ask the same map, and delegated access (SSH keys, game sub-users) is
revoked when the console goes.

## Outcome per work package

**WP1 — the map (audit C13, C13-H1, C13-H1b, C13-H1c, C13-FM).**

- `permissionFor(action, params)` throws 422 `service_action_unknown` for anything outside the map; `ServiceActionPermissionMapTest`
  keeps `PERMISSIONS` and `ServiceActionWorkflow::ACTIONS` equal in both directions.
- Copies: `backup.delete` → `backup.delete`, `gbackup.delete` → `game.manage`, `snapshot.delete` → `compute.vm.delete`; all HIGH +
  step-up. The catalogue's CRITICAL rating of `backup.delete` stays documentation (D29.2: no customer four-eyes;
  `IdentityCommandAuthorizer` forces CRITICAL only for staff-audience permissions).
- Console line: `command.run`, `command.send`, `shell.*`, `access.reset`, `rescue.start`, `subuser.create` → `service.console`;
  `schedule.create` is `service.console` unless every task is a power or backup task (fail closed). Ending access
  (`rescue.stop`, `subuser.delete`) stays `service.manage`. FTP/database logins stay `service.manage` (D29.3).
- `STEP_UP` = every `DestructivePreview::ACTIONS` (now including `archive.restore`, `database.delete`, `staging.push`,
  `staging.delete`, `backup.delete`, `gbackup.delete`, `snapshot.delete`) + `panel.password`, `access.reset`, `rescue.start`.
  `HIGH_RISK` = `STEP_UP` + `resize`, `mailbox.backup_retention`. No service action is CRITICAL.
- `mailbox.backup_retention` → `backup.policy.manage`; `requiresApproval()` only with `allow_prune` (customers stay refused).
- Protected copies: `gbackup.delete`, `snapshot.delete` and `gbackup.lock` (unlock) refuse a protected / final / legal-hold row
  at request (409 `backup_protected`); `gbackup.delete` and `snapshot.delete` ask again at run time (`ServiceActionWorkflow`).
- Review round 2: unlocking a game backup (`gbackup.lock` with anything but a definite `locked=true`) asks `game.manage`, as
  deleting one does (fail closed). A backup schedule that keeps fewer days or generations than it keeps now
  (`PUT /v1/services/{id}/backups/schedule`, `WebToolsCommandHandler::assertMayThin`) asks the bus's own decision on
  `backup.delete` at the service (fresh step-up, never a token): the next tick prunes to the new values, so it is a deletion.
  A Discord button starts its run under the button's own idempotency key (a double click finds the first operation).
- Assistant: `staging.push` is no longer a proposal, nor a chat-sentence intent (`ServiceIntent`, review round 1);
  `AssistantScope::mayRun()` answers false for an unmapped action.
- Roles: descriptions of `svc_manage` / `svc_console` and of the permission `service.console` say what they now give.

**WP2 — secondary gates (D29.7).**

- Spec apply (`ServiceSpecService::apply`): every step builds its own `ServiceActionCommand` and asks
  `CommandAuthorizer::authorize`; a refused step is skipped as `forbidden:<action>` / `step_up_required:<action>` (no rollback);
  on a `token:` session a `service.console` step is skipped as `token_scope:<action>`; the run carries the step's own
  `authorizedPermission` (H315). `WebToolsCommandHandler` no longer hands the spec command's `service.manage` down.
- Action hooks: `staging.push` out of `ALLOWED`; create filters params, asks `permissionFor()`, refuses a step-up action
  (403 `step_up_required`), then the permission; trigger refuses unknown / step-up / forbidden with the reason in `last_result`,
  hook stays enabled, no operation; the run carries `authorizedPermission`.
- Discord: `staging.push` out of `BUTTON_ACTIONS`; execute filters params, maps an unmapped action and a step-up action to
  `discord_action_forbidden`, asks the action's own permission; the run carries `authorizedPermission`.
- `RevokeDelegatedAccess` keeps SSH keys / game sub-users only for somebody who still holds `service.console` (shared-service
  end and `lostServices`); the `role_changed` pre-filter stays `service.manage`.

## Files

Source: `domains/Services/Commands/ServiceActionCommand.php`, `domains/Services/ServiceService.php`,
`domains/Services/ServiceSpecService.php`, `domains/Services/Commands/WebToolsCommandHandler.php`,
`domains/Services/Listeners/RevokeDelegatedAccess.php`, `domains/Provisioning/Workflows/ServiceActionWorkflow.php` (two local
hunks), `domains/Integrations/ActionHookService.php`, `domains/Integrations/DiscordService.php`,
`domains/Identity/Authorization/PermissionCatalog.php`, `domains/Identity/Authorization/RoleCatalog.php`,
`domains/Support/Assistant/AssistantProposals.php`, `domains/Support/Assistant/AssistantScope.php`,
`domains/Support/Assistant/AssistantService.php` (three lines: the `staging.push` confirm flag of the removed proposal),
`domains/Support/Assistant/ServiceIntent.php` (review round 1: the `staging.push` rule removed; `$m[1]` without `?? ''`),
`domains/Services/Commands/WebToolsCommand.php` (review round 2: the risk comment only),
`phpstan-baseline.neon` (one stale `ServiceIntent` entry removed, none added — shared file, a one-hunk deletion at rebase).

Tests: `tests/Feature/Services/ServiceActionPermissionMapTest.php` (new, 12), `tests/Feature/Services/ServiceActionRoleMatrixTest.php`
(new, 7), `tests/Feature/Services/ServiceActionSecondaryGatesTest.php` (new, 12); step-up grants added (no weakened assertion) in
`tests/Feature/Compliance/LegalHoldRetentionTest.php`, `tests/Feature/Provisioning/WebFeatureActionsTest.php`,
`tests/Feature/Services/MailBackupRetentionTest.php`, and (review round 2, both lower backup retention as the owner)
`tests/Feature/Provisioning/WebToolsFeatureTest.php`, `tests/Feature/Services/BackupScheduleMissesTest.php`.

Docs: `docs/runbooks/service-sharing-and-assistant.md` (capability table). Task records: `.ai/tasks/TASK-0029.md`,
`.ai/reports/TASK-0029-gate.md`, this file.

Not touched (shared-file contract): `DestructivePreview.php`, `CustomerActionParams.php`, config, routes, migrations, events,
OpenAPI, `ServiceController.php`, token scopes.

## Contracts

- No route, event, schema, migration or config change.
- `POST /v1/services/{id}/actions`: new 403 `access_not_approved` (permission missing per the map), 403 `step_up_required`
  (new step-up actions), 409 `backup_protected` (gbackup.delete / snapshot.delete / gbackup.lock unlock on a protected row).
- `GET /v1/services/{id}/actions/{action}/preview` with an unknown action: 422 `service_action_unknown`.
- `PUT /v1/services/{id}/spec`: 200 with refused steps in `skipped` (`forbidden:`, `step_up_required:`, `token_scope:` + action).
- `PUT /v1/services/{id}/backups/schedule` (review round 2): lowering `days` or `generations` below what the schedule keeps now
  answers 403 `step_up_required` (no fresh step-up, or a token) or 403 `access_not_approved` (no `backup.delete` at the service);
  keeping or raising is unchanged. `POST …/actions` `gbackup.lock` with `locked` other than a definite true: 403
  `access_not_approved` without `game.manage`.
- Discord button: a repeated click of the same button answers with the first operation instead of starting a second one.
- `POST /v1/hooks/actions`: `staging.push` → 422 `action_param_invalid`; `monitoring.set` → 422 `service_action_unknown`.
  Hook runs: `accepted:false` with `step_up_required` / `service_action_unknown` / `forbidden`.
- For TASK-0030: `permissionFor()` can return `service.manage`, `service.delete`, `backup.restore`, `backup.delete`, `game.manage`,
  `compute.vm.delete`, `backup.policy.manage`, `service.panel_account.manage`, `service.console` (payload-aware for
  `schedule.create`), and throws 422 `service_action_unknown` otherwise. The scope map must deny or map every one of these. The
  `token_scope:<action>` skip in `ServiceSpecService` may be replaced by a `TokenScopes` check.

## Verification

- VERIFIED — red first: WP1 16 tests (1 passed, 11 failed, 4 errors) and WP2 7/7 failing against the pre-change source; details
  in the task Log.
- VERIFIED — review round 1 red first: ServiceActionSecondaryGatesTest 'does not offer pushing staging to production from a chat
  sentence' failed ("not to contain 'staging.push'") before the `ServiceIntent` rule was removed; the other round-1 tests are
  pins / regression guards (green by design), see the task Findings.
- VERIFIED — verification run 2026-09-26 (after review round 1, code commit `dd51269`): full suite `php artisan test --compact`
  → 1394/1394 passed, 19 378 assertions, no failures, so nothing to compare with `052ceff`; re-run on the committed code
  `dd51269` → 1394/1394 again. `vendor/bin/pint --test` → passed. `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` → first 2
  errors (`nullCoalesce.offset`, `ServiceIntent.php:70,91`): removing the `staging.push` rule changed the regex match shape,
  so the existing baseline entry for that line no longer matched. Fixed at the cause (every rule opens with a group that
  always takes part in a match, so `$m[1]` is used without `?? ''`) and the now-stale entry removed → 0 errors;
  `phpstan-baseline.neon` has 6 lines fewer against `052ceff` and no new entry.
- VERIFIED — review round 2 red first (resumed after a usage-limit cut; the worktree was clean at `d15d014`, so the earlier
  attempt had resolved nothing): with only the new tests, the three task files → 31 tests, 25 passed, 6 not (gbackup.lock unlock
  mapped/allowed as managing in the map, the matrix and over HTTP; developer and power-token schedule lowering → 200; Discord
  double click → two operations). Green: the task files plus WebToolsFeature, BackupFrequencyAsSold, ComputeBackupSchedule,
  BackupScheduleMisses, DiscordIntegration, GameToolsFeature → 83/83. Full suite → 1399/1399 (19 441 assertions);
  `pint --test` passed; phpstan 0 errors, no baseline entry added. Code commit `2717944`.
- VERIFIED — earlier gate `.ai/reports/TASK-0029-gate.md` PASS (Pint, Larastan, Pest, frontend build).
- NOT VERIFIED — that a Pterodactyl scheduled backup task deletes the oldest unlocked backup at the limit (review round 2
  assumption from upstream knowledge; no repo evidence, no live call allowed).
- ASSUMED — the staff role matrix claims of WP1 hold unchanged under WP2 (the WP1 tests still pass; not re-examined by hand).
- NOT TESTED — CI PostgreSQL, live panels (Pterodactyl, Proxmox), the Discord slash-command path beyond the button path
  (same `execute()`), a real person's step-up through the browser.

## Concerns

- Residual risk, not fixed here: `service.manage` (svc_manage, developer, `services:power` tokens) still reaches code execution
  as the site user through `cron.create`/`cron.update`, `file.save`, `node.create`, file upload and deploy hooks, and inside a
  game container through `gfile.save`/`gfile.upload` (plugin jars, configs) and `variable.set` (egg startup variables);
  `schedule.run` fires an existing schedule, also a console schedule the owner made (the owner's fixed command, not arbitrary).
  The console line covers shells and root, not code execution.
- Residual risk, not fixed here: the console line holds from now on. Console access made before this fix through the old
  `service.manage` default — game sub-users (`subuser.create`), root password / SSH keys (`access.reset`), console-command
  schedules — stays on the panels while its holder keeps only svc_manage; `RevokeDelegatedAccess` acts only on future role or
  share events, so an agency that already used the hole keeps its shell. See Follow-ups.
- decisions_needed: `subuser.create` and a console-command `schedule.create` ask `service.console` but no fresh step-up,
  although both are persistent console grants (a sub-user under another e-mail outlives a revocation; a schedule runs the
  command for ever) — unlike `access.reset` / `rescue.start`. Recommendation: add both to the step-up (both are rare; the
  schedule one payload-aware like the permission). Not a defect against D29.3/D29.5 as decided; the current behaviour is
  pinned in `ServiceActionPermissionMapTest` ('does not yet ask a fresh step-up for a persistent console grant') so that a
  change is a deliberate diff.
- Staff widening at bus level only: `backup_dr_admin` holds `backup.delete`; `infrastructure_admin` / `backup_dr_admin` hold
  `backup.policy.manage`. No current HTTP entry point lets them reach a customer service: over the customer API they are refused (`/v1/services/{id}` asks `service.read` first; proved in
  `ServiceActionRoleMatrixTest`).
- API tokens have no step-up grant: every new step-up action (database.delete, staging.push/delete, archive.restore,
  backup/gbackup/snapshot.delete) is now refused to tokens — intended, but a behaviour change for integrations.
- `monitoring.set` is a dead entry in `ActionHookService::ALLOWED` and `DiscordService::BUTTON_ACTIONS` (not a service action;
  now refused as unknown instead of being attempted).
- `AssistantService.php` is outside the listed owned paths (three-line removal tied to the removed `staging.push` proposal).
  `WebToolsCommand.php` likewise (review round 2: a comment that said nothing there destroys data).
- Residual (review round 2): `schedule.toggle` (`service.manage`) can re-activate a console-command schedule the owner disabled —
  same class as `schedule.run`; payload-aware would need the stored schedule, which the one pure map cannot read.
- Residual (review round 2, NOT VERIFIED): if the installed Pterodactyl rotates the oldest unlocked backup away when a scheduled
  backup task hits the limit, a `svc_manage` guest's backup schedule (still managing, D29.4) can thin out copies the owner left
  unlocked; locked ones are safe now that unlocking asks `game.manage`. decisions_needed if confirmed: backup tasks in a
  schedule ask `game.manage`, or sub-hourly backup cron is refused.
- Residual (review round 2): an unmapped action throws 422 inside `authorize()`, before the bus writes its `denied` audit row.
  Not reachable by customers (ServiceController validates `in:ACTIONS`; hooks/Discord/spec catch it first); CommandBus is
  platform code outside this task.
- Pre-existing, seen in review round 2: `WebToolsCommandHandler::backupSchedule` caps days/generations at the PLAN options, so a
  customer who re-saves the schedule under a `backup-plus` add-on policy (e.g. 30/30) lowers it to the plan (7/7). Since this
  round that at least takes a fresh step-up and `backup.delete`; whether the cap should include active add-ons is a follow-up.

## Follow-ups

- D29.8: family permissions `mail.manage`, `database.manage`, `apps.deploy`, `compute.vm.manage`, `service.credentials.rotate` in
  the day-to-day actions (needs its own decision).
- Four-eyes for staff deletion of customer copies.
- Remove `monitoring.set` from hooks / Discord or route it to `UptimeMonitor`.
- `site.delete` decision: it is HIGH + step-up (in `DestructivePreview`, as before) but asks `service.manage`; whether deleting an
  included site should ask `service.delete` is left to the owner.
- UI: gate features by permission in the panel.
- Console access made before TASK-0029 (review round 1): an operator command, `--dry-run` by default with an explicit
  `--apply` (owner rule: no mass change), that lists game sub-users, platform-recorded SSH keys and console-command schedules
  created by principals who now lack `service.console`, and on `--apply` revokes them through the bus.
- The step-up decision for `subuser.create` / console `schedule.create` (decisions_needed above).
- Review round 2: verify on staging whether the installed Pterodactyl rotates unlocked backups for scheduled backup tasks
  (then the decision above); the backup-schedule cap vs. active backup add-ons; optional denied-audit for an unmapped action
  in CommandBus.

## Release notes for existing customers

(One combined note with TASK-0030.) No existing service, customer or plan is changed; this is a tightening of authorization.

- Roles are re-seeded on deploy; only the descriptions of `svc_manage` and `svc_console` change.
- `developer`, `svc_manage` and `services:power` tokens can no longer delete backups, game backups or VM snapshots — neither
  directly nor by setting the backup schedule to keep fewer days or generations: lowering the schedule's retention now takes
  `backup.delete` and a fresh step-up (owner / org_admin; tokens are refused). Keeping or raising it is unchanged.
- Unlocking a game backup now takes `game.manage` (owner, org_admin, game_operator); developer, cloud_operator and
  `svc_manage` / `svc_console` guests can still lock one.
- A repeated click on the same Discord button no longer starts the action twice.
- `svc_manage` can no longer reset root access, start rescue mode, create game sub-users or schedule console commands; SSH keys
  and game sub-users of somebody who loses the console are revoked.
- A fresh step-up is now asked for database.delete, staging.push, staging.delete, archive.restore, backup.delete, gbackup.delete
  and snapshot.delete. API tokens cannot give a step-up, so they are refused these actions.
- Stored action hooks for `staging.push` stop with `step_up_required` (the hook stays enabled and says why); the Discord "push
  staging" button, the assistant's "replace production with staging" proposal and the chat-sentence button ("přenes staging do
  produkce") are gone.
- Spec apply reports refused steps in `skipped` instead of running them.
- Staff, bus-level only: `backup_dr_admin` (`backup.delete`) and `infrastructure_admin` / `backup_dr_admin`
  (`backup.policy.manage`) now satisfy the action permission, but no current HTTP entry point lets them reach a customer service
  (proved by `ServiceActionRoleMatrixTest`). Four-eyes for staff deletion of customer copies is a follow-up.

## Docs for integration

Not edited here (shared-file contract); texts to add at integration.

**`docs/runbooks/production-readiness-audit.md` §7, new row (next free number after 119; the orchestrator assigns it):**

| 120 | Oprávnění akcí služby | **Nalezeno a opraveno 2026-09-25 (TASK-0029, onboarding audit C13, C13-H1, C13-H1b, C13-H1c, C13-FM):** `ServiceActionCommand::permissionFor()` měl výchozí větev — 121 ze 132 akcí služby spadlo na `service.manage` s běžným rizikem. Kdo směl web restartovat, směl mu i mazat zálohy, herní zálohy a snapshoty VM (bez druhého ověření; stačil i token `services:power`), sedm ze třinácti ničivých akcí (`DestructivePreview`) nechtělo druhé ověření a `archive.restore` přepsal web bez něj. Host se sdílením „Služba: správa“ (výslovně bez shellu) se dostal k rootu a konzoli přes `access.reset`, `rescue.start`, `subuser.create` a plán s příkazem konzole. Opraveno: mapa `ServiceActionCommand::PERMISSIONS` jmenuje každou akci workflow právě jednou a nemá výchozí hodnotu — akce, kterou nikdo nezařadil, se odmítne (422 `service_action_unknown`) dřív, než cokoli proběhne, i pro systém. Smazání kopie je vlastní oprávnění (`backup.delete`, u hry `game.manage`, snapshot VM `compute.vm.delete`) s rizikem HIGH a čerstvým druhým ověřením — i nepřímo: plán záloh, který ponechá méně dní nebo generací, než drží teď (další běh plánovače smaže zbytek), chce stejné rozhodnutí o `backup.delete` s druhým ověřením, a odemčení herní zálohy chce `game.manage`; katalogové CRITICAL u `backup.delete` zůstává popisem (zákazník čtyři oči nemá, rozhodnutí D29.2). Shell a root (terminál, příkazy, SSH klíče, `access.reset`, `rescue.start`, herní sub-uživatelé, plán s příkazem konzole) chtějí `service.console`; FTP a databázové přihlášení zůstávají správou. Každá ničivá akce je HIGH s druhým ověřením. Chráněnou, závěrečnou nebo právně blokovanou herní zálohu či snapshot nejde smazat ani odemknout (409 `backup_protected`, znovu při běhu). Vedlejší brány — deklarativní spec, action hooky, Discord — se ptají stejné mapy: hook ani tlačítko nikdy nespustí akci s druhým ověřením, spec přeskočí zakázaný krok a na tokenu i krok konzole. SSH klíče a herní sub-uživatelé se odeberou, když člověk přijde o konzoli, i když správu drží dál. Zbývá: `service.manage` pořád dosáhne spuštění kódu jako uživatel webu (cron, `file.save`, `node.create`, nahrání souboru, deploy hooky) a v herním kontejneru (`gfile.save`, `gfile.upload`, `variable.set`); přístup ke konzoli vzniklý před opravou starou výchozí větví (herní sub-uživatelé, root heslo / SSH klíče, plány s příkazem) zůstává, dokud ho neodebere operátorský příkaz (výchozí `--dry-run`); sub-uživatel a plán s příkazem konzole zatím nechtějí druhé ověření (rozhodnutí vlastníka); rodinná oprávnění (D29.8) | ✔ | `tests/Feature/Services/ServiceActionPermissionMapTest.php`, `ServiceActionRoleMatrixTest.php`, `ServiceActionSecondaryGatesTest.php`. Po nasazení: integrace na API tokenech, které mazaly zálohy nebo pushovaly staging, dostanou 403 — oznámit; uložené hooky `staging.push` hlásí `step_up_required` |

**`docs/context/CURRENT_STATE.md`, one bullet:**

- Service actions (TASK-0029, audit C13): an exhaustive permission map with no default — an unmapped action is refused with
  422 `service_action_unknown`; backup, game-backup and snapshot deletion ask their own permission with a fresh step-up, every
  destructive action is HIGH + step-up, a backup schedule that keeps fewer copies asks the same as deleting them, unlocking a
  game backup asks `game.manage`, shells/root/rescue/game sub-users/console schedules are `service.console`; spec apply,
  action hooks, Discord and the chat intents ask the same map and never run a step-up action; delegated SSH keys and game
  sub-users go with the console. Open: console access made before the fix stays until an operator command (`--dry-run`
  default) revokes it; step-up for game sub-users / console schedules awaits the owner's decision.

**`docs/runbooks/security-boundaries.md`:**

In §20, replace the bullet "Those five actions now ask for `service.console`. Nothing else moves, and no role loses anything it
had: …" with:

* Those actions ask for `service.console`, and since TASK-0029 so does everything else that hands over the server itself:
  `access.reset` (new root password / keys), `rescue.start`, a game panel sub-user (`subuser.create` — the panel and its console,
  under another e-mail that outlives a revocation) and a schedule with a console command (`schedule.create` unless every task is
  power or backup — fail closed). Ending access (`rescue.stop`, `subuser.delete`) stays managing. FTP and database logins stay
  `service.manage`: they give what managing already gives. SSH keys and game sub-users of somebody who loses the console are
  revoked (`RevokeDelegatedAccess`), even when they keep managing.

New section after §25:

## 26. A service action asks for its own permission

* `ServiceActionCommand::PERMISSIONS` names every `ServiceActionWorkflow::ACTIONS` entry once and has no default: an action
  nobody placed in the map is refused with 422 `service_action_unknown` before anything runs, whoever asks (the system included).
  A default arm had sent 121 of 132 actions to `service.manage` at NORMAL risk (audit C13-H1).
* Deleting a copy is its own permission — `backup.delete` for web/managed/mail sets, `game.manage` for a game backup,
  `compute.vm.delete` for a VM snapshot — at HIGH with a fresh step-up. The catalogue rates `backup.delete` CRITICAL; that stays
  documentation, because the authorizer forces CRITICAL only for staff-audience permissions and customers have no four-eyes
  (D29.2). The day-to-day manager (developer, a `svc_manage` guest, a `services:power` token) cannot thin out the owner's backups
  — neither by deleting them nor through the backup schedule: keeping fewer days or generations than the schedule keeps now is a
  deletion (the next tick prunes to it) and asks the same `backup.delete` decision with a fresh step-up
  (`WebToolsCommandHandler::assertMayThin`). Unlocking a game backup asks `game.manage`, as deleting one does.
* Every `DestructivePreview` action is HIGH with a fresh step-up (`archive.restore` was the one restore without it, C13-H1c).
  No service action is CRITICAL; the operator's `mailbox.backup_retention` (`backup.policy.manage`) takes four-eyes only when it
  prunes.
* A copy the platform keeps — protected, the final archive, under a legal hold — is not deleted or unlocked by a service action on
  any panel: 409 `backup_protected` at request, and the run asks again before the panel call.
* Secondary gates ask the same map: spec apply authorizes every step as its own `ServiceActionCommand` and reports a refused step
  in `skipped`; on a token session it skips a console step (`token_scope:`); action hooks and Discord buttons never run an action
  that needs a fresh step-up (a URL or a chat click cannot give one), and refuse an unmapped action. Each run carries the step's
  own permission for the H315 re-check.
* Residual: `service.manage` still reaches code execution as the site user (cron, `file.save`, `node.create`, uploads, deploy
  hooks) and inside a game container (`gfile.save`, `gfile.upload`, `variable.set`); `schedule.run` fires an existing schedule,
  also a console one the owner made, and `schedule.toggle` can switch such a schedule back on. The console line covers shells
  and root, not code execution.
* Residual (not verified): if the installed Pterodactyl rotates the oldest unlocked backup away when a scheduled backup task
  reaches the limit, a backup schedule made by somebody who only manages can thin out the copies the owner left unlocked; a
  locked copy is the owner's protection.
* Residual: console access made before TASK-0029 through the old default (game sub-users, root password / SSH keys, console
  schedules) stays until an operator command (`--dry-run` by default) lists and revokes it; `RevokeDelegatedAccess` acts only
  on future events. A game sub-user and a console schedule ask `service.console` but no fresh step-up (owner decision open).

Tests: `tests/Feature/Services/ServiceActionPermissionMapTest.php`, `tests/Feature/Services/ServiceActionRoleMatrixTest.php`,
`tests/Feature/Services/ServiceActionSecondaryGatesTest.php`.

**`.ai/PROJECT_STATE.md` item 9:** the first of the three HIGH findings (service actions falling through `permissionFor()`) is
resolved by TASK-0029.

## Next

Rebase in the order 0029 → 0030 → 0031; TASK-0030's token scope map must cover every permission string listed under Contracts.

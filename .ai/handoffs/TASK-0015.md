# Handoff — TASK-0015

```
From / To:        onhost-security → orchestrator
Task / Status:    TASK-0015 / SELF_VERIFIED
```

**Outcome.** A customer account can no longer be erased by any administrator within half an hour. Erasure is the
owner's act (`organization.close` + step-up), waits 14 days, can be cancelled by anybody who manages the organization,
and is refused while the account still holds a registered domain.

**Files.** `DataRequestCommand` + handler (new, registered in `DomainServiceProvider`); `ComplianceController`
(`requestData` through the bus, new `cancel`, fresh request key); `routes/api.php`; `ComplianceService`
(`execute_after`, `cancelDataRequest`, the scheduler waits, `active_domains`); config + `.env.example`; router +
lexicon; OpenAPI regenerated; tests.

**Verification.**

- VERIFIED — failing-first: an org admin and a non-stepped-up owner both got 202; no grace period; a domain did not
  block. After: 403, 403 `step_up_required`, the scheduler leaves the request alone for 14 days, cancel works and
  a cancelled request never runs, after the period it runs, `active_domains` blocks.
- VERIFIED — gates on the branch rebased onto development (with TASK-0014): Pest **937 / 14 945**, Larastan 0,
  Pint clean.
- NOT TESTED — no live mail/notification delivery.

**Concerns.** The ISPConfig client `onh_<org>` keeps the customer's company name and e-mail after an erasure; the
platform created that client and should neutralise it (follow-up). No e-mail yet when an erasure is scheduled —
only the in-app notification.

**Next.** The metering programme (the owner's rule 3).

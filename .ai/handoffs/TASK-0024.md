# Handoff — TASK-0024

```
From / To:        onhost-provisioning → orchestrator (then onhost-reviewer, onhost-qa, onhost-security)
Task / Status:    TASK-0024 / SELF_VERIFIED
Branch / base:    fix/TASK-0024-backup-operations-doctor-rows-frequency on stack/base-0017-0019
```

**Outcome per package.**

- **backup-ops (owner points 1 and 18) — done.** `onhost:doctor` shows whether server and database backups sold to
  customers are really taken: `backups.compute` rule state vs sold, Proxmox instances without `backup_storage` (blocking
  only while the rule is on), stalled/paused schedules of every scheduled family (web, managed, mail, cloud, data),
  `meta.delete_blocked`, `meta.orphan_volumes`, backup tick freshness/budget (720 s)/errors, as-sold state, server
  coverage. A server backup retried after a lost POST /vzdump answer adopts the first volume carrying the row's marker
  instead of dumping again; extra marked volumes are only recorded in `meta.orphan_volumes`, never deleted. Frequency and
  retention as sold (`1h` → hourly; one backup a day kept for `backup_days` on web/managed) only behind the new
  default-off rule `backups.as_sold`; read-only review first with `onhost:backups:frequency-plan`.
- **mail-backups (owner point 3) — done.** Behind the new default-off rule `mail.backup_retention`: a mail plan's new
  mailbox is created with `backup_interval=daily`, `backup_copies=backup_days` (Mail Business 14, Mail Enterprise 30); a
  paid plan change applies the new number to the service's own mailboxes (a downgrade is held, applied only by an
  operator's `--apply --allow-prune`); existing mailboxes only via `onhost:mail:backup-retention` (dry run by default).
  Only mailboxes the adapter proves are ours (`onh_` client group on the domain row and mailbox, address inside the domain)
  are written. Fixed on the way: `setAutoresponder` sent a partial `mail_user_update` that dropped the backup fields; it
  now merges like every other mailbox update.

**Files.** New: `app/Console/Commands/BackupFrequencyPlan.php`, `app/Console/Commands/MailBackupRetention.php`,
`domains/Services/Web/BackupOperationsCheck.php`, `domains/Services/Web/BackupDailyKeepers.php`,
`domains/Services/Mail/MailboxBackupPolicy.php`, `domains/Services/Mail/MailboxBackupRetentionPlan.php`,
`domains/Provisioning/Workflows/MailboxBackupRetentionStep.php`, `providers/Contracts/MailboxBackupRetention.php`,
`providers/IspConfig/IspConfigMailBackups.php`, tests `tests/Feature/Platform/DoctorBackupOpsTest.php`,
`tests/Feature/Services/BackupFrequencyAsSoldTest.php`, `tests/Feature/Services/ServerBackupRetryTest.php`,
`tests/Feature/Services/MailBackupRetentionTest.php`, `tests/Contract/IspConfigMailBackupRetentionContractTest.php`.
Edited (minimal hunks): `app/Console/Commands/Doctor.php`, `domains/Services/Web/BackupScheduler.php`,
`domains/Services/Commands/WebToolsCommandHandler.php`, `domains/Provisioning/AutomationLedger.php` (+2 rules),
`domains/Provisioning/Workflows/ServiceActionWorkflow.php`, `domains/Provisioning/Workflows/ProvisionMailDomainWorkflow.php`,
`domains/Services/ServiceService.php` (one family gate), `domains/Services/CustomerActionParams.php`,
`domains/Services/Commands/ServiceActionCommand.php`, `domains/Services/PlanChangeService.php`,
`domains/Services/Metering/MetricRegistry.php`, `domains/Catalog/PlanPromises.php` (KNOWN_GAPS `backup_days` text only),
`providers/IspConfig/IspConfigWebProvider.php`, `providers/IspConfig/IspConfigMailTools.php`,
`providers/Contracts/MailProvider.php` (docblock), `resources/views/admin/integrations.blade.php`, docs
`docs/runbooks/backups.md`, `docs/runbooks/go-live-checklist.md`, `docs/runbooks/provider-onboarding.md`,
`docs/provider-adapters/ispconfig.md`, `.ai/tasks/TASK-0024.md`. 35 files, +2811/−34 against the base.

**Contracts.**

- Automation rules (hot file, appended as one block each): `backups.as_sold` right after `backups.compute`, then
  `mail.backup_retention`; both `default_off`, settings key `automation.enabled` as before.
- Artisan commands (auto-discovered classes, no routes/console.php change): `onhost:backups:frequency-plan {--limit=500}`
  (read-only); `onhost:mail:backup-retention {--apply} {--allow-prune} {--service=*} {--chunk=200}` (read-only without
  `--apply`; `--apply` refused while the rule is off).
- Core action `mailbox.backup_retention` (family mail, HIGH risk, customers get 403 `operator_only`, param `allow_prune`);
  resize param `apply_mailbox_backup` (set only by `PlanChangeService` for family mail).
- Provider contract `Providers\Contracts\MailboxBackupRetention` (`mailboxBackupRetention(domain)`,
  `setMailboxBackupRetention(domain, mailboxId, copies)`, `MAX_COPIES = 365`), implemented by `IspConfigWebProvider`
  (reads `client_get`, `client_get_groupid`, `mail_domain_get`, `mail_user_get`; write `mail_user_update`);
  `MailProvider::createMailbox` optional `backup_copies`.
- Data in existing JSON columns: `backups.meta.orphan_volumes`, `tags.mail_backup`, ledger stat `backups.run.seconds`,
  scheduler array key `as_sold`; audit action `service.mail_backup.retention`; MetricRegistry rows may carry `kept_under`.
- No route, event, migration, config key, .env, NotificationRouter, events-catalog, permission or role change.

**Verification.**

- VERIFIED (this run, whole branch at 24ce2eb before the handoff commit): `php artisan test --compact` 1047/1047 passed
  (15 599 assertions, 262 s); `vendor/bin/pint --test` passed; `vendor/bin/phpstan analyse --memory-limit=2G
  --no-progress` 0 errors with `phpstan-baseline.neon` unchanged against the base; `tests/Feature/Catalog/PlanPromisesTest.php`
  (ratchet) green inside the suite. Failing-first evidence for both packages is recorded in the task file (backup-ops
  27 tests red → green; mail-backups 35 tests red → green). Forbidden paths untouched (CURRENT_STATE,
  production-readiness-audit, security-boundaries, web-tools, migrations, routes/console.php, apps/surfaces).
- ASSUMED: PVE's storage listing returns `notes` (TASK-0019 assumption); "one backup a day for `backup_days` beyond the
  generations" is what the owner means by "Zálohy 30/90 dní"; ISPConfig `mail_user` default `backup_interval='none'`,
  the remote API accepts `backup_interval=daily` and `backup_copies` up to 30, `mail_user_get` returns `sys_groupid`, the
  panel prunes copies above a lowered `backup_copies` at its next run.
- NOT TESTED: a real Proxmox or ISPConfig (staging); PostgreSQL (JSON-path `whereIn` on
  `entitlements->backup_frequency`, `desired->action` query — CI pest-postgres decides); tick duration on production.

**Concerns.**

1. Mail-server disk: 30 daily copies of Mail Enterprise mailboxes (up to 100 × 25 GB) — size and monitor the backup
   directory before switching `mail.backup_retention` on.
2. A 15-minute plan can still overlap backups on one service and overrun ticks (the doctor now shows the overrun).
3. `automation.toggle` is NORMAL risk, so switching a default-off rule on needs no step-up (risk-policy package, not here).
4. `kept_under` for web/managed relies on backup-ops' reading of `backups.as_sold`; drop that entry if the owner reads
   "Zálohy N dní" differently.
5. The plan-change mailbox step does not wait for the panel queue (a late datalog error must not roll back a paid plan
   change); the next dry run or the doctor shows it.
6. Web plans' mailboxes (web family, mail-hosting add-on) get no retention — outside owner decision 3.

**Follow-ups.** Staging ISPConfig checks listed under ASSUMED; staging Proxmox check of `notes` in the storage listing;
catalogue versions should spell `hourly` instead of `1h` (new plan versions only); risk-policy for `automation.toggle`.

**Release notes for existing customers.** Nothing changes for any existing service at deploy: both behaviour changes sit
behind default-off rules and every existing-service path is an operator command with a dry run by default. The doctor
rows and `onhost:backups:frequency-plan` are read-only. The only unconditional behaviour changes are fixes: a retried
server backup after a lost Proxmox answer no longer leaves a second full VM copy (orphans are reported, never deleted),
and changing an autoresponder no longer resets a mailbox's backup settings. Historical ISPConfig/aaPanel resources are
never written (not proven ours → reported only).

## Docs for integration

### docs/runbooks/production-readiness-audit.md (Czech, audit style)

- "Zálohy serverů a DB — provoz (TASK-0024, backup-ops): `onhost:doctor` nově hlásí stav pravidla `backups.compute` proti
  prodaným zálohám, instance Proxmoxu bez `backup_storage` (blokující až při zapnutém pravidle), zastavené a pozastavené
  plány všech rodin (i `cloud`/`data`), zálohy, jejichž svazek nešlo smazat (`meta.delete_blocked`), osiřelé svazky
  (`meta.orphan_volumes`, jen hlášení, nic se nemaže) a běh `onhost:backups:run` (čerstvost, rozpočet 720 s, chyby).
  Opakovaná záloha po ztracené odpovědi na POST /vzdump převezme svazek s vlastní značkou místo druhého vzdumpu.
  Frekvence podle ceníku (`1h` = hodinově) a denní zálohy po dobu `backup_days` jen s pravidlem `backups.as_sold`
  (výchozí vypnuto); dopad ukáže `onhost:backups:frequency-plan` (nic nemění). Stav: hotovo v kódu, zapnutí je
  rozhodnutí vlastníka; neověřeno proti skutečnému Proxmoxu."
- "Zálohy schránek podle tarifu (TASK-0024, mail-backups, rozhodnutí 3): poštovní tarify prodávají `backup_days` (14/30),
  ale `mail_user_add` nikdy neposlal `backup_interval`/`backup_copies`, takže schránky měly výchozí nastavení panelu
  (předpoklad: žádné zálohy). Nově s pravidlem `mail.backup_retention` (výchozí vypnuto): nová schránka poštovního tarifu
  dostane denní zálohy na `backup_days` kopií, zaplacená změna tarifu je přenese na vlastní schránky služby (snížení jen
  na pokyn provozu, `--allow-prune`, protože panel smaže přebytečné kopie), stávající schránky jen přes
  `onhost:mail:backup-retention` (bez `--apply` jen výpis, u snížení „will prune N existing copies“). Zapisuje se jen do
  domény prokazatelně naší (klient `onh_`, jeho skupina na doméně i schránce, adresa v doméně); cizí schránky se jen
  hlásí. Oprava: nastavení autoresponderu už nemaže nastavení záloh schránky. Doctor hlásí služby pozadu. Stav: hotovo
  v kódu, zapnutí je rozhodnutí vlastníka; neověřeno proti skutečnému ISPConfigu (staging), kapacitu záložního disku
  poštovního serveru je třeba dimenzovat předem."

### docs/context/CURRENT_STATE.md (English bullets)

- "Backup operations (TASK-0024 backup-ops): the doctor reports server/database backup readiness (`backups.compute` vs
  sold, missing `backup_storage`, delete-blocked and orphaned volumes, stalled/paused schedules of every family, backup
  tick age/budget/errors); a retried Proxmox backup adopts its first marked volume instead of dumping twice (extra marked
  volumes are reported in `meta.orphan_volumes`, never deleted); plans are backed up as often and as long as sold (`1h` →
  hourly, one backup a day kept for `backup_days` on web/managed) only under the default-off rule `backups.as_sold`,
  reviewed first with `onhost:backups:frequency-plan`."
- "Mailbox backups (TASK-0024 mail-backups): under the default-off rule `mail.backup_retention` a mail plan's new mailboxes
  are created with daily ISPConfig backups kept for `backup_days` (14/30), a paid plan change applies the new number to
  the service's own mailboxes (fewer copies only with the operator's `--allow-prune`), existing mailboxes only via
  `onhost:mail:backup-retention` (dry run by default); only mailboxes proven to be the platform's (`onh_` client group +
  domain) are written; the doctor lists mail services behind their plan; `MetricRegistry` counts `backup_days` as kept per
  family only while that family's rule is on (`kept_under`)."

### docs/runbooks/security-boundaries.md

- "Mailbox backup retention (TASK-0024): `mailbox.backup_retention` is operator-only (CustomerActionParams 403
  `operator_only`, HIGH for staff); the ISPConfig adapter writes a mailbox's retention only after proving the mail domain
  (`onh_` client, its group, the domain name) and the mailbox (same group, address inside the domain); the copy count
  comes from the service's entitlements, never from a request."
- backup-ops adds nothing here (the backup marker is still derived from the row id only; orphaned volumes are never
  deleted).

### docs/runbooks/web-tools.md

- The backup schedule ceiling (`PUT /v1/services/{id}/backups/schedule`) reads `1h` as `hourly` only under
  `backups.as_sold`; otherwise unchanged.
- Autoresponder changes are merged into the stored mailbox record (the mailbox's backup fields survive).

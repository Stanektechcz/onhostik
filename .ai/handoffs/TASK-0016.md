# Handoff — TASK-0016

```
From / To:        onhost-security → orchestrator
Task / Status:    TASK-0016 / SELF_VERIFIED
```

**Outcome.** A customer can no longer reach another customer's mailbox on the shared mail server through the
mailbox tools. `autoresponder.set`, `spam.policy`, `filter.create`, `filter.delete`, `mailbox.backup`,
`mailbox.restore` and `fetchmail.create` look the mailbox up in the service's own mail domains before any panel
write; the per-mailbox listings (`mail_autoresponder`, `mail_filters`, `mail_spam` with `remote_id`) answer 404 for
a mailbox that is not the service's; a fetchmail destination must be in the service's domain (422) and be one of
its mailboxes (saga).

**Files.** `ServiceActionWorkflow` (`OWN_MAIL_TARGETS`, the guard generalised from three actions to ten);
`MailDomains::ownRow` (new); `ServiceFeatures::ownMailboxRef` (new, used by the three listings); `ServiceService`
(`fetchmail.create` destination through `MailAddresses::assertOwn`); new test
`tests/Feature/Services/MailboxToolsOwnershipTest.php`; audit row 103; CURRENT_STATE.

**Contracts.** Listing 404 `mailbox_not_found` (was 200 with a stranger's data); `fetchmail.create` 422
`mail_domain_not_yours`; saga failure `not_ours`. No schema/config/event changes.

**Verification.**

- VERIFIED — failing-first on the unfixed code: 8 of 11 tests failed (five actions SUCCEEDED against mailbox 32 of
  `cizi.cz`, fetchmail accepted a foreign and a non-existent destination, the listing returned 200). After the fix
  the file is 10 / 10 green, `MailboxOwnershipTest` still green.
- VERIFIED — gates in the worktree: Pint clean, Larastan 0 errors, whole suite with `ONHOST_WIDTH_GUARD=1`
  **947 tests / 14 992 assertions** green.
- ASSUMED — `mailbox.backup` guard: same code path as the tested actions, but the action is feature-gated off on
  ISPConfig (no on-demand backup), so no test drives it.
- NOT TESTED — live panels (tests only, `Http::fake` + `preventStrayRequests`).

**Concerns.** One extra `mail_user_get` listing per mailbox action and per uncached per-mailbox read (the price of
the proof). The mailbox ref is still built with the first/resolved mail domain's node and client — harmless once
ownership is proven, but a service with mail domains on different servers would need the ref from the owning domain.

**Next.** Integrate onto `development`; no follow-up required for this hole.

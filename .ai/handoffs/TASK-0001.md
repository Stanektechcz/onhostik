# Handoff: TASK-0001

- **From / To:** orchestrator (Claude session "ONHOST multi-agent development system") → human (integration decision)
- **Task / Status:** TASK-0001 AI orchestration bootstrap / SELF_VERIFIED, reviewed; awaiting integration approval
- **Branch:** `chore/TASK-0001-ai-orchestration` (rebased on `development` `1e11f62`)

## Outcome

ONHOST has a repository-backed AI team layer: baseline + gate that separates pre-existing failures from regressions,
knowledge maps derived from the code, 15 `onhost-*` agents, `/ai-*` workflow skills, one-worktree-per-task tooling
with path locks and a live board, and a session start protocol in `CLAUDE.md`. No application behaviour changed.

## Files

`.ai/**`, `.claude/agents/onhost-*.md`, `.claude/skills/ai-*/SKILL.md`, `.claude/settings.json`,
`scripts/ai/gate.ps1`, `scripts/ai/task.ps1`, `scripts/ai/tests/Orchestration.Tests.ps1`; edited `brain.ps1`
(two commands + argument forwarding), `CLAUDE.md` (two sections), `AGENTS.md` (one section).

## Contracts

`brain.ps1 gate`, `brain.ps1 task` added; existing `brain.ps1` commands unchanged. No routes, schema, config keys,
events or provider calls changed.

## Verification

- VERIFIED: baseline at `8e4614a` (Pest 856/856, Larastan 0, Pint, build, audits, migrations on scratch SQLite);
  `Orchestration.Tests.ps1` all pass; gate classification (pre-existing vs regression, failing assertion, crash while
  loading); `task claim/release/board` incl. overlap refusal (exit 2); `task start` (worktree, junction, task commit,
  lock) incl. two concurrent starts getting distinct ids; `task finish` refusals (dirty, unmerged) and removal;
  gitleaks on both commits; final gate report `.ai/reports/TASK-0001-gate.md`.
- ASSUMED: Claude Code honours the `ask`/`deny` patterns in `.claude/settings.json` as documented (not exercised
  from a fresh session); new agents and skills load in a session started after merge (this session predates them).
- NOT TESTED: `task start` with a real `composer install` end to end (install was exercised manually for this
  worktree); PostgreSQL CI and e2e for this branch (not pushed).

## Concerns

- Pre-existing: order-dependent `RenewalGuardTest` (in baseline, follow-up suggested); `Config.Tests.ps1` fails until
  the Brain branch config is merged or the test adapted; the vault's `Project-Docs` junction points at a stale clone.
- Bash is not sandboxed for "read-only" agents (`.ai/SECURITY_RULES.md` §7).

## Next

Human: review `git diff development...chore/TASK-0001-ai-orchestration`; if accepted, push and open a PR into
`development` (or fast-forward locally), then run `.\brain.ps1 task finish -Id TASK-0001` from the main checkout.

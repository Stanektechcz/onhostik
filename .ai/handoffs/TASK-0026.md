# Handoff — TASK-0026

```
From / To:        onhost-docs → orchestrator (reviewers: onhost-reviewer, onhost-qa; onhost-security for the runbook)
Task / Status:    TASK-0026 / SELF_VERIFIED (part A; part B = the final docs wave after TASK-0020 … TASK-0025 merge)
Branch / base:    fix/TASK-0026-owner-decisions-2026-09-25-recorded-adr on stack/base-0017-0019
```

**Outcome.** Documentation only. The owner's 25 decisions of 2026-09-25 are recorded once, with authority, in
ADR-0007; each row names its implementing task (TASK-0019 … TASK-0025), how existing customers are protected, and the
state of the code on 2026-09-25 (with file:line where it describes built behaviour). Decision 22 (taking over a
historical site) has an operator runbook that uses existing tools only and never binds or touches the historical
resource. The ONHOST-BRAIN vault mirrors the decisions (outside Git of this repository).

## Outcome per package

| Package | Outcome |
| --- | --- |
| ADR-0007 | `docs/adr/0007-owner-decisions-2026-09-25.md`: `**Status:** accepted`, context (the three standing rules of 2026-09-24), a 25-row decision table, consequences, "Not decided here". Row 23 = TASK-0025 (pay-and-restore), row 22 → runbook, rows 21/24 postponed. |
| Decisions index | `.ai/DECISIONS.md` lists ADR-0007; next ADR number is 0008. |
| Runbook, decision 22 | `docs/runbooks/historical-site-import.md` (ticket, two ownership proofs, NEW site on its temporary `<short id>.web.onhost.cz` name, archive made by the owner or read-only outside the platform, `import.run` by the customer, domain pairing, what is not available); listed in `docs/runbooks/README.md`. |
| Vault (not in this Git) | `Hosting/OWNER-DECISIONS-2026-09-25.md` (new hub, rows 1–25), 16 P0 cards → `assessed` (H01 H02 H03 H06 H07 H08 H09 H11 H12 H13 H14 H16 H17 H18 H23 H29), H30/H428/H429/H304 stay `proposed` with an owner-decision section, H06/H23 record postponed OV/anti-DDoS, `Hosting/README.md` (770 cards), `Hosting/ASSESSMENT-2026-09-19.md`, `Hosting/METERING-PLAN-2026-09-24.md` (phase owners, decided questions), `System/CAPABILITY-COVERAGE.md` regenerated. |

## Files (repository)

- `docs/adr/0007-owner-decisions-2026-09-25.md` (new)
- `docs/runbooks/historical-site-import.md` (new)
- `docs/runbooks/README.md` (+1 row)
- `.ai/DECISIONS.md` (+1 row, next number 0008)
- `.ai/tasks/TASK-0026.md`, `.ai/handoffs/TASK-0026.md`

No PHP, config, route, migration, seeder, surface or hot file changed. None of the shared-file contract's hot or
central files is touched, so the rebase into the stack cannot conflict except on `.ai/DECISIONS.md` if another task
also adds an ADR (none is planned).

## Contracts

None. The docs only cite existing names: rule `backups.compute`, command `onhost:backups:compute-plan`,
`ONHOST_UPLOAD_MAX_BYTES`, `ONHOST_FOUR_EYES`, action `import.run`, permission `billing.wallet.spend` (as TASK-0021's
name), role `svc_manage`.

## Verification

VERIFIED (HEAD `348fc7c` before the handoff commit, in this worktree):

- `php artisan test --compact` → 985/985 passed, 15230 assertions, exit 0.
- `vendor/bin/pint --test` → passed. `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` → 0 errors;
  `phpstan-baseline.neon` unchanged against `stack/base-0017-0019`.
- Every file:line the ADR and the runbook cite was re-read on this branch and still says what the docs claim
  (AutomationLedger:48, CatalogSeeder:38/:44/:156-159/:165-167, BackupScheduler:37/:265,
  IdentityCommandAuthorizer:68-69, PermissionCatalog:151, ServiceActionCommand:35-48/:56-58, Addons:38-41,
  ServiceService:1198-1212/:1479, ImportService:204, UsageGuard:37, ServiceFeatures:213, AaPanelWebProvider:96-99,
  IspConfigWebProvider:188-197/:585-590, WebMigration:17-29, backups.md:354, ADMIN_CONFIGURATION_ARCHITECTURE.md:122).
  `ONHOST_UPLOAD_MAX_BYTES` and `ONHOST_FOUR_EYES` exist in `config/onhost.php`; `onhost:backups:compute-plan` exists in
  `routes/console.php`.
- Red/green acceptance script (session scratchpad, 50 checks): red 45/47 failed before the edits, green 50/50 after.
- `scripts/ai/tests/Orchestration.Tests.ps1` passed; vault coverage assertions of `Test-ONHOST-Brain.ps1` passed.

ASSUMED:

- ADR rows marked "Pending" describe what TASK-0020 … TASK-0025 were opened to do; their line numbers will move once
  those tasks merge (the ADR cites base-branch lines).
- Runbook step 7 (pairing the real domain) when the new site lands on the historical vhost's node — derived from code,
  never run. The runbook says to stop and escalate in that case.

NOT TESTED:

- The full `Test-ONHOST-Brain.ps1` (its refresh step writes in the main checkout).
- Any live panel behaviour; the runbook was not exercised against ISPConfig or aaPanel.

## Concerns

- Line-number citations in ADR-0007 go stale as the stack lands (ServiceService.php, CatalogSeeder.php and
  PermissionCatalog.php are changed by other tasks). Part B should refresh them after the rebase.
- Row 13 says TASK-0022 corrects `ADMIN_CONFIGURATION_ARCHITECTURE.md:122`; if TASK-0022 does not, part B must.
- Row 20 relies on TASK-0021's `billing.wallet.spend`; if the name changes in integration, the ADR row must follow.
- The provider error texts ("adopting it is an explicit operator decision") now have a procedure behind them; rewording
  them is a code change and was left out.

## Follow-ups

- Part B (final docs wave): apply "Docs for integration" below and refresh ADR line numbers after the stack merges.
- Run `Update-CodeMap.ps1` on the merged main checkout so `System/CODE-MAP.md` lists ADR-0007; the vault links to
  `Project-Docs/adr/0007-…` and the runbook resolve only after this branch reaches the main checkout.
- A staff path that runs `import.run` for a customer, and mailbox import from a historical mail domain: each needs its
  own task (and a security review for the first).

## Release notes for existing customers

Nothing changes for any existing customer, service or historical site from this task: it adds documents only. The
decisions it records reach existing customers only through the implementing tasks, and there only through new plan
versions, `default_off` rules or `--dry-run` operator commands (see each ADR row's "Existing customers" column). The one
deploy-time change the ADR flags is TASK-0021's `billing.wallet.spend` role change (row 20), which belongs in that
task's release notes.

## Docs for integration

Part B texts for the final docs wave (not applied here):

- `docs/context/CURRENT_STATE.md` (English). Replace the sentence "Adoption of a historical site is an explicit
  operator decision of its own (brain H304), never a side effect of an order." with: "A historical site is taken over
  only at its owner's explicit request, as a customer-run import into a NEW platform-created site with operator
  assistance (ADR-0007, decision 22; `docs/runbooks/historical-site-import.md`); the historical resource itself is
  never bound or modified, and never as a side effect of an order." Add a bullet: "Owner decisions of 2026-09-25 are
  recorded in ADR-0007 (25 decisions, each naming its implementing task TASK-0019 … TASK-0025); the 16 P0 vault cards
  are `assessed`."
- `docs/runbooks/production-readiness-audit.md`, a new row in the audit's style: "Rozhodnutí vlastníka 2026-09-25 |
  25 otevřených otázek (zálohy, ceník, měření, čtyři oči, identita, odstoupení, historické weby) blokovalo navazující
  práci | zapsáno v ADR-0007 s úkolem u každého bodu (TASK-0019 … TASK-0025); převzetí historického webu jen jako import
  do nového webu (`docs/runbooks/historical-site-import.md`); 16 karet P0 v trezoru `assessed` | dokumentace, bez
  změny kódu". Row 1: replace "rozhodnout frekvence záloh v tarifech" with "frekvence záloh = ceník (ADR-0007 bod 18);
  `1h` scheduler nezná → TASK-0024". Row 6: append "ADR-0007 bod 13: HIGH = step-up, čtyři oči jen CRITICAL a změny cen
  a tarifů (TASK-0022); výjimky jednotlivých operací zůstávají k rozhodnutí."
- `docs/runbooks/security-boundaries.md`: under the historical-site guards (or the section on panel resources), add:
  "Taking over a historical site never binds it: the only procedure is an import into a NEW site
  (`historical-site-import.md`, ADR-0007 decision 22), and no staff path runs `import.run` for a customer."
- `docs/runbooks/web-tools.md`, Import row: append "; a historical site (not created by ONhost) only via
  historical-site-import.md".
- `docs/runbooks/backups.md` (after TASK-0019/0024): the frequency is what the plan version sells (ADR-0007 §18),
  there is no lower platform default, and the `1h` gap is closed by TASK-0024's operator command. In the
  `backups.compute` section: "Owner decision (ADR-0007 §1): switch on once compute-plan lists no MISSING
  backup_storage." In "Still NOT built": PITR → "off the price list in a new plan version (ADR-0007 §2, TASK-0022)";
  mail `backup_days` → "decided: retention from backup_days (ADR-0007 §3, TASK-0024)".
- `docs/runbooks/approvals.md` (after TASK-0022): "Owner decision 2026-09-25 (ADR-0007 §13): HIGH is a fresh step-up
  and nothing more; CRITICAL and every price or plan change take the second person. With `ONHOST_FOUR_EYES=false`
  these are waived and audited like every critical action."

# Handoff — TASK-0021

## Review round 3 — resolutions (2026-09-25)

```
From / To:        onhost-backend → orchestrator (re-review: billing lens)
Task / Status:    TASK-0021 / SELF_VERIFIED
```

2 MEDIUM resolved as the orchestrator decided, none declined (red/green evidence in `.ai/tasks/TASK-0021.md` Findings).

- **Auto-renew default and non-holder orders (MEDIUM):** the organization's standing auto-renew default is the holder's
  standing consent to renewals paid from credit.
  - `OrganizationService::update()`: switching `auto_renew_default` on (when it is not already on) needs
    `billing.wallet.spend` (403 `credit_spend_not_allowed`, Czech). Switching it off, or re-sending the current value, stays
    open. A refused request applies nothing.
  - New `CreditOrderPolicy::followsStandingDefault()` means the switch is on, the actor is not `system` and the actor is not
    a holder. `CheckoutService::placeOrder()` stamps such orders (any payment mode, card and bank included) with
    `meta.renewal_consent = organization_default`. `DomainService::createFromOrderItem()` registers their domains with
    `auto_renew = organization.auto_renew_default`. `DomainService::register()` (direct command) and `transferIn()` apply
    the same default instead of the non-holder's `auto_renew` or the model default `true`.
  - `SubscriptionService::ensureForService()` already used the organization default and is unchanged (pinned by a guard
    test). A domain subscription copies the domain's flag.
- **Report crash (MEDIUM):** `onhost:orders:credit-approval-report` no longer asks the Authorizer about a `ServiceAccount`.
  Active service accounts are listed as the kind `service account (always held)`, and disabled ones are left out. The label
  uses ASCII parentheses rather than the suggested em dash.

Files: `domains/Organizations/OrganizationService.php`, `domains/Orders/{CreditOrderPolicy,CheckoutService}.php`,
`domains/Domains/DomainService.php`, `app/Console/Commands/CreditApprovalReport.php`,
`tests/Feature/Orders/CreditRenewalConsentTest.php` (new, 8 tests), `tests/Feature/Orders/CreditOrderApprovalTest.php` (+1 test).

Contracts: new order meta key `renewal_consent` (internal; not presented by the API). No route, event or config change.

Owner rules: everything is behind `ONHOST_ORDER_CREDIT_APPROVAL` (default off). Existing subscriptions, domains and orders
are not rewritten; only orders placed and domains created after the switch is on are affected. No historical resource is
touched.

Verification (VERIFIED): whole suite 1041 passed / 0 failed; `pint --test` passed; phpstan 0 errors, baseline unchanged.

Concerns / not changed:
- A staff-assisted order (`source=staff`) is not held for approval, but a staff user is not a holder in the organization.
  Its items therefore also follow the organization default (the safe side).
- The same rule is not applied to the marketplace, whose monthly subscriptions are created `auto_renew=true`. A marketplace
  order is already holder-only (`assertMaySpend`).

## Review round 2 — resolutions (2026-09-25)

```
From / To:        onhost-backend → orchestrator (re-review: billing lens)
Task / Status:    TASK-0021 / SELF_VERIFIED
```

1 HIGH and 2 LOW resolved, none declined (details and red/green evidence in `.ai/tasks/TASK-0021.md` Findings).

- **Stale plan change on approval (HIGH):** new `domains/Orders/HeldPlanChangeCheck.php`, called first in
  `CreditOrderApprovals::approve()`. Each `upgrade_of` line must still match: active subscription, the service on
  `plan_change.from_plan`, same `from_period` and `old_net_minor`, no renewal since `placed_at`, no other plan-change line of
  the service paid since `placed_at`, and the priced share of the period within one day of today's. Otherwise 409
  `order_approval_stale` (+ `reason`), Czech message "reject it, the requester orders again"; the decision rolls back
  (no hold, no document, order keeps waiting until rejected or expired). Chosen over refusing to hold plan-change orders so a
  member can still ask for an upgrade.
- **Cancel vs approval race (LOW):** `CheckoutService::transition()` gained an optional `$onlyFrom` re-checked on the locked
  row (409 `order_state_changed`); `cancel()` passes its allowed states.
- **Resubmitted held cart (LOW):** the placement pre-check `assertCoverable()` runs after the duplicate-fingerprint lookup.

Files: `domains/Orders/HeldPlanChangeCheck.php` (new), `domains/Orders/CreditOrderApprovals.php` (2 lines),
`domains/Orders/CheckoutService.php` (local hunks), `tests/Feature/Orders/CreditOrderApprovalStaleTest.php` (new, 7 tests).

Verification (VERIFIED): whole suite 1032 passed / 0 failed; `pint --test` passed; phpstan 0 errors, no baseline entries.

Concerns / not changed (pre-existing, outside the task):
- `PlanChangeService::apply()` still does not re-validate `from_plan`/period for bank-paid plan changes (14-day window).
- An owner (or the usage watch) placing the same upgrade cart within the 15-minute duplicate window may receive the member's
  waiting order back when the fingerprint matches (the frozen fraction is rounded to 4 decimals, so only within minutes) instead of paying at once; nothing is charged twice, but the owner then has to approve it.
- `PlanChangeService::prorationFraction()` reads `time()`, not the application clock; the new check uses `now()`.

**Docs for integration (addendum to the security-boundaries section 22 text below):** "An approval re-validates every
plan-change line of the held order (`Orders\HeldPlanChangeCheck`): a line the service has outgrown (other plan, renewed
period, a competing paid change, more than a day of proration elapsed) is refused with 409 `order_approval_stale` and nothing
is reserved."

## Review round 1 — resolutions (2026-09-25)

```
From / To:        onhost-backend → orchestrator (re-review: onhost-reviewer, onhost-qa, onhost-security, billing)
Task / Status:    TASK-0021 / SELF_VERIFIED
```

All 2 HIGH, 6 MEDIUM and 7 LOW findings are resolved. None was declined. Each finding and its red/green evidence is in
`.ai/tasks/TASK-0021.md` under Findings. In short:

- **Expiry race (HIGH + MEDIUM):** `CreditOrderApprovals::expirePending()` now only lists the candidate ids. Each order is
  then expired in its own transaction under `lockForUpdate`, and it is skipped when it is no longer pending. An approval
  that commits while the nightly prune runs now stays PAID/approved with its hold active. The pre-existing
  `CommerceHousekeeping::expireUnpaid` loop for PENDING_PAYMENT has the same unguarded shape. It is not part of this task
  and was not changed.
- **Standing credit consents (HIGH + MEDIUM):** a non-holder can no longer switch these on:
  - `auto_upgrade` (service policy), with an audit detail `auto_upgrade_enabled_by`
  - domain auto-renew
  - subscription auto-renew

  All three return `credit_spend_not_allowed` (403) with a Czech message. Switching off stays open to everyone. The
  scheduled renewals, the usage-watch upgrades and `retryPastDue` still run as `system`. **Decision recorded:** an
  auto-renewal or auto-upgrade that is on is the standing consent of whoever switched it on, and only a holder can now
  switch it on. Existing services, domains and subscriptions that already have it on are not touched, per the owner rule
  against en-masse changes. **Owner to confirm:** a switch that a non-holder turned on before the deploy keeps working.
  `service.policy` audit rows show who turned it on from now on.
- **Service accounts (MEDIUM):** the new test found a real crash. `Authorizer::can()` calls `getAuthIdentifier()`, which
  `ServiceAccount` does not have, so a service-account credit order threw an Error. `CreditOrderPolicy::holds()` now
  returns false for a ServiceAccount without asking the Authorizer, so the order is held. **Out of scope and unresolved:**
  the Authorizer defect itself. Every permission check with a ServiceAccount principal crashes the same way, and so does
  `IdentityCommandAuthorizer` for `service_account` contexts. It needs its own task in the identity domain.
- **Approvers (MEDIUM + LOW):** the policy bindings are intersected with the organization's current memberships, so a stale
  binding no longer mails a former member. The literal owner stays. Expired bindings were already filtered, and a test now
  pins this.
- **Placement pre-check (LOW):** it now subtracts `domainReserve()` like `hold()` does. `Quote` gained a typed `@property`
  for `lines` so that Larastan reads the JSON cast. There are no new baseline entries.
- **Work offer (LOW):** the gate now runs after the locked "same answer twice" return, so a repeat approval is a no-op, not a 403.
- **Seam money (LOW):** the approvals total now comes from `Order::total()->toDecimal()`.
- **Test-only (LOW):** a replayed approval Idempotency-Key returns the first result, and a personal API token gets 403 on the
  approval route. Both passed at the first run and pin existing guarantees.
- **Scope (MEDIUM):** the task file's Scope section now lists every touched path.

New or changed files in this round:
- `domains/Orders/{CreditOrderApprovals,CreditOrderPolicy,CheckoutService}.php`
- `domains/Orders/Models/Quote.php`
- `domains/Services/Commands/WebToolsCommandHandler.php`
- `domains/Domains/DomainService.php`
- `domains/Billing/SubscriptionService.php`
- `domains/Support/WorkOfferService.php`
- `app/Http/Controllers/Web/SurfaceDataController.php`
- `tests/Feature/Orders/{CreditOrderApprovalTest,CreditSpendGateTest}.php`
- `.ai/tasks/TASK-0021.md`

**Verification (round 1).**
- VERIFIED: the 2 credit test files 35/35 (240 assertions). The red evidence per finding is in the task file.
- VERIFIED: whole suite `php artisan test --compact` 1025 passed, 0 failed (15542 assertions).
- VERIFIED: `vendor/bin/pint --test` passed.
- VERIFIED: `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` reported 0 errors. `phpstan-baseline.neon` is unchanged.
- NOT TESTED: PostgreSQL. The expiry now uses `lockForUpdate` per order, and SQLite ignores row locks, so the race test simulates the concurrent commit instead.

**Docs for integration (addendum to security-boundaries section 22):** "Standing credit consents: switching on
`tags.policy.auto_upgrade` (`WebToolsCommandHandler` `policy.set`), domain auto-renew (`DomainService::setAutoRenew`) or
subscription auto-renew (`SubscriptionService::setAutoRenew`) calls `assertMaySpend()`. Switching off stays open. What the
platform then does as `system` (renewal scheduler, usage watch, `retryPastDue`) is the consent of the holder who switched it
on. `CreditOrderApprovals::expirePending()` locks and re-reads each order before it expires it (an approval may commit
meanwhile). A ServiceAccount never holds `billing.wallet.spend` in `CreditOrderPolicy` (its orders wait for a person)."
Audit row addendum (Czech, same row): „Automatické navýšení tarifu a automatické prodloužení domény či předplatného smí
zapnout jen vlastník nebo fakturační správce (vypnout kdokoli); noční úklid neschválených objednávek už nezruší objednávku,
kterou někdo mezitím schválil.“

## Final verification (whole task, 2026-09-25)

```
From / To:        onhost-backend → orchestrator (reviewers: onhost-reviewer, onhost-qa, onhost-security)
Task / Status:    TASK-0021 / SELF_VERIFIED (packages "identity" and "credit-orders")
Branch / base:    fix/TASK-0021-password-change-ends-old-tokens-panel-pa on stack/base-0017-0019
```

- VERIFIED: whole suite `php artisan test --compact`: 1015 passed, 0 failed (15478 assertions). The earlier baseline
  failure `Billing/RenewalGuardTest` passed in this run as well.
- VERIFIED: `vendor/bin/pint --test` passed on the whole repository.
- VERIFIED: `vendor/bin/phpstan analyse --memory-limit=2G --no-progress` reported 0 errors on the whole repository.
  `phpstan-baseline.neon` is unchanged against the base.
- VERIFIED: no migration, no `apps/surfaces/*.dc.html` edit, and no edit to CURRENT_STATE, production-readiness-audit,
  security-boundaries or web-tools. The text for those files is below, under "Docs for integration".
- NOT TESTED: PostgreSQL (CI `pest-postgres`), the panel approvals card in a browser, and live panels (not allowed).

**Outcome per package.** identity: done (decisions 14 and 15). credit-orders: done (decision 20), behind
`ONHOST_ORDER_CREDIT_APPROVAL`, which is off by default. See the two package sections below.

**Shared-file contract.** The hot files have delimited TASK-0021 blocks. The only edits to existing lines are these four:
the org_admin, billing_admin and platform_owner lines of `RoleCatalog`, one comment in `NotificationRouter`, and the
`security.password_changed` row of `events-catalog.md`. The owner decisions require each of these, and each is described
below. `billing.wallet.spend` (customer, NORMAL, owner + billing_admin, not org_admin/member) is defined here, and
TASK-0025 must use exactly that name.

### Release notes for existing customers

- **On deploy, whatever the switches say:** `AuthorizationSeeder` (run by `infra/aapanel/deploy.sh`) updates the roles.
  After that, only the organization owner can set the game panel password. Owners and billing admins hold
  `billing.wallet.spend`. An org_admin can no longer grant the `billing_admin` role (`role_above_own`). Nothing is revoked
  at deploy, and no existing service, order or token is changed.
- **From the first password change after deploy:** a user who changes the password loses their personal API tokens. The
  browser that made the change stays signed in. Integrations that run on a personal token must be given a new token
  (service-account tokens are not affected). Operators can restore the old behaviour with
  `ONHOST_PASSWORD_CHANGE_REVOKES_API_ACCESS=false`.
- **Credit approval: nothing changes until an operator switches it on.** Before setting `ONHOST_ORDER_CREDIT_APPROVAL=true`,
  run `php artisan onhost:orders:credit-approval-report`, which is read-only. Consider telling customers whose org_admins
  or partners pay from credit.

## Package "identity" (owner decisions 14 and 15)

```
From / To:        onhost-backend → orchestrator
Task / Status:    TASK-0021 / IN_PROGRESS (package "identity" self-verified; the credit package follows)
```

**Outcome.** A password change in the portal now revokes every personal Sanctum token of that user, in every
organization. The browser session that made the change stays signed in, as before. Service-account tokens, other people's
tokens and integration secrets are not touched. An operator switch, `ONHOST_PASSWORD_CHANGE_REVOKES_API_ACCESS` (default
`true`), brings back the old "tokens kept" path and its `security-password-kept` mail without a deploy. The forgotten-password
reset already revoked all personal tokens. That is kept, and it now goes through the same helper and reports the count.
`panel.password` (sets the game panel account password) now needs the new HIGH customer permission
`service.panel_account.manage`. Only the `owner` role holds it: not org_admin, not developer, game_operator or cloud_operator,
no svc_* share role, not guest, not platform_owner. `ServiceService::requestAction` also refuses anyone who is not the
organization's `owner_user_id` acting in person (`owner_only_action`, 403, Czech message). The step-up is unchanged.
**Nothing reveals a panel password today.** The `panel_access` resource returns URL, username and e-mail only, so there is
no reveal path to close. The decision's "reveal/reset" is covered by the reset (set) path.

**Files.**

- `domains/Identity/ApiAccessRevocation.php` (new): `revokePersonalTokens(User, ?PersonalAccessToken $keep)`. It works only
  through the morph-scoped `User::tokens()` and marks rows revoked. It never deletes them.
- `app/Http/Controllers/Api/V1/MeController.php`: `changePassword` revokes when the switch is on. The audit detail and
  `security.password_changed` then carry `api_access=revoked` and `api_access_count`. With the switch off, the payload is
  byte for byte the old one.
- `app/Http/Controllers/Api/V1/AuthController.php`: `confirmPasswordReset` uses the helper. The count goes into the event
  and the audit detail.
- `config/onhost.php` and `.env.example`: `identity.password_change_revokes_api_access` / `ONHOST_PASSWORD_CHANGE_REVOKES_API_ACCESS`,
  in a delimited TASK-0021 entry.
- `domains/Notifications/NotificationRouter.php`: comment only. No routing change: `revoked` already sends `security-password`.
- `domains/Identity/Authorization/PermissionCatalog.php`: `service.panel_account.manage` (customer, HIGH) and
  `const OWNER_ONLY = ['organization.close', 'service.panel_account.manage']`, both in delimited TASK-0021 blocks.
- `domains/Identity/Authorization/RoleCatalog.php`: the org_admin line now reads `array_diff($allCustomer, self::orgAdminWithheld())`.
  **This is the one place for the org_admin exclusions.** The credit package adds `billing.wallet.spend` to
  `orgAdminWithheld()` and does not touch the role line. platform_owner drops `self::STAFF_NEVER = ['service.panel_account.manage']`.
- `domains/Services/Commands/ServiceActionCommand.php`: `permissionFor('panel.password') = 'service.panel_account.manage'`.
  Risk and step-up lists are unchanged.
- `domains/Services/Access/OwnerOnlyActions.php` (new), plus one call in `ServiceService::requestAction` just before the
  legal-hold check.
- `docs/architecture/events-catalog.md`: the `security.password_changed` row now says `revoked` after both a change and a reset.
- Tests: `tests/Feature/Http/PanelApiTest.php` (old "kept" test rewritten into a revoke test plus a switch-off test),
  `tests/Feature/Http/AuthApiTest.php` (reset pin), `tests/Feature/Identity/PermissionMatrixTest.php` (new column and a
  holder test), `tests/Feature/Provisioning/GameToolsFeatureTest.php` (owner-only test).

**Contracts.** No route, migration or command. The event payload gains `api_access_count` on the revoked path, and the key
avoids the `token` fragment. New config key and env var (above). New permission `service.panel_account.manage`. New error
slug `owner_only_action` (403). A non-owner who calls the endpoint gets `access_not_approved` from the bus first.
`features()` shows `panel_access` as `{enabled:false, reason:'permission'}` to non-owners, through the existing gate.
`ServiceFeatures.php` itself is not edited.

**Release note.** The RoleCatalog change reaches every organization through `AuthorizationSeeder` on deploy
(`infra/aapanel/deploy.sh` runs it). After that, org_admin, developer, game_operator, cloud_operator, svc_manage and
svc_console holders, and platform_owner, can no longer set a game panel password. Owners keep it, with the same step-up.
If the code goes live before the seeder runs, even owners are refused until it runs. This fails closed.
`onhost:doctor` "roles in the database match the catalog" must be green after the deploy. Nothing is revoked at deploy:
tokens are revoked only when a user changes their password afterwards.

**Verification.**

- VERIFIED red, before the code: 4 failures and 1 error. PanelApiTest revoke case (`2 is identical to 0`). PermissionMatrixTest,
  both cases (the owner was denied because the permission did not exist; the holder list was `[]`). GameToolsFeatureTest
  (org_admin saw `panel_access` enabled). AuthApiTest reset case (`Undefined array key "api_access_count"`). The switch-off
  case passed before the change, as it should: it is a guard.
- VERIFIED green: the same 4 files, 32 tests and 503 assertions. Related suites also pass: `tests/Feature/{Identity,Services,Organizations,Http,Notifications,Platform,Provisioning}`,
  `Compliance/AccountErasureTest`, `Support/AssistantProposalsTest` and `Catalog/PlanPromisesTest`, 580 tests in all.
- VERIFIED: `.\brain.ps1 gate -Quick -Tests …` returned PASS_WITH_PREEXISTING_FAILURES. The only failure is
  `Billing/RenewalGuardTest`, which is in the baseline list. Pint passes on the changed files, and Larastan on the changed
  PHP files reports 0 errors.
- NOT TESTED: full suite and full-repo Larastan; PostgreSQL (CI `pest-postgres`); the live Pterodactyl panel (no write flows
  against live panels).
- ASSUMED: after a reset, other SPA sessions are dropped by Sanctum's password-hash session check. The reset path does not
  delete DB sessions itself. This is unchanged by the package.

**Concerns.** Password change and reset are still controller writes outside the CommandBus, as before; this is not widened.
Support, platform_owner included, can no longer set a customer's game panel password. Such requests must go to the owner.
The `panel_access` resource (URL, username, e-mail) stays readable with `service.read`. `NotificationRouter`,
`events-catalog.md` and the org_admin line of `RoleCatalog` changed existing lines. Each change is one line, in place.

### Docs for integration

- **docs/runbooks/production-readiness-audit.md, row 28 (Hesla), replace the status text:** „**Vyřešeno 2026-09-25
  (rozhodnutí vlastníka 14, TASK-0021):** změna hesla v portálu zruší všechny osobní API tokeny uživatele ve všech
  organizacích; prohlížeč, který heslo měnil, zůstane přihlášený. Tokeny servisních účtů, cizí tokeny a tajemství integrací
  (action hooks, on-call feedy, SLA sondy, Discord) se nemění — jde se jen přes `User::tokens()`. Obnova hesla tokeny rušila
  už dřív, teď hlásí i počet. Přepínač `ONHOST_PASSWORD_CHANGE_REVOKES_API_ACCESS=false` vrací dřívější stav (tokeny zůstávají,
  e-mail `security-password-kept` to říká) | ✔ | —“
- **Audit, new row (panel password):** „Herní panel | **Opraveno 2026-09-25 (rozhodnutí vlastníka 15, TASK-0021):** heslo
  k účtu herního panelu (`panel.password`) nastavoval každý, kdo směl službu spravovat — správce organizace, herní operátor,
  vývojář i člověk, se kterým byla služba sdílena s konzolí; účet panelu přitom otevírá všechny servery toho účtu. Teď jen
  vlastník organizace: nové oprávnění `service.panel_account.manage` (HIGH, step-up) má jen role `owner` (ne org_admin, ne
  svc_*, ne host, ne platform_owner) a `ServiceService::requestAction` navíc odmítne každého, kdo není `owner_user_id` jednající
  sám za sebe (`owner_only_action`). Záložka se ostatním ukáže zavřená s důvodem `permission`. Heslo panelu platforma nikde
  neukazuje | ✔ | Po nasazení ověřit, že `onhost:doctor` hlásí role v databázi shodné s katalogem (AuthorizationSeeder)“
- **docs/context/CURRENT_STATE.md (replace the bullet "The password mail tells the truth"):** "**A password change ends the
  old API tokens** (owner decision 14, TASK-0021): changing the password revokes every personal API token of the user in every
  organization (a reset always did); the changing browser stays signed in; service-account tokens and integration secrets are
  untouched. `ONHOST_PASSWORD_CHANGE_REVOKES_API_ACCESS=false` restores the old 'tokens kept' path with its truthful mail.
  **The game panel password is the owner's** (decision 15): `panel.password` needs `service.panel_account.manage`, held only
  by the `owner` role, and `OwnerOnlyActions` refuses anyone but the organization's `owner_user_id` in person."
- **docs/runbooks/security-boundaries.md:** a bullet in the sessions/tokens section: "A password change revokes the user's
  personal API tokens (switch `onhost.identity.password_change_revokes_api_access`, default on); a reset always does. A
  service account's tokens and the integration secrets are not personal and are never touched (`ApiAccessRevocation` goes
  through the morph-scoped `User::tokens()`)." A new section "21. A panel account password belongs to the organization
  owner": "`panel.password` maps to `service.panel_account.manage` (HIGH, step-up), which only the `owner` role holds —
  `PermissionCatalog::OWNER_ONLY`, `RoleCatalog::orgAdminWithheld()` and `RoleCatalog::STAFF_NEVER` keep it off org_admin and
  platform_owner. `Services\Access\OwnerOnlyActions` refuses in `requestAction` anyone who is not the organization's
  `owner_user_id` acting in person (staff, system, AI, impersonation included). Tests: `PermissionMatrixTest` (holder test),
  `GameToolsFeatureTest` ('lets only the organization owner set the game panel password')."
- **docs/runbooks/web-tools.md (game `panel_access` line):** "`panel_access` (`panel.password` — HIGH, step-up, organization
  owner only (`service.panel_account.manage`, `OwnerOnlyActions`); refused for panel administrators)".

**Next.** The credit package: add `billing.wallet.spend` to `PermissionCatalog`, add it to `RoleCatalog::orgAdminWithheld()`,
and grant it to `billing_admin`. (Done, see below.)

## Package "credit-orders" (owner decision 20)

```
From / To:        onhost-backend → orchestrator
Task / Status:    TASK-0021 / SELF_VERIFIED (both packages)
```

**Outcome.** Account credit is spent by the organization owner and the billing admin only: new customer permission
`billing.wallet.spend` (NORMAL). With the switch `ONHOST_ORDER_CREDIT_APPROVAL=true` (config `onhost.orders.credit_approval.enabled`,
**default false**):

- An order paid from credit (`wallet`, `postpaid`; config `modes`) placed by anybody else stays `NEW` with
  `meta.approval = {state: pending, requester_id, requester_name, project_id, opened_at}`. Nothing is reserved, no document,
  nothing provisioned. A read-only funds + budget check refuses at placement what the organization could not pay anyway (402).
  Every owner and billing admin gets an in-app notification (with `user_id`) and the mail `order-approval-required`.
- `POST /v1/orders/{order}/approval {decision: approve|reject, reason?}` (`DecideOrderApprovalCommand`, permission
  `billing.wallet.spend`, NORMAL): approve reserves the credit and marks the order paid through the same code as a normal
  credit order (`CheckoutService::payFromCredit`), so the risk hold, the statement and the fulfilment follow as usual; not
  enough credit gives 402 and the order keeps waiting (the whole decision rolls back). Reject needs a reason, cancels, gives
  the promo use back, tells the requester (in-app + mail `order-approval-rejected`). `GET /v1/orders?approval=pending` lists them.
- Orders nobody decided are cancelled after `ONHOST_ORDER_CREDIT_APPROVAL_EXPIRE_DAYS` (7) by the existing nightly
  `onhost:commerce:prune` (`CommerceHousekeeping::expireUnpaid` adds `CreditOrderApprovals::expirePending()`); routes/console.php untouched.
- Paying from credit at once is refused to non-holders with `credit_spend_not_allowed` (403) and a Czech message
  ("Z kreditu organizace může platit jen její vlastník nebo fakturační správce. Požádejte ..."): invoice `pay_from_wallet`
  (card and bank stay open), manual domain renewal (`DomainService::renew`; the scheduler's system context is exempt),
  marketplace order, work-offer approval (decline stays open), paid archive download.
- Exempt: the `system` actor (renewals, auto-upgrades, housekeeping), staff-assisted orders (`source=staff` + global
  `staff.customer.manage`), the literal `owner_user_id`. An actor nobody can name (an AI run without a person, an unknown
  service account) is held or refused: fails closed. Card (`gateway`) and `bank` orders are never gated.
- Panel: the four "Uhrazeno z kreditu" messages say "Objednávka čeká na schválení ..." when the API answers `approval: pending`;
  the Fakturace pending-payments card (existing `pendingLedger` seam) shows the waiting orders first, with Schválit/Zamítnout
  buttons (seam data `billing.approvals`, `billing.approval_expire_days`); `OnhostStore` gets an `approval` flag and
  `orderApproval()`. No SurfaceRenderer or prototype change.
- Read-only operator report: `php artisan onhost:orders:credit-approval-report [--organization=] [--limit=500]`.

**org_admin line.** `RoleCatalog::orgAdminWithheld()` = `OWNER_ONLY` + `RoleCatalog::CREDIT_SPENDING` (`['billing.wallet.spend']`);
the org_admin role line is not touched. billing_admin gets it through `RoleCatalog::BILLING_ADMIN_EXTRA`, appended to its line
(the one existing-line edit in that hot file). owner and platform_owner get it through `$allCustomer`; partner does not.

**Files.** New: `domains/Orders/CreditOrderPolicy.php`, `domains/Orders/CreditOrderApprovals.php`,
`domains/Orders/Commands/DecideOrderApprovalCommand.php`, `app/Console/Commands/CreditApprovalReport.php`,
`tests/Feature/Orders/CreditOrderApprovalTest.php`, `tests/Feature/Orders/CreditSpendGateTest.php`.
Changed: `domains/Orders/CheckoutService.php` (policy injected, held branch, `payFromCredit`, `assertCoverable`),
`domains/Orders/Commands/OrdersCommandHandler.php`, `domains/Orders/CommerceHousekeeping.php`,
`app/Http/Controllers/Api/V1/OrderController.php`, `app/Http/Presenters/Presenters.php` (`approval` block),
`app/Http/Controllers/Web/SurfaceDataController.php` (billing seam `approvals`), `routes/api.php`,
`app/Providers/DomainServiceProvider.php`, `contracts/openapi/onhost-v1.yaml` (regenerated, one path),
`domains/Identity/Authorization/{PermissionCatalog,RoleCatalog}.php`, `config/onhost.php`, `.env.example`,
`domains/Notifications/{NotificationRouter,NotificationService,Lexicon}.php`, `database/seeders/NotificationTemplateSeeder.php`,
`docs/architecture/events-catalog.md`; gates in `domains/Invoicing/Commands/InvoicingCommandHandler.php`,
`domains/Domains/DomainService.php`, `domains/Marketplace/MarketplaceService.php`, `domains/Support/WorkOfferService.php`,
`domains/Services/ServiceArchiveService.php`; panel scripts `apps/surfaces/api/onhost-panel-{shop,order,tools,billing}.api.js`,
`apps/surfaces/api/onhost-store.api.js`; `tests/Feature/Identity/PermissionMatrixTest.php`.

**Contracts.** Route `POST /v1/orders/{order}/approval`; `GET /v1/orders?approval=pending`; order presenter `approval`
(`state`, `requester{id,name}`, `opened_at`, `decided_at`, `decided_by{id,name}`, `reason`); place-order result `approval`.
Events `order.approval.required|approved|rejected|expired` (payload keys `number, total, mode, requester_id, requester,
decider, reason, days`; none hits the outbox redaction); `order.placed` gains `approval: pending` for held orders. Error slugs
`credit_spend_not_allowed` (403), `order_not_awaiting_approval` (409), `approver_lacks_permission` (403), `reason_required`
(422), `order_approval_decision_invalid` (422). Mail templates `order-approval-required`, `order-approval-rejected` (cs+en).
Config `onhost.orders.credit_approval.{enabled,modes,expire_days}`, env `ONHOST_ORDER_CREDIT_APPROVAL`,
`ONHOST_ORDER_CREDIT_APPROVAL_EXPIRE_DAYS`. Command `onhost:orders:credit-approval-report`. No migration. TASK-0025 should call
`CreditOrderPolicy::assertMaySpend()` (or check `billing.wallet.spend`) on its pay-and-restore credit path.

**Release note.** The RoleCatalog change reaches every organization through `AuthorizationSeeder` on deploy
(`infra/aapanel/deploy.sh`). Effects **independent of the switch**: owners, billing admins and platform_owner hold
`billing.wallet.spend`, and **an org_admin can no longer grant (or invite into) the `billing_admin` role**, because
`OrganizationsCommandHandler::mayGrant` refuses a role wider than one's own (`role_above_own`); only the owner can. Everything
else changes only when an operator sets `ONHOST_ORDER_CREDIT_APPROVAL=true`: run `onhost:orders:credit-approval-report`
first, and consider telling customers whose org_admins or partners order from credit. `onhost:doctor` role drift must be green
after the deploy.

**Verification.**
- VERIFIED red before the code: 29 tests, 17 failed for the intended reasons (see the task log), 11 guards passed.
- VERIFIED green: the 3 focused files 32/32 (198 assertions); related suites
  `tests/Feature/{Orders,Identity,Http,Finance,Notifications,Platform,Support,Marketplace,Domains,Services,Organizations,Compliance,Catalog,Billing,Provisioning,Partners,Loyalty}`
  823/824, the one failure being `Billing/RenewalGuardTest` (baseline); `.\brain.ps1 gate -Quick -Tests ...`
  PASS_WITH_PREEXISTING_FAILURES; Pint clean; Larastan 0 errors on the changed PHP files; `node --check` on the 5 panel
  scripts; `LexiconCoverageTest` green (English lines added); `EnvTemplateTest`, `TenantIsolationSweepTest`, `PlanPromisesTest` green.
- NOT TESTED: full suite and full Larastan; PostgreSQL (the JSON path `meta->approval->state` is the style the code already
  uses for `meta->fingerprint` and `meta->review->state`); the panel approvals card in a browser (the seam data and the
  script contents are asserted); no live panels.
- ASSUMED: at approval the budget check uses the approver's context scoped to the requester's project (`meta.approval.project_id`).

**Concerns / deviations.**
- Marketplace orders and work-offer approvals are refused (403) instead of held: `MarketplaceOrder` has no pending state or
  meta column and migrations are reserved for other tasks; a work-offer approval is itself the commitment. Both fail closed
  with a message saying whom to ask.
- The whole behaviour stays default-off (spec, and the owner rule against changing existing customers en masse); the
  orchestrator did not ask for default-on. Only the RoleCatalog effect above lands at deploy.
- The mail `order-approval-required` links to `/panel/fakturace`; the card there is asserted by data, not in a browser.
- A held order the requester cancels keeps `meta.approval.state = pending` in its presenter (state CANCELLED); lists and
  decisions filter on state NEW, so it is cosmetic.
- The org_admin role description still names only the two owner-only rights; not updated because the role line was not to be
  touched. The docs task could mention credit.
- The duplicate-order window (15 min, same cart fingerprint) returns a waiting order to the owner if the owner submits the
  same cart; the owner can then approve it.
- `CheckoutService` (central) changed in several small hunks; the logic lives in the new classes.

### Docs for integration (credit-orders)

- **docs/runbooks/production-readiness-audit.md, new row (Kredit):** „Kredit | **Opraveno 2026-09-25 (rozhodnutí vlastníka 20,
  TASK-0021):** z kreditu organizace platil každý, kdo směl objednávat: správce organizace, partner, vývojář s právem stáhnout
  archiv, správce domén při ručním prodloužení. Teď jen vlastník a fakturační správce (`billing.wallet.spend`). Objednávku
  placenou z kreditu (wallet, postpaid) od kohokoli jiného platforma podrží (nic nerezervuje, nevystaví ani nezřizuje) a
  vlastníkovi i fakturačním správcům pošle upozornění a e-mail; schválení ji zaplatí z kreditu, zamítnutí nebo 7 dní bez
  rozhodnutí ji zruší bez poplatku. Okamžité platby z kreditu (faktura, ruční prodloužení domény, marketplace, schválení
  placeného zásahu, stažení archivu) ostatním odmítne s vysvětlením, koho požádat; karta a převod zůstávají všem. Přepínač
  `ONHOST_ORDER_CREDIT_APPROVAL` je zatím vypnutý | ◐ | Před zapnutím spustit `onhost:orders:credit-approval-report`, pak
  `ONHOST_ORDER_CREDIT_APPROVAL=true`; po nasazení ověřit `onhost:doctor` (role v databázi = katalog)“
- **docs/context/CURRENT_STATE.md, new bullet:** "**Credit is the owner's and the billing admin's** (owner decision 20,
  TASK-0021): `billing.wallet.spend` (owner, billing_admin; withheld from org_admin in `RoleCatalog::orgAdminWithheld()`).
  With `ONHOST_ORDER_CREDIT_APPROVAL=true` (default off) a credit order of anybody else waits for their approval
  (`POST /v1/orders/{id}/approval`, expiry 7 days in `onhost:commerce:prune`) and immediate credit payments (invoice, domain
  renewal, marketplace, work offer, archive download) are refused to them; card and bank are unaffected. Read-only
  `onhost:orders:credit-approval-report` before switching on."
- **docs/runbooks/security-boundaries.md, new section "22. Spending the organization's credit":** "`Orders\CreditOrderPolicy`
  decides who may spend credit: holders of `billing.wallet.spend` in the organization, or the literal `owner_user_id`. The
  principal is read from the CommandContext as the bus does (`onBehalfOfUserId ?? actorId`), so an AI run or a service
  account without a person fails closed; `system` is exempt. Orders are held (`CreditOrderApprovals`), immediate payments call
  `assertMaySpend()`. A role that carries the permission cannot be granted by an org_admin (`mayGrant`: `role_above_own`).
  Tests: `CreditOrderApprovalTest`, `CreditSpendGateTest`, `PermissionMatrixTest`. Every new credit-spending path must call
  `CreditOrderPolicy::assertMaySpend()` or go through a held order."
- **docs/runbooks/approvals.md, new section "Customer approval of credit orders":** "Separate from the staff four-eyes: the
  customer's owner or billing admin approves a credit order another member placed (panel Fakturace card or
  `POST /v1/orders/{id}/approval`). Switch `ONHOST_ORDER_CREDIT_APPROVAL` (default off), modes `wallet`/`postpaid`
  (`onhost.orders.credit_approval.modes`), expiry `ONHOST_ORDER_CREDIT_APPROVAL_EXPIRE_DAYS` (7) in `onhost:commerce:prune`.
  Before switching on: `php artisan onhost:orders:credit-approval-report` (read-only). Staff-assisted orders and platform
  (system) orders are never held."

**Next.** Review (onhost-security for the money path); the owner decides when to set `ONHOST_ORDER_CREDIT_APPROVAL=true`;
TASK-0025 wires `CreditOrderPolicy::assertMaySpend()` into pay-and-restore.

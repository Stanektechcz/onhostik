# Handoff — TASK-0027

```
From / To:        onhost-billing + onhost-backend (C1-C4), onhost-docs (docs wave) → orchestrator
                  (reviewers: onhost-reviewer, onhost-qa; onhost-security for C1 and the security-boundaries runbook)
Task / Status:    TASK-0027 / SELF_VERIFIED
Branch / base:    fix/TASK-0027-stack-coherence-and-the-docs-that-descri, top of the linear stack
                  development + TASK-0019 + 0017 + 0020 + 0026 + 0024 + 0021 + 0022 + 0023 + 0025
                  + cherry-picked TASK-0018 and TASK-0003; the whole stack reaches development in ONE pull request.
Reviewer diff:    git diff fix/TASK-0025-money-lifecycle-pay-and-restore-consumer...HEAD (includes the TASK-0018 and
                  TASK-0003 cherry-picks, which carry their own gate reports)
```

## Outcome

The stacked tasks agree with each other, and the documents describe the stack as built.

| Package | Outcome |
| --- | --- |
| C1 one credit gate | Paying for a restore, taking back a cancellation that bills again and a recorded restore request all ask `CreditOrderPolicy::assertMaySpend` (switch `ONHOST_ORDER_CREDIT_APPROVAL` off: whoever may pay from the credit / may resume; on: owner + billing admin, 403 `credit_spend_not_allowed`). `ReinstateServiceCommand::PERMISSION` = `billing.wallet.topup`. `reinstatement_spend_required` is gone. |
| C2 auto-renew after a restore | A restore keeps the subscription's previous auto-renew (the cancellation's saga record, else the cancelled row's own value, nothing = off), whoever triggers it; `onhost:billing:reinstatement-audit --apply` warns when it stays off. |
| C3 router arms | `service.restore_test.failed`, `service.database.import.failed`, `service.backup.schedule.paused`, `service.backup.schedule.stalled` each have one arm telling the customer (unchanged) and staff (internal, `hot`); structural test proves no event is named by two arms. |
| C4 shared PHP workers | Catalogue revision `2026-09-shared-php-workers` (shape `drop_undelivered`) drops `php_workers_dedicated` from every plan whose current version sells it where `PlacementRules::undelivered()` (today only `eshop/shop-peak`); applied only by the operator command `onhost:catalog:revise`; existing plan versions, prices and held versions untouched. `apply()` reads each revision's pending changes just before it runs (fixes a stale `base_version` refusal when two revisions touch one plan). |
| Docs wave | Adapter pages (ISPConfig, aaPanel), runbooks (security-boundaries, web-tools, backups, approvals, billing-dunning, pricing), production-readiness audit rows 105-119, go-live checklist Status column + operator steps, CURRENT_STATE, ADR-0007 implementation addendum, historical-document headers, DAILY_WORKFLOW, PROJECT_STATE, TECH_STACK, baseline re-measured, TASK-0005 ... 0016 marked INTEGRATED. |

## Files

Code: `domains/Billing/{ServiceReinstatement,SubscriptionService}.php`, `domains/Billing/Commands/ReinstateServiceCommand.php`,
`app/Console/Commands/ReinstatementAudit.php`, `domains/Notifications/NotificationRouter.php`,
`domains/Catalog/{CatalogRevisions,PlanPromises}.php`, `domains/Provisioning/Scheduling/{CapacityDoctor,PlacementRules}.php`,
`domains/Services/Metering/MetricRegistry.php`.
Tests: `tests/Feature/Billing/{PayAndRestoreTest,ReinstatementAuditCommandTest}.php`,
`tests/Feature/Notifications/NotificationRouterArmsTest.php` (new), `tests/Feature/Catalog/{CatalogRevisionTest,PlanPromisesTest}.php`,
`tests/Feature/Platform/DoctorCommandTest.php`, `tests/Feature/Services/{BackupScheduleMissesTest,DatabaseImportSafetyTest,RestoreTestTest}.php`.
Docs/state: `docs/provider-adapters/{ispconfig,aapanel}.md`, `docs/runbooks/*` (see the diff), `docs/context/CURRENT_STATE.md`,
`docs/adr/0007-owner-decisions-2026-09-25.md`, `docs/development/*`, `.ai/PROJECT_STATE.md`, `.ai/TECH_STACK.md`,
`.ai/baseline/baseline.{json,md}`, `.ai/tasks/TASK-0001, 0005 ... 0016, 0027`, `.ai/decisions/AI-0001-orchestration-layer.md`,
`.ai/reports/TASK-0027-gate.md`, `.claude/agents/onhost-docs.md`.

## Contracts

- `POST /v1/services/{service}/reinstate`: bus permission `billing.wallet.spend` → `billing.wallet.topup`; under the credit
  approval switch 403 `credit_spend_not_allowed` (+ `permission: billing.wallet.spend`). Customer resume of a cancellation
  with `services.reinstate` on: `reinstatement_spend_required` replaced by `credit_spend_not_allowed` under the same switch.
  OpenAPI regenerated: unchanged.
- New catalogue revision id `2026-09-shared-php-workers`; doctor capacity row wording. No event, schema, migration or config key.

## Verification

VERIFIED (2026-09-25, stack tip `04f3b05`, clean tree, this worktree):
- `php artisan test --compact`: 1364/1364 passed, 18258 assertions, 388 s.
- `vendor/bin/pint --test`: passed.
- `vendor/bin/phpstan analyse --memory-limit=2G --no-progress`: 0 errors; `git diff development -- phpstan-baseline.neon`: empty.
- `scripts/ai/tests/Orchestration.Tests.ps1`: all passed.
- Earlier on `edb635b`: `.\brain.ps1 gate -Task TASK-0027` full PASS (`.ai/reports/TASK-0027-gate.md`); failing-first evidence
  for C1-C4 is in the task log.

ASSUMED: behaviour on PostgreSQL (CI) matches SQLite; not run locally.
NOT TESTED: live panels, staging, `onhost:catalog:revise` against real data (operator step), `Config.Tests.ps1`
(pre-existing failure, unchanged).

## Concerns

- C1 consequence (recorded in `docs/runbooks/billing-dunning.md`): with the credit approval switch off (default) an
  org_admin, and for the resume a `service.manage` guest, may commit the organization to renewals — the same as for
  invoices, auto-renew and auto-upgrade under TASK-0021's switch-off semantics. Owner should confirm.
- C2 consequence: a restored service whose customer had auto-renew off and whose period is over ends again at the next
  renewal pass unless paid for or a holder switches auto-renew on.
- C4 takes effect only when an operator runs `onhost:catalog:revise`; existing subscriptions of
  shop-peak keep their version.

## Next

Orchestrator: reviews (onhost-reviewer, onhost-qa, onhost-security), then the single stack pull request to `development`
after the human's go-ahead. No push was made.

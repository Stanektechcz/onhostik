# Audit připravenosti na produkci (2026-09-14)

Komplexní audit platformy ONhost po bloku §5p: co je hotové, co chybí, co je potřeba doladit a v jakém pořadí, aby
šlo do produkce bez překvapení. Zdroje: `php artisan onhost:doctor` (59 kontrol, 0 FAIL, 30 WARN na dev),
`onhost:integrations:health`, plná testovací sada (Pest 355 testů na SQLite i PostgreSQL 16 v CI, Playwright 5 specifikací), kód, konfigurace,
dokumentace v `docs/` a runbooky. Priority: **P0** blokuje spuštění, **P1** do prvního měsíce provozu, **P2** zlepšení.

## 1. Stav dnes v číslech

| Oblast | Stav |
| --- | --- |
| Testy | Pest 355 testů / 9 350 asercí zelené — v CI na SQLite **i PostgreSQL 16** (produkční databáze); Playwright 5 specifikací (veřejný checkout, panel zákazníka, game workbench, mail workbench, staff konzole); Larastan level 5 s baseline; `composer audit` bez nálezů |
| API | OpenAPI 397 cest / 443 operací (`php artisan onhost:openapi`) |
| Události | 81+ řádků katalogu (`docs/architecture/events-catalog.md`), všechny routované v `NotificationRouter` |
| Automatizace | 60 plánovaných běhů, 63 vlastních příkazů + 8 tříd, 15 pravidel s vypínačem v konzoli (`AutomationLedger::RULES`) |
| Infrastruktura jako kód | 7 Ansible rolí s molecule scénáři (vč. `onhost_clamav`), workflow `ansible-roles` (lint + matice), edge šablony Caddy/nginx; dev stack `infra/docker-compose.yml` s clamd, OTel collector, Tempo, Grafana (dashboard *ONhost · provoz*) a Alertmanager |
| Kvalita kódu | Pint, Larastan level 5 (`phpstan.neon` + baseline typového dluhu, běží v CI), `composer audit` v CI, 0 TODO/FIXME v domains/app/providers/platform |
| Živé integrace | aaPanel cz1 healthy (8.0.6, ~1,4 s); herní panel gamepanel.onhost.cz **bez uložených klíčů**; ostatní instance jsou laboratorní (disabled) |

## 1b. Co přibylo při přípravě produkce (2026-09-14, po §5u)

- **Zálohy platformy**: `onhost:platform:backup` (pg_dump / mysqldump / SQLite + tar.gz soukromých souborů, manifest se
  SHA-256, retence) denně 02:15, `onhost:platform:backup:verify` 03:15; doctor a metrika
  `onhost_platform_backup_verified_timestamp`, alert `OnhostBackupStale`.
- **Příprava produkce**: `onhost:production:prepare --purge-dev-accounts --legal --cache`; právnická osoba se čte z
  `config('onhost.legal_entity')` (po `config:cache` `env()` nefunguje — opraveno i ve VIES klientovi a doctoru);
  `.env.example` je připravený jako staging/produkční šablona se všemi klíči (prázdné hodnoty = doplní provozovatel,
  `production:` poznámky = přepínače pro produkci); `onhost:secrets:set` ukládá tajemství mimo provider instance.
- **PostgreSQL**: CI job `pest-postgres` odhalil a opravil: `nodes.remote_id` integer (Proxmox posílá `prg2-n1`),
  `rated_usage.charged_transaction_id` 40 znaků, `substr()` nad timestampem v reportu tržeb, a hlavně devět míst
  „insert + catch unique“, která na PostgreSQL zrušila celou probíhající transakci (věrnostní body při zaplacené
  objednávce, čísla tiketů/incidentů, webhook plateb, sondy SLA) — teď běží v savepointu.
- **Audit**: parametry akcí služeb už nikdy nenesou privátní klíč certifikátu ani obsah souborů (test dosud
  procházel jen kvůli tomu, že SQLite bere `"payload"` jako řetězec).
- **Monitoring**: metriky `onhost_automation_alive`, `onhost_virus_scanner_up`, `onhost_oncall_*`; alerty
  `OnhostAutomationDead`, `OnhostVirusScannerDown`, `OnhostOnCallUnacknowledged`, `OnhostNobodyOnCall`; Alertmanager
  posílá pravidla do `POST /v1/webhooks/alertmanager` → on-call alert s eskalací a rotou; Grafana dashboard.
- **CI**: Pest na SQLite + PostgreSQL, Larastan, `composer audit`, e2e a Ansible role běží i pro větev `development`.

## 2. P0 — blokátory spuštění

1. **Prostředí a runtime** — `APP_ENV=production`, `APP_DEBUG=false`, `APP_URL` s https; přechod z SQLite na
   PostgreSQL (`DB_CONNECTION`), Redis pro cache/session/queue (`CACHE_STORE`, `SESSION_DRIVER`,
   `QUEUE_CONNECTION` jsou dnes `database`), `php artisan schedule:run` v cronu každou minutu a `queue:work`
   pod systemd (doctor hlásí „no tick / no heartbeat“). Fronty: `default, mails, provider-*`; `onhost:queue:scale`
   pro škálování workerů.
2. **Tajemství** — `ONHOST_SECRETS_DRIVER=openbao` (nebo `db`), nikdy `env`. Uložit klíče všech produkčních instancí
   přes `php artisan onhost:integrations:secret <instance> <klíč> --check`; **klíče herního panelu, které se objevily v
   chatu, po uložení v panelu regenerovat**. Klíče, které dnes chybí: `pterodactyl-gamepanel` (`application_key`,
   `client_key`), a všechny produkční instance Proxmox/PBS/ISPConfig/PowerDNS/RKE2 (dnes jen lab `env://`).
3. **Identita a přístup** — `ONHOST_STAFF_MFA_REQUIRED=true`, smazat vývojové účty (`DevAccountSeeder`, doctor: 6
   účtů), platform_owner jen jako break-glass, JIT přístupy (`iam.jit.request`) zapnuté, revize rolí v
   `RoleCatalog` (nové oprávnění `capacity.manage`).
4. **Platby a doklady** — Comgate merchant + live mód + `COMGATE_RECURRING` pro uložené karty; bankovní účet
   (`ONHOST_BANK_IBAN` / `ONHOST_BANK_ACCOUNT`) na proformách; `ONHOST_BANK_FIO_TOKEN` pro import výpisů;
   `LegalEntitySeeder` s reálným IČO/DIČ/IBAN (dnes placeholder); revize `resources/legal/*.md` právníkem a
   bump verzí; e-fakturace (`EInvoiceProvider`) podle rozhodnutí o povinnosti.
5. **Doručování pošty** — `MAIL_MAILER` mimo `log`, odesílatel místo `hello@example.com`, SPF/DKIM/DMARC,
   `ONHOST_STAFF_DIGEST_TO` pro provozní přehledy; kontrola šablon (`resources/views/mail`) na reálném klientu.
6. **Registrátoři** — `WEDOS_TEST_MODE=false`, allow-list produkční IPv4 u WEDOS; Subreg API přístup (login
   `onhost_api` dnes odmítán — `500.104`); `onhost:registrar:costs` + aktualizace ceníků (10 TLD s cenou, subreg
   ceny zastaralé), pinování TLD, worker konzumuje `provider-registrar`.
7. **Provisioning** — placement pro každý produkt (doctor: `vps, vds, database, ipv4, backup-plus` bez instance);
   herní panel: klíče uložené, bootstrap hotový (5 šablon, placement); limity uzlu se nastavují z konzole přes
   Application API (*Herní uzly → Limity uzlu / Změřit RAM*), paměť uzlu ONHOST-GAME-TEST-01 nastavena na 14 964 MB,
   disk uzlu 200 000 MB — **první Spigot 1.21.8 běží** (`srv_01m2era5hx2abepxmy79v1rfp5`, 45.67.217.22:6665,
   sestavený BuildTools skriptem, který platforma zapsala přes API; start, příkazy i log z konzole ONhost).
   Katalog prodává 22 šablon (nest Minecraft + Onhost Gamehosting), všechny namapované přes API; povinné proměnné
   eggů hlídá quote (§5s) — **DayZ se nenabízí, dokud se neuloží Steam účet** (konzole *Šablony her → Proměnné
   provozovatele* nebo `onhost:game:operator-variable STEAM_USER` a `STEAM_PASS`), CS2 si token GSLT vyžádá od zákazníka při objednávce.
8. **Edge a konzole** — `ONHOST_CONSOLE_RELAY_KEY` + `ONHOST_CONSOLE_RELAY_URL` (relay pro živou konzoli),
   `ONHOST_METRICS_TOKEN`, `ONHOST_CLAMAV_HOST` (antivir nahrávek, §5r-4; role `onhost_clamav`, doctor hlídá stáří signatur), `ONHOST_TRACE_URL`, nasazení edge role (`infra/ansible/edge.yml`) s on-demand TLS pro stavové domény
   zákazníků.

## 3. P1 — první měsíc provozu

**Provoz a spolehlivost**
- Zálohy databáze a `storage/app/private` — **hotovo**: `onhost:platform:backup` (pg_dump custom / mysqldump / SQLite,
  tar.gz soukromých souborů, manifest se SHA-256, retence) a `onhost:platform:backup:verify` (kontrolní součty +
  `pg_restore --list` / integrity check); zbývá S3 bucket mimo server a cvičná obnova na stagingu. PBS instance pro
  zálohy VPS (`pbs-cz1` je lab).
- Externí sondy na třech lokalitách (role `onhost_probe`) + registrace tokenů v Incidenty → sondy; 2-of-3 kvorum
  je připraveno v `SlaService`.
- Alerting mimo platformu — **hotovo v §5q-1**: on-call eskalace (`ONHOST_ONCALL_PROVIDER` pagerduty | opsgenie |
  webhook, klíč v secret store, `ONHOST_ONCALL_INBOUND_SECRET` pro zpětný webhook pageru); zbývá nastavit provider,
  rotaci služeb v pageru a ověřit `POST /v1/staff/oncall/test`.
- Centrální logy a error tracking — **hotovo v §5q-2** bez SDK: `SENTRY_DSN` (redigované obálky s korelačním id),
  `OTEL_EXPORTER_OTLP_ENDPOINT` (spany příkazů, kroků operací a volání providerů); zbývá nasadit collector a Sentry
  projekt, retence auditu a logů podle GDPR (Compliance runbook).
- Kapacita: `capacity.auto_order` nechat vypnuté do první ruční objednávky; nastavit `options.node_order` na
  instanci s dodavatelem (Hetzner token v secret store); `ONHOST_NODE_BOOTSTRAP_SSH_KEY` + callback base.
- BMC inventář: `tags.bmc` na produkčních uzlech (Redfish URL, chassis, secret_ref), práh
  `ONHOST_BMC_TEMP_WARN_C` podle datacentra.

**Bezpečnost**
- Rotace všech tajemství, která prošla chatem nebo laboratoří (Pterodactyl, WEDOS heslo, Subreg).
- CSP (`SecurityHeaders`) rozšířit o relay a CDN origin produkce; `throttle:auth` 10/min a `throttle:probes`
  600/min zkontrolovat proti reálnému provozu; Turnstile na registraci a checkoutu je **hotový v §5q-6** — nastavit
  `TURNSTILE_SITE_KEY` / `TURNSTILE_SECRET_KEY` (bez klíčů vypnuto).
- `composer audit` a Larastan (level 5, `phpstan-baseline.neon` = dnešní typový dluh, nový kód musí projít) běží v CI —
  **hotovo**; zbývá Dependabot a postupné umazávání baseline.
- Penetrační test API (idempotence, step-up, org scope) a prototypových ploch; kontrola, že `demo` mód je v
  produkci vypnutý (`ONHOST_DEMO=false`).

**Finance a obchod**
- Rozhodnout výchozí sazby: provize partnerů (`partners.tiers`), marketplace komise 20 %, chargeback procento,
  věrnostní úrovně a kredity (`loyalty.levels`), regionální ceny; vše je v nastavení/konfiguraci, ale výchozí
  hodnoty jsou z prototypu.
- Dunning žebřík a texty (`billing-dunning.md`), `onhost:billing:runway` na reálných datech, DPH OSS pro EU B2C.
- Pravidla automatického schvalování (`partners.auto_approve`, `ONHOST_PARTNER_AUTO_*`) potvrdit s financemi.

**Zákaznická zkušenost**
- Prototypové části bez dat (seam #17, `docs/ui/data-seams.md`): KPI směny, hardwarový inventář, grafy
  monitoringu, SK lokalizace (UI-03), in-browser Babel (UI-02 → předkompilace pro rychlost načtení).
- Playwright rozšířit o partnerský portál (smluvní podmínky, marketplace), admin kapacitu/věrnost a staff konzoli
  serveru; mobilní průchody.
- Šablony e-mailů v EN existují, in-app notifikace v EN jsou **hotové v §5q-7** (`Lexicon`); zbývá projít
  překlady rodilým mluvčím a doplnit fráze, které test `NotificationLocaleTest` neprochází.

## 4. P2 — zlepšení

- Terminál přímo v konzoli — **hotovo v §5q-3** (websocket klient k relay, přehrání posledních řádků, textové
  soubory do 512 kB); zbývá binární přenos souborů a VNC pro Proxmox.
- Molecule na reálných dodavatelích (noční běh proti lab instancím; dnes noční `onhost:nodes:check` hlásí regrese
  prerekvizit, role Proxmox/ISPConfig mají stub CLI).
- Přílohy důkazů a exporty na S3 — **hotovo v §5q-4** (`ONHOST_FILES_DISK=s3`, podepsané odkazy, `onhost:files:prune`);
  zbývá bucket, IAM klíče v `.env` a antivirový sken nahrávek.
- Jedna tabulka vah rizika je hotová; přidat review přesnosti přes čas (trend) a export do BI.
- Odhad nákladů kampaní do měsíčního reportu financí; A/B test kampaní.
- Fleet: BMC teploty a PSU jsou v řádku; přidat graf a historii ze vzorků (`node_usage_samples`).
- Automatické objednání uzlu má vypínač i měsíční rozpočtový strop — **hotovo v §5q-5**
  (`ONHOST_CAPACITY_BUDGET_MONTHLY_MINOR`, `PUT /v1/staff/capacity/budget`); zbývá strop odsouhlasit s financemi.

## 5. Kontrolní seznam před dnem D

1. `php artisan onhost:doctor` → 0 FAIL, WARN jen vědomé. Předtím `php artisan onhost:production:prepare --purge-dev-accounts --legal --cache`
   (smaže vývojové účty, zapíše právnickou osobu z `ONHOST_LEGAL_*`/`ONHOST_BANK_*`, nacachuje konfiguraci) a
   `php artisan onhost:platform:backup && php artisan onhost:platform:backup:verify` (záloha DB + soukromých souborů
   na `ONHOST_PLATFORM_BACKUP_DISK`, ověření obnovitelnosti; denně 02:15/03:15, doctor hlídá stáří).
2. `php artisan onhost:integrations:health` → všechny produkční instance up, `onhost:nodes:check` bez varování.
3. `php artisan test --compact` + `npm run e2e` proti stagingu; `vendor/bin/phpstan analyse`.
4. Zálohy ověřené obnovou; runbooky `incident-response.md`, `provider-outage.md`, `release-and-rollback.md`
   projité s provozem.
5. Právní texty schválené, `LegalEntitySeeder` s produkčními hodnotami, DPH pravidla (`TaxRuleSeeder`) zkontrolované.
6. Klíče panelů uložené, rotované, `ONHOST_SECRETS_DRIVER=openbao`; vývojové účty pryč; MFA pro staff.
7. Sondy, on-call webhook a stavová stránka nasazené; první uzly s `tags.bmc`.

## 6. Návrhy dalšího bloku (§5q) — realizováno 2026-09-14, viz `preproduction-audit.md` §5q a §5r

1. **On-call eskalace**: PagerDuty/Opsgenie adaptér za `WebhookDispatcher` s potvrzením a eskalací po X minutách.
2. **Error tracking a trasování**: Sentry + OpenTelemetry pro operace a fronty; korelace s `CommandContext`.
3. **Websocket konzole ve staff stránce**: klient k relay, přehrávání posledních řádků, přenos souborů.
4. **S3 úložiště důkazů a exportů** s podepsanými odkazy a retencí.
5. **Rozpočtový strop kapacity**: měsíční limit objednávek uzlů, schválení nad limit financemi.
6. **Turnstile na registraci a checkoutu** jako další signál rizikového modelu.
7. **EN lokalizace notifikací a e-mailů**: šablony podle jazyka organizace.

## 7. Review 2026-09-20 — co revize našla a co z toho zůstává otevřené

Čtyři souběžné revize (registrátoři a DNS, adaptéry čtyř panelů, obslužná strana a účty, cesta objednávka → peníze).
Opravené nálezy jsou v `docs/runbooks/security-boundaries.md` a `docs/runbooks/billing-dunning.md`. Tady je zbytek:
sloupec **Ověřeno** říká, jestli jsem tvrzení sám přečetl v kódu (✔), nebo ho zatím jen hlásí revize (–). Nic z toho
není ověřeno proti skutečným panelům.

### Před spuštěním (P0)

| # | Oblast | Nález | Ověřeno | Návrh |
| --- | --- | --- | --- | --- |
| 1 | Zálohy na vyžádání | **Opraveno 2026-09-20** (`docs/runbooks/backups.md`): záloha webu je vlastní archiv platformy (soubory + každá databáze, mimo uzel), čerstvá, nebo operace selže; panelová záloha (hry, VM) se pozná podle jména z odpovědi, jinak jako položka, která před krokem v seznamu nebyla; obnova na Pterodactylu čeká, až panel přestane hlásit `restoring_backup`; totéž pravidlo platí pro závěrečný archiv VM a herního serveru. Cestou nalezeno a opraveno: oba souborové transporty odmítaly cestu `.` (zabalení celého webu nikdy nefungovalo proti skutečnému uzlu), `backup`/`restore` na mailové službě sahaly na cizí web, volání `sites_web_domain_backup` a `mail_user_backup` posílala id webu místo id zálohy, export databáze na aaPanelu bral a mazal „nejnovější“ dump. Zbývá: ověřit na stagingu (ISPConfig volání podle dokumentace API, ne podle živého panelu) a rozhodnout frekvence záloh v tarifech — každá záloha webu teď jde přes řídicí rovinu | ✔ | Kontroly v `backups.md` → „Staging checks“; velikost záložního disku a `backup_frequency` tarifů |
| 2 | ISPConfig | `awaitStatus` sleduje délku fronty úloh **celého serveru** (`monitor_jobqueue_count`), ne svou úlohu: prázdná fronta = „úspěch“ i po chybě, cizí zápisy na vytíženém serveru = čekání až do timeoutu (hladovění). Rozbor 2026-09-20: čtení záznamu zpět (`*_get`) nic nedokáže — API mění databázi rozhraní hned, server změnu uplatní až svým cronem (jednou za minutu; **proto změny na ISPConfigu nemohou být „v řádu sekund“**, dokud uzel nepouští `server.sh` častěji). Přesné sledování nabízí `sys_datalog_get_by_tstamp` / `sys_datalog_get` (ISPConfig 3.2: sloupce `status` = pending/ok/error a `error`): najít vlastní záznam podle `dbtable` + `dbidx` + času zápisu a čekat na jeho stav | ✔ kód, – API | **Krok 1 hotov 2026-09-20:** noční kontrola uzlů (`onhost:nodes:check`) se panelu sama zeptá (`datalog_api`) a zapíše jména polí, která vrací (`prereqs.probes.datalog_fields`); doba akcí podle panelu je vidět v `onhost:doctor` a na provozní nástěnce. **Krok 2 hotov 2026-09-21:** zápis, který umí `IspConfigConnector` pojmenovat (mapa `DATALOG_TABLES`), nese svůj řádek změnového logu v handle a `awaitStatus` sleduje **vlastní** záznam: `status = error` operaci **nechá selhat s hláškou serveru** (dřív prázdná fronta znamenala úspěch, ať server udělal cokoli), `ok` ji dokončí okamžitě bez ohledu na to, kolik cizích úloh ve frontě stojí (dřív se čekalo do timeoutu). Zapíná se **na důkaz**: jen u panelu, jehož noční sonda opravdu vrátila pole `status`; jinak platí přesně původní chování. Špatně odhadnutá tabulka může jen minout (shoda je na tabulku **i** index), nikdy trefit cizí záznam. Zbývá: na uzlech častější běh `server.sh` (systemd timer 10–15 s) — bez toho je spodní hranice odezvy minuta, ať platforma sleduje cokoli |
| 3 | Registrace domény | **Zčásti opraveno 2026-09-20:** po ztracené odpovědi musí jméno zůstat registru neznámé `ONHOST_DOMAIN_RECREATE_AFTER_SECONDS` (600 s), než se `domain-create` pošle znovu, a opakovaný příkaz se zapíše jako další pokus (`…:r2`) — dřív spadl na unikátním `clTRID` a operace selhala, i když registr doménu zaregistroval. Zbývá: Subreg objednávky nenesou vlastní identifikátor, ztracená odpověď `Make_Order` je nedohledatelná | ✔ | U Subregu posílat vlastní identifikátor a objednávku dohledat |
| 4 | Limity tarifu | Rozbor 2026-09-20: klient v ISPConfigu je **jeden na organizaci** (`onh_<org>`) a jeho limity se nastaví z první služby; do panelu se ale nikdo nepřihlašuje a limity tarifu počítá platforma sama před každou akcí (`featureParams` → `$limit()`), takže limity klienta jsou spíš kosmetické — pokud je vzdálené API nevynucuje (neověřeno na živém panelu). Změna tarifu mění kvóty **webu** (`hd_quota`, `pm_max_children`, `backup_copies`). aaPanel kvóty nemá (disk hlídá `UsageWatch`). Skutečná mezera: entitlementy `outbound_mail_per_hour`, `db_connections`, `inodes`, `traffic` se prodávají a nikde neuplatňují | ✔ kód, – panel | Na stagingu založit druhý web téže organizace na ISPConfigu s tarifem `sites: 1` (ověří, že API limity klienta nevynucuje); neuplatňované entitlementy buď měřit v `UsageWatch`, nebo je z tarifů vyřadit |
| 5 | aaPanel | **Opraveno 2026-09-20:** `resize` už nevolá `SetPHPMaxChildren` — aaPanel má jeden PHP-FPM pool na verzi PHP pro **celý uzel**, takže změna tarifu jednoho zákazníka nastavovala `pm.max_children` všem webům všech zákazníků (přechod na tarif se dvěma workery by uzel přiškrtil). Pool je věc velikosti uzlu; limity tarifu drží platforma v entitlementech | ✔ | Pokud má tarif na aaPanelu omezovat souběh webu, nabízí panel limit na web (`SetLimitNet`: `perserver`) — ověřit na stagingu a rozhodnout přepočet workerů na spojení |
| 6 | Schvalování | **Opraveno 2026-09-20** (`docs/runbooks/approvals.md`): odmítnutí kritické akce samo otevře žádost, rozhodne ji někdo jiný s právem akci provést a s vlastním druhým ověřením, schválení platí jednou pro přesně ten příkaz; příkaz už nemůže kritické oprávnění obsluhy prohlásit za „vysoké“ (právní blokaci dřív zadával jeden člověk). Provoz jednoho člověka = `ONHOST_FOUR_EYES=false` na serveru, ne v aplikaci. Zbývá rozhodnutí vlastníka: příkazy smějí dál říct, že jedna jejich operace pod HIGH oprávněním druhé ověření nepotřebuje — `CatalogCommand` (koncepty tarifů), `ComplianceCommand` (cyber.*, timer.submit, abuse kromě pozastavení), `DomainCommand` (`contact` = **založení** kontaktu, ne změna držitele — ta v API neexistuje, viz položka 23; `publish_ds`), `IncidentCommand`, `InvoiceCommand` (zákaznické operace). `LoyaltyCommand` opraven 2026-09-20: úroveň nese promo kredit a ruční body mohou zákazníka přes úroveň přenést, obojí teď chce druhé ověření | ✔ | Projít zbylé operace bez druhého ověření |
| 7 | Role obsluhy | **Opraveno 2026-09-20:** sedm oprávnění nedržela žádná pojmenovaná role (`catalog.manage`, `notification.template.manage`, `feature_flag.manage`, `staff.customer.manage` a tři nouzová) — ceník, šablony, přepínače a věrnost šly obsluhovat jen jako PlatformOwner. Nová role `product_manager` (katalog, šablony, přepínače); věrnost, sandboxový kredit a ceny k `billing_finance_admin` (servisní role na peníze dál nedosáhnou, H348). Bez pojmenované role zůstávají záměrně jen `iam.break_glass`, `provider.secret.view`, `secret.rotate`; hlídá to `PermissionMatrixTest`. Po nasazení přiřadit role skutečným lidem (seeder rolí běží při každém deployi) | ✔ | Rozdat role; PlatformOwner nechat jako nouzový účet |
| 8 | Transfer lock | Jen místní příznak; registr o zámku neví. Po opravě úniku kódu je to skutečná brána k vydání AUTH-ID. **2026-09-20:** panel to tak i říká („Zámek vydání převodního kódu“; zapnutý zámek = ONhost kód nevydá, registr o něm neví, dříve vydaný kód platí dál) — dřív tvrdil „Převod pryč je zablokovaný“ | ✔ | Zámek u registrátora (`clientTransferProhibited`), kde ho API nabízí — ověřit na stagingu u WEDOS a Subreg |
| 9 | Prémiové domény | **Opraveno 2026-09-20:** prémiové jméno (Subreg `price.premium`) hledání nenabízí (`reason: premium`), sága ho těsně před `domain-create` znovu ověří a neregistruje (ani jméno mezitím obsazené; bez odpovědi registrátora create čeká), prodloužení ani transfer za ceníkovou cenu neproběhnou. Dřív by jedna objednávka „pay.cz za 199 Kč“ stála náš kredit u registrátora cenu registru — doloženo třemi testy proti starému kódu | ✔ | Prodej prémiových jmen (cena registru + marže, individuální nabídka) je obchodní rozhodnutí |
| 10 | NSSET | Sdílený NSSET je v tabulce klíčovaný jen handle (unikátní), takže u druhého registrátora se založení přeskočí. U `.cz` to nejspíš nevadí — NSSET je objekt registru a doména u jiného registrátora na něj smí odkazovat; u registrů, kde to neplatí, by registrace selhala po zaplacení | ✔ kód, – registr | Ověřit na stagingu registrací `.cz` přes druhého registrátora; teprve podle výsledku měnit klíč |
| 11 | Pozastavení | **Opraveno 2026-09-20:** pozastavení (i deaktivace zrušené služby) vypíná také cron úlohy a FTP účty webu a pamatuje si, které vypnulo; obnovení zapne právě ty. VM, kterou si zákazník vypnul sám, se při obnovení nespouští (`suspension_was_running`). Nalezeno při tom: aaPanel `updateCron` s nezměněným `active` úlohu přepnul — vypnul (přepínač panelu je „toggle“). Databáze přepínač nemají (web je zastavený, vzdálený přístup je vlastní oprávnění) | ✔ kód, – panely | Na stagingu ověřit `ftp?action=SetStatus` (aaPanel) a úplný záznam u `sites_ftp_user_update` / `sites_cron_update` (ISPConfig) |
| 12 | Jména na uzlu | **Opraveno 2026-09-20:** prefix služby (30 bitů) je sloupec s unikátním indexem (`services.name_prefix`, migrace `000760`); id, jehož prefix už má jiná — i zrušená — služba, se při zakládání nahradí dřív, než cokoli vznikne. aaPanel porovnává `prefix_`, ne jen prvních osm znaků. Existující kolize pojmenuje `onhost:doctor` | ✔ | Na stagingu: `onhost:doctor` → „every service has a node name prefix of its own“ |
| 13 | Tajemství | **Opraveno 2026-09-20** (`security-boundaries.md` §11): operace po dokončení zapomene hesla a klíče, která nesla (databáze, FTP, schránky, fetchmail, klíč certifikátu); vygenerované heslo správce WordPressu se ukáže 30 minut a jen tomu, kdo službu spravuje — dřív ho četl každý, kdo směl vypsat operace, i čtenář a host; neúspěšný běh si vstup drží 7 dní kvůli opakování; průběžný úklid vyčistí i všechny starší řádky. Po nasazení: migrace `000740`, první běh `onhost:operations:forget-secrets` projde historii po dávkách | ✔ | Po nasazení zkontrolovat, že `operations` bez `secrets_scrubbed_at` ubývají |

### První měsíc (P1)

| # | Oblast | Nález | Ověřeno | Návrh |
| --- | --- | --- | --- | --- |
| 14 | Doklady | **Opraveno 2026-09-20:** storno zaplacené objednávky (`PAID`/`FAILED → CANCELLED`) dobropisuje nedodané řádky všech jejích daňových dokladů a vrací rezervaci; postpaid pohledávka i DPH se odúčtují. Dobropis ví, kterou řádku opravuje (`corrects_line_id`): nic nejde dobropisovat dvakrát, doklad s dobropisem se platí jen do zbytku, částečný dobropis řádky dělí DPH poměrně. Provize partnera se vrací s dobropisem (už dřív); body za objednávku se u zadržené objednávky nepřipisují | ✔ | Body věrnostního programu u dobropisu po aktivaci zůstávají (rozhodnutí vlastníka) |
| 15 | Vratky | **Opraveno 2026-09-20:** vratka za zrušenou službu se počítá ze zaplacených řádků dokladů (hrubá částka, zbylé dny), ne z ceníkové ceny jednoho období bez DPH (roční předplatné vracelo podíl z jednoho měsíce; sleva 80 % vracela víc, než bylo zaplaceno). Vrací se dobropisem proti výnosům a DPH (`returnToCredit`), ne jako „příjem na bankovní účet chargeback“, a jako nevyplatitelný kredit — bonusový kredit se vratkou neměnil v hotovost (ověřeno mutací). Nezaplacená faktura se jen zmenší. Totéž marketplace | ✔ | — |
| 16 | Spotřebitel | Čtrnáctidenní odstoupení není mechanika (jen souhlas se zřeknutím) | – | Žádost, lhůta, poměrná část, vratka |
| 17 | DPH | **Opraveno 2026-09-20:** daňový doklad v jiné měně (faktura, doklad o přijaté platbě, opravný doklad) nese kurz ČNB platný ke dni plnění a DPH v CZK — po sazbách, součty jsou součtem sazeb; na PDF, v e-faktuře (`TaxCurrencyCode` CZK + celková daň v CZK, BT-6/BT-111) i v API (`czk`). Opravný doklad se přepočítá kurzem **původního** plnění (§ 42). Kurzy stahuje `onhost:fx:sync` (pracovní dny 14:40, denně 06:10; veřejný seznam ČNB bez klíče) a při vystavení se banka zeptá nejvýš jednou za čtvrt hodiny. Když banka neodpoví, doklad se **vystaví** s `czk_pending` a doplní ho další `onhost:fx:sync`; doktor hlídá doklady čekající déle než den a stáří kurzů | ✔ | Účetní: potvrdit, že směrnice firmy používá denní kurz ČNB (ne měsíční pevný) — jinak změnit zdroj kurzu |
| 18 | Čas | **Opraveno 2026-09-20:** datum na dokladu je den v sídle prodávajícího (`AccountingClock`, `ONHOST_BILLING_TIMEZONE`): číselné řady faktur i objednávek, DUZP, období řádků a data v UBL. Dřív doklad vystavený 1. 1. v 00:30 dostal číslo loňské řady a DUZP 31. 12. (ověřeno mutací: `FV-2026-0001`). „Splatné dnes" v upomínkách zůstává okamžikové | ✔ | Už vystavené doklady se nemění; při přechodu roku zkontrolovat první doklad |
| 19 | Dobíjení | **Opraveno 2026-09-20:** denní strop (počítadlo vracelo nejvýš 1, takže strop 2 nikdy nezabral) i měsíční limit (ukládal se, nečetl se) se počítají na skutečně provedených platbách uloženou kartou, v účetním dni a měsíci; nezdařený pokus se do dne počítá | ✔ | — |
| 20 | Ceník | **Opraveno 2026-09-20:** `first_period_only` se čte — odškrtnutý kód zlevňuje i obnovy (procentní podílem, pevný částkou, kterou řádek dostal); dřív se sleva slíbená natrvalo obnovila za plnou cenu. Nalezeno při tom: kód na pevnou částku se uplatňoval **na každý řádek** košíku (kód „100 Kč“ dal na třech řádcích 300 Kč) — je to teď rozpočet objednávky. `prices.promo_periods` nikde nastavit nejde (nečte se, nenabízí se) | ✔ | — |
| 21 | Dunning | **Opraveno 2026-09-20:** pozastavení, které se nepovedlo, se žádá znovu každý den (akce `suspend_retry`, nový idempotency klíč) a obsluha dostane „Neplacená služba stále běží“; případ se zavře jako `TERMINATED` až ve chvíli, kdy služba opravdu neběží — běžící služba se dřív ke zrušení ani nezadala a případ se zavřel podle kalendáře. `onhost:services:release-stranded` běží každých deset minut | ✔ | — |
| 22 | Domény | **Částečně opraveno 2026-09-20:** výpis registrátora bez stavu (Subreg) už neznamená „active“ — reconcile z něj bere jen data a na domény, které výpis nevysvětlí (nejsou `ACTIVE`, nebo mají datum v minulosti), se zeptá registru jednotlivě (nejvýš 100 za noc); doména v karanténě se dřív vracela jako `ACTIVE`. Prodloužení se zkouší denně i v ochranné lhůtě po expiraci (`ONHOST_DOMAIN_GRACE_RETRY_DAYS`, výchozí 20) a po dobití kreditu hned — dřív se den před expirací vzdalo a doména propadla, i když zákazník druhý den dobil. Potvrzení prodloužení datem expirace už dělá `ConfirmRenewalStep`. **Konec života domény (2026-09-20):** doména, kterou žádný registrátor nevypisuje a na kterou registrátor odpoví „nemám“, se po 36 h (`ONHOST_DOMAIN_MISSING_CONFIRM_HOURS`) uzavře — `TRANSFERRED_OUT` před expirací, `DELETED` po ní; prodlužování se zastaví, zákazník i obsluha to dostanou jednou, doktor ukazuje čekající i osiřelé DNS zóny; když ji registrátor začne znovu vypisovat, vrátí se. Dřív zůstala navždy `ACTIVE`, dál se plánovalo prodloužení a stejné hlášení chodilo každou noc. Výmaz u registru **na žádost** se nenabízí (nevratné, volání registrátorů neověřena) — vypnutí automatického prodlužování nechá doménu propadnout | ✔ kód, – registr | Ověřit na stagingu u `.cz`, kolik dní po expiraci registr prodlužuje za běžnou cenu, a podle toho nastavit lhůtu |
| 23 | DNS | **Opraveno 2026-09-20 (zbývají glue záznamy a změna registranta):** `onhost:dns:drift` každou noc porovná, co poskytovatel DNS skutečně vrací, s tím, co drží platforma (jen zóny vlastního DNS; rozdíl se ohlásí jednou a zůstává v doktorovi; zóna, která u poskytovatele **není vůbec**, je `hot`; srovnání, které se nepodařilo, má vlastní řádek doktora). Oprava je `POST /v1/dns/zones/{zone}/republish` (tlačítko „Publikovat zónu znovu"): poskytovatel dostane to, co drží platforma, zmizelá zóna se založí znovu, výsledek se ověří druhým čtením. Zóna má strop počtu záznamů (500) i čekajících změn (200). **PowerDNS:** TXT se četl v drátové podobě s uvozovkami — TXT záznam **nešel nahradit ani smazat** (staré SPF zůstalo vedle nového) a každá zóna se SPF by se „lišila" navždy. **WEDOS zóna:** opakovaná dávka už nezdvojuje řádky, commit se pošle vždy, přejmenovaný záznam je smazání + přidání (`dns-row-update` jméno nebere — WEDOS dál vracel staré jméno), dva MX na stejný host rozliší priorita | ✔ kód | Na stagingu ověřit, že `dns-rows-list` vrací i řádky před commitem a v jaké podobě vrací TXT. Glue záznamy a `updateContact` podle potřeby zákazníků |
| 24 | Pterodactyl | **Opraveno 2026-09-20:** adaptér čte `tls_ca` / `verify_tls` instance jako ostatní (`TlsOptions`; démoni Wings mohou mít vlastní `wings_tls_ca`). Odkazy, které panel vrací pro přenos (stažení zálohy, nahrání souboru), se následují jen na FQDN vlastních uzlů panelu a bez přesměrování — prolomený panel jinak mohl nechat control plane stáhnout cokoli z management sítě do archivu zákazníka. Přenos archivu při migraci už běží jako samostatná dlouhá úloha (`TransferGameArchive`), ne v kroku ságy | ✔ | Na stagingu: panel s vlastní CA → vložit certifikát do `tls_ca` v konzoli integrací |
| 25 | Kompenzace | **Opraveno 2026-09-20:** kompenzace mažou jen přes `CompensationGuard` — vazbu musela zapsat právě tato operace a panel musí zdroj potvrdit stejným důkazem jako u zrušení (`ServiceIdentityCheck`). Doloženo testem proti starému kódu: po klonu, který selhal, protože rezervované vmid mezitím obsadil někdo jiný, kompenzace **zastavila a smazala cizí běžící VM**. Nepotvrzený zdroj zůstane, zapíše se do auditu a obsluha dostane upozornění | ✔ | — |
| 26 | Audit | **Opraveno 2026-09-20:** čtení citlivých dat obsluhou se zapisuje (`staff.read.customer|ticket|mail_outbox|integration`, jednou za čtvrt hodiny na člověka a věc) a zákazník to vidí ve vlastním auditu. Nalezeno při tom: `support.ticket.read` měl jen vlastník platformy — fronta i detail tiketu vracely každé roli podpory 403; stejně finance u faktury a správce záloh u zálohy; dvě provozní nástěnky chtěly zákaznické `service.read` globálně. Dva strážní testy | ✔ | Po nasazení přiřadit role skutečným lidem a projít konzoli pod rolí podpory, ne vlastníka |
| 27 | Košík | **Opraveno 2026-09-20:** řádek s množstvím N se při nacenění rozpadne na N řádků (`l1`, `l1#2`, …) — každý má svou cenu, svůj díl slevy (kód na pevnou částku se pořád utratí jednou), svou službu, předplatné i řádek dokladu. Doplňkové řádky se kopírují s rodičem, každá kopie ke svému. Košík na webu množství nabízel a server ho odmítal (422) — objednávka „2× VPS“ nešla dokončit vůbec. Odmítá se: doména, změna tarifu, řádek se jménem webu, víc než 10 kusů na řádek a víc než 50 řádků na objednávku | ✔ | — |
| 28 | Hesla | Změna hesla neruší API tokeny (obnova ano). **2026-09-20:** e-mail i upozornění po **změně** hesla tvrdily „všechna ostatní přihlášení a API tokeny byly odhlášeny“ — nepravda; kdo měnil heslo kvůli úniku, věřil, že tokeny jsou mrtvé. Zpráva teď říká, co se stalo: přihlášení odhlášena, tokeny **zůstávají platné**, kolik jich je a kde je zrušit (šablona `security-password-kept`); po obnově hesla (tokeny se ruší) zůstává původní text | ✔ | Rozhodnutí vlastníka trvá: rušit tokeny i při změně, nebo jen upozornit (dnešní stav) |
| 29 | Objednávky | **Nalezeno a opraveno 2026-09-20:** `POST /v1/orders/{id}/transition` bral od držitele `staff.order.manage` libovolný cílový stav mimo command bus — agent podpory s vlastní organizací označil svou nezaplacenou objednávku za zaplacenou a služby se zřídily bez peněz a bez dokladu (doloženo testem proti starému kódu). Člověk teď smí objednávku jen zrušit; ruší ten, kdo smí objednávat (dřív stačilo čtení organizace). Konzole volala neexistující `/v1/staff/orders/{id}/transition` | ✔ | Na stagingu spustit `onhost:doctor` — kontrola „every order being delivered was paid and documented“ najde objednávky, které stará cesta vyrobila |
| 30 | Databáze | **Nalezeno a opraveno 2026-09-20:** workflow `tests` (job `pest-postgres`) byl červený 92 běhů po sobě od 15. 9. a nikdo se nedíval. Ruční připsání kreditu obsluhou zapisovalo do účetní knihy jako `reference_id` celý idempotentní klíč (81 znaků) do sloupce `varchar(60)` — SQLite délku neřeší, PostgreSQL příkaz odmítne: **v produkci by připsání kreditu skončilo chybou 500**. Klíč příkazu navíc obsahoval čas na sekundy. Dokončený požadavek se stejným `Idempotency-Key` sice přehraje HTTP vrstva, ale když odpověď uložená není (proces spadne po zápisu a před uložením odpovědi, odpověď je 5xx, dva stejné požadavky běží zároveň), byl opakovaný požadavek pro sběrnici **novým příkazem**: kredit připsaný podruhé, u asistované objednávky druhá objednávka zaplacená z kreditu zákazníka, u založení služby obsluhou druhá služba, u ručního přidělení bodů druhé body (doloženo testy, které uloženou HTTP odpověď smažou). Oprava: sloupec rozšířen (migrace `000790`), klíč předávaný doménám má strop 120 znaků (dlouhá hlavička se uloží jako otisk), ruční kredit, asistovaná objednávka, založení služby obsluhou a přidělení bodů používají `onceKey()` (klíčem příkazu je hlavička sama), očekávané porušení unikátnosti běží v savepointu | ✔ | Po každém pushi zkontrolovat `gh run list --workflow tests`; release jen ze zeleného commitu |
| 31 | AI asistent | **Nalezeno a opraveno 2026-09-20:** (a) volání modelu nemělo žádný strop — obecný limit API je 120 dotazů za minutu na uživatele či token, každý dotaz je volání modelu s několika tisíci tokeny kontextu; skript se zákaznickým tokenem mohl provozovateli nadělat účet, aniž by sáhl na službu. `AssistantBudget`: 40 odpovědí modelu na člověka za hodinu (obsluha 120), 300 na organizaci za den, denní strop tokénů platformy; po vyčerpání se **nic neodmítá** — asistent odpovídá podle nápovědy a řekne to, obsluha to dostane jednou denně, doktor ukazuje čerpání. (b) id konverzace asistenta pro obsluhu mělo 82 znaků pro sloupec 80: **na PostgreSQL by každý dotaz obsluhy na asistenta skončil chybou 500** (migrace `000800`) | ✔ | Stropy nastavit podle ceníku modelu a provozu (`ONHOST_AI_*`) |
| 32 | Git deploy | **Nalezeno a opraveno 2026-09-20:** API přijalo příkaz buildu do 2 000 znaků a adresu repozitáře do 300 pro sloupce 500 / 250 — na PostgreSQL chyba 500; hranice teď říká doménová služba (422), platí i pro asistenta a CLI. Nalezeno statickým porovnáním validací se šířkami sloupců | ✔ | — |
| 33 | AI asistent | **Nalezeno a opraveno 2026-09-20 (bezpečnost):** model směl navrhnout **jakoukoli** akci služby s vlastními parametry i vlastním popiskem tlačítka a zákazník potvrzoval dialog, který ukazoval jen ten popisek. Model po prompt-injekci (tiket, název souboru, stránka) mohl nabídnout „Vyčistit cache“ a kliknutí spustilo `command.run` s `curl … \| sh`, uložilo PHP soubor, změnilo FTP heslo, založilo cron, smazalo databázi nebo přesměrovalo web — tlačítko neslo třídu `SAFE_WRITE`. Teď platí seznam (`AssistantProposals`): co na něm není, asistent nikdy nenavrhne; na seznamu jsou jen vratné/opakovatelné akce, jejichž parametr je volba, ne náklad; parametr mimo seznam neprojde; **popisek skládá platforma**, ne model; nic, co chce čerstvé ověření | ✔ | — |
| 34 | VPS | **Doplněno 2026-09-20:** server se zřizuje jen s SSH klíči z objednávky a přístup pak nešel změnit — kdo klíč ztratil nebo ho nezadal, do vlastního serveru se nedostal. Akce `access.reset` nastaví nové klíče a/nebo heslo přes cloud-init (jen to, co je zadáno; síť a uživatel zůstávají), chce čerstvé ověření, heslo operace po doběhu zapomene, asistent ji nenavrhuje | ✔ kód, – panel | Na stagingu ověřit na podporovaných obrazech, že cloud-init po restartu nové heslo/klíče použije. Dál chybí: reinstalace OS (destruktivní — až se zálohou předem), rescue režim, připojení ISO, reverzní DNS |
| 35 | Doplňky | **Nalezeno a opraveno 2026-09-21:** doplněk je účetní řádek, jehož jediným úkolem je změnit službu, ke které patří — a pět ze sedmi prodávaných doplňků neměnilo **nic**: hodinové zálohy, schránky, CDN, OV certifikát i anti-DDoS profil se účtovaly každý měsíc a nedodalo se nic. Dva zbylé zapsaly zálohovací politiku v podobě, kterou plánovač neumí přečíst (`schedule.frequency`, `retention.days`, `retention.generations` × `{daily: '02:30'}`), takže i ti zákazníci měli plán svého tarifu. A **žádný doplněk nešlo zrušit**: nemá vazbu na panel, takže `requestAction` i kontrola identity ho odmítly (409) a předplatné účtovalo dál. Teď platí `Addons`: co platforma neumí dodat, není v prodeji (`ssl` a `anti-ddos-pro` jsou `draft`), co doplněk rodiči přidá, se zapíše i s původní hodnotou a zrušení to přesně vrátí (hodnotu, kterou mezitím někdo změnil, nechá být), zálohovací doplňky píší plán ve slovech plánovače a `mail-hosting` je konečně dosažitelný z košíku | ✔ | Rozhodnutí vlastníka: zda a kdy doplnit prodej OV certifikátů (reseller) a anti-DDoS profil; do té doby zůstávají mimo prodej |
| 36 | Tarify | **Nalezeno a opraveno 2026-09-21:** tarif nese dvě strojově čtené sady (`entitlements`, `limits`) a pět čísel v nich nečetl **žádný kód**: `cpu_seconds_per_day` (webhosting, Managed WordPress, e-shop, na míru), `db_connections` a `outbound_mail_per_hour` (webhosting), `pps_limit` (VPS). Zákazník je ani neviděl — vypadala jako limity jen v editoru tarifů. `inodes` se prodávalo, oba panely počet souborů webu **měří** a nikdo to nikdy neporovnal; přenos dat se prodával větou („fair-use 500 GB/měs“), zatímco `UsageWatch` čte číslo `traffic_gb` — limit přenosu tedy nikdy neplatil. Teď platí `PlanPromises`: číslo v tarifu buď čte kód, nebo je to deklarovaný fair-use slib a tak je i napsaný v ceníku; `traffic_gb` a `inodes` jsou měřené (upozornění na 85 % a 95 %); z tarifu jde klíč konečně **odebrat** (`null` v `plan.publish` — schéma dosud umělo jen růst) | ✔ | Doktor: „no plan on sale promises a number nothing applies“. Na stagingu po nasazení vydat novou verzi tarifů, aby z nich odešla stará čísla |
| 37 | Reverzní DNS | **Nalezeno a opraveno 2026-09-21:** `IpAddress.rdns` byl sloupec a nic víc — zřizování do něj zapsalo jméno serveru a **žádný PTR se nikdy nikam nepublikoval**. Každý VPS, který platforma dodala, byl bez reverzního záznamu, takže pošta z něj odcházela do prázdna (většina příjemců ji odmítne) — a doplněk IPv4 přitom prodává `rdns: true`. Záznam navíc zůstal na adrese i po jejím uvolnění, takže další nájemce zdědil reverzní jméno toho předchozího. Teď: `DnsService::reverseZoneFor()` najde reverzní zónu, kterou platforma drží (nejdelší delegace vyhrává, IPv4 i IPv6), `syncPtr()` ji publikuje stejnou cestou jako ostatní systémové záznamy (takže ji hlídá i noční kontrola rozdílů a umí ji opravit „Publikovat zónu znovu“), uvolnění adresy záznam smaže. Zákazník si jméno nastaví sám (`rdns.set` na kartě Síť) — zadává **jméno, nikdy adresu**; kde platforma reverzní zónu nemá, se možnost vůbec nenabízí. Migrace `000810`: reverzní zóna je provozovatelova, ne zákazníkova | ✔ kód, – registr | Na stagingu: nechat si od poskytovatele rozsahu delegovat reverzní zónu na naše NS a ověřit, že se PTR opravdu resolvuje |
| 38 | Záchranný režim | **Doplněno 2026-09-21 ([[Hosting/H233]]):** server, kterému nenabíhá vlastní systém, byl pro majitele nedostupný — panel neuměl nastartovat nic jiného, takže každý rozbitý VPS byl tiket a ruční zásah v hypervizoru. Nově `rescue.start` / `rescue.stop`: obraz **jen z ISO úložiště uzlu** (co tam provozovatel nedal, nejde nastartovat), okno místo stavu (`ONHOST_RESCUE_HOURS`, výchozí 8 h; `onhost:services:rescue-expire` každých deset minut server vrátí), a zpátky se nastaví **přesně to, co se našlo** (pořadí bootu i mechanika se zapisují do `tags.rescue.previous`, nikdy se nehádá výchozí hodnota). Start je HIGH s čerstvým ověřením (je to root na discích zákazníka) a asistent ho nenavrhuje; ukončení ověření nechce — ven se musí dát vždy | ✔ kód, – panel | Na stagingu nahrát ISO na `iso_storage` a ověřit, že se VM z něj opravdu nastartuje a po ukončení nabootuje zpět z disku. Zbývá z bodu 34: reinstalace OS (destruktivní — až se zálohou předem) a připojení vlastního ISO |
| 39 | Přepsání dat | **Nalezeno a opraveno 2026-09-21:** pravidlo vlastníka zní, že se nic nesmaže, nevyčistí ani jinak nenaruší, dokud to není plnohodnotně zazálohované — platilo jen pro zrušení služby. **Obnova ze zálohy přepsala web, návrat na snapshot zahodil všechno od jeho pořízení a herní reinstalace přepsala soubory serveru, a žádná z nich si nenechala kopii toho, co zničila.** Panel zákazníkovi jen doporučil, ať si zálohu udělá sám; kdo obnovil špatnou zálohu nebo se vrátil o den dál, neměl cestu zpět. Teď každý z těch tří řetězců začíná krokem „Záloha před přepsáním": server dostane okamžitý snapshot u poskytovatele, ostatní stejný archiv jako při zrušení (soubory + každá databáze, čerstvé, jinak krok selže). Kopie je chráněná a drží se po dobu retence podle `DeletionPolicy` (60 dní). **Když se kopii nepodaří pořídit, nic se nepřepíše** | ✔ | Strážní test drží pořadí kroků — kdo přidá destruktivní akci, musí jím projít. Na stagingu ověřit spotřebu místa: obnova a rollback teď vždy vyrobí kopii |
| 40 | Zákaznické UI | **Nalezeno a opraveno 2026-09-21 ([[Hosting/H412]], [[Hosting/H413]]):** mapa funkcí služby se počítala **bez ohledu na to, kdo se ptá** — vlastník i člověk se sdíleným přístupem jen pro čtení dostali stejnou odpověď. Spolupracovník tedy viděl všechna tlačítka a pravdu se dozvěděl až jako 403 po kliknutí; a pozastavená služba vypadala úplně stejně jako panel, který tu věc neumí vůbec. Teď `features($service, $actor)` filtruje podle **téhož oprávnění, které si potom vyžádá command bus** (panel a server se tedy nemohou rozejít) a nedostupná funkce říká **proč**: `permission` (smíte se dívat, ne konat) nebo `state` (služba právě neběží). Bez actora se nic nemění — vnitřní brány se ptají, co umí *služba*, ne kdo se ptá | ✔ | Doplnit zbylé důvody (`plan`, `panel`, `unreachable`) tam, kde je `features()` už zná, a promítnout je do textů v panelu |
| 41 | Potvrzení akce | **Doplněno 2026-09-21 ([[Hosting/H414]]):** „Opravdu?" není potvrzení. Destruktivní akce měla nanejvýš příznak `confirm: true`, který neříká nic o tom, co se potvrzuje. Nový náhled (`GET /v1/services/{id}/actions/{action}/preview`) odpovídá jménem služby, **konkrétními věcmi, které zmizí** (databáze a schránky podle panelu), tím, co na nich visí (doplňky, staging, DNS zóna, předplatné), a **jak daleko zpátky se dá vrátit** — pojmenovaně, protože kopie před přepsáním i závěrečný archiv teď existují. Odpověď nese **otisk cíle**; když se s ním pošle akce a cíl se mezitím změnil (jiná záloha, přibyla databáze, změnil se stav služby), platforma potvrzení **odmítne** (409 `target_changed`) místo toho, aby zničila něco, co člověk nikdy neviděl — to je akceptační scénář karty | ✔ | Prošít náhled do potvrzovacích dialogů v panelu (dnes je volitelný: kdo otisk pošle, je chráněn) |
| 42 | Plánované zálohy | **Nalezeno a opraveno 2026-09-21 ([[Hosting/H434]], [[Hosting/H435]], [[Hosting/H446]]):** slot, který se nepodařilo spustit, se počítal jako „skipped“ a **neřeklo se nic**. Hodinový plán na webu, jehož záloha trvá déle než hodinu, tedy míjel každý slot potichu — a protože hodinový doplněk teď opravdu funguje (řádek 35), je to plán, který si někdo může koupit. Souběh sám je zakázaný zámkem operace na službu, takže se zálohy nikdy nepřekryjí; tichý byl jen ten výpadek. Teď se každý slot zapíše do `tags.backup_schedule` (kdy naposled proběhl, kolik slotů po sobě se minulo a proč), třetí zmeškaný v řadě se jednou ohlásí zákazníkovi i obsluze (`service.backup.schedule.stalled`) a pak už jen zřídka, a úspěšný slot počítadlo vynuluje | ✔ | Doktor: „backup schedules keeping up“. Na stagingu sledovat, jestli hodinový plán na velkém webu stíhá — jinak nabízet 6h |
| 43 | Plánované příkazy | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H438]], [[Hosting/H441]]):** plánovač aaPanelu **je kořenový crontab uzlu** — `AddCrontab` nemá pole uživatele a úloha typu `toShell` je skript, který démon panelu spouští jako **root**. Zákazníkův příkaz šel dovnitř právě jako ten skript, takže kdokoliv na sdíleném webhostingu vyplnil formulář cronu, spouštěl příkazy jako root vedle souborů, databází a klíčů všech ostatních. ISPConfig tohle nikdy neměl (cron patří klientovi webu a je v jailu). Nově příkaz putuje jako **data** v citovaném here-dokumentu předaném vlastnímu uživateli webu (`cd <kořen webu>` + `su -s /bin/bash <uživatel>`), čte se zpátky nezměněný a nový `CronCommand` odmítne příkaz po jednotlivých větách — `php cron.php; sudo …` neprojde. Druhá půlka: `DeleteSite` se crontabu nedotýká, takže **zrušená služba nechávala své úlohy běžet dál navždy**; `terminate()` je teď odstraní podle jména (`onhost:<služba>:…`), cizí nechá být | ✔ | Na stagingu ověřit, že úloha pod uživatelem webu opravdu běží (`ensureAgent` musí proběhnout dřív). Plán, který aaPanel neumí vyjádřit, se teď odmítá místo tichého spuštění v jiný čas |
| 44 | Pozastavení | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H440]]):** hloubka pozastavení uměla jen web — vypnula cron a FTP a pamatovala si co vypnula. **Herní server žádnou takovou hloubku neměl:** panel server zastaví, ale řádky plánů zůstanou nabité, takže každý slot dál střílí proti serveru, který nemusí běžet, a po obnovení nikdo neví, co bylo zapnuté. `SuspensionDepth` je teď řízená tabulkou druhů (`cron`, `ftp`, `schedule`) podle toho, co adaptér umí; herní plány se vypínají a zapínají stejným mechanismem paměti a plán, který si zákazník vypnul sám, zůstane vypnutý. Ověřeno také, že plánované zálohy hloubku nepotřebují — plánovač se dívá jen na ACTIVE/DEGRADED, takže pozastavená služba nové nedělá a stávající si nechá | ✔ | Na stagingu pozastavit herní službu a zkontrolovat v panelu, že plány zhasly |
| 45 | Chybující automatizace | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H447]]):** plánovač uměl ohlásit slot, který se nepodařilo **spustit** (řádek 42), ale o zálohu, která se spustila a **selhala**, se nestaral vůbec. Horší: `due()` považoval za „slot proběhl“ jen běžící nebo hotovou zálohu, takže selhaná záloha se v témže okně spouštěla **při každém ticku plánovače** — celé balení webu znovu a znovu, dokud okno neuplynulo. Teď se selhaný pokus počítá jako pokus toho slotu (operace si vlastní opakování řeší sama), pět selhání po sobě plán **sám zastaví**, jednou to řekne zákazníkovi i obsluze (`service.backup.schedule.paused`) a plán se rozeběhne až když ho člověk znovu nastaví. Nic se nemaže, hotové zálohy zůstávají | ✔ | Doktor: „no backup schedule is waiting for a person“. Nalezeno při tom: úspěšný start přepisoval celý záznam v `tags.backup_schedule` a mazal tím počítadlo selhání — teď se slučuje |
| 46 | Import databáze | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H456]], [[Hosting/H467]]):** import SQL se píše **přes živou databázi** a byl to jediný destruktivní úkon **bez kopie toho, co přepisuje** — řádek 39 zavřel obnovu, návrat snapshotu i reinstalaci, ale na import se zapomnělo. MySQL aplikuje dump příkaz po příkazu, takže soubor, který se v půlce zlomí, nechá databázi napůl starou a napůl novou a web z ní dál obsluhuje. Řetězec je teď **místo → kopie → import**: velikost dumpu se čte z gzip patičky (ne z komprimované velikosti), potřeba se počítá jako dvojnásobek (data, indexy a co engine potřebuje při nahrávání) a porovná se se zbytkem tarifu; pak se vyexportuje **právě ta jedna databáze** (ne celý web, ten se nemění), chráněná na 60 dní; a teprve pak import. Když panel dump **definitivně odmítne**, kopie se vrátí zpět a zákazník i obsluha se to dozví; u chyby, kterou lze opakovat (timeout), se nesahá na nic — import může na uzlu pořád běžet a přepsat ho starými řádky by bylo horší | ✔ | Rozlišení „odmítl“ vs. „neodpověděl“ se bere z kódu chyby panelu, ne z příznaku `retryable` operace — ten je po vyčerpání pokusů `false` i u timeoutu |
| 47 | Účty databáze | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H462]]):** účet databáze je to, čím se aplikace přihlašuje. Smazání účtu, na kterém databáze pořád visí, panel provedl bez jediného slova a web se při dalším dotazu neohlásil — tiše, bez chyby v naší administraci. `dbuser.delete` se teď nejdřív podívá, které databáze ten účet používají, a odmítne s jejich výčtem (409 `dbuser_in_use`). Účet, na kterém nic nevisí, jde jako dřív | ✔ | **Vědomé rozhodnutí u `database.delete`:** kopie se tam nedělá. Zákazník o smazání požádal, viděl náhled a potvrdil otisk cíle; chráněný archiv, který sám nesmaže, by znamenal, že platforma drží smazaná data 60 dní. Chrání ho náhled a plánované zálohy — kdyby to vlastník chtěl jinak, je to jednořádková změna |
| 48 | Testy v noci | **Nalezeno a opraveno 2026-09-22:** `DunningEnforcementTest` posouval hodiny o rovný den mezi kroky, jenže případ upomínek si říká, kdy se na něj smí znovu sáhnout — **zítra v 6:00**. Běh, který začal mezi půlnocí a šestou UTC, tedy skončil **před** tou hodinou, případ nebyl splatný, opakovaný pokus o pozastavení se nekonal a služba zůstala aktivní. Test padal **každou noc šest hodin** a zčervenal CI; regrese to nebyla, stejně padá i na starších revizích. Hodiny se teď posouvají na 7:00 následujícího dne, což navíc říká, co ten krok znamená | ✔ | Nalezeno tak, že jsem pustil celou sadu v 00:05 UTC — v tom okně se to jediné projeví. **Po každé změně něčeho časového pustit sadu jednou uvnitř 00:00–06:00 UTC** |
| 49 | Test obnovy | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H458]]):** doplňky `backup-7` a `backup-30` se prodávají s `restore_test: monthly`, ceník slibuje „Test obnovy měsíčně“, tarif to zapíše do `backup_policies.restore_test` — a **žádný kód nikdy žádnou obnovu neotestoval**. Jediná další zmínka v celé platformě je věrnostní mise, která o test žádá **zákazníka**. Slib se prodával a neplnil ho nikdo. Nový `RestoreTest` obnoví nejnovější sadu **do databáze, kterou si k tomu sám založí** (test nesmí odpovídat na otázku tím, že přepíše živá data), výsledek soudí **kolotočem** — obnovená databáze se vyexportuje zpět a porovnají se tabulky, které dump nesl — a zkušební databázi **vždy odstraní**, takže nespotřebuje ani místo, ani limit počtu databází. Běží jako řádná operace přes sběrnici (`restore.test`), takže má audit, idempotenci i outbox | ✔ | Doktor: „every promised restore test has been made“. Nalezeno při tom: část sady se jmenuje `.sql`, ale může nést gzip bajty (aaPanel vydává `.sql.gz`) — čtečka tabulek proto rozhoduje podle prvních dvou bajtů, ne podle přípony |
| 50 | Převzetí uzlu | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H471]]):** `nodes.state` měl výchozí hodnotu `active` a **obě cesty, kterými uzel vzniká, ho tam rovnou postavily** — objevení z panelu (čtyři větve: Proxmox, ISPConfig, aaPanel, herní panel) a hlavně `NodeBootstrap::activate()`, které čerstvě nainstalovaný stroj volá **z vlastního boot skriptu**. Jeden curl z cloud-initu a plánovač na něj postavil platícího zákazníka: neznámá velikost, možná plný disk, nevyplněná failure domain (takže rozprostření přes hostitele tiše přestalo něco znamenat). Uzel se teď rodí jako `qualifying` — je vidět, je sledovaný, a plánovač ho nevidí (`isSchedulable()` je `state === active`). Nový `NodeQualification` zkontroluje, co platforma sama ví (instance je použitelná a její prerekvizity neselhaly, panel uzel nedávno potvrdil, zná svou velikost, má region/roli/failure domain, na disku zbývá rezerva) a `accept()` ho do nabídky pustí, **jen když všechny povinné body projdou**; zapíše kdo a kdy, i vědomě přijatou výjimku (H478) | ✔ | Doktor: „no node is waiting to be qualified“ a **„every node in the offer has been qualified“** — ten druhý ukáže, kolik stávajících uzlů kvalifikací nikdy neprošlo. Body, které potřebují shell na uzlu (čas H472, DNS H473, odchozí provoz H474, management rozhraní H480) i syntetická služba (H479) se hlásí jako `not_checked` s důvodem — **kontrola, kterou nikdo neudělal, není splněná kontrola** |
| 51 | Zkušební služba | **Doplněno 2026-09-22 ([[Hosting/H479]]):** uzel může projít každou kontrolou na papíře — odpovídá, zná svou velikost, na disku má místo — a přesto neumět vyrobit tu jednu věc, kvůli které existuje. Jediný důkaz je jednu vyrobit: `SyntheticService` na přesně tomhle uzlu založí zahazovací zdroj přes `provision()` (kontrakt, který má každý adaptér), počká na asynchronní úlohu, ověří, že ho panel ukazuje, odstraní ho a ověří, že je pryč. **Zdroj, který se nepodařilo odstranit, uzel shodí** — karta výslovně říká, že vytvoření nestačí, a uzel, který nechává věci za sebou, nechá za sebou i zákazníkovu zrušenou službu. Odstranění se zkusí ještě jednou ve `finally`; co zůstane, se jmenuje (`leftover`), ohlásí (`node.synthetic.leftover`) a doktor to vypíše. Co se zakládá, **napíše vlastník** do `onhost.provisioning.qualification.synthetic.<role>` — nic se nehádá; role bez šablony nezaloží nic a **dokud šablona není, žádný panel se nedotkne**. Jakmile šablona je, stane se zkušební služba **povinným** bodem kvalifikace a platí jen běh mladší 7 dní | ✔ | Šablonu nastavit **na testovacím rozsahu**: `php artisan onhost:nodes:qualify --synthetic=<uzel>`. Na výchozím nastavení (prázdné) se nic nevytvoří |
| 52 | Aplikace webu | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H505]], [[Hosting/H500]], [[Hosting/H440]]) — bezpečnost:** Node.js aplikace webu na aaPanelu je objekt **panelu, ne webu** (`project/nodejs/*`). Tři věci: (1) **vlastnictví se určovalo holým prefixem cesty** — `/www/wwwroot/shop.cz` „vlastnil“ i aplikace `/www/wwwroot/shop.cz.eu`, takže zákazník jednoho webu mohl **zastavit, restartovat nebo smazat aplikaci jiného zákazníka**; teď platí kořen webu nebo cokoliv pod jeho lomítkem. (2) `DeleteSite` vzal soubory a **aplikaci nechal**: startuje s každým bootem (`is_power_on`), drží si **port** a panel dál směruje domény webu na ten port — další zákazník, který by dostal stejný port, by **přijímal provoz cizího webu**; běžící proces si smazání souborů ani nevšimne a odpovídá z paměti. `terminate()` teď aplikace webu nejdřív zastaví a pak odstraní (H505: port se uvolní až po potvrzeném zastavení). (3) Pozastavený web vypnul vhost a aplikace **dál odpovídala** přes vlastní proxy; `SuspensionDepth` ji teď zastaví a při obnovení spustí přesně ty, které zastavil | ✔ | Na stagingu: web s Node.js aplikací zrušit a ověřit v panelu, že projekt zmizel a port je volný. Cestou opraven i testovací pomocník, který si test půjčoval z cizího souboru (samostatně spuštěný padal) |
| 53 | Firewall VPS | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H501]], [[Hosting/H503]]):** tři věci. (1) Pravidla se nahrazovala **smaž všechno → přidej nová** se zapnutým firewallem a `policy_in: DROP`: po dobu přidávání VM zahazovala všechno, a když panel některé pravidlo odmítl, **zůstala s polovičními pravidly pod DROP a bez SSH**. Teď **nejdřív postav, pak zbourej**: nová pravidla jdou nahoru, zatímco stará ještě stojí pod nimi, stará se odstraní odspodu, teprve pak se přepnou volby — a když cokoliv selže, vrátí se **celá předchozí politika**, pravidla i volby (H503). (2) Pravidla se posílala **bez pozice**, a pravidlo vytvořené bez pozice jde v Proxmoxu nahoru — **zákazníkův seznam vyšel pozpátku**, a na firewallu vyhrává první shoda („pusť kancelář na 22, ostatní zahoď“ se obrátilo). Teď se vkládají odzadu, každé nahoru, což dává správné pořadí **ať panel parametr `pos` při vytváření ctí, nebo ne** — test to ověřuje pro obě chování. (3) Nic zákazníkovi neřeklo, že ukládá pravidla, která mu zavřou vlastní terminál: `FirewallPolicy` teď odmítne sadu, která zavře vzdálenou správu (SSH 22 ani RDP 3389), nebo úplně všechno, dokud to zákazník nepotvrdí (`accept_lockout`), a pojmenuje cestu zpět — **konzoli v panelu**, která jde přes hypervizor a firewall ji neovlivní (H501: nezávislá cesta nápravy) | ✔ | Na stagingu: firewall VPS s několika pravidly a zkontrolovat v Proxmoxu jejich pořadí |
| 54 | Mazání herního serveru | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H505]], [[Hosting/H488]]):** adaptér mazal herní server přes `DELETE …/servers/{id}/force`. Vynucené smazání přeskočí všechno, co neodpoví: když byl Wings nebo databázový host v tu chvíli dole, panel smazal záznam a **svět zůstal na disku Wings a databáze „visí na hostu“** (slovy samotného kódu panelu) — natrvalo, s daty zrušeného zákazníka, a nic, co by o nich vědělo. Teď běžné smazání: panel soubory i databáze odstraní, nebo odmítne (404 od Wings — server tam není — bere jako hotovo); odmítnutí shodí krok a operace to zkusí znovu, až bude uzel zpět. Vynutit lze jen vědomým přepínačem instance `terminate_force` pro uzel, který je pryč natrvalo, a výsledek to nese (`forced: true`). **Proxmox je v pořádku** — `terminate()` posílá `purge=1` (VM zmizí ze záloh, replikace i HA) a `destroy-unreferenced-disks=1` | ✔ | Na stagingu po smazání herní služby zkontrolovat na Wings adresář záloh: finální archiv tam vyrobí **zamčenou** zálohu `onhost-final-*`, stáhne ji k nám a na panelu ji nechá — jestli ji panel při smazání serveru odstraní, ověřené není |
| 55 | Expirace archivu VPS | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H488]]) — ochrana osobních údajů:** finální archiv VPS je **zamčená záloha vzdump** na záložním serveru; sada na disku platformy drží jen metadata. Po uplynutí retence `FinalArchive::prune()` smazal tu sadu a archiv označil `expired` — **a celý obraz disku zrušeného zákazníka zůstal na záložním serveru navždy**: zamčený, takže ho žádná prune úloha nevezme, a v platformě nic, co by o něm ještě vědělo. Platforma tvrdila, že data smazala, a neudělala to. Teď se nejdřív odstraní kopie u poskytovatele (nový kontrakt `ExpiringBackups`, v Proxmoxu odemknout a smazat podle volid — úložiště je v něm zakódované, VM i její uzel už dávno neexistují) a **teprve pak** je archiv expirovaný; dokud data u poskytovatele jsou, archiv zůstává `completed` a proč, říká `meta.expiry_blocked`. Svazek, který už neexistuje, se bere jako smazaný. Zákonná blokace (H18) platí dál | ✔ | Doktor: „expired archives are gone from the provider too“. Na stagingu: po uplynutí retence u zrušeného VPS ověřit v PBS, že zamčená záloha zmizela. **Otevřené:** `reserveVmid()` bere nejnižší volné VMID, takže se číslo zrušeného VPS znovu přidělí — nové zálohy pak padají do téže skupiny `vm/<vmid>` v PBS vedle zbytků předchozího zákazníka |
| 56 | Čísla VM v Proxmoxu | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H488]], [[Hosting/H505]]):** `reserveVmid()` bral `/cluster/nextid`, tedy **nejnižší volné číslo** — číslo VPS, které se právě zrušilo a smazalo, dostala hned další objednávka. Vazba zrušené služby to číslo drží dál (vazby jsou historie a trojice instance/typ/číslo je unikátní), takže **klon se udělal, vazba selhala, objednávka skončila chybou a nový VM zůstal na uzlu bez vazby**; kdyby se navázal, jeho zálohy by padaly do skupiny `vm/<číslo>` na záložním serveru vedle zamčeného finálního archivu předchozího zákazníka. Dvě souběžné objednávky navíc mohly sáhnout po stejném čísle. Teď `VmidReservations` drží číslo od chvíle, kdy ho operace vybere (tabulka `vmid_reservations`, unikátní klíč na cluster a číslo): nové číslo je vždy nad vším, co platforma na clusteru kdy držela nebo navázala, přeskočí čísla živých hostů i čísla, pro která má záložní úložiště zálohy, a Proxmox ho při výběru potvrdí jako volné. Číslo, které někdo zabral těsně před klonem, se „spálí“ a bere se další. **Souvisí:** když se odpověď na klon ztratila, dokud se kopíroval disk (dočasná konfigurace bez jména a popisu), opakovaný krok udělal **druhý VM**; teď na svůj klon počká. A Proxmox píše důvod odmítnutí do stavového řádku HTTP a nemá 404 pro hosta — platforma četla jen tělo `{"data":null}`, takže každé odmítnutí bylo „server error“ a smazaný VM nešlo poznat; teď se důvod čte, „already exists“ je konflikt, zámek (záloha, klon) se přečká, a chybějící konfigurace znamená „nenalezeno“ — **až když VM není nikde v clusteru** (VM přesunutý HA na jiný uzel se za smazaný nebere a identita ho na starou vazbu nesmaže) | ✔ | Doktor: „guest numbers are the platform's own“. Instance: `vmid_min`/`vmid_max` volitelně (rozsah pro zákaznické VM). Na stagingu: zrušit VPS, objednat nový a ověřit, že dostal vyšší číslo; `pvesh get /cluster/nextid` porovnat. **Otevřené → řádek 58:** VM přesunutý HA na jiný uzel nechával vazbu na starém uzlu |
| 57 | Ochranný vypínač panelů | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H288]], [[Hosting/H02]]):** vypínač (circuit breaker) instance panelu po pěti neúspěšných voláních za sebou zastaví na minutu všechna volání na ten panel — posuzoval ale volání jen podle stavového kódu HTTP. (1) **Proxmox** odpovídá HTTP 500 i na běžné odmítnutí jednoho hosta (zamčený zálohou, neexistuje) a HTTP 595/596, když API uzel nedosáhne na jiný uzel clusteru; **Pterodactyl** předává 5xx, když neodpovídá démon Wings jednoho uzlu. Pět takových odpovědí — stačí jeden zákazník, který opakuje akci na VM v okně zálohy, nebo jeden spadlý uzel — **zastavilo správu všech VPS, resp. všech herních serverů na všech uzlech**. (2) **ISPConfig, WEDOS a Subreg** hlásí vlastní poruchy v těle odpovědi HTTP 200: úspěch zapsaný podle kódu vynuloval počítadlo dřív, než adaptér poruchu nahlásil, takže **vypínač u nich nesepnul nikdy**, ať panel selhával jakkoli dlouho. (3) **Subreg** počítal HTTP 5xx dvakrát (vypínač sepnul po třech chybách). Teď požadavek s `judgedByCaller` nechá verdikt adaptéru: odmítnutí jednoho zdroje a nedostupný uzel nejsou porucha panelu, porucha v těle odpovědi ano; volání, které nepřineslo žádnou odpověď, počítá klient dál. Přímá volání démona Wings (nahrání souboru, systémové informace) mají vlastní vypínač pro každý uzel (`<instance>:node-<id>`) | ✔ | `tests/Feature/Provisioning/PanelBreakerTest.php`. Na stagingu: při údržbě jednoho uzlu Wings ověřit, že herní servery na ostatních uzlech jdou dál spravovat; `GET /v1/staff/integrations` ukazuje stav vypínače |
| 58 | VM přesunutý HA | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H288]], [[Hosting/H02]]):** když HA po výpadku uzlu přesune VM na jiný uzel (nebo ho tam někdo přesune ručně), vazba služby dál jmenovala starý uzel — **každá další akce šla na starý uzel a tam selhala**: restart, záloha i zrušení (kontrola identity nesmaže VM podle vazby, která jmenuje jiný uzel). Nic vazbu neopravovalo, zbývala ruční úprava databáze. Teď reconciler (každých 5–15 minut čte stav každé služby) vazbu, záznam VM i uzel služby **přesune za VM** — ale jen když se VM prokáže jako tato služba (štítek služby, jinak jméno, které mu dala platforma); stroj pod stejným číslem bez takového důkazu se nenásleduje a otevře se bezpečnostní drift. Přesun zapíše audit `provisioning.guest.relocated` a událost `service.relocated` pro operátory. **Souvisí:** interní upozornění uzlů, služeb a platformy (`node.*`, `service.*`, `platform.*`) padala v inboxu správy pod „finance“ — teď „infra“ s odkazem na uzly/služby | ✔ | `tests/Feature/Provisioning/GuestRelocationTest.php`. Na stagingu: migrovat VM v Proxmoxu na jiný uzel a do 15 minut ověřit, že vazba služby jmenuje nový uzel a restart ze správy prošel |
| 59 | Verze panelů | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H530]], [[Hosting/H514]], [[Hosting/H521]], [[Hosting/H511]]):** každý adaptér deklaruje verze panelu, proti kterým byl ověřen (`supportedVendorVersions()`), a health sonda každou minutu zapisuje verzi, kterou panel hlásí — **nic tyto dvě věci neporovnávalo**. Panel aktualizovaný na verzi, kterou nikdo neověřil (aktualizátorem výrobce, operátorem, automatickou aktualizací), **dál dostával nové objednávky**; nová instance na neznámé verzi šla rovnou do nabídky; a ISPConfig verzi vůbec nehlásil, takže jeho aktualizace nešla ani poznat. Teď `PanelVersionGate`: změnu verze (i návrat) nebo novou instanci ověří — verze musí být deklarovaná **a** kontroly, na které platforma spoléhá, musí na panelu projít (`NodePrerequisites::check()`: volání adaptéru, práva API uživatele, cron a klientské API). Jinak je instance **zadržená**: `isUsable()` = false, **nové objednávky tam nejdou**, stávající služby se spravují dál, operátoři dostanou upozornění. Deklarovaná verze s neprošlými kontrolami se znovu ověří každých 15 minut a uvolní se sama; nedeklarovanou přijme jen operátor (`onhost:integrations:versions --accept=<instance> --reason=…`) a jen na procházejících kontrolách — kdo, kdy a proč zůstane v záznamu i auditu. První pohled na panel, který už obsluhuje zákazníky, bere jeho verzi jako výchozí stav (nic se nezastaví při nasazení) a doktor ho jmenuje, pokud ho adaptér nedeklaruje. ISPConfig hlásí verzi (`server_get_app_version`); API Pterodactylu žádnou verzi nemá — tam brána nepůsobí a runbook říká, co zkontrolovat ručně | ✔ | `tests/Feature/Provisioning/PanelVersionGateTest.php`, runbook `docs/runbooks/panel-upgrade.md`. Doktor: tři řádky v části providers. Na stagingu po nasazení: `php artisan onhost:integrations:versions` — výchozí stavy, které adaptér nedeklaruje, zkontrolovat a přijmout nebo aktualizovat |
| 60 | Automatické odstavení uzlu | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H288]], [[Hosting/H02]]):** přehled provozu každých 5 minut odstaví uzel, na kterém operace jen selhávají, a vrátí ho, když zase fungují — počítal ale operace **celé instance panelu** pro každý její uzel. Na Proxmox clusteru nebo herním panelu s více uzly se **spadlý uzel nikdy neodstavil** (úspěchy ostatních uzlů převážily jeho chyby) a **nové servery na něj dál padaly**; v tichém okně se naopak **odstavily všechny uzly instance najednou** a cluster nebral žádný nový VPS. A uzel odstavený v tichém okně se vrátil, když **panel** odpověděl na dvě sondy — ať byl uzel sám nahoře, nebo ne. Teď se počítají operace služeb umístěných na tom uzlu a zpět jde uzel, až odpoví **on sám**: Proxmox uzel podle stavu `online` v clusteru, herní uzel přes svého démona Wings; panel, který je sám jediným uzlem (aaPanel, ISPConfig), podle sondy panelu | ✔ | `tests/Feature/Provisioning/NodeDrainTest.php`. Na stagingu: při výpadku jednoho uzlu clusteru ověřit v přehledu provozu, že se odstavil jen on a ostatní dál berou objednávky |
| 61 | Upgrade panelu: identity a běžící úlohy | **Nalezeno a opraveno 2026-09-22 ([[Hosting/H517]], [[Hosting/H519]]):** (1) brána verzí (řádek 59) ověřovala po změně verze volání a práva, ale ne to, že **prostředky na panelu jsou pořád ty, které platforma zná** — upgrade, který přečísloval nebo přejmenoval, co panel drží, prošel jako ověřený a další akce na „1042“ by šla na cizí stroj. Teď důkaz zahrnuje vzorek až tří služeb instance (`ServiceIdentityCheck`: prostředek existuje a jméno nebo vlastník, které panel hlásí, sedí); jinak je verze zadržená. (2) Instance šla do údržby i ve chvíli, kdy pro platformu **právě prováděla úlohu** (klon, záloha, obnova) — ta se upgradem přerušila a dál se sledovala naslepo. Teď přechod do údržby (a vypnutí) odmítne `409 instance_tasks_running` se seznamem úloh; `acknowledge_running=true` je nechá doběhnout po údržbě a audit zapíše které | ✔ | `tests/Feature/Provisioning/PanelVersionGateTest.php`, runbook `docs/runbooks/panel-upgrade.md` |
| 62 | AI chat spravuje služby | **Doplněno 2026-09-22:** asistent smí navrhnout jen akce z bezpečného seznamu (`AssistantProposals`: vratné nebo opakovatelné, parametr je volba nebo odkaz, žádné ověření totožnosti) — seznam měl 12 akcí a **nic z toho, co platforma umí od té doby**. Přidáno podle stejného pravidla: test obnovy zálohy, vytvoření stagingu, návrat předchozího nasazení, ukončení záchranného režimu, start/stop/restart Node.js aplikace (nikdy smazání), HTTP/3, zapnutí/vypnutí herního plánu, export databáze, záloha schránky, wildcard certifikát; model k tomu čte seznamy aplikací a nasazení. Popis nástroje pro model se **generuje ze seznamu** (dřív ručně psaná věta vedle něj — při každé nové akci by se rozešly). Tlačítko, které by skončilo chybou, se nekreslí: test obnovy jen pro dokončenou zálohu **této** služby (`Backup::restorableOnto()` — stejné pravidlo jako při stisknutí). Odmítnutí řekne modelu **proč** (služba to nenabízí / vyžaduje ověření totožnosti / nese příkaz, obsah, heslo, cíl nebo mazání), aby zákazníkovi poradil, kde to udělá sám | ✔ | `tests/Feature/Support/AssistantManagesMoreTest.php` — každé nakreslené tlačítko se v testu opravdu stiskne přes zákaznické API |
| 63 | Weby, které služba nese | **Nalezeno a opraveno 2026-09-22:** webhostingová služba není jen web, pro který byla objednána — platforma pod ni staví **další weby téhož zákazníka** jako samostatné služby bez vlastní faktury (`tags.billing = included`, `tags.parent_service_id`): dnes testovací kopii (staging), zítra další weby, které tarif prodává. **Nic je nikdy neukončilo spolu se službou, ke které patří.** Zrušený webhosting nechal testovací kopii běžet dál **navždy** — se soubory a databázemi zákazníka na uzlu, bez jakéhokoliv archivu, bez faktury a bez toho, aby ji kdokoliv viděl. Dialog zrušení přitom zákazníkovi **slibuje** „Odstraní se i testovací kopie webu.“ (`DestructivePreview`). Stejně tak pozastavení: neplacená služba dál obsluhovala z testovací kopie. Nově `IncludedServices` (jediné místo, které rozhoduje, co ke službě patří — vždy s kontrolou organizace, aby cizí řádek s cizím `parent_service_id` nikdy nesáhl na cizí zdroj) a dva kroky v `ServiceActionWorkflow`: zrušení i odstranění pošlou každý nesený web **do jeho vlastního zrušení** (takže každý dostane svůj závěrečný archiv dřív, než se cokoliv smaže) a zahodí odkaz na staging; pozastavení a obnovení se propíší také, a platforma sundá **právě tu blokaci, kterou sama dala** (web, který si zákazník vypnul sám, nebo který drží zneužití, zůstane vypnutý). Web, který se nikdy nedoprovisionoval, se jen uzavře — u panelu není co mazat. Náhled zrušení teď další weby **jmenuje** | ✔ | `tests/Feature/Services/IncludedSitesTest.php` (5 testů: zrušení, pozastavení, obnovení, vlastní pozastavení zákazníka, cizí služba a placená služba se nesmí dotknout) |
| 64 | „10 webů“ tarifu | **Nalezeno a opraveno 2026-09-22:** každý webhostingový tarif na ceníku uvádí počet webů („1 web“, „10 webů“, „50 webů“) a stránka pro agentury slibuje „deset až padesát webů pod jedním účtem“. Zákazník však uměl přidat jen **další doménu na tentýž web** — alias se stejným kořenem, stejným PHP, stejným certifikátem a stejnými soubory — a číslo z tarifu se v panelu dokonce zobrazovalo jako **limit těchto aliasů** (`sites - 1`). Prodávali jsme deset webů a dodávali jeden. Nově je další web tarifu **skutečný web**: vlastní kořen, vlastní verze PHP, vlastní certifikát, databáze, FTP, zálohy i panel — jako nesená služba (řádek 63), kterou zakládá obyčejná sága webu (`site.create` → `SiteWorkflow` → `ProvisionWebsiteWorkflow`) a odebírá **jeho vlastní zrušení** (`site.delete`, vysoké riziko + ověření totožnosti, takže archiv vznikne dřív než se cokoliv smaže). **Prostor tarifu se mezi weby dělí, nikdy nenásobí:** ISPConfig sčítá `hd_quota` webů klienta proti jeho `limit_web_quota`, takže dva weby nemohou držet celý tarif — nový web dostane, co je volné, zbytek se vezme z hlavního webu ve stejném běhu (žádná druhá operace, žádný souběh), a nikdy se žádnému webu nenechá míň, než už má uložené (+15 % rezerva; zjišťuje se z uzlu). Limit `subdomains` znamená zase to, co má: další **jména** na jednom webu | ✔ | `tests/Feature/Services/PlanSitesTest.php` (4 testy: založení s vlastním kořenem a rozdělením prostoru, strop tarifu, odmítnutí, které by web poškodilo nebo vzalo cizí jméno, odebrání jen přes vlastní zrušení) |
| 65 | Změna tarifu webhostingu | **Nalezeno a opraveno 2026-09-22:** (1) **Změna tarifu nekontrolovala, co služba už má.** Webhosting se třemi weby se dal přesunout na tarif, který prodává jeden — a ty dva, které tarif přestal platit, běžely dál; stejně tak mohl prostor tarifu klesnout pod to, co weby už mají přidělené, což panel odmítne **až po zaplacení objednávky**. Nově `PlanFit` odmítne změnu **dřív, než se hnou peníze** (v nabídce košíku i při uplatnění zaplacené položky, protože služba mohla mezitím povyrůst) a **vyjmenuje všechny důvody najednou**: počet webů (a které odebrat), prostor, testovací kopii, verzi PHP, kterou nový tarif nenabízí. Nabídka tarifů u služby teď u každého tarifu říká, jestli na něj služba pasuje (`fits`, `blockers`). Záměrně se kontroluje jen to, co platforma **sama ví** — počty, které vlastní panel (databáze, schránky, FTP), se nekontrolují: ceník by závisel na vzdáleném volání a ty limity už hlídá `ServiceFeatures` před každým novým kusem (po snížení tarifu zákazníkovi zůstane, co má, a nepřidá další). (2) **Zaplacený upgrade se nedostal na panel.** Limity klienta v ISPConfigu se zapisovaly **jen při jeho založení** — zákazník, který si koupil „10 webů“ a 50 GB, měl na panelu dál `limit_web_domain = 1` a starou kvótu, takže panel druhý web i větší kvótu odmítl. Teď `resize()` limity klienta dorovná podle tarifu (ISPConfig při update nahrazuje celý záznam, takže se klient načte a sloučí; heslo a `sys_*` se nikdy neposílají zpět). Když panel klientské funkce odmítne (může chybět oprávnění), řekne se to v datech operace a změna webu pokračuje. (3) **Prostor se při změně tarifu rozdělí:** web služby dostane, co tarif nechá po ostatních webech (`site_nvme_gb`), ne celý tarif | ✔ | `tests/Feature/Services/PlanFitTest.php`, `tests/Contract/IspConfigContractTest.php` (limity klienta se posunou s tarifem; odmítnutí klientských funkcí změnu nezhatí) |
| 66 | Vyčerpaný tarif | **Nalezeno a opraveno 2026-09-22:** platforma využití **měřila** (85 % řekne zákazníkovi, 95 % může objednat vyšší tarif) a při **100 % neudělala nic**. Na ISPConfigu zastaví zápis sám uzel (kvóta webu je disková kvóta) — zákazník najednou nemůže nic uložit a od nás neslyší ani slovo; na aaPanelu **kvóta vůbec není**, takže jeden web může zaplnit disk celého uzlu a zastavit tím všechny ostatní zákazníky na něm. Nově existuje úroveň `full`: zákazník se o vyčerpaném tarifu dozví (jiný text než varování, `hot`), automatické navýšení tarifu se spustí i odtud, a `UsageGuard` **odmítne akce, kterými by služba nabyla** (uložení souboru, nahrání souboru, rozbalení archivu, instalace aplikace, nová databáze, import, staging, deploy, nová schránka, další web) s návodem, jak uvolnit místo. Cesty ven zůstávají otevřené: mazání souborů, databází, zálohy, obnovy i změna tarifu nikdy neodmítnuty. Rozhoduje **poslední měření** uložené na službě, takže se kvůli tomu nevolá panel, a jen **dokud je čerstvé** (26 h) — zastaralé číslo nesmí zákazníkovi blokovat web navždy | ✔ | `tests/Feature/Services/StorageFullTest.php` (odmítnutí, otevřené cesty ven, zastaralé měření, žebříček úrovní a jedno upozornění denně) |
| 67 | Zaplněný uzel | **Nalezeno a opraveno 2026-09-22:** pravidlo, které brání zaplnění uzlu, v plánovači **je** — uzel, kde by obsazený disk plus to, co nová služba potřebuje, přesáhly 85 % disku, už nic nedostane — jenže u webových uzlů (ISPConfig, aaPanel) **neměřilo nic**: `nodes.usage.disk_used_gb` se zapisuje při synchronizaci Proxmox clusteru a herního panelu a u webového uzlu **nikdy**. Číslo zůstávalo 0, pravidlo tedy nikdy nesepnulo a **skoro plný sdílený uzel dál bral nové weby**; stejnou nulu četla i přejímka uzlu (`NodeQualification`, rezerva 15 %) a pokaždé prošla. Oba panely to přitom umí říct a adaptéry měly `nodeLoad()` připravené — **nikdo ho nevolal**. Nově `NodeUsageSync` (příkaz `onhost:nodes:usage`, každých 15 minut) zapíše obsazenost i velikost disku z panelu (aaPanel ze seznamu disků — bere `/www`, jinak kořen, a rozumí lidským velikostem „1.8T“; ISPConfig z monitoru) a upozorní operátory, když uzlu zbývá míň, než si sám drží (jednou denně na uzel, událost `node.disk.low`) | ✔ | `tests/Feature/Provisioning/NodeDiskWatchTest.php` |
| 68 | Provoz na aaPanelu | **Nalezeno a opraveno 2026-09-22:** každý webhostingový tarif prodává objem přenesených dat. ISPConfig ho počítá (`trafficquota_get_by_user` → `this_month`), **aaPanel počítadlo provozu nemá vůbec**, takže se na managed hostingu prodané číslo měřilo **proti nule** — zákazník mohl přenést stonásobek tarifu a platforma hlásila 0 %. Uzel přitom každou odpověď zapisuje do access logu webu i s počtem odeslaných bajtů, takže odtud číslo pochází: sčítá se **jen přes tento měsíc** (soubory tohoto měsíce včetně rotovaných a komprimovaných, a jen řádky, jejichž vlastní razítko je z tohoto měsíce — konec minulého měsíce v souboru rotovaném prvního se nepočítá), pole s bajty se hledá **podle místa za stavovým kódem**, ne na pevné pozici, takže uzel s mírně jiným formátem logu počítá dál. Formát, který platforma nepozná, vrátí **„nezměřeno“ místo sebevědomé nuly** — „žádný provoz“ a „neumíme to přečíst“ nejsou pro zákazníka totéž. Ptáme se nejvýš dvakrát denně (vlastní cache); měsíční kvóta čerstvější číslo nepotřebuje | ✔ | `tests/Feature/Provisioning/WebTrafficTest.php` |
| 69 | Limity certifikační autority | **Nalezeno a opraveno 2026-09-22:** Let's Encrypt odmítne duplicitní certifikát po pěti stejných sadách za týden a účet po třech stech objednávkách za tři hodiny — řekne to HTTP 429 a hlavičkou `Retry-After`. Platforma to četla jako obyčejnou chybu: certifikát rovnou selhal a **noční obnova se hlásila znovu každou noc**, což je přesně to, čemu limit brání. Teď je to vlastní odpověď (`RATE_LIMIT` + `retryAfterSeconds`), takže běh **čeká tu dobu, kterou autorita sama určila** (rozumné meze: minuta až den; „0“ ani „za měsíc“ se neberou doslova), na certifikát se zapíše `rate_limited_until` a obnova ho do té doby přeskakuje. Obnovy se navíc **rozprostřou**: sto webů založených během jednoho odpoledne má certifikáty, které vyprší v řádu minut po sobě, takže by se obnovovaly jeden den navždy — každý certifikát dostane podle svého id stálý posun do šesti dnů. **Cestou nalezeny dvě chyby, které by zastavily první certifikát vůbec:** vytvoření ACME účtu předávalo `null` do parametru deklarovaného jako `string` (TypeError ještě před odesláním požadavku) a klíč účtu se generoval bez vlastní konfigurace OpenSSL, takže na hostiteli bez `openssl.cnf` nevznikl | ✔ | `tests/Feature/Provisioning/AcmeRateLimitTest.php` |
| 70 | Konec karantény | **Nalezeno a opraveno 2026-09-22:** proces pro zneužití (DSA: oznámení → triáž → sdělení zákazníkovi → zásah → stížnost) je postavený a pozastavení služby drží **vlastní blokace** (`SuspensionHold::ABUSE`), aby ji nikdo nesundal omylem — jenže **ji nikdy nikdo nesundal vůbec**: `liftHold(ABUSE)` se nevolalo z žádného místa v kódu. Zákazník, kterému platforma dala za pravdu ve stížnosti, zůstal pozastavený **navždy**; uzavření případu jen zapsalo stav. Nově případ, který pozastavil službu, **nelze uzavřít bez odpovědi**, co se službou (`restore`): buď se vrací do provozu — blokace se sundá a služba se obnoví, pokud ji nedrží ještě něco jiného (nezaplacená faktura, vlastní pauza zákazníka) — nebo pozastavení trvá a je to v záznamu, v auditu, v události `abuse.case.closed` i v tiketu zákazníka. Službu, kterou drží **jiný otevřený případ**, uzavření tohoto případu nikdy neuvolní | ✔ | `tests/Feature/Compliance/AbuseQuarantineTest.php` (5 testů: odmítnutí bez rozhodnutí, návrat, trvající pozastavení, druhý případ, případ bez dopadu na službu) |
| 71 | Logy webu na ISPConfigu | **Nalezeno a opraveno 2026-09-22:** zákazník sdíleného webhostingu si v ONhostu nemohl přečíst **vlastní access ani error log** — adaptér odpovídal „vzdálené API logy nemá, použijte logovací pipeline (Loki)“, což je odpověď pro operátory, ne pro člověka, jehož web vrací chyby. Managed zákazník (aaPanel) logy měl. Uzel přitom drží logy webu v adresáři `log/` vedle `web/` — přesně to, co zákazník vidí přes SFTP — a platforma do toho adresáře už sahá jako agentní uživatel webu. Logy jsou tedy nově zapnuté i na ISPConfigu; když uzel log ještě nenapsal, řekne se to srozumitelně | ✔ | `tests/Feature/Provisioning/WebLogsTest.php` |
| 72 | Úroveň WAF | **Nalezeno a opraveno 2026-09-22:** každý webhostingový tarif uvádí úroveň WAF („základní“, „standard“, „WAF + CDN“) a ta byla **jen popisek**: vykreslila se na ceníku, předala se do panelu jako řetězec a **nikdo ji nikdy neporovnal s tím, co umí server toho webu**. Panely přitom nedělají totéž — aaPanel umí per-web limity požadavků a spojení, ISPConfig ne (nginx chce `limit_req_zone` v http bloku uzlu, který zákaznickému webu nepatří) — takže tarif slibující omezení rychlosti na sdíleném ISPConfig uzlu **nedodal z té části nic a nikdo to neřekl**. Nově `WafLevels` říká na jednom místě, co která úroveň slibuje; služba hlásí `promised`, `delivered` a `missing`, zákazník to vidí v zabezpečení webu i s tím, co s tím dělat; doktor jmenuje tarif v prodeji, jehož panel slib neudrží, a **úroveň, kterou nikdo nedefinoval, se netváří jako „basic“** — je hlášená jako neznámá | ✔ | `tests/Feature/Catalog/WafPromiseTest.php` |
| 73 | Adresa v cizí doméně | **Nalezeno a opraveno 2026-09-22 (bezpečnost):** `mailbox.create`, `alias.create`, `forward.create` a `list.create` ověřovaly jen to, že hodnota **vypadá** jako e-mailová adresa, a poslaly ji na panel tak, jak přišla. Na sdíleném panelu žije pošta všech zákazníků v jedné instalaci, takže žádost o `ceo@cizi-domena.cz` byla žádost, kterou platforma **byla ochotná za zákazníka vyslovit** — a jestli by ji panel odmítl, nebylo nikdy na nás předpokládat. Je to stejná třída chyby, kterou audit nachází opakovaně: hodnota od zákazníka se dostane do volání providera, aniž by se poměřila s tím, co ten zákazník skutečně vlastní. Nově `MailAddresses::assertOwn()`: adresu lze vytvořit jen v doméně, kterou služba hostuje (poštovní domény služby; u webu web a jeho další jména), a odmítnutí přijde **dřív, než se panel na cokoliv zeptá**. Kam pošta míří (cíl aliasu, přesměrování, catch-all) omezené záměrně není — přeposílat vlastní poštu ven je smysl té funkce | ✔ | `tests/Feature/Services/MailAddressOwnershipTest.php` |
| 74 | Schránky k webhostingu | **Nalezeno a opraveno 2026-09-23:** každý webhostingový tarif uvádí počet schránek („5 schránek“, „50 schránek“, „500 schránek“), ceník ho tiskne a placený doplněk ho zvyšuje — webová služba přitom **nenabízela žádnou akci se schránkami**: seznam funkcí nesl `mail` (číslo jako popisek), ale akce visí na `mailboxes`, které větev pro web nikdy nenastavila. A i po zapnutí by schránka neměla kam vzniknout: ISPConfig drží schránky **uvnitř poštovní domény** a sága webu žádnou nezakládala (`createMailDomain()` volala jen sága samostatné poštovní služby). Nově web nabízí schránky i aliasy tam, kde panel poštu umí (aaPanel ne), poštovní doména vznikne **při první adrese** i s klíčem DKIM a s publikací MX, SPF, DMARC a DKIM do zóny, pokud je naše — a když zóna naše není nebo DNS zrovna zápis nevezme, **schránka stejně vznikne** a potřebné záznamy zůstanou v operaci vypsané. Doména se ruší se službou (`purge`), takže po zákazníkovi nezůstane. **Cestou nalezeno a opraveno:** klíč DKIM se generoval bez vlastní konfigurace OpenSSL — na hostiteli bez `openssl.cnf` **nešlo založit poštovní doménu vůbec**, tedy ani u samostatné poštovní služby (stejná past jako u klíče účtu ACME, řádek 69) Poštovní nástroje panelu (přesměrování, catch-all, automatické odpovědi, antispam, filtry, konference, fetchmail, zálohy a využití schránek) nabízí web stejně jako poštovní služba a **každá poštovní akce webu jede na jeho poštovní doméně**, ne na webu — jinak by na odděleném poštovním serveru mířila na cizí číslo a cizí uzel. Závěrečný archiv webu nově obsahuje i jeho poštu (schránky, aliasy, veřejný klíč DKIM), protože odstranění tu doménu bere s sebou — archiv webu ji dřív neobsahoval vůbec | ✔ | `tests/Feature/Provisioning/WebMailboxesTest.php` |
| 75 | Přístup ke schránce | **Doplněno 2026-09-23:** platforma uměla schránku založit a předat k ní heslo — a pak **neřekla vůbec nic o tom, kam ho zadat**: žádný server, žádný port, žádné šifrování. Je to první věc, na kterou se zákazník ptá podpory v den, kdy si nastavuje telefon. Nově `MailSettings` drží ta čísla na jednom místě (IMAP 993 SSL/TLS, POP3 995, SMTP 587 STARTTLS, přihlašovací jméno je **celá adresa**, server je tentýž, na který ukazuje MX domény) a zdroj `mail_access` je ukazuje v panelu vedle webmailu. Hlavní část je ale **automatická konfigurace**: Thunderbird si sáhne pro `autoconfig.<doména>/mail/config-v1.1.xml`, Outlook pošle dotaz na `autodiscover.<doména>/autodiscover/autodiscover.xml` — platforma na obojí odpovídá a klient se nastaví sám; zákazník zadá jen adresu a heslo. Odpověď se dává **jen pro doménu, které platforma poštu opravdu hostuje** (záznamy jsou veřejné a zeptat se může kdokoliv), a neznámé jméno dostane prosté 404. Potřebné DNS záznamy (`autoconfig` CNAME, `_autodiscover._tcp` SRV) se publikují spolu s MX, SPF, DMARC a DKIM | ✔ | `tests/Feature/Provisioning/MailAutoconfigTest.php` |
| 76 | Napadený web | **Doplněno 2026-09-23:** platforma uměla web za zneužití dát do karantény (řádek 70) a zase ho pustit ven, ale **nikdy si ničeho sama nevšimla**: napadený WordPress rozesílající spam nebo hostující malware našli až ti, komu to přišlo — blocklist, abuse oddělení cizího hostingu, návštěvníci zákazníka — a to už je adresa uzlu na seznamu a doplácejí na to všichni ostatní zákazníci na něm. Nově `SiteIntegrityCheck` (příkaz `onhost:sites:integrity`, denně) hledá dvě stopy, které jde přečíst tím, co platforma na uzlu **už má**: spustitelný kód ve složkách, kam se jen nahrávají soubory návštěvníků, a otisky hotových webshellů (`eval(base64_decode(`, `assert($_POST[`, starý trik s `preg_replace` /e, systémové volání s argumentem přímo z požadavku). Obojí je jeden `find` a jeden `grep`, omezený časem i počtem, mimo `vendor` a `node_modules`. **Hlásí a nikdy nezasáhne** — falešný nález existuje (plugin, který si legitimně drží PHP pod `uploads/`) a web vypnutý pravidlem, které nikdo neověřil, je horší než to, co pravidlo hledalo; zásah zůstává u případu zneužití, který má odůvodnění, karanténu i cestu zpět. Řádek, který nevypadá jako cesta v tomhle webu (chyba shellu, cizí zákazník), se za nález nebere | ✔ | `tests/Feature/Provisioning/SiteIntegrityTest.php` |
| 77 | Pošta pozastavené služby | **Nalezeno a opraveno 2026-09-23:** webová služba teď může mít schránky (řádek 74) a **pozastavení se jich nedotklo**: schránky neplatícího zákazníka jsou spamový relay s fakturou a web v karanténě za zneužití dál rozesílal přesně to, kvůli čemu v karanténě je. Hloubka pozastavení (cron, FTP, plány, Node.js aplikace) má nově i **poštu**: vypne se **odesílání**, příjem zůstává — nic adresovaného zákazníkovi se neztratí — a pamatuje si, které schránky vypnula, takže schránka, kterou si zákazník vypnul sám, zůstane po obnovení vypnutá. **Cestou nalezena vážnější chyba:** úprava schránky posílala na ISPConfig **jen změněná pole**, jenže panel bere obsah aktualizace jako celý záznam (proto `updateSite` záznam načítá a slučuje) — přejmenování schránky nebo vypnutí odesílání tak mohlo zbytek schránky (kvótu, jméno, příznaky) přepsat výchozími hodnotami. Teď se záznam načte a sloučí; **uložené heslo se nikdy neposílá zpět** (panel vrací jeho otisk a ten by se zahašoval podruhé, což by zákazníka do vlastní pošty nepustilo) | ✔ | `tests/Feature/Provisioning/SuspendedMailTest.php` |
| 78 | Čísla tarifu napříč weby | **Nalezeno a opraveno 2026-09-23:** tarif „10 webů a 2 databáze“ prodává dvě databáze **za tarif**, ne za každý web. Každý nesený web je vlastní služba s kopií čísel tarifu a limit se počítal na panelu jednoho webu proti jeho vlastní kopii — tarif s deseti weby tedy rozdal **desetinásobek** databází, schránek, FTP účtů, cronů a subdomén, a každá z nich stojí na sdíleném uzlu skutečné místo a výkon. `PlanAllowance` počítá tato čísla přes **všechny weby tarifu** proti číslu placené služby (web, na kterém se změna děje, se čte čerstvě, ostatní z posledního přečtení, které jim maže jejich vlastní zápis); panel, který neodpoví, odklad limitu do kroku beze změny (H02) zachovává. Vlastní webu zůstává jeho podíl místa, jeho jeden web a jeho shellový účet (ten v ISPConfigu patří webu, takže společné počítání by devět z deseti webů nechalo bez SSH). **Druhá díra téže rodiny:** změna tarifu končila u služby, na které byla koupena — nesené weby běžely dál s čísly a funkcemi tarifu, který zákazník opustil. `ServiceSites::spread()` je teď donese na každý web, který tarif platí (i u add-onů), přičemž každý si nechá svůj podíl místa a svůj jeden web | ✔ | `tests/Feature/Services/PlanGroupLimitsTest.php` |
| 79 | Pošta na druhé doméně hostingu | **Nalezeno a opraveno 2026-09-23:** webhosting odpovídá za víc jmen a platforma dovolí založit adresu v kterékoli doméně služby (`MailAddresses`, řádek 73) — **poštovní doménu ale zakládala vždy pro jméno webu**, ať adresa říkala cokoli. Uzel pak poštu pro druhou doménu nikdy nepřijal: schránka vypadala založeně a nic jí nechodilo. A všechno, co poštu služby prochází, bralo **první** poštovní doménu a tam skončilo — schránky druhé domény se nevypisovaly (takže se ani nepočítaly proti číslu tarifu; u ISPConfig fixtury se nevypisovalo dokonce nic, protože vazba webu nenese jméno domény), při pozastavení se nezastavily a před smazáním služby se nezazálohovaly. Nová `MailDomains` je jediné místo, které ví, kde pošta služby leží: doména se zakládá pro **doménu adresy**, druhá doména dostane vlastní poštovní doménu (s vlastním DKIM a DNS), výpisy se čtou přes všechny domény služby, pozastavení zastaví odesílání ve všech (a každou schránku zapíše přes doménu, které patří), úklid smaže všechny a závěrečný archiv má part pro každou z nich. Totéž platí pro **vlastní vypnutí odesílání** zákazníkem (`sending.set`, které používá i upomínkový režim „jen příjem“): vypínalo jen první doménu, takže služba dál relayovala z druhé. **Navíc:** AI chat dostal poštu webové služby (schránky, aliasy, přeposílání, spotřebu a **nastavení připojení**) a nový strážný test nutí u každého nového výpisu rozhodnout, jestli na něj chat smí odpovídat | ✔ | `tests/Feature/Provisioning/WebMailDomainsTest.php`, `tests/Feature/Support/AssistantListingsTest.php` |
| 80 | Vlastnictví systémových DNS záznamů | **Nalezeno a opraveno 2026-09-23:** do jedné zóny publikují systémové záznamy tři části platformy — sága webu (A/AAAA), sága pošty (MX, SPF, DMARC, DKIM, autoconfig) a webová služba při první schránce. `syncSystemRecords` maže **každý** systémový záznam, o který nebyl požádán (to potřebuje odpojení domény), a všichni tři se ptali, aniž by řekli, které záznamy jsou jejich. Druhý v pořadí tedy smazal záznamy prvního: **první schránka na webhostingu s doménou v našem DNS shodila web** a opětovné publikování záznamů webu zase zastavilo poštu. Každý publikující teď pojmenuje, co je jeho (`web:<služba>`, `mail:<doména>`), a smí sáhnout jen na vlastní záznamy, na záznamy **téhož druhu**, jaký publikuje (web, který se stěhuje, přebírá parkovací adresu; záznam z doby, kdy publikující ještě neměli jméno, se nahradí místo zdvojení) a na to, co vedle publikovaného nemůže stát (jméno je buď CNAME, nebo všechno ostatní). **Cestou nalezena druhá, horší chyba:** autoconfig SRV záznam (řádek 75) nesl prioritu uvnitř obsahu, což náš vlastní validátor odmítá — a krok chybu DNS jen zapíše a pokračuje, takže od jeho zavedení vznikla schránka a doména nedostala **žádné** poštovní záznamy. Záznamy pro poštu domény se teď staví na jednom místě (`MailSettings::records`, používají obě ságy — poštovní služba autoconfig záznamy nikdy nedostala) a strážný test žene vše, co platforma generuje, validátorem, který to bude muset přijmout | ✔ | `tests/Feature/Dns/SystemRecordOwnersTest.php` |
| 81 | Co na doménu odpovídá veřejné DNS | **Doplněno 2026-09-23:** platforma zapsala adresu webu i poštovní záznamy do vlastní zóny a **nikdy se nepodívala, co odpovídá svět**. Zákazník s DNS jinde je nepřidá, záznam ze zóny vypadne, doména se přestěhuje — a první, kdo to zjistí, je zákazník, jemuž nejde web nebo se vrací pošta. Platforma navíc neviděla ani to, že **vlastní** publikování neprošlo (řádek 80: neplatný autoconfig SRV nechal domény bez jediného poštovního záznamu). `PublicDnsCheck` (denně, `onhost:dns:check`) porovná u každé webové, spravované a poštovní služby adresu domény s adresou uzlu, MX s naším poštovním serverem, SPF, **klíč DKIM, kterým se pošta opravdu podepisuje**, a DMARC. Co je naše, **opraví** (chybějící záznam ve vlastní zóně doplní a mlčí — to není zákazníkovo), co může změnit jen zákazník, mu jednou denně řekne v panelu i v oznámení a rovnou ukáže, co kam zadat. `RecordResolver` je samostatný seam (nikdy `HostResolver` od `EgressGuard`, ten odpovídá na bezpečnostní otázku), s cache a **jen** z plánovaného běhu — panel čte, co kontrola zapsala, takže na resolver nikdo nečeká. Do konzole automatizace přibyly `sites.integrity`, `nodes.usage` a `dns.check` a strážný test: pravidlo, které jde vypnout, musí konzole umět zobrazit | ✔ | `tests/Feature/Dns/PublicDnsCheckTest.php`, `tests/Feature/Provisioning/AutomationSwitchTest.php` |
| 82 | Certifikát podle skutečného DNS | **Nalezeno a opraveno 2026-09-23:** certifikát vydává autorita, která si nejdřív ověří, že jméno je opravdu obsluhované u nás. Platforma o něj žádala bez ohledu na DNS: panel to zkusil, autorita odmítla, zákazník dostal chybu panelu, kterou nikdo nepřečte — a **každé neúspěšné ověření se počítá do limitu autority pro to jméno (pět za hodinu)**, takže doména, která na nás zatím nemíří, dokázala protopit celodenní příděl uzlu, na kterém teprve bude bydlet. `CertificateAutoIssuer` na DNS čekal, ale jen u webu označeného `pending_dns`; samotná akce `ssl.issue` — tlačítko zákazníka, spárovaná doména, opakované vystavení — se nedívala nikdy. Nová `DomainPointing` je jediné místo, které ví, jestli jméno už odpovídá adresou uzlu (dosud tři kopie téže úvahy: auto-issuer s vlastním `dns_get_record`, `PublicDnsCheck` a nic v akci). Krok teď žádá **jen o jména, která už míří na uzel**, zbytek zapíše do `certificate_waiting` (jedno nepřipravené jméno jinak shodí celý certifikát i pro jména připravená) a když nemíří žádné, **panel se vůbec neptá** a operace se opakovaně odkládá o půl hodiny s českým vysvětlením. Když platforma adresu uzlu nezná, nevymýšlí si důvod odmítnout a nechá rozhodnout panel jako dřív | ✔ | `tests/Feature/Provisioning/CertificateDnsTest.php` |
| 83 | Odstavení uzlu a co na něm zůstává | **Nalezeno a opraveno 2026-09-23:** evakuace uzlu (co obsluha spustí předtím, než sáhne na hardware) hledala služby k přestěhování **jen mezi rodinami, které mají vlastní ságu** (herní servery a virtuály). Webhostingy na sdíleném uzlu se tedy nepřestěhovaly, neodmítly — a ani se nevypsaly: obsluha četla „started: 2, skipped: 0“ a měla za to, že uzel je prázdný. Evakuace teď projde **všechno, co na uzlu žije**; co se přestěhovat nedá, skončí v `skipped` se jménem, rodinou a důvodem (`migration_unsupported`) a spočítá se do nového `staying`, které jde i do auditu. **Stěhování webu mezi uzly zatím neexistuje** — to je samostatná mechanika (záloha → web na cílovém uzlu → obnova → přepnutí DNS → úklid zdroje), která je dalším krokem; do té doby je aspoň pravda vidět | ✔ | `tests/Feature/Provisioning/NodeEvacuationTest.php` |
| 84 | Stěhování webu mezi uzly | **Doplněno 2026-09-23:** webhosting se nedal přestěhovat vůbec — ságu měly jen herní servery a virtuály (řádek 83), takže **sdílený webový uzel nešlo vyprázdnit** a jedinou cestou byla záloha, nová objednávka a zákazník, který si web přenese sám. Nová `WebMigrationWorkflow` (`web.migrate`) jde v pořadí bezpečného přesunu: cílový uzel → **co se dá přenést** → čerstvá záloha (soubory a každá databáze, jinak krok selže) → web na cílovém uzlu → přenos dat → přepnutí → certifikát → úklid zdroje. **Klíč k tomu, aby web po přestěhování opravdu fungoval:** databázi otevírá `wp-config.php` jménem, uživatelem a heslem, které platforma nemůže přepsat — proto se databáze přenáší **jen** když platforma její heslo drží (`DatabaseCredentials`), a když ne, migrace se odmítne **jménem té databáze** a nic se nezaloží (půlka přestěhovaného webu je horší než nepřestěhovaný). Na cílovém uzlu vznikne databáze se **stejným jménem, uživatelem i heslem** a přihlašovací údaje se přepíšou na její nové číslo. Zákazník běží na zdroji, dokud není kopie celá; selhání před přepnutím vezme rozestavěný cíl zpět (`CompensationGuard`) a zdroj se nemaže nikdy před přepnutím. **Cestou nalezeno:** `start()` zapisoval `tags.migration` **až po** spuštění operace, takže rychlý worker (a synchronní fronta v testech) mohl přepsat výsledek ságy zpět na `running` | ✔ | `tests/Feature/Provisioning/WebMigrationTest.php` |
| 85 | Vyprázdnění uzlu z konzole | **Doplněno 2026-09-24:** vyprázdnit uzel šlo jen přes cestu, která v URL říká `game` (`integrations/{instance}/game/nodes/{node}/evacuate`) — **webový uzel, tedy přesně ten, který hostingová firma vyřazuje, se z konzole vyprázdnit nedal vůbec**, ani poté, co se weby naučily stěhovat (řádek 84). Nová cesta patří uzlu: `POST /v1/staff/nodes/{node}/evacuate` (uzel si panel najde sám, herní cesta zůstává). V konzoli u každého uzlu přibylo **„Vyprázdnit uzel“**, které po potvrzení a důvodu ukáže, kolik služeb se stěhuje a **co na uzlu zůstává i s důvodem** — obsluha tedy vidí, co musí dořešit ručně, než hardware vypne | ✔ | `tests/Feature/Provisioning/NodeEvacuationRouteTest.php` |
| 86 | Blocklisty odesílacích adres | **Doplněno 2026-09-24:** na sdíleném uzlu odesílají všichni zákazníci ze stejné adresy. Stačí jeden spammer — napadený WordPress, schránka s uniklým heslem — a adresa se octne na blocklistu: **od té chvíle se vrací pošta všem ostatním zákazníkům na uzlu** a platforma se to dozvídala od zákazníků, po dnech a po jednom tiketu. Blocklisty odpovídají přes DNS a platforma má od řádku 81 vlastní resolver, takže se jich ptá sama: denně `onhost:mail:blocklist` prověří adresu každého uzlu proti seznamům z `onhost.mail.blocklists` (Spamhaus ZEN, SpamCop, Barracuda; prázdný seznam = kontrola nedělá nic) a výsledek zapíše do `Node.tags.blocklist`. Provozu to řekne jednou denně na uzel i s kódem, **zákazníkovi nikdy** — adresa je naše a vyřazení ze seznamu taky. Kontrola se jen dívá; odlistování je práce člověka přes formulář daného seznamu | ✔ | `tests/Feature/Dns/BlocklistCheckTest.php` |
| 87 | Služby podle stavu v panelu | **Doplněno 2026-09-24:** panel řadil služby jen podle toho, **co to je** (webhosting, domény, servery), nikdy podle toho, **co se s nimi děje** — zákazník s třiceti službami a jednou pozastavenou musel otevřít každou kategorii; filtr v API (`GET /v1/services?state=`) uživatel panelu nikdy neuvidí. Kategorie plochy mají vlastní seam (`OnhostPanelNav.cats` čte `nav.categories`), takže stavové pohledy přicházejí stejnou cestou jako každá jiná kategorie — **bez jediné změny prototypu**: „Vyžadují pozornost“ (pozastavené, degradované, selhané), „Zřizují se“ a „Rušené“ (podle data zrušení). Pohled se nabídne, jen když něco obsahuje; obsahuje **tytéž řádky** jako kategorie, ve které služba žije, a servírovaný panel je vynechává z počítání i z vyhledávání, aby se služba nezapočítala dvakrát | ✔ | `tests/Feature/Http/PanelStateViewsTest.php` |
| 88 | Co se ztratí mezi kopií a přepnutím | **Nalezeno a opraveno 2026-09-24:** sága stěhování webu (řádek 84) kopírovala **běžící** web: objednávka, komentář nebo nahraná fotka zapsané mezi zálohou a přepnutím zůstaly na starém uzlu, zatímco zákazník už byl obsluhován z nového — tedy **tiše ztracené a nikdo by se to nedozvěděl**. Herní migrace server před zálohou zastavuje ze stejného důvodu. Nový krok `freezeStep` web před kopií zastaví (`suspend` na zdroji) a kompenzace ho při jakémkoli selhání **před přepnutím** zase spustí, takže zákazník zůstane tam, kde byl, a s běžícím webem; při úspěchu se zdroj nezapíná, protože se maže. Web už pozastavený (neplatič) se nezastavuje ani nezapíná. **Panel zároveň přestal lhát:** u webu říkal „stěhování serveru“ a „server bude po dobu přenosu vypnutý“ — teď jmenuje web a vysvětluje, že je vypnutý **po dobu kopírování, aby se nic z objednávek ani z databáze neztratilo** | ✔ | `tests/Feature/Provisioning/WebMigrationTest.php` |
| 89 | Komu patří jméno webu | **Nalezeno a opraveno 2026-09-24:** na sdíleném uzlu vybírá webový server odpovídající web **výhradně podle jména v požadavku**, takže kdo drží jméno, dostane provoz, certifikát i všechno další. Platforma to hlídala na jediném místě — při přidání dalšího webu ke stávající službě — a **nikde jinde**. `ssl.issue` bral jakákoli jména z požadavku: kontrola z řádku 82 (`DomainPointing`) sice ověří, že jméno opravdu odpovídá adresou uzlu, jenže **doména souseda odpovídá adresou téhož uzlu**, takže strážce postavený právě pro tohle je nedokáže rozlišit — platforma tedy žádala autoritu o certifikát na cizí doménu, každé neúspěšné ověření spotřebuje jedno z **pěti za hodinu pro to jméno** (soused, nebo konkurent hostovaný u nás, se tak nemusel k certifikátu dostat vůbec) a jméno, které projde, skončí na cizím certifikátu; `ssl.wildcard` totéž o pole dál. `subdomain.add` bral také jakékoli jméno, takže si zákazník mohl napsat sousedovu doménu do `server_name` vlastního webu — učebnicový únos na sdíleném hostingu — a ještě tím utratil vlastní limit tarifu. A **objednávka**, tedy způsob, jakým každý hosting doopravdy vzniká, kopírovala `config.domain` (nebo `fqdn`) a `config.aliases` z košíku do `desired_spec` nepřečtené (`items.*.config` je validované jako `array` a víc ne): dvě služby mohly držet jedno jméno a seznam dalších jmen nebyl kontrolovaný ani na to, jestli to jsou domény. Nová `SiteNames` je jediné místo, které na to odpovídá (používá ji i `ServiceSites`): jméno, které drží jiná **živá** služba v témže jmenném prostoru, je její; jméno **pod** doménou jiné organizace je její (jejich wildcard by ho předal tomu, kdo ho tady obsluhuje); jméno pod vlastním jménem je vždy vaše; cokoli jiného je volné a rozhodují o něm vrstvy, které za to už odpovídají. **Web a pošta jsou dva jmenné prostory** — `muj-web.cz` je běžně web i doména schránek — a uvnitř každého patří jméno jedné službě, čímž skončily i dva poštovní hostingy na jedné doméně. Košík odmítne dřív, než kdokoli zaplatí (`site_name_taken` 409, tam, kde se odmítá vyprodaný server), u **vlastní** služby ji pojmenuje, u cizí nikdy; `desiredSpec` se zeptá ještě jednou uvnitř transakce, která službu zakládá, takže závod dvou košíků o jedno jméno skončí tam, a ne u dvou vhostů | ✔ | `tests/Feature/Services/SiteNameClaimsTest.php` |
| 90 | Nárok na jméno, který nikdo nedoložil | **Doplněno 2026-09-24, protiváha řádku 89:** jakmile jméno patří jedné službě, stačí objednat nejlevnější hosting na `firma.cz`, nikam ho nenasměrovat — a **skutečný vlastník už u nás hostovat nemůže**: košík ho odmítne a tím to skončí. Pro nárok totiž nemuselo platit vůbec nic. Po poctivém zákazníkovi se nic nového nechce: platforma už zná **tři důkazy** o jménu — doména je u nás vedená, její DNS zónu provozujeme my, nebo jméno už odpovídá adresou uzlu, který ho obsluhuje (to poslední denní kontrola DNS stejně počítá). `SiteClaim` se proto veze s ní (resolver se neptá dvakrát) a zapisuje výsledek do `tags.name_claim`. Nárok, ke kterému se déle než odkladná lhůta (`ONHOST_SITE_CLAIM_GRACE_DAYS`, výchozí 30 dní) nenašel žádný důkaz, je **věc provozu**, ne zákazníka: ten cizí squat neodstraní a platforma živé službě jméno sama nesebere — jen o tom jednou denně řekne (`service.name_unproved`, interní). Odmítnutí zároveň přestalo být slepou uličkou: **říká cestu ven** („napište podpoře"), a když ten, koho odmítáme, jméno doložit umí (má u nás doménu nebo zónu), zatímco držitel ne, vznikne v auditu `service.name_disputed` s důkazy — podpora tedy nerozhoduje podle tvrzení proti tvrzení | ✔ | `tests/Feature/Services/SiteClaimProofTest.php` |
| 91 | Certifikát, který tiše vypršel | **Nalezeno a opraveno 2026-09-24:** certifikát webu platí devadesát dní a panel ho obnovuje — dokud ho neobnoví. Doména se přestěhuje, zákazníkem přidané přesměrování spolkne `/.well-known/acme-challenge`, panelu se rozbije vlastní klient, autorita jméno omezí: obnova selže, **nikde to nikdo neřekne** a jednoho rána vidí každý návštěvník varování prohlížeče. Platforma HTTPS prodává a dozvěděla by se to poslední — **a navíc tvrdila opak**: `ServiceHealthCheck` odpovídal z `tags.access.certificate`, příznaku zapsaného **jednou, při prvním vystavení**, takže panel, asistent i podpora svorně hlásili „certifikát je vystavený“ i den po jeho vypršení. Vlastní **wildcard** certifikáty logiku platnosti měly (`ManagedCertificate`, `onhost:certificates:renew`); běžný certifikát webu, který má skoro každý hosting, žádnou a nikdo je neprocházel. Nová `CertificateWatch` (denně 04:40, vypínatelná) proto **nikomu nevěří a podívá se**: jeden TLS handshake na **vlastní adresu uzlu** s jménem webu v SNI přečte certifikát, kterým se uzel opravdu hlásí (aaPanel `notAfter` hlásí, ISPConfig nic, a u Let's Encrypt drží certifikát na disku, ne v řádku API). Handshake je z řídicí roviny bezpečný: adresa je **naše**, ne kam míří zákaznické DNS, protistrana se záměrně **neověřuje** (vypršelý certifikát je právě to hledané) a po handshaku se neposílá nic. Mezi devadesáti a deseti dny panel obnovuje a nechá se na pokoji (žádost navíc by jen protopila příděl autority na duplicitní certifikáty); pod deset dní panel prokazatelně neobnovil, takže si platforma jednou denně řekne sama běžnou akcí `ssl.issue` (která už sama hlídá řádek 82 i 89). Vypršelý certifikát — a také certifikát, který **jméno vůbec nepokrývá** (vhost propadl na výchozí web uzlu, což žádný panel nevidí) — jde jednou denně **provozu**, zákazníkovi nikdy: obnova je naše, a když je příčinou jeho DNS, řekne mu to denní kontrola DNS | ✔ | `tests/Feature/Services/CertificateExpiryTest.php` |
| 92 | Číslo od zákazníka není důkaz vlastnictví | **Nalezeno a opraveno 2026-09-24 (TASK-0005):** vzdálené API ISPConfigu se obsluhuje **jednou správcovskou relací na instanci** a nikdy se neptá, čí záznam dostalo pod `primary_id` — adaptér si to sám píše v komentářích metod, které se hlídají („a mail domain's id among web sites is a stranger's site“). Platforma přitom u `remote_id` od zákazníka kontroluje **jen tvar** (`/^[A-Za-z0-9:_.-]{1,120}$/`). Devět z jedenácti metod adaptéru si proto id nejdřív najde ve **vlastním** výpisu webu a co nenajde, odmítne. **Tři to nedělaly** a na sdíleném uzlu, kde jdou id po sobě, to byl průnik mezi zákazníky: `setShellKey` vložil váš SSH klíč sousedovi (čtení jeho souborů i databáze z `wp-config.php`), nebo prázdným klíčem sousedovi ten jeho smazal; `setDbUserPassword` přepsal heslo cizího databázového uživatele; `deleteDbUser` sice výpis načítal, ale jen kvůli konfliktu „ještě vlastní databáze“, takže **cizí** id (`$user === null`) podmínkou propadlo a uživatel se smazal. Stejná třída chyby byla v poště: `mailbox.update`, `mailbox.delete` a `alias.delete` stavěly `ResourceRef` rovnou ze zákazníkova parametru, takže šlo převzít nebo smazat **libovolnou schránku na poštovním serveru**. Pravidlo má teď jedno místo (`IspConfigWebProvider::ownRow`) a pošta se řeší tam, kde už je precedens u certifikátu — před `match` v ságe, přes existující `MailDomains::across`. **aaPanel byl v pořádku**: každá jeho metoda si id proti vlastnímu výpisu ověřuje, a co neumí, rovnou odmítne | ✔ | `tests/Contract/IspConfigOwnershipTest.php`, `tests/Feature/Provisioning/MailboxOwnershipTest.php` |
| 93 | Vzaté zpět zrušení nekřísilo nesené weby | **Nalezeno a opraveno 2026-09-24 (TASK-0006):** zrušení služby jde po celou odkladnou dobu vzít zpět — udělá to prosté `resume` a `cancellationCleared` vynuluje `terminate_at`. Jenže ho vynuluje **jen u služby, na které bylo zavoláno**. Nesené weby (testovací kopie a weby, které tarif prodává jako „10 webů“) ukončil `endIncludedServicesStep` vlastním `terminate`, takže každý dostal **vlastní** `terminate_at` a žádné `included.held_by` — a podmínka kroku pro obnovení chtěla `held_by` **a zároveň** `terminate_at === null`, tedy obě poloviny nepravdivé. Zákazníkův hosting tak běžel dál, zaplacený a živý, zatímco `onhost:services:purge` bral jeden nesený web po druhém v den, kdy jim doběhlo třicet dní: **soubory, databáze i schránky smazané na panelu**. U tarifu s deseti weby to je devět zničených webů poté, co si to zákazník rozmyslel — a nešlo je ani zachránit ručně, protože jméno pořád držel web, který se chystal zmizet. Krok ukončení teď každý web, který sám ukončil, **označí** (`included.ended_by`) — stejně jako větev pozastavení značí, co držela — a obnovení je vezme zpět. **Smazání se ruší zvlášť a jako první** (nová `ServiceService::undoScheduledDeletion`): ať se s obnovením stane cokoli, úklid už web, jehož zrušení bylo vzato zpět, nevezme. Blokace se zvedá jen ta vlastní (`review`) a jen když je jediná — web držený kvůli zneužití zůstane pozastavený, ale **bez naplánovaného smazání**. Web, který si zákazník zrušil sám dřív, označený není, takže zůstává na své cestě ven | ✔ | `tests/Feature/Services/IncludedSitesTest.php` |
| 94 | Správa sdílené služby není shell | **Nalezeno a opraveno 2026-09-24 (TASK-0007):** katalog rolí si hranici kreslí sám — `svc_manage` („Service: manage“) slibuje *akce a nastavení: restart, PHP, databáze, cron, soubory, deploye, schránky*, `svc_console` („Service: console“) slibuje *terminál, VNC a herní konzoli* a nese správu s sebou, protože *konzole je víc než správa, nikdy míň* (H334). Dvě role existují právě proto, aby zákazník mohl předat agentuře běžnou správu **bez shellu na svém webu**. `ServiceActionCommand::permissionFor()` ale posílal na `service.manage` všechno kromě hrstky akcí — **včetně webového terminálu (`command.run`), SSH účtů webu (`shell.create`, `shell.key`, `shell.delete`) a herní konzole (`command.send`)**. Jediná role, která má existovat proto, aby shell nedala, tedy shell dávala: agentura si mohla otevřít terminál, přečíst `wp-config.php` a s ním databázi, nebo si na web nasadit vlastní klíč. Těch pět akcí teď žádá `service.console`. **Katalog oprávnění se nemění** a žádná role nepřišla o nic, co měla (vlastník, org admin, developer, cloud i game operator ho už drží); dotčená je jedině sdílená `svc_manage`. Protože `permissionFor` je tatáž mapa pro sběrnici, řádek operace i `ServiceFeatures::features($service, $actor)`, panel terminál kolegovi bez konzole rovnou **přestane nabízet**. **Poznámka k metodě:** tenhle nález přišel ze šestihlediskového auditu označený jako kritická díra, a v té podobě **neplatil** — `web-tools.md` staré mapování popisoval jako záměr. Skutečná vada byla užší: popis role sliboval něco, co kód nedělal | ✔ | `tests/Feature/Services/ServiceAccessShareTest.php` |
| 95 | Limity klientského účtu patří organizaci | **Nalezeno a opraveno 2026-09-24 (TASK-0008):** ISPConfig drží **jeden klientský účet na organizaci** a na něm vymáhá limity (kolik webů, kolik místa, kolik databází a schránek). Platforma je zapsala **jednou**, z tarifu té služby, která se zřizovala první, a `ensureClient()` existujícího klienta vracel, aniž by na limity sáhl. Dva běžné webhostingy (`sites = 1` každý) tedy nechaly klienta na `limit_web_domain = 1` a panel druhý web odmítl — **zákazník zaplatil a objednávka selhala na panelu**; totéž s prostorem, databázemi i schránkami. Druhá půlka byla zrcadlová: **změna tarifu** posílala rovněž jen entitlementy jedné služby, takže upgrade či downgrade malé služby přepsal limity klienta a **sebral místo a weby všem ostatním webům téhož zákazníka**. Nová `ClientAllowance` sečte, co organizace na tom panelu drží; dvě pravidla dělají součet správným: **nesené weby se nepřičítají** (tarif „10 webů“ už říká `sites = 10` a `nvme_gb = 50`, jeho děti *jsou* těch deset a dělí se o totéž místo) a **počítá se vše kromě TERMINATED** (rušená služba drží web na panelu až do úklidu). Součet cestuje specifikací jako `client_entitlements` vedle vlastních `entitlements`, u změny tarifu se **cílová** čísla dané služby dosadí místo uložených, a limity se srovnávají přes existující `updateClientLimits()`, která panel volá jen při skutečné odchylce. Specifikace bez toho klíče se chová přesně jako dřív. **Zbývá:** klienti už založení na živém panelu si staré limity nesou, dokud se zákazníkovi něco nezřizuje — jednorázový průchod neexistuje | ✔ | `tests/Contract/ClientAllowanceTest.php`, `tests/Contract/IspConfigContractTest.php` |
| 96 | Obnovení, které nedokázalo zapnout všechno | **Nalezeno a opraveno 2026-09-24 (TASK-0009):** pozastavení služby vypne i to, co by běželo dál — cron, FTP, plány herního serveru, aplikace, odesílání pošty — a **zapamatuje si, co vypnulo**; obnovení pak zapne přesně to. Jenže `resume()` si nechával nezapnuté jen tehdy, když panel **neodpověděl** (`TRANSIENT`, `UNKNOWN`); u jakéhokoli jiného odmítnutí — a `status:false` z aaPanelu je `VALIDATION` — zapsal **prázdnou paměť**. Cron tedy zůstal na panelu vypnutý, platforma zapomněla, že ho kdy vypnula, a **žádné další obnovení už ho zapnout nemohlo**: zákazník zaplatil dlužnou fakturu, web zase jel a jeho naplánované úlohy nikdy nespustily. `resume_errors` skončily v kontextu operace, který nikdo nečte, a služba se ohlásila jako ACTIVE. Nově si paměť nechává **všechno, co zůstalo vypnuté, ať byl důvod jakýkoli** (co opravdu zmizelo, zapomíná `attempt(gone: true)`), mezi dočasné chyby přibyly `CIRCUIT_OPEN` a `RATE_LIMIT` (rozepnutý jistič ani limit nejsou „nikdy“, ale „teď ne“), krok obnovení vydá provozu `service.resume.incomplete` a zdravotní kontrola služby přestala tvrdit, že je celá — nález `suspension_left` pojmenuje, co je pořád vypnuté, slovy zákazníka („naplánované úlohy“, ne `cron`). **Služba se přesto vrátí do ACTIVE** — web jede a zákazník zaplatil; co zbývá, je práce provozu, ne důvod držet zákazníka vypnutého | ✔ | `tests/Feature/Services/SuspensionDepthTest.php` |
| 97 | Pozastavení, které panel zčásti odmítl | **Nalezeno a opraveno 2026-09-24 (TASK-0010):** protějšek řádku 96 na cestě dovnitř. Pozastavení vypíná i to, co kolem služby běží dál — cron, FTP, plány herního serveru, aplikace, odesílání pošty. Když panel vypnutí **odmítl** (`status:false` z aaPanelu je `VALIDATION`, ne timeout), krok `pauseExtrasStep` přesto vrátil `StepResult::done`, sága zapsala SUSPENDED a platforma tvrdila, že je hotovo: web byl zastavený, zákazník odstřižený a **cron, kvůli kterému karanténa vznikla, běžel dál** — přesně ten případ, kvůli kterému tato třída existuje (karanténovaný web rozesílal poštu ze svého cronu). `pause_errors` skončily v kontextu operace, který nikdo nečte. Nově `pause()` vrací i `left` (co se vypnout nepodařilo), krok to zapíše do **vlastní** značky `tags.suspension_still_running` — `tags.suspension` patří blokacím a zápis stavu ho přepíše celý — a vydá provozu `service.suspend.incomplete` i s důvodem pozastavení. **Pozastavení platí dál:** zastavený web je lepší než nezastavený a selhání kroku by službu vrátilo do ACTIVE, tedy běžící *a* s cronem. Odmítnuté položky se záměrně **nepamatují jako vypnuté** (platforma je nevypnula, takže je žádné obnovení nesmí „zapnout zpět"), a záznam mizí ve chvíli, kdy služba zase běží — i když krok obnovení svou práci přeskočí, protože pozastavení nevyplo nic. Zdravotní kontrola o tom mlčí záměrně: její text čte přes asistenta i zákazník a pozastavenému zákazníkovi se neříká, co mu ještě běží | ✔ | `tests/Feature/Services/SuspensionDepthTest.php` |
| 98 | Smazaný web na ISPConfigu po sobě nechal data i přístupy | **Nalezeno a opraveno 2026-09-24 (TASK-0011):** zrušení webhostingu na ISPConfigu volalo `sites_web_domain_delete` — a to maže **jeden řádek**, vhost. Databáze (`web_database`), jejich přihlášení (`web_database_user`), FTP účty (`ftp_user`), SSH účty (`shell_user`), naplánované úlohy (`cron`) i každé další jméno webu (alias a subdoména jako vlastní `web_domain`) visí na webu přes `parent_domain_id` a v tom jednom volání nejsou jmenovány. Zda panel uklidí za API sám, platforma **nesmí předpokládat** — a co zůstalo, byla data zrušeného zákazníka na živém uzlu dávno po každé slíbené retenci, jeho FTP, SSH a databázové heslo **dál funkční na sdíleném stroji**, nikdy neuvolněný disk a alias vhost, který dál odpovídal na jméno, o němž si platforma myslela, že ho nikdo nedrží (`SiteNames` zná jen vlastní evidenci). aaPanelový adaptér to dělal odjakživa (`terminate()` nejdřív zruší cron a Node aplikace, `DeleteSite` bere soubory, databáze i FTP) — ISPConfigový ne. Nově `dropSiteChildren()` odstraní děti webu **před** ním, v pořadí, které panel přijme (další jména → databáze → jejich přihlášení → FTP → SSH → cron); každý výpis je omezený `parent_domain_id` na tento web, takže na souseda nelze dosáhnout, a přihlášení se sbírají **před** smazáním databází (`listDbUsers` je hledá skrz ně) a mažou **až po nich**, a jen když už žádnou databázi nevlastní. Odmítnutí zrušení nezastaví — zákazník má právo se služby zbavit a finální archiv je pořízen dřív — ale vrátí se jako `leftover` a krok o tom vydá provozu `service.purge.leftover` i s id, která jsou po smazání webu jediné vodítko. Stejně se chová úklid starého uzlu po stěhování webu | ✔ | `tests/Contract/IspConfigContractTest.php` |
| 99 | Hlavní vazba služby byla hod mincí | **Nalezeno a opraveno 2026-09-24 (TASK-0012):** webhosting nedrží jen svůj web — poštovní doména, kterou dostal pro schránky, je vlastní zdroj a vlastní řádek `provider_bindings`, zapsaný **toutéž operací ve stejné sekundě**. `Service::primaryBinding()` řadil jen podle `created_at`, takže při shodě času rozhodovala databáze: SQLite vrátil řádek zapsaný dřív, PostgreSQL kterýkoli — a v noční CI dvakrát vrátil ten poštovní. Na této odpovědi visí **všechno, co se ptá „jaký zdroj ta služba je“**: důkaz před smazáním (`ServiceIdentityCheck` pak odmítl archivovat službu, kterou nepoznal — `binding_type`, 9/5 bodů), odkaz, s nímž se posílá akce do panelu, rekonsiliátor i nabídka funkcí panelu. To je přesně tvar incidentu, kvůli kterému `ServiceIdentityCheck` vznikl (číslo poštovní domény se kryje s číslem cizího webu). Nově rozhoduje **rod služby**: `ServiceIdentityCheck::TYPES` je veřejná (obě otázky mají jednu odpověď) a `primaryBinding()` vezme první vazbu vlastního druhu, `created_at` a id rozhodují až uvnitř druhu. Nalezeno při tom: `Service::bindings()` nemělo generický typ, takže se na něj PHPStan nikdy nepodíval | ✔ | `tests/Feature/Services/PrimaryBindingTest.php` |
| 100 | Zrušený web dál ukazoval na uzel | **Nalezeno a opraveno 2026-09-24 (TASK-0013):** hosting se do DNS zapisuje třemi vydavateli, každý se svým jménem: A/AAAA webu v zákazníkově vlastní zóně (`web:<služba>`), co přidalo spárování další domény (`service:<služba>`) a poštovní záznamy každé poštovní domény služby (`mail:<doména>` — MX, SPF, DMARC, DKIM, autoconfig). Zrušení služby **neodstranilo ani jeden**. Zákazníkovi, kterému hosting skončil, tak doména dál mířila na uzel, kde jeho web už není — návštěvník tam dostane to, čím uzel odpovídá na jméno, které nezná, a na sdíleném uzlu to bývá web jiného zákazníka — a jeho pošta se dál doručovala na uzel, který ji nepřijímá, se SPF a DKIM, které ho pořád autorizují. Nikdo si toho nevšiml: noční kontrola odchylek porovnává poskytovatele s tím, co drží platforma, a oba ty záznamy pořád měly. Nově `ServiceDnsCleanup` odstraní při odstranění služby právě to, co daná služba vydala (a jméno hostingu v platformní zóně), záznam, který si udělal zákazník sám (`managed_by = customer`), nechává být a cizího vydavatele se nedotkne; doména zůstane nemířit nikam, ne na parkovací stránku — platforma přestane tvrdit, kde doména bydlí, jakmile z ní nic nehostuje (parkování je vlastní akce). Nalezeno při tom: krok úklidu DNS selhání **spolkl** s komentářem „rekonsiliátor to zopakuje“ — nezopakuje: `commit()` publikuje nejdřív k poskytovateli a při chybě nechá databázi nedotčenou, takže platforma i poskytovatel dál drží totéž a odchylka žádná není. Zrušení kvůli DNS nepadne, ale co zůstalo publikované, se jmenovitě ohlásí provozu (`service.purge.leftover`, druh `dns`) | ✔ | `tests/Feature/Dns/ServiceDnsCleanupTest.php` |
| 101 | Historický web mohl platforma převzít a smazat | **Nalezeno a opraveno 2026-09-24 (TASK-0014), podle pravidla vlastníka:** „Do již existujících webů na ISPconfig a AAPanel nesmíme nijak zasahovat.“ Provisioning byl idempotentní tím nejvstřícnějším způsobem: našel na uzlu jméno a když tam web toho jména už byl, odpověděl „už existuje“ a použil ho — na ISPConfigu web i poštovní doménu, na aaPanelu web. Sága pak službu přivázala jako `managed_by: onhost` (tak se zapisuje **každá** vazba a nikdo tu hodnotu nikdy nečetl) a od té chvíle mohla platforma **historický web** pozastavit, změnit mu kvótu a počet PHP procesů, vypnout mu cron a na konci služby ho **smazat i s databázemi** — pojistky smazání nepomohly, doména seděla a systémový uživatel webu taky. Stačilo, aby si zákazník objednal hosting na jméno, které na uzlu už bydlí. Nově je zdroj, který na panelu už byl, náš **jen když to říká sám panel**: na aaPanelu poznámka `onhost:<služba>`, kterou platforma píše do každého webu, co vytvoří (od první verze adaptéru), na ISPConfigu klient, kterého platforma pro organizaci založila (`onh_…`), jako vlastník webu (systémová skupina `client{N}` nebo kořen `/var/www/clients/client{N}/`) či poštovní domény (skupina klienta, `client_get_groupid`). Cokoli jiného je historické a je odmítnuto `CONFLICT` **dřív, než odejde jediný zápis** — nevznikne klient, nesahá se na jeho limity, nezapíše se vazba; převzetí je samostatné rozhodnutí operátora (brain H304), nikdy vedlejší účinek objednávky. Totéž pro databázi stejného jména jiného webu. Vazba nově nese, jak zdroj vznikl (`provenance`: `created` nebo `recovered`). **Nalezeno při tom, a horší:** samostatná poštovní služba se vázala bez jména domény a každý dotaz na schránky je `LIKE '%@<doména>'` — s chybějícím jménem tedy `'%@'`, **všechny schránky celého sdíleného poštovního serveru**. Pozastavení neplatiče vypnulo odesílání pošty všem, historickým zákazníkům včetně; a webhosting bez poštovní domény se na schránky ptal přes svůj web (taky bez jména), takže zákazník viděl všechny schránky serveru a kontrola vlastnictví, která id schránky porovnává s tímto výpisem, prošla pro kteroukoli z nich. Nově poštovní doména nese své jméno, webhosting bez poštovní domény nemá žádné schránky (`MailDomains::across`), a adaptér i poštovní nástroje odmítnou dotaz s prázdným jménem | ✔ | `tests/Feature/Provisioning/ResourceProvenanceTest.php` |
| 102 | Smazání celého účtu smí jen vlastník, se step-upem a lhůtou | **Nalezeno a opraveno 2026-09-24 (TASK-0015), podle pravidla vlastníka:** „Po zrušení služeb zůstává klientský účet v ONHOST panelu.“ Automaticky účet nic nezavírá — jediná cesta, která to dělá, je žádost zákazníka o výmaz (GDPR čl. 17), a ta byla otevřená **komukoli, kdo smí upravit profil organizace** (`organization.manage`, drží ho každý admin), mimo CommandBus, bez step-upu, bez druhého pohledu a bez možnosti vzít ji zpět. Plánovač ji do 30 minut provedl: všem členům včetně vlastníka přepsal e-mail a heslo (už se nepřihlásí), organizaci zavřel a smazal archivy zrušených služeb. Katalog přitom vždy říkal, co to je — `organization.close`, „Close the organization and schedule data deletion“, kterou drží jen vlastník — a nikdo se na ni neptal. Nově jde žádost přes CommandBus (`DataRequestCommand`): výmaz chce `organization.close` a čerstvý step-up; **čtyři oči záměrně ne** — organizace o jednom člověku nemá koho se zeptat a právo na výmaz nesmí záviset na kolegovi. Místo druhé osoby stojí čas: výmaz čeká `onhost.compliance.deletion_grace_days` (14 dní, `ONHOST_DELETION_GRACE_DAYS`) a do té doby ho kdokoli, kdo organizaci spravuje, zruší (`POST /v1/data-requests/{id}/cancel`); zákazník dostane v panelu oznámení s datem. Účet, který ještě drží **registrovanou doménu**, se nevymaže (`active_domains`) — doména by dál obnovovala pro zavřenou organizaci s adresou, kterou nikdo nečte. Nalezeno při tom: odvozený klíč idempotence (uživatel + trasa + tělo) by druhou stejnou žádost přehrál — nový výmaz po zrušeném by vrátil ten zrušený; bez hlavičky `Idempotency-Key` je proto každá žádost nová | ✔ | `tests/Feature/Compliance/AccountErasureTest.php` |
